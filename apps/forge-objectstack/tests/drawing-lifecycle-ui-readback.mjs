import assert from'node:assert/strict';import{readFile,writeFile}from'node:fs/promises';import{connect}from'../scripts/api-client.mjs';const path='.objectstack/acceptance/drawing-lifecycle-report.json',report=JSON.parse(await readFile(path,'utf8')),api=await connect(process.env.FORGE_URL||'http://localhost:4380');const r=await api.request('/data/forge_drawing_distribution/'+report.ids.browserDistribution);assert.equal(r.status,200);const record=r.value.record;assert.deepEqual({status:record.status,confirmation:record.confirmation_status,receipt:record.receipt_status},{status:'sent',confirmation:'confirmed',receipt:'not_required'});report.browserVerification={status:'passed',verifiedAt:new Date().toISOString(),recordId:record.id,pageUrl:(process.env.FORGE_URL||'http://localhost:4380')+'/_console/apps/forge/page/page_drawing_workspace',screenshot:'docs/evidence/drawing-lifecycle/forge-drawing-distribution-confirmed.png',observed:['真实内置浏览器登录 Forge 图纸管理页面','发放记录页签回读两条 V1.0 受控发放','点击浏览器样本的确认接收并在对话框确认办理','页面刷新为全部已确认且 API 回读 confirmation_status=confirmed','工作台展示 RISEMAP 对照的档案、版本、评审、发布、变更、发放、客户图纸和关联查询页签']};await writeFile(path,JSON.stringify(report,null,2)+'\n');console.log('PASS browser confirmation persisted and API readback matches');
