import { forgeProductUiCss, forgeProductUiRuntime } from './product-ui.js';

const giftCss = JSON.stringify(forgeProductUiCss + `
.gift-page .fp-shell{width:min(1460px,100%);padding-top:20px}.gift-page .gift-hero{position:relative;overflow:hidden;display:flex;align-items:flex-end;min-height:112px;margin-bottom:12px;padding:20px;border-radius:12px;background:linear-gradient(112deg,#a92f76 0%,#da5365 52%,#f59b32 100%);color:#fff}.gift-page .gift-hero:after{content:'✦';position:absolute;right:36px;top:18px;color:rgba(255,255,255,.45);font-size:35px}.gift-page .gift-hero-main{display:flex;align-items:center;gap:13px;position:relative;z-index:1}.gift-page .gift-hero-icon{display:grid;place-items:center;width:46px;height:46px;border-radius:11px;background:rgba(255,255,255,.16)}.gift-page .gift-hero-icon svg{width:25px;height:25px}.gift-page .gift-hero h1{margin:0;font-size:24px}.gift-page .gift-hero p{margin:3px 0 0;color:#fff2f4}.gift-page .gift-tabs{display:flex;gap:24px;overflow:auto;border-bottom:1px solid var(--fp-line);margin-bottom:14px}.gift-page .gift-tab{position:relative;display:flex;align-items:center;gap:7px;min-height:42px;padding:0 2px;border:0;background:none;color:#5f6b7a;white-space:nowrap;cursor:pointer}.gift-page .gift-tab.active{color:#245bdb;font-weight:650}.gift-page .gift-tab.active:after{content:'';position:absolute;left:0;right:0;bottom:-1px;height:2px;background:#245bdb}.gift-page .gift-tab svg{width:16px;height:16px}.gift-page .gift-count{display:grid;place-items:center;min-width:20px;height:20px;padding:0 6px;border-radius:999px;background:#eef3ff;color:#245bdb;font-size:11px}.gift-page .gift-metrics{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:10px;margin-bottom:14px}.gift-page .gift-metric{padding:14px;border:1px solid var(--fp-line);border-radius:9px;background:#fff}.gift-page .gift-metric-head{display:flex;align-items:center;gap:7px;color:var(--fp-muted);font-size:12px}.gift-page .gift-metric strong{display:block;margin-top:8px;font-size:22px}.gift-page .gift-metric small{display:block;margin-top:3px;color:var(--fp-muted)}.gift-page .gift-toolbar{display:flex;align-items:center;gap:8px;padding:11px 14px;border-bottom:1px solid var(--fp-line)}.gift-page .gift-toolbar-end{display:flex;gap:7px;margin-left:auto}.gift-page .gift-filter{display:grid;grid-template-columns:minmax(250px,1fr) repeat(2,minmax(145px,190px)) auto;gap:9px;padding:10px 14px;border-bottom:1px solid var(--fp-line)}.gift-page .fp-table{min-width:1120px}.gift-page .gift-pagination{display:flex;justify-content:space-between;align-items:center;padding:12px 14px;color:var(--fp-muted);font-size:13px}.gift-page .gift-pages{display:flex;align-items:center;gap:6px}.gift-page .gift-page-current{display:grid;place-items:center;width:30px;height:30px;border-radius:6px;background:#245bdb;color:#fff}.gift-page .gift-catalog-metrics{grid-template-columns:repeat(3,minmax(0,1fr))}.gift-page .gift-catalog-layout{display:grid;grid-template-columns:190px minmax(0,1fr);gap:14px}.gift-page .gift-categories{padding:14px}.gift-page .gift-categories h3{margin:0 0 10px;font-size:14px}.gift-page .gift-category-list{display:grid;gap:5px}.gift-page .gift-category{width:100%;padding:8px 9px;border:0;border-radius:6px;background:transparent;text-align:left;cursor:pointer}.gift-page .gift-category.active{background:#245bdb;color:#fff}.gift-page .gift-catalog-tools{display:flex;align-items:center;gap:8px;padding:11px 14px;border-bottom:1px solid var(--fp-line);flex-wrap:wrap}.gift-page .gift-catalog-tools .fp-search{min-width:220px;flex:1}.gift-page .gift-catalog-tools-end{display:flex;gap:7px;margin-left:auto}.gift-page .gift-grid{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:12px;padding:14px}.gift-page .gift-card{padding:14px;border:1px solid var(--fp-line);border-radius:9px;background:#fff}.gift-page .gift-card-top{display:flex;justify-content:space-between;gap:8px}.gift-page .gift-card p{margin:6px 0;color:var(--fp-muted);font-size:12px}.gift-page .gift-card-price{font-size:18px;font-weight:700}.gift-page .gift-empty{min-height:250px;display:grid;place-items:center}.gift-page .fp-modal{width:min(760px,100%)}@media(max-width:940px){.gift-page .gift-filter{grid-template-columns:1fr 1fr}.gift-page .gift-metrics{grid-template-columns:repeat(2,minmax(0,1fr))}.gift-page .gift-grid{grid-template-columns:repeat(2,minmax(0,1fr))}}@media(max-width:680px){.gift-page .gift-hero{min-height:104px}.gift-page .gift-toolbar{align-items:flex-start;flex-wrap:wrap}.gift-page .gift-toolbar-end{margin-left:0}.gift-page .gift-filter,.gift-page .gift-metrics{grid-template-columns:1fr}.gift-page .gift-catalog-layout{grid-template-columns:1fr}.gift-page .gift-category-list{display:flex;overflow:auto}.gift-page .gift-category{width:auto;white-space:nowrap}.gift-page .gift-grid{grid-template-columns:1fr}.gift-page .gift-catalog-tools-end{margin-left:0}.gift-page .gift-hero h1{font-size:21px}}
\n.gift-page .gift-metric-head svg{width:16px;height:16px;flex:none}
`);

const giftManagementSourceBase = `
const css=${giftCss};
const GiftIcon=({name='gift'})=>{const p={gift:<><path d="M20 12v10H4V12M2 7h20v5H2zM12 7v15M12 7H7.5a2.5 2.5 0 1 1 2.2-3.7L12 7Zm0 0h4.5a2.5 2.5 0 1 0-2.2-3.7L12 7Z"/></>,list:<><path d="M8 6h13M8 12h13M8 18h13"/><path d="M3 6h.01M3 12h.01M3 18h.01"/></>,store:<><path d="M3 9l2-5h14l2 5"/><path d="M5 13v8h14v-8M9 21v-6h6v6"/><path d="M3 9a3 3 0 0 0 6 0 3 3 0 0 0 6 0 3 3 0 0 0 6 0"/></>,flow:<><path d="M6 3v12M18 9v12"/><path d="m3 6 3-3 3 3M15 18l3 3 3-3"/></>,refresh:<><path d="M21 12a9 9 0 1 1-3-6.7"/><path d="M21 3v6h-6"/></>,grid:<><rect x="3" y="3" width="7" height="7"/><rect x="14" y="3" width="7" height="7"/><rect x="3" y="14" width="7" height="7"/><rect x="14" y="14" width="7" height="7"/></>};return <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="1.8" strokeLinecap="round" strokeLinejoin="round" aria-hidden="true">{p[name]||p.gift}</svg>};
const TABS=[['礼品申请','gift'],['礼品清单','list'],['礼品库管理','store'],['礼品库流水','flow']],statusText={draft:'草稿',pending:'待审批',approved:'已批准',issued:'已发放',active:'启用',inactive:'停用'},categoryNames=['全部','实物礼品','兑换券码','充值卡','现金红包','报销额度','生日福利','节日福利(春节/端午/中秋)','司庆福利','入职周年','婚育福利','高温/防寒福利','体检福利','团建福利'];
function App(){const adapter=useAdapter(),[state,setState]=React.useState({loading:true,rows:[],error:''}),[tab,setTab]=React.useState(0),[q,setQ]=React.useState(''),[status,setStatus]=React.useState('all'),[category,setCategory]=React.useState('全部'),[dialog,setDialog]=React.useState(null),[busy,setBusy]=React.useState(false),[toast,setToast]=React.useState(''),[view,setView]=React.useState('grid'),[recommended,setRecommended]=React.useState(false),[sort,setSort]=React.useState('default');
async function request(path,options={}){const headers={'Content-Type':'application/json',...(adapter?.getAuthHeaders?.()||{}),...(options.headers||{})},raw=String(adapter?.baseUrl||''),base=raw.endsWith('/')?raw.slice(0,-1):raw,transport=adapter?.fetchImpl||fetch,response=await transport(base+'/api/v1'+path,{credentials:'include',...options,headers}),payload=await response.json().catch(()=>({}));if(!response.ok)throw new Error((typeof payload.error==='string'?payload.error:payload.error?.message)||payload.message||'请求失败');return payload}
async function load(){setState(s=>({...s,loading:true,error:''}));try{const p=await request('/data/forge_administration_record?$top=500'),all=p.records||p.data?.records||[];setState({loading:false,rows:all.filter(x=>String(x.page_key||'').startsWith('gift_')),error:''})}catch(e){setState(s=>({...s,loading:false,error:String(e.message||e)}))}}
React.useEffect(()=>{load()},[]);function meta(x){try{return JSON.parse(x.details||'{}')}catch{return {}}}const applications=state.rows.filter(x=>x.page_key==='gift_management'),gifts=state.rows.filter(x=>x.page_key==='gift_catalog'),flows=state.rows.filter(x=>x.page_key==='gift_flow');let rows=(tab===0?applications:tab===3?flows:gifts).filter(x=>(status==='all'||x.status===status)&&(category==='全部'||x.category===category)&&(!q.trim()||Object.values({...x,...meta(x)}).some(v=>String(v||'').toLowerCase().includes(q.trim().toLowerCase()))));if(tab===1&&recommended)rows=rows.filter(x=>meta(x).recommended);if(tab===1&&sort!=='default')rows=[...rows].sort((a,b)=>sort==='price_asc'?Number(a.amount||0)-Number(b.amount||0):Number(b.amount||0)-Number(a.amount||0));
const appTotal=applications.reduce((s,x)=>s+Number(x.amount||0),0),approvedTotal=applications.filter(x=>['approved','issued'].includes(x.status)).reduce((s,x)=>s+Number(x.amount||0),0),inFlows=flows.filter(x=>x.category==='in'),outFlows=flows.filter(x=>x.category==='out'),sum=f=>f.reduce((s,x)=>s+Number(x.amount||0),0);
function reset(){setQ('');setStatus('all');setCategory('全部');setRecommended(false);setSort('default')}function open(kind){setDialog({kind,title:'',code:(kind==='application'?'GA':'GI')+'-'+Date.now().toString().slice(-6),category:'实物礼品',status:kind==='application'?'draft':'active',owner_name:'',department:'',start_on:new Date().toISOString().slice(0,10),amount:'',quantity:'',source_ref:'',recipient:'',progress:'0',spec:'',location:'',recommended:false,error:''})}
async function save(){if(!dialog?.title.trim())return setDialog(d=>({...d,error:dialog.kind==='application'?'请填写申请标题':'请填写礼品名称'}));if(!dialog.code.trim())return setDialog(d=>({...d,error:'请填写编号'}));if(dialog.amount!==''&&Number(dialog.amount)<0)return setDialog(d=>({...d,error:'金额不能小于 0'}));if(dialog.kind==='application'&&(Number(dialog.progress)<0||Number(dialog.progress)>100))return setDialog(d=>({...d,error:'审批进度应在 0 到 100 之间'}));setBusy(true);try{const details=JSON.stringify(dialog.kind==='application'?{recipient:dialog.recipient,progress:Number(dialog.progress||0)}:{spec:dialog.spec,location:dialog.location,recommended:!!dialog.recommended,inbound:Number(dialog.quantity||0),outbound:0});await request('/data/forge_administration_record',{method:'POST',body:JSON.stringify({page_key:dialog.kind==='application'?'gift_management':'gift_catalog',code:dialog.code.trim(),title:dialog.title.trim(),category:dialog.category,status:dialog.status,owner_name:dialog.owner_name.trim()||null,department:dialog.department.trim()||null,start_on:dialog.start_on||null,amount:dialog.amount===''?null:Number(dialog.amount),quantity:dialog.quantity===''?null:Number(dialog.quantity),source_ref:dialog.source_ref.trim()||null,details})});setDialog(null);setToast('记录已保存');await load()}catch(e){setDialog(d=>({...d,error:String(e.message||e)}))}finally{setBusy(false)}}
function exportRows(){if(!rows.length)return;const heads=tab===0?['编号','申请标题','类别','申请人','赠送对象','金额','申请日期','状态','审批进度']:tab===2?['编号','礼品名称','分类','规格','单价','当前库存','累计入库','累计出库','库存价值','存放位置','状态']:['流水号','类型','礼品名称','规格','数量','单价','金额','事由','关联单据','操作人','日期'];const body=rows.map(x=>{const m=meta(x),v=tab===0?[x.code,x.title,x.category,x.owner_name,m.recipient,x.amount,x.start_on,statusText[x.status]||x.status,m.progress]:tab===2?[x.code,x.title,x.category,m.spec,x.amount,x.quantity,m.inbound,m.outbound,Number(x.amount||0)*Number(x.quantity||0),m.location,statusText[x.status]||x.status]:[x.code,x.category==='in'?'入库':'出库',x.title,m.spec,x.quantity,m.unit_price,x.amount,x.details,x.source_ref,x.owner_name,x.start_on];return v.map(y=>'"'+String(y??'').replaceAll('"','""')+'"').join(',')});const blob=new Blob(['\\ufeff'+[heads.join(','),...body].join('\\n')],{type:'text/csv;charset=utf-8'}),url=URL.createObjectURL(blob),a=document.createElement('a');a.href=url;a.download=TABS[tab][0]+'.csv';a.click();URL.revokeObjectURL(url)}
const metrics=tab===0?<div className="gift-metrics"><Metric title="申请总额" value={'¥'+appTotal.toLocaleString('zh-CN',{minimumFractionDigits:2})} note={'共 '+applications.length+' 笔'}/><Metric title="待处理审批" value={applications.filter(x=>x.status==='pending').length} note="需及时审核"/><Metric title="已批准金额" value={'¥'+approvedTotal.toLocaleString('zh-CN',{minimumFractionDigits:2})} note="含已发放"/><Metric title="已发放" value={applications.filter(x=>x.status==='issued').length} note="已完成发放"/></div>:tab===3?<div className="gift-metrics"><Metric title="总流水记录" value={flows.length} note={'入库 '+inFlows.length+' · 出库 '+outFlows.length}/><Metric title="入库金额" value={'¥'+sum(inFlows).toLocaleString('zh-CN',{minimumFractionDigits:2})} note={'入库 '+inFlows.reduce((s,x)=>s+Number(x.quantity||0),0)+' 件'}/><Metric title="出库金额" value={'¥'+sum(outFlows).toLocaleString('zh-CN',{minimumFractionDigits:2})} note={'出库 '+outFlows.reduce((s,x)=>s+Number(x.quantity||0),0)+' 件'}/><Metric title="净变动" value={(sum(inFlows)-sum(outFlows)>=0?'+':'')+'¥'+(sum(inFlows)-sum(outFlows)).toLocaleString('zh-CN',{minimumFractionDigits:2})} note="入库 - 出库"/></div>:null;
function Metric({title,value,note}){return <div className="gift-metric"><span className="gift-metric-head"><GiftIcon/>{title}</span><strong>{value}</strong><small>{note}</small></div>}
const filter=<div className="gift-filter"><input className="fp-input fp-search" aria-label="搜索" placeholder={tab===0?'搜索编号/标题/申请人...':tab===2?'搜索礼品名称/编号...':'搜索礼品/单号/备注...'} value={q} onChange={e=>setQ(e.target.value)}/><ForgeSelectControl aria-label="类型" value={category} onChange={e=>setCategory(e.target.value)}><option value="全部">全部类型</option>{(tab===3?['in','out']:categoryNames.slice(1)).map(x=><option key={x} value={x}>{x==='in'?'入库':x==='out'?'出库':x}</option>)}</ForgeSelectControl>{tab===0&&<ForgeSelectControl aria-label="状态" value={status} onChange={e=>setStatus(e.target.value)}><option value="all">全部状态</option>{Object.entries(statusText).slice(0,4).map(x=><option key={x[0]} value={x[0]}>{x[1]}</option>)}</ForgeSelectControl>}<button className="fp-button" onClick={reset}>重置</button></div>;
function toolbar(){return <div className="gift-toolbar">{tab===0&&<button className="fp-button primary" onClick={()=>open('application')}>＋ 新建申请</button>}{tab===2&&<button className="fp-button primary" onClick={()=>open('gift')}>＋ 新增礼品</button>}<button className="fp-button" disabled={!rows.length} onClick={exportRows}>导出</button><div className="gift-toolbar-end"><button className="fp-icon-button" aria-label="刷新" title="刷新" onClick={load}><GiftIcon name="refresh"/></button><ForgeListSettings/></div></div>}
function table(){const cols=tab===0?['编号','申请标题','类别','申请人','赠送对象','金额','申请日期','状态','审批进度','操作']:tab===2?['编号','礼品名称','分类','规格','单价','当前库存','累计入库','累计出库','库存价值','存放位置','状态','操作']:['流水号','类型','礼品名称','规格','数量','单价','金额','事由','关联单据','操作人','日期'];return <><div className="fp-table-wrap"><table className="fp-table"><thead><tr>{cols.map(x=><th key={x}>{x}</th>)}</tr></thead><tbody>{rows.length?rows.map(x=>{const m=meta(x),cells=tab===0?[x.code,x.title,x.category,x.owner_name,m.recipient,x.amount==null?'—':'¥'+Number(x.amount).toLocaleString('zh-CN'),x.start_on,<ForgeStatus value={x.status} label={statusText[x.status]||x.status}/>,(m.progress||0)+'%',<button className="fp-button small" onClick={()=>setDialog({kind:'view',row:x,extra:m})}>查看</button>]:tab===2?[x.code,x.title,x.category,m.spec,x.amount==null?'—':'¥'+Number(x.amount).toLocaleString('zh-CN'),x.quantity||0,m.inbound||0,m.outbound||0,'¥'+(Number(x.amount||0)*Number(x.quantity||0)).toLocaleString('zh-CN'),m.location,<ForgeStatus value={x.status} label={statusText[x.status]||x.status}/>,<button className="fp-button small" onClick={()=>setDialog({kind:'view',row:x,extra:m})}>查看</button>]:[x.code,x.category==='in'?'入库':'出库',x.title,m.spec,x.quantity,m.unit_price,x.amount,x.details,x.source_ref,x.owner_name,x.start_on];return <tr key={x.id}>{cells.map((v,i)=><td key={i}>{v??'—'}</td>)}</tr>}):<tr><td colSpan={cols.length} className="fp-empty-cell"><ForgeEmpty title={tab===0?'暂无数据':tab===2?'暂无礼品数据':'暂无流水记录'} description={tab===0?'新建礼品申请后可跟踪审批和发放。':tab===2?'新增礼品后可维护库存与存放位置。':'礼品发生入库或出库后将在此形成流水。'} action={tab<3?<button className="fp-button primary" onClick={()=>open(tab===0?'application':'gift')}>{tab===0?'新建申请':'新增礼品'}</button>:null}/></td></tr>}</tbody></table></div><div className="gift-pagination"><span>共 {rows.length} 条记录 · 每页 10 条</span><div className="gift-pages"><span className="gift-page-current">1</span><span>第 1 页</span></div></div></>}
const catalog=<><div className="gift-metrics gift-catalog-metrics"><Metric title="礼品种类" value={gifts.length+' 种'} note="当前礼品库"/><Metric title="涵盖分类" value={new Set(gifts.map(x=>x.category).filter(Boolean)).size+' 类'} note="可筛选分类"/><Metric title="平均单价" value={'¥'+(gifts.length?Math.round(gifts.reduce((s,x)=>s+Number(x.amount||0),0)/gifts.length):0)} note="分类口径"/></div><div className="gift-catalog-layout"><aside className="fp-card gift-categories"><h3>分类</h3><div className="gift-category-list">{categoryNames.map(x=><button key={x} className={'gift-category '+(category===x?'active':'')} onClick={()=>setCategory(x)}>{x}</button>)}</div></aside><section className="fp-card"><div className="gift-catalog-tools"><button className={'fp-button '+(recommended?'primary':'')} onClick={()=>setRecommended(x=>!x)}>推荐</button><ForgeSelectControl aria-label="排序"><option>默认排序</option><option>价格从低到高</option><option>价格从高到低</option></ForgeSelectControl><input className="fp-input fp-search" aria-label="搜索礼品" placeholder="搜索礼品..." value={q} onChange={e=>setQ(e.target.value)}/><div className="gift-catalog-tools-end"><button className={'fp-icon-button '+(view==='grid'?'active':'')} aria-label="网格视图" onClick={()=>setView('grid')}><GiftIcon name="grid"/></button><button className="fp-icon-button" aria-label="列表视图" onClick={()=>setView('list')}><GiftIcon name="list"/></button></div></div><div className="gift-toolbar"><span>共 {rows.length} 种礼品</span></div>{rows.length?(view==='grid'?<div className="gift-grid">{rows.map(x=><article className="gift-card" key={x.id}><div className="gift-card-top"><strong>{x.title}</strong>{meta(x).recommended&&<span className="fp-badge">推荐</span>}</div><p>{x.category} · {meta(x).spec||'默认规格'}</p><span className="gift-card-price">¥{Number(x.amount||0).toLocaleString('zh-CN')}</span></article>)}</div>:<div>{table()}</div>):<div className="gift-empty"><ForgeEmpty title="未找到匹配的礼品" description="调整分类、推荐条件或搜索关键词后重试。"/></div>}</section></div></>;
if(state.loading)return <div className="forge-product gift-page"><style>{css}</style><div className="fp-shell"><ForgeLoading label="正在加载礼品管理"/></div></div>;
return <div className="forge-product gift-page"><style>{css}</style><div className="fp-shell"><section className="gift-hero"><div className="gift-hero-main"><span className="gift-hero-icon"><GiftIcon/></span><div><div className="fp-eyebrow" style={{color:'#fff2f4'}}>行政管理 / 礼品管理</div><h1>礼品管理</h1><p>员工礼品申请、审批、库存与发放，覆盖商务赠礼与节日福利</p></div></div></section>{state.error&&<ForgeNotice tone="error">{state.error}</ForgeNotice>}{toast&&<ForgeNotice tone="success" onClose={()=>setToast('')}>{toast}</ForgeNotice>}<div className="gift-tabs">{TABS.map((x,i)=><button key={x[0]} className={'gift-tab '+(tab===i?'active':'')} onClick={()=>{setTab(i);reset()}}><GiftIcon name={x[1]}/>{x[0]}<span className="gift-count">{i===0?applications.length:i===1||i===2?gifts.length:flows.length}</span></button>)}</div>{metrics}{tab===1?catalog:<section className="fp-card">{toolbar()}{filter}{table()}</section>}</div><ForgeDialog open={!!dialog&&dialog.kind!=='view'} title={dialog?.kind==='application'?'新建礼品申请':'新增礼品'} subtitle={dialog?.kind==='application'?'记录赠送对象、金额与审批状态':'维护礼品规格、库存与存放位置'} error={dialog?.error} busy={busy} confirmLabel="保存记录" onCancel={()=>!busy&&setDialog(null)} onConfirm={save}><div className="fp-detail-grid"><div className="fp-field"><label>{dialog?.kind==='application'?'申请标题':'礼品名称'} *</label><input className="fp-input" aria-label="名称" value={dialog?.title||''} onChange={e=>setDialog(d=>({...d,title:e.target.value}))}/></div><div className="fp-field"><label>编号 *</label><input className="fp-input" aria-label="编号" value={dialog?.code||''} onChange={e=>setDialog(d=>({...d,code:e.target.value}))}/></div><div className="fp-field"><label>类别</label><ForgeSelectControl value={dialog?.category||'实物礼品'} onChange={e=>setDialog(d=>({...d,category:e.target.value}))}>{categoryNames.slice(1).map(x=><option key={x}>{x}</option>)}</ForgeSelectControl></div>{dialog?.kind==='application'?<><div className="fp-field"><label>申请人</label><input className="fp-input" value={dialog?.owner_name||''} onChange={e=>setDialog(d=>({...d,owner_name:e.target.value}))}/></div><div className="fp-field"><label>赠送对象</label><input className="fp-input" value={dialog?.recipient||''} onChange={e=>setDialog(d=>({...d,recipient:e.target.value}))}/></div><div className="fp-field"><label>申请日期</label><ForgeDateInput value={dialog?.start_on||''} onChange={e=>setDialog(d=>({...d,start_on:e.target.value}))}/></div><div className="fp-field"><label>审批进度 (%)</label><input className="fp-input" type="number" min="0" max="100" value={dialog?.progress||''} onChange={e=>setDialog(d=>({...d,progress:e.target.value}))}/></div></>:<><div className="fp-field"><label>规格</label><input className="fp-input" value={dialog?.spec||''} onChange={e=>setDialog(d=>({...d,spec:e.target.value}))}/></div><div className="fp-field"><label>当前库存</label><input className="fp-input" type="number" min="0" value={dialog?.quantity||''} onChange={e=>setDialog(d=>({...d,quantity:e.target.value}))}/></div><div className="fp-field"><label>存放位置</label><input className="fp-input" value={dialog?.location||''} onChange={e=>setDialog(d=>({...d,location:e.target.value}))}/></div><label className="fp-field" style={{display:'flex',alignItems:'center',gap:8}}><input type="checkbox" checked={!!dialog?.recommended} onChange={e=>setDialog(d=>({...d,recommended:e.target.checked}))}/>推荐礼品</label></>}<div className="fp-field"><label>{dialog?.kind==='application'?'申请金额':'单价'}</label><input className="fp-input" inputMode="decimal" value={dialog?.amount||''} onChange={e=>setDialog(d=>({...d,amount:e.target.value}))}/></div><div className="fp-field"><label>状态</label><ForgeSelectControl value={dialog?.status||'draft'} onChange={e=>setDialog(d=>({...d,status:e.target.value}))}>{Object.entries(statusText).map(x=><option key={x[0]} value={x[0]}>{x[1]}</option>)}</ForgeSelectControl></div></div></ForgeDialog><ForgeDialog open={dialog?.kind==='view'} title={dialog?.row?.title||'礼品详情'} subtitle={dialog?.row?.code||''} confirmLabel="关闭" hideCancel onCancel={()=>setDialog(null)} onConfirm={()=>setDialog(null)}><div className="fp-detail-grid">{[['分类','category'],['申请人 / 负责人','owner_name'],['状态','status'],['日期','start_on'],['金额','amount'],['数量','quantity']].map(([label,key])=><div className="fp-field" key={key}><label>{label}</label><div>{dialog?.row?.[key]??'—'}</div></div>)}</div></ForgeDialog></div>}
export default App;${forgeProductUiRuntime}`;

export const giftManagementSource = giftManagementSourceBase.replace(
  '<ForgeSelectControl aria-label="排序"><option>默认排序</option><option>价格从低到高</option><option>价格从高到低</option></ForgeSelectControl>',
  '<ForgeSelectControl aria-label="排序" value={sort} onChange={e=>setSort(e.target.value)}><option value="default">默认排序</option><option value="price_asc">价格从低到高</option><option value="price_desc">价格从高到低</option></ForgeSelectControl>',
);
