import { forgeProductUiCss, forgeProductUiRuntime, forgeWorkPageUiCss } from './product-ui.js';

const timesheetCss = `
div:has(>.forge-time){max-width:none!important;margin:0!important}
div:has(>.forge-time)>div.space-y-2{display:none!important}
.forge-time{min-height:100%;background:var(--fp-bg);color:var(--fp-text)}.forge-time .time-shell{width:min(1380px,100%);margin:0 auto;padding:20px 22px 42px}.forge-time .time-header{display:flex;align-items:flex-start;justify-content:space-between;gap:24px;margin-bottom:16px}.forge-time .time-crumb{display:flex;align-items:center;gap:7px;margin-bottom:5px;color:var(--fp-muted);font-size:12px}.forge-time .time-header h1{margin:0;font-size:24px;line-height:1.35;letter-spacing:-.02em}.forge-time .time-header p{margin:5px 0 0;color:var(--fp-muted)}.forge-time .time-actions{display:flex;align-items:center;gap:8px;flex-wrap:wrap}.forge-time .time-header-actions{gap:12px;flex-wrap:nowrap}.forge-time .time-export-group{display:inline-flex;align-items:center}.forge-time .time-header-button{display:inline-flex;min-width:92px;height:36px;align-items:center;justify-content:center;gap:7px;padding:0 14px;white-space:nowrap}.forge-time .time-export-group .time-header-button{border-radius:0}.forge-time .time-export-group .time-header-button:first-child{border-radius:8px 0 0 8px}.forge-time .time-export-group .time-header-button:last-child{margin-left:-1px;border-radius:0 8px 8px 0}.forge-time .time-header-button.primary{min-width:112px}.forge-time .time-range{display:flex;align-items:center;gap:10px;flex-wrap:wrap;padding:14px 16px}.forge-time .time-range-label{display:flex;align-items:center;gap:7px;margin-right:4px;color:var(--fp-muted);font-size:12px;font-weight:620}.forge-time .time-kpis{display:grid;grid-template-columns:repeat(4,minmax(150px,1fr));gap:11px;margin:14px 0}.forge-time .time-kpi{padding:15px 16px;border:1px solid var(--fp-line);border-radius:10px;background:#fff}.forge-time .time-kpi-head{display:flex;align-items:center;gap:7px;color:var(--fp-muted);font-size:11px}.forge-time .time-kpi strong{display:block;margin-top:6px;font-size:22px;letter-spacing:-.02em}.forge-time .time-kpi small{display:block;margin-top:2px;color:var(--fp-muted)}.forge-time .time-chart{padding:16px}.forge-time .time-chart-head{display:flex;align-items:center;justify-content:space-between;margin-bottom:14px}.forge-time .time-chart-head h2{margin:0;font-size:14px}.forge-time .time-chart-head span{color:var(--fp-muted);font-size:11px}.forge-time .time-bars{display:grid;grid-template-columns:repeat(7,1fr);gap:12px;height:150px;align-items:end}.forge-time .time-day{display:flex;height:100%;flex-direction:column;align-items:center;justify-content:flex-end;gap:6px}.forge-time .time-bar-track{display:flex;width:22px;height:110px;align-items:flex-end;border-radius:6px;background:#f1f3f7;overflow:hidden}.forge-time .time-bar{width:100%;min-height:2px;border-radius:6px;background:linear-gradient(180deg,#5b87eb,var(--fp-primary))}.forge-time .time-day b{font-size:10px;color:var(--fp-muted);font-weight:600}.forge-time .time-day span{font-size:11px;color:var(--fp-muted)}.forge-time .time-form-card{padding:17px;margin-bottom:14px}.forge-time .time-form-head{display:flex;align-items:flex-start;justify-content:space-between;gap:18px;margin-bottom:15px}.forge-time .time-form-head h2{margin:0;font-size:15px}.forge-time .time-form-head p{margin:3px 0 0;color:var(--fp-muted);font-size:12px}.forge-time .time-form{display:grid;grid-template-columns:repeat(4,minmax(160px,1fr));gap:13px 15px}.forge-time .time-form .wide{grid-column:span 2}.forge-time .time-record-head{display:flex;align-items:center;justify-content:space-between;gap:16px;padding:0 16px;border-bottom:1px solid var(--fp-line)}.forge-time .time-tabs{display:flex;gap:22px}.forge-time .time-tab{height:44px;border:0;border-bottom:2px solid transparent;background:transparent;color:var(--fp-muted)}.forge-time .time-tab.active{border-bottom-color:var(--fp-primary);color:var(--fp-primary);font-weight:650}.forge-time .time-filter{display:flex;align-items:center;gap:8px;flex-wrap:wrap;padding:13px 16px;border-bottom:1px solid var(--fp-line)}.forge-time .time-review{min-width:260px}.forge-time .time-table-empty{height:180px;text-align:center;color:var(--fp-muted)}.forge-time .time-cost{margin-top:14px}.forge-time .time-cost-head{display:flex;align-items:center;justify-content:space-between;padding:14px 16px;border-bottom:1px solid var(--fp-line)}.forge-time .time-cost-head h2{margin:0;font-size:14px}.forge-time .time-limit{margin-top:12px;padding:10px 12px;border:1px solid #f2d7a7;border-radius:9px;background:var(--fp-warning-soft);color:#8b5200;font-size:12px}
@media(max-width:980px){.forge-time .time-kpis{grid-template-columns:1fr 1fr}.forge-time .time-form{grid-template-columns:1fr 1fr}.forge-time .time-header{display:block}.forge-time .time-actions{margin-top:13px}}@media(max-width:680px){.forge-time .time-shell{padding:14px 12px 30px}.forge-time .time-kpis,.forge-time .time-form{grid-template-columns:1fr}.forge-time .time-form .wide{grid-column:auto}.forge-time .time-bars{gap:5px}.forge-time .time-record-head{display:block}.forge-time .time-filter{align-items:stretch}.forge-time .time-filter>*{width:100%}}
`;

const projectTimesheetCostSource = `
const css=${JSON.stringify(forgeProductUiCss + forgeWorkPageUiCss + timesheetCss)};
function App(){
  const adapter=useAdapter(),now=new Date(),today=now.toLocaleDateString('sv-SE',{timeZone:'Asia/Shanghai'}),weekStartDate=new Date(now);weekStartDate.setDate(now.getDate()-((now.getDay()+6)%7));const weekEndDate=new Date(weekStartDate);weekEndDate.setDate(weekStartDate.getDate()+6);const iso=date=>date.toLocaleDateString('sv-SE',{timeZone:'Asia/Shanghai'}),defaultStart=iso(weekStartDate),defaultEnd=iso(weekEndDate);
  const [data,setData]=React.useState({loading:true,projects:[],timesheets:[],costs:[],items:[],members:[],users:[],currentUser:null,errors:[]}),[busy,setBusy]=React.useState(false),[decision,setDecision]=React.useState(null),[showForm,setShowForm]=React.useState(false),[rangeStart,setRangeStart]=React.useState(defaultStart),[rangeEnd,setRangeEnd]=React.useState(defaultEnd),[reviewStatus,setReviewStatus]=React.useState('all'),[projectFilter,setProjectFilter]=React.useState(''),[form,setForm]=React.useState({project_id:'',worker_id:'',code:'',work_item_id:'',work_on:today,work_content:'',time_type:'normal',hours:'',hourly_rate:'0',review_comment:''});
  const sources=[['projects','forge_project','项目'],['timesheets','forge_project_timesheet','工时'],['costs','forge_project_cost_entry','成本'],['items','forge_project_work_item','任务'],['members','forge_project_member','项目成员'],['users','sys_user','人员']];
  async function load(){setData(d=>({...d,loading:true,errors:[]}));const [userResult,...results]=await Promise.allSettled([adapter.find('sys_user',{where:{id:'{current_user_id}'},$top:1}),...sources.map(async([key,object,label])=>{const result=await adapter.find(object,{$top:500});return{key,label,rows:Array.isArray(result?.data)?result.data:[]}})]),next={loading:false,projects:[],timesheets:[],costs:[],items:[],members:[],users:[],currentUser:userResult.status==='fulfilled'&&Array.isArray(userResult.value?.data)?userResult.value.data[0]||null:null,errors:[]};if(!next.currentUser)next.errors.push('当前登录人');results.forEach((result,index)=>{if(result.status==='fulfilled')next[result.value.key]=result.value.rows;else next.errors.push(sources[index][2])});next.projects=next.projects.filter(row=>!['settled','terminated','archived'].includes(row.status));setData(next);setForm(current=>({...current,worker_id:current.worker_id||next.currentUser?.id||'',project_id:next.projects.some(row=>row.id===current.project_id)?current.project_id:''}))}
  React.useEffect(()=>{load()},[]);
  async function request(path,options){const response=await fetch('/api/v1'+path,{credentials:'include',headers:{'Content-Type':'application/json'},...options}),payload=await response.json().catch(()=>({}));if(!response.ok)throw new Error(payload.error?.message||payload.message||'请求失败');return payload}
  async function invoke(object,action,id,params={}){setBusy(true);try{await request('/actions/'+object+'/'+action+'/'+id,{method:'POST',body:JSON.stringify({params})});await load()}catch(error){setData(current=>({...current,errors:[String(error.message||error)]}))}finally{setBusy(false)}}
  function ask(action,id,name,params={},danger=false){setDecision({action,id,name,params,danger,step:'review'})}
  function confirmDecision(){const current=decision;if(!current)return;if(current.step!=='confirm')return setDecision({...current,step:'confirm'});setDecision(null);invoke('forge_project_timesheet',current.action,current.id,current.params)}
  const money=value=>'¥'+Number(value||0).toLocaleString('zh-CN',{minimumFractionDigits:2,maximumFractionDigits:2}),timeType={normal:'正常工时',overtime:'加班工时',travel:'出差工时'},statusText={draft:'草稿',pending_review:'待审核',approved:'已通过',rejected:'已驳回',allocated:'已归集'},userName=id=>{const user=data.users.find(row=>row.id===id);return user?.display_name||user?.name||user?.username||'未分配'},projectName=id=>data.projects.find(row=>row.id===id)?.name||'—';
  const filteredSheets=data.timesheets.filter(row=>(!projectFilter||row.project_id===projectFilter)&&(!rangeStart||row.work_on>=rangeStart)&&(!rangeEnd||row.work_on<=rangeEnd)&&(reviewStatus==='all'||row.status===reviewStatus)),totalHours=filteredSheets.reduce((sum,row)=>sum+Number(row.hours||0),0),approvedHours=filteredSheets.filter(row=>row.status==='approved').reduce((sum,row)=>sum+Number(row.hours||0),0),totalCost=filteredSheets.reduce((sum,row)=>sum+Number(row.cost_amount||0),0),pending=filteredSheets.filter(row=>row.status==='pending_review').length,averageRate=totalHours?totalCost/totalHours:0;
  const project=data.projects.find(row=>row.id===form.project_id),projectItems=data.items.filter(row=>row.project_id===form.project_id&&row.item_type==='task'),memberIds=new Set(data.members.filter(row=>row.project_id===form.project_id&&row.active!==false).map(row=>row.user_id));if(project?.manager_id)memberIds.add(project.manager_id);if(data.currentUser?.id)memberIds.add(data.currentUser.id);const availableUsers=data.users.filter(row=>memberIds.has(row.id)),projectCosts=data.costs.filter(row=>!projectFilter||row.project_id===projectFilter);
  const days=Array.from({length:7},(_,index)=>{const date=new Date(rangeStart||defaultStart);date.setDate(date.getDate()+index);const value=iso(date),hours=filteredSheets.filter(row=>row.work_on===value).reduce((sum,row)=>sum+Number(row.hours||0),0);return{value,label:['周一','周二','周三','周四','周五','周六','周日'][index],hours}}),maxDay=Math.max(8,...days.map(day=>day.hours));
  function newCode(){const d=new Date(),pad=value=>String(value).padStart(2,'0');return 'TS-'+d.getFullYear()+pad(d.getMonth()+1)+pad(d.getDate())+'-'+pad(d.getHours())+pad(d.getMinutes())+pad(d.getSeconds())}
  function openForm(){setForm(current=>({...current,code:newCode(),worker_id:data.currentUser?.id||current.worker_id,work_on:today,work_content:'',hours:'',work_item_id:'',time_type:'normal'}));setShowForm(true)}
  function saveDraft(){if(!project||!form.worker_id||!form.work_content.trim()||!(Number(form.hours)>=0.25&&Number(form.hours)<=24)||!(Number(form.hourly_rate)>=0)){setData(current=>({...current,errors:['请完整填写项目、人员、工作内容、0.25 至 24 小时工时和非负费率']}));return}ask('project_record_timesheet',project.id,'保存工时草稿',{code:form.code,worker_id:form.worker_id,work_item_id:form.work_item_id||null,work_on:form.work_on,work_content:form.work_content,time_type:form.time_type,hours:Number(form.hours),hourly_rate:Number(form.hourly_rate),remarks:'项目工时登记'});setShowForm(false)}
  function downloadTimesheets(){const head=['工时单号','日期','人员','关联项目','工作内容','工时','类型','费率','工时成本','状态'],rows=filteredSheets.map(row=>[row.code,row.work_on,userName(row.worker_id),projectName(row.project_id),row.work_content,row.hours,timeType[row.time_type]||row.time_type,money(row.hourly_rate),money(row.cost_amount),statusText[row.status]||row.status]);downloadCsv('项目工时.csv',[head,...rows])}
  function downloadTasks(){const head=['任务','项目','负责人','计划开始','计划结束','进度','状态'],rows=data.items.filter(row=>row.item_type==='task').map(row=>[row.name,projectName(row.project_id),userName(row.owner_id),row.planned_start_on,row.planned_end_on,Number(row.progress||0)+'%',row.status]);downloadCsv('项目任务.csv',[head,...rows])}
  function downloadCsv(filename,rows){const csv=rows.map(row=>row.map(value=>'"'+String(value??'').replaceAll('"','""')+'"').join(',')).join('\\n'),blob=new Blob(['\\ufeff'+csv],{type:'text/csv;charset=utf-8'}),url=URL.createObjectURL(blob),anchor=document.createElement('a');anchor.href=url;anchor.download=filename;anchor.click();URL.revokeObjectURL(url)}
  const decisionLabel={project_record_timesheet:'保存工时草稿',project_timesheet_submit:'提交工时审核',project_timesheet_approve:'审核通过',project_timesheet_reject:'驳回工时'},decisionMessage=current=>current.step!=='confirm'?'请核对工时记录与业务影响，下一步进入最终确认。':current.action==='project_timesheet_reject'?'驳回后不会形成项目人工成本，审核意见将保留。':current.action==='project_timesheet_approve'?'审核通过后将形成可追溯的项目人工成本。':current.action==='project_timesheet_submit'?'提交后进入待审核状态，需项目负责人处理。':'保存后形成草稿，可继续提交审核。';
  if(data.loading)return <div className="forge-product forge-time"><style>{css}</style><ForgeLoading label="正在加载工时管理"/></div>;
  return <><div className="forge-product forge-work-page forge-time"><style>{css}</style><div className="time-shell">
    <header className="time-header"><div><div className="time-crumb"><Icon icon="briefcase-business" size={14}/><span>项目管理</span><span>/</span><strong>工时管理</strong></div><h1>工时管理</h1><p>记录项目工时，审核通过后自动进入项目人工成本。</p></div><div className="time-actions time-header-actions"><div className="time-export-group"><button className="fp-button time-header-button" onClick={downloadTimesheets}><Icon icon="download" size={14}/><span>导出工时</span></button><button className="fp-button time-header-button" onClick={downloadTasks}><Icon icon="list-checks" size={14}/><span>导出任务</span></button></div><button className="fp-button primary time-header-button" onClick={openForm}><Icon icon="plus" size={15}/><span>填报工时</span></button></div></header>
    {data.errors.length>0&&<ForgeNotice tone="error" onClose={()=>setData(current=>({...current,errors:[]}))}>{data.errors.join('；')}</ForgeNotice>}
    <section className="fp-card"><div className="time-range"><span className="time-range-label"><Icon icon="calendar-range" size={15}/> 日期范围</span><ForgeDateInput aria-label="工时开始日期" value={rangeStart} onChange={event=>setRangeStart(event.target.value)}/><span className="fp-secondary">至</span><ForgeDateInput aria-label="工时结束日期" value={rangeEnd} onChange={event=>setRangeEnd(event.target.value)}/><button className="fp-button small" onClick={()=>{setRangeStart(defaultStart);setRangeEnd(defaultEnd)}}>本周</button></div></section>
    <div className="time-kpis"><div className="time-kpi"><div className="time-kpi-head"><Icon icon="clock-3" size={14}/><span>总工时</span></div><strong>{totalHours.toFixed(2)}h</strong><small>{filteredSheets.length} 条记录</small></div><div className="time-kpi"><div className="time-kpi-head"><Icon icon="trending-up" size={14}/><span>工时成本</span></div><strong>{money(totalCost)}</strong><small>平均 {money(averageRate)}/h</small></div><div className="time-kpi"><div className="time-kpi-head"><Icon icon="badge-check" size={14}/><span>已通过工时</span></div><strong>{approvedHours.toFixed(2)}h</strong><small>审核后计入成本</small></div><div className="time-kpi"><div className="time-kpi-head"><Icon icon="circle-alert" size={14}/><span>待审核</span></div><strong>{pending}</strong><small>需项目负责人处理</small></div></div>
    <section className="fp-card"><div className="time-chart"><div className="time-chart-head"><h2>工时分布</h2><span>{rangeStart} ~ {rangeEnd}</span></div><div className="time-bars">{days.map(day=><div className="time-day" key={day.value}><b>{day.hours?day.hours+'h':''}</b><div className="time-bar-track"><div className="time-bar" style={{height:Math.max(2,day.hours/maxDay*100)+'%'}}></div></div><span>{day.label}</span></div>)}</div></div></section>
    {showForm&&<section className="fp-card time-form-card"><div className="time-form-head"><div><h2>填报项目工时</h2><p>填报人默认取当前登录用户，保存后形成草稿。</p></div><button className="fp-button small" onClick={()=>setShowForm(false)}>收起</button></div><div className="time-form"><div className="fp-field"><label>项目 <span className="fp-required">*</span></label><ForgeSelectControl aria-label="工时项目" value={form.project_id} onChange={event=>setForm({...form,project_id:event.target.value,work_item_id:''})}><option value="">请选择项目</option>{data.projects.map(row=><option key={row.id} value={row.id}>{row.code} · {row.name}</option>)}</ForgeSelectControl></div><div className="fp-field"><label>人员 <span className="fp-required">*</span></label><ForgeSelectControl aria-label="工时人员" value={form.worker_id} onChange={event=>setForm({...form,worker_id:event.target.value})}><option value="">请选择人员</option>{availableUsers.map(row=><option key={row.id} value={row.id}>{userName(row.id)}</option>)}</ForgeSelectControl></div><div className="fp-field"><label>日期 <span className="fp-required">*</span></label><ForgeDateInput aria-label="工时日期" value={form.work_on} onChange={event=>setForm({...form,work_on:event.target.value})}/></div><div className="fp-field"><label>关联任务</label><ForgeSelectControl aria-label="关联任务" value={form.work_item_id} onChange={event=>setForm({...form,work_item_id:event.target.value})}><option value="">不关联具体任务</option>{projectItems.map(row=><option key={row.id} value={row.id}>{row.name}</option>)}</ForgeSelectControl></div><div className="fp-field wide"><label>工作内容 <span className="fp-required">*</span></label><textarea className="fp-textarea" aria-label="工作内容" value={form.work_content} onChange={event=>setForm({...form,work_content:event.target.value})}/></div><div className="fp-field"><label>工时类型</label><ForgeSelectControl aria-label="工时类型" value={form.time_type} onChange={event=>setForm({...form,time_type:event.target.value})}><option value="normal">正常</option><option value="overtime">加班</option><option value="travel">出差</option></ForgeSelectControl></div><div className="fp-field"><label>工时(h) <span className="fp-required">*</span></label><input className="fp-input" aria-label="工时" type="number" min="0.25" max="24" step="0.25" value={form.hours} onChange={event=>setForm({...form,hours:event.target.value})}/></div><div className="fp-field"><label>费率(元/h) <span className="fp-required">*</span></label><input className="fp-input" aria-label="工时费率" type="number" min="0" step="0.01" value={form.hourly_rate} onChange={event=>setForm({...form,hourly_rate:event.target.value})}/></div><div className="fp-field" style={{display:'flex',justifyContent:'flex-end'}}><label>&nbsp;</label><button className="fp-button primary" disabled={busy} onClick={saveDraft}>保存草稿</button></div></div><div className="time-limit">RISEMAP 已观察到非项目工时、闲置工时和批量模板；Forge 当前数据模型只支持项目工时，本页未把未实现能力包装成可点击入口。</div></section>}
    <section className="fp-card"><div className="time-record-head"><div className="time-tabs">{[['all','全部'],['pending_review','待审核'],['approved','已通过'],['rejected','已驳回']].map(([value,label])=><button className={'time-tab '+(reviewStatus===value?'active':'')} key={value} onClick={()=>setReviewStatus(value)}>{label}</button>)}</div><span className="fp-secondary">工时记录 {filteredSheets.length}</span></div><div className="time-filter"><ForgeSelectControl aria-label="工时项目筛选" value={projectFilter} onChange={event=>setProjectFilter(event.target.value)}><option value="">全部项目</option>{data.projects.map(row=><option key={row.id} value={row.id}>{row.code} · {row.name}</option>)}</ForgeSelectControl>{filteredSheets.some(row=>row.status==='pending_review')&&<input className="fp-input time-review" aria-label="工时审核意见" placeholder="填写审核意见后处理待审核记录" value={form.review_comment} onChange={event=>setForm({...form,review_comment:event.target.value})}/>}</div><div className="fp-table-wrap"><table className="fp-table" style={{minWidth:1100}}><thead><tr><th>工时单号</th><th>日期</th><th>人员</th><th>关联项目</th><th>工作内容</th><th>工时</th><th>类型</th><th>费率</th><th>工时成本</th><th>状态</th><th>操作</th></tr></thead><tbody>{filteredSheets.map(row=><tr key={row.id}><td>{row.code}</td><td>{row.work_on}</td><td>{userName(row.worker_id)}</td><td>{projectName(row.project_id)}</td><td>{row.work_content}</td><td>{row.hours}h</td><td>{timeType[row.time_type]||row.time_type||'—'}</td><td>{money(row.hourly_rate)}</td><td>{money(row.cost_amount)}</td><td><span className={'fp-status '+(row.status==='approved'?'success':row.status==='rejected'?'danger':'warning')}>{statusText[row.status]||row.status}</span></td><td><div className="time-actions">{row.status==='draft'&&<button className="fp-button small primary" disabled={busy} onClick={()=>ask('project_timesheet_submit',row.id,row.code)}>提交审核</button>}{row.status==='pending_review'&&<><button className="fp-button small primary" disabled={busy||!form.review_comment.trim()} onClick={()=>ask('project_timesheet_approve',row.id,row.code,{review_comment:form.review_comment})}>通过</button><button className="fp-button small danger" disabled={busy||!form.review_comment.trim()} onClick={()=>ask('project_timesheet_reject',row.id,row.code,{review_comment:form.review_comment},true)}>驳回</button></>}</div></td></tr>)}{!filteredSheets.length&&<tr><td className="time-table-empty" colSpan="11">当前条件下暂无工时记录</td></tr>}</tbody></table></div><div className="fp-pagination"><span>共 {filteredSheets.length} 条记录 · 每页 20 条</span><span>1 / 1</span></div></section>
    <section className="fp-card time-cost"><div className="time-cost-head"><h2>人工成本归集</h2><span className="fp-secondary">已审核工时形成的成本记录</span></div><div className="fp-table-wrap"><table className="fp-table"><thead><tr><th>成本编号</th><th>来源工时</th><th>发生日期</th><th>总额</th><th>已归集</th><th>剩余</th><th>状态</th></tr></thead><tbody>{projectCosts.map(row=><tr key={row.id}><td>{row.code}</td><td>{data.timesheets.find(sheet=>sheet.id===row.source_id)?.code||'—'}</td><td>{row.occurred_on}</td><td>{money(row.total_amount)}</td><td>{money(row.allocated_amount)}</td><td>{money(row.remaining_amount)}</td><td><span className="fp-status success">{statusText[row.status]||row.status}</span></td></tr>)}{!projectCosts.length&&<tr><td className="time-table-empty" colSpan="7">暂无人工成本归集记录</td></tr>}</tbody></table></div></section>
  </div></div>{decision&&<ForgeDialog open title={(decision.step==='confirm'?'确认':'')+decisionLabel[decision.action]} subtitle={decision.name} danger={decision.danger} busy={busy} confirmLabel={decision.step==='confirm'?'确认执行':'下一步'} onCancel={()=>!busy&&setDecision(null)} onConfirm={confirmDecision}><div className="fp-secondary">{decisionMessage(decision)}</div></ForgeDialog>}</>;
}
export default App;
${forgeProductUiRuntime}
`;

export const ProjectTimesheetCostPage = { name: 'page_project_timesheet_cost', label: '工时管理', icon: 'clock-3', type: 'app' as const, kind: 'react' as const, source: projectTimesheetCostSource };
