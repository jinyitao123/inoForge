import { forgeProductUiCss, forgeProductUiRuntime } from './product-ui.js';

const loanCss = JSON.stringify(forgeProductUiCss + `
.loan-page .fp-shell{width:min(1460px,100%);padding-top:20px}.loan-page .loan-hero{position:relative;overflow:hidden;display:flex;align-items:flex-end;min-height:108px;margin-bottom:12px;padding:20px;border-radius:12px;background:linear-gradient(112deg,#22664d 0%,#3e875d 58%,#5d9b62 100%);color:#fff}.loan-page .loan-hero:after{content:'➜';position:absolute;right:44px;top:26px;color:rgba(255,255,255,.32);font-size:42px}.loan-page .loan-hero-main{display:flex;align-items:center;gap:13px;position:relative;z-index:1}.loan-page .loan-hero-icon{display:grid;place-items:center;width:46px;height:46px;border-radius:11px;background:rgba(255,255,255,.16)}.loan-page .loan-hero-icon svg{width:25px;height:25px}.loan-page .loan-hero h1{margin:0;font-size:24px}.loan-page .loan-hero p{margin:3px 0 0;color:#eefbf3}.loan-page .loan-tabs{display:flex;gap:2px;overflow:auto;margin-bottom:14px;padding:4px;border:1px solid var(--fp-line);border-radius:8px;background:#fff}.loan-page .loan-tab{display:flex;align-items:center;gap:5px;padding:7px 12px;border:0;border-radius:6px;background:transparent;color:var(--fp-muted);white-space:nowrap;cursor:pointer}.loan-page .loan-tab.active{background:#eef3ff;color:#245bdb;font-weight:650}.loan-page .loan-count{font-size:11px}.loan-page .loan-toolbar{display:flex;align-items:center;gap:8px;padding:11px 14px;border-bottom:1px solid var(--fp-line)}.loan-page .loan-toolbar-end{display:flex;gap:7px;margin-left:auto}.loan-page .loan-filter{display:grid;grid-template-columns:minmax(280px,1fr) minmax(130px,180px) minmax(130px,180px) auto;gap:9px;padding:10px 14px;border-bottom:1px solid var(--fp-line)}.loan-page .fp-table{min-width:1180px}.loan-page .loan-direction{display:flex;align-items:center;gap:6px;font-weight:650}.loan-page .loan-direction svg{width:16px;height:16px}.loan-page .loan-footer{display:flex;justify-content:space-between;align-items:center;padding:12px 14px;color:var(--fp-muted);font-size:13px}.loan-page .loan-pages{display:flex;align-items:center;gap:6px}.loan-page .loan-page-current{display:grid;place-items:center;width:30px;height:30px;border-radius:6px;background:#245bdb;color:#fff}.loan-page .loan-rules{display:grid;grid-template-columns:auto 1fr;gap:10px;margin-top:14px;padding:14px;border:1px solid #cfe7d8;border-radius:9px;background:#f3fbf6;color:#315d45}.loan-page .loan-rules svg{width:20px;height:20px}.loan-page .loan-rules strong{display:block;margin-bottom:5px}.loan-page .loan-rules p{margin:3px 0;font-size:12px;line-height:1.55}.loan-page .loan-direction-cards,.loan-page .loan-object-types,.loan-page .loan-reasons{display:flex;gap:8px;flex-wrap:wrap}.loan-page .loan-direction-card{flex:1;min-width:210px;padding:12px;border:1px solid var(--fp-line-strong);border-radius:8px;background:#fff;text-align:left;cursor:pointer}.loan-page .loan-direction-card.active{border-color:#245bdb;box-shadow:0 0 0 1px #245bdb}.loan-page .loan-direction-card strong,.loan-page .loan-direction-card span{display:block}.loan-page .loan-direction-card span{margin-top:3px;color:var(--fp-muted);font-size:12px}.loan-page .loan-choice{padding:7px 10px;border:1px solid var(--fp-line);border-radius:6px;background:#fff;cursor:pointer}.loan-page .loan-choice.active{border-color:#245bdb;background:#eef3ff;color:#245bdb}.loan-page .loan-line{display:grid;grid-template-columns:minmax(220px,1fr) 90px 100px 34px;gap:8px;align-items:center;margin-bottom:8px}.loan-page .loan-line-remove{border:0;background:transparent;color:var(--fp-muted);font-size:20px;cursor:pointer}.loan-page .loan-add-line{margin-bottom:4px}.loan-page .loan-dialog-actions{display:flex;justify-content:flex-end;margin-top:12px}.loan-page .fp-modal{width:min(780px,100%)}@media(max-width:860px){.loan-page .loan-filter{grid-template-columns:1fr 1fr}.loan-page .loan-footer{align-items:flex-start;gap:10px;flex-direction:column}}@media(max-width:680px){.loan-page .loan-hero{min-height:104px}.loan-page .loan-hero h1{font-size:21px}.loan-page .loan-toolbar{align-items:flex-start;flex-wrap:wrap}.loan-page .loan-toolbar-end{margin-left:0}.loan-page .loan-filter,.loan-page .fp-detail-grid{grid-template-columns:1fr}.loan-page .loan-line{grid-template-columns:1fr 80px 90px 30px}.loan-page .loan-rules{grid-template-columns:1fr}.loan-page .loan-direction-card{min-width:100%}}
.loan-page .fp-empty-cell .fp-empty{position:sticky;left:0;width:min(1000px,calc(100vw - 380px));max-width:100%}@media(max-width:680px){.loan-page td.fp-empty-cell{position:sticky;left:0;display:block;width:calc(100vw - 48px);box-sizing:border-box}.loan-page .fp-empty-cell .fp-empty{width:100%;max-width:none;transform:none}}
`);

const loanManagementSourceBase = `
const css=${loanCss};
const LoanIcon=({name='loan'})=>{const p={loan:<><path d="M4 12h12M11 5l7 7-7 7"/><circle cx="5" cy="12" r="3"/></>,refresh:<><path d="M21 12a9 9 0 1 1-3-6.7"/><path d="M21 3v6h-6"/></>,info:<><circle cx="12" cy="12" r="9"/><path d="M12 11v5M12 8h.01"/></>,out:<><path d="M5 12h14M14 7l5 5-5 5"/></>,in:<><path d="M19 12H5M10 7l-5 5 5 5"/></>};return <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="1.8" strokeLinecap="round" strokeLinejoin="round" aria-hidden="true">{p[name]||p.loan}</svg>};
const TABS=[['全部','all'],['草稿','draft'],['待审批','pending'],['借用中','in_progress'],['逾期未还','overdue'],['已归还','completed'],['已驳回','rejected']],statusText={draft:'草稿',pending:'待审批',in_progress:'借用中',overdue:'逾期未还',completed:'已归还',rejected:'已驳回'},objectTypes=['客户','内部员工','供应商','其他单位'],reasons=['测试使用','客户试用借出','项目现场借用','维修借用'],units=['台','套','个','件','卷'];
function App(){const adapter=useAdapter(),[state,setState]=React.useState({loading:true,rows:[],error:''}),[tab,setTab]=React.useState('all'),[q,setQ]=React.useState(''),[direction,setDirection]=React.useState('all'),[objectType,setObjectType]=React.useState('all'),[dialog,setDialog]=React.useState(null),[busy,setBusy]=React.useState(false),[toast,setToast]=React.useState('');
async function request(path,options={}){const headers={'Content-Type':'application/json',...(adapter?.getAuthHeaders?.()||{}),...(options.headers||{})},raw=String(adapter?.baseUrl||''),base=raw.endsWith('/')?raw.slice(0,-1):raw,transport=adapter?.fetchImpl||fetch,response=await transport(base+'/api/v1'+path,{credentials:'include',...options,headers}),payload=await response.json().catch(()=>({}));if(!response.ok)throw new Error((typeof payload.error==='string'?payload.error:payload.error?.message)||payload.message||'请求失败');return payload}
async function load(){setState(s=>({...s,loading:true,error:''}));try{const p=await request('/data/forge_administration_record?$top=500'),all=p.records||p.data?.records||[];setState({loading:false,rows:all.filter(x=>x.page_key==='loan_management'),error:''})}catch(e){setState(s=>({...s,loading:false,error:String(e.message||e)}))}}
React.useEffect(()=>{load()},[]);function meta(x){try{return JSON.parse(x.details||'{}')}catch{return {}}}function effectiveStatus(x){return x.status==='in_progress'&&x.end_on&&x.end_on<new Date().toISOString().slice(0,10)?'overdue':x.status}let rows=state.rows.filter(x=>(tab==='all'||effectiveStatus(x)===tab)&&(direction==='all'||meta(x).direction===direction)&&(objectType==='all'||meta(x).object_type===objectType)&&(!q.trim()||Object.values({...x,...meta(x)}).some(v=>String(v||'').toLowerCase().includes(q.trim().toLowerCase()))));const counts=Object.fromEntries(TABS.map(x=>[x[1],x[1]==='all'?state.rows.length:state.rows.filter(r=>effectiveStatus(r)===x[1]).length]));
function reset(){setQ('');setDirection('all');setObjectType('all')}function open(){setDialog({direction:'out',object_type:'客户',object_name:'',items:[{name:'',quantity:'1',unit:'台'}],reason:'测试使用',custom_reason:'',start_on:new Date().toISOString().slice(0,10),end_on:new Date(Date.now()+30*86400000).toISOString().slice(0,10),remarks:'',error:''})}function updateLine(i,key,value){setDialog(d=>({...d,items:d.items.map((x,n)=>n===i?{...x,[key]:value}:x)}))}function addLine(){setDialog(d=>({...d,items:[...d.items,{name:'',quantity:'1',unit:'台'}]}))}function removeLine(i){setDialog(d=>({...d,items:d.items.filter((_,n)=>n!==i)}))}
async function save(status){const item=dialog?.items?.find(x=>!x.name.trim());if(!dialog?.object_name.trim())return setDialog(d=>({...d,error:'请选择或填写借用对象'}));if(item)return setDialog(d=>({...d,error:'请填写全部物品名称或型号'}));if(dialog.items.some(x=>Number(x.quantity)<=0))return setDialog(d=>({...d,error:'物品数量必须大于 0'}));if(!dialog.reason&&!dialog.custom_reason.trim())return setDialog(d=>({...d,error:'请选择或填写借用原因'}));if(!dialog.start_on||!dialog.end_on)return setDialog(d=>({...d,error:'请选择借用日期和预计归还日期'}));if(dialog.end_on<dialog.start_on)return setDialog(d=>({...d,error:'预计归还日期不能早于借用日期'}));setBusy(true);try{const code=(dialog.direction==='out'?'LO':'LI')+'-'+Date.now().toString().slice(-8),details=JSON.stringify({direction:dialog.direction,object_type:dialog.object_type,object_name:dialog.object_name.trim(),items:dialog.items,reason:dialog.custom_reason.trim()||dialog.reason,actual_return:null});await request('/data/forge_administration_record',{method:'POST',body:JSON.stringify({page_key:'loan_management',code,title:dialog.items.map(x=>x.name+' × '+x.quantity+x.unit).join('；'),category:dialog.direction==='out'?'借出':'借入',status,owner_name:'Dev Admin',start_on:dialog.start_on,end_on:dialog.end_on,quantity:dialog.items.reduce((s,x)=>s+Number(x.quantity||0),0),details,remarks:dialog.remarks.trim()||null})});setDialog(null);setToast(status==='draft'?'借用单草稿已保存':'借用单已提交审批');await load()}catch(e){setDialog(d=>({...d,error:String(e.message||e)}))}finally{setBusy(false)}}
function exportRows(){if(!rows.length)return;const heads=['单号','方向','物品明细','借用对象','原因','借用日期','预计归还','实际归还','经办人','状态'],body=rows.map(x=>{const m=meta(x);return [x.code,m.direction==='in'?'借入':'借出',x.title,m.object_type+' / '+m.object_name,m.reason,x.start_on,x.end_on,m.actual_return,x.owner_name,statusText[x.status]||x.status].map(y=>'"'+String(y??'').replaceAll('"','""')+'"').join(',')});const blob=new Blob(['\\ufeff'+[heads.join(','),...body].join('\\n')],{type:'text/csv;charset=utf-8'}),url=URL.createObjectURL(blob),a=document.createElement('a');a.href=url;a.download='借出管理.csv';a.click();URL.revokeObjectURL(url)}
if(state.loading)return <div className="forge-product loan-page"><style>{css}</style><div className="fp-shell"><ForgeLoading label="正在加载借出管理"/></div></div>;
return <div className="forge-product loan-page"><style>{css}</style><div className="fp-shell"><section className="loan-hero"><div className="loan-hero-main"><span className="loan-hero-icon"><LoanIcon/></span><div><div className="fp-eyebrow" style={{color:'#eefbf3'}}>行政管理 / 借出管理</div><h1>借出管理</h1><p>物品借出与外部借入（客户 / 内部员工 / 供应商 / 其他单位）的登记、审批、归还核销</p></div></div></section>{state.error&&<ForgeNotice tone="error">{state.error}</ForgeNotice>}{toast&&<ForgeNotice tone="success" onClose={()=>setToast('')}>{toast}</ForgeNotice>}<div className="loan-tabs">{TABS.map(x=><button key={x[1]} className={'loan-tab '+(tab===x[1]?'active':'')} onClick={()=>setTab(x[1])}>{x[0]} <span className="loan-count">{counts[x[1]]}</span></button>)}</div><section className="fp-card"><div className="loan-toolbar"><button className="fp-button" disabled={!rows.length} onClick={exportRows}>⇅ 导出</button><button className="fp-button primary" onClick={open}>＋ 新建借用单</button><div className="loan-toolbar-end"><button className="fp-icon-button" aria-label="刷新" title="刷新" onClick={load}><LoanIcon name="refresh"/></button><ForgeListSettings/></div></div><div className="loan-filter"><input className="fp-input fp-search" aria-label="搜索" placeholder="搜索单号 / 物品 / 对象..." value={q} onChange={e=>setQ(e.target.value)}/><ForgeSelectControl aria-label="方向" value={direction} onChange={e=>setDirection(e.target.value)}><option value="all">全部方向</option><option value="out">借出</option><option value="in">借入</option></ForgeSelectControl><ForgeSelectControl aria-label="对象" value={objectType} onChange={e=>setObjectType(e.target.value)}><option value="all">全部对象</option>{objectTypes.map(x=><option key={x}>{x}</option>)}</ForgeSelectControl><button className="fp-button" onClick={reset}>重置</button></div><div className="fp-table-wrap"><table className="fp-table"><thead><tr>{['单号 / 方向','物品明细','借用对象','原因','借用 / 预计归还','实际归还','经办人','状态','操作'].map(x=><th key={x}>{x}</th>)}</tr></thead><tbody>{rows.length?rows.map(x=>{const m=meta(x);return <tr key={x.id}><td><div>{x.code||'—'}</div><span className="loan-direction"><LoanIcon name={m.direction==='in'?'in':'out'}/>{m.direction==='in'?'借入':'借出'}</span></td><td>{x.title}</td><td>{m.object_type} / {m.object_name}</td><td>{m.reason||'—'}</td><td>{x.start_on||'—'} / {x.end_on||'—'}</td><td>{m.actual_return||'—'}</td><td>{x.owner_name||'—'}</td><td><ForgeStatus value={x.status} label={statusText[x.status]||x.status}/></td><td><button className="fp-button small" onClick={()=>setDialog({kind:'view',row:x,extra:m})}>查看</button></td></tr>}):<tr><td colSpan="9" className="fp-empty-cell"><ForgeEmpty title="暂无借用记录" description="新建借用单后可跟踪审批、借用、逾期与归还核销。" action={<button className="fp-button primary" onClick={open}>新建借用单</button>}/></td></tr>}</tbody></table></div><div className="loan-footer"><span>共 {rows.length} 条记录 · 当前页借出 {rows.filter(x=>meta(x).direction!=='in').length} · 借入 {rows.filter(x=>meta(x).direction==='in').length} · 每页 10 条</span><div className="loan-pages"><span className="loan-page-current">1</span><span>第 1 页</span></div></div></section><aside className="loan-rules"><LoanIcon name="info"/><div><strong>借用规则</strong><p>借入：来源不限供应商，客户设备送检、合作方调借、员工个人物品均可登记；只记数量、不计成本、不产生应付，可转借。</p><p>借出与借入：对象类型统一为客户、内部员工、供应商和其他单位；借给同事不产生往来挂账，物品挂个人名下，逾期提醒到人。</p><p>归还核销：借出物品归还后核销借出单；借测物品归还来源方后核销借入单；借用原因可在系统业务设置维护。</p></div></aside></div><ForgeDialog open={!!dialog&&dialog.kind!=='view'} title="新建借用单" subtitle="提交后进入审批流，审批通过后借用生效" error={dialog?.error} busy={busy} confirmLabel="提交审批" onCancel={()=>!busy&&setDialog(null)} onConfirm={()=>save('pending')}><div className="fp-field"><label>业务方向 *</label><div className="loan-direction-cards">{[['out','借出','借给客户、员工、供应商或其他单位'],['in','借入','从供应商、客户、合作方等借入']].map(x=><button key={x[0]} className={'loan-direction-card '+(dialog?.direction===x[0]?'active':'')} onClick={()=>setDialog(d=>({...d,direction:x[0]}))}><strong>{x[1]}</strong><span>{x[2]}</span></button>)}</div></div><div className="fp-field"><label>{dialog?.direction==='in'?'借入来源':'借出对象'} *</label><div className="loan-object-types">{objectTypes.map(x=><button key={x} className={'loan-choice '+(dialog?.object_type===x?'active':'')} onClick={()=>setDialog(d=>({...d,object_type:x}))}>{x}</button>)}</div><input className="fp-input" aria-label="借用对象" placeholder={'搜索或填写'+(dialog?.object_type||'对象')+'名称...'} value={dialog?.object_name||''} onChange={e=>setDialog(d=>({...d,object_name:e.target.value}))}/></div><div className="fp-field"><label>物品明细 *</label>{dialog?.items.map((line,i)=><div className="loan-line" key={i}><input className="fp-input" aria-label={'物品名称 '+(i+1)} placeholder="物品名称 / 型号" value={line.name} onChange={e=>updateLine(i,'name',e.target.value)}/><input className="fp-input" aria-label={'数量 '+(i+1)} type="number" min="1" value={line.quantity} onChange={e=>updateLine(i,'quantity',e.target.value)}/><ForgeSelectControl aria-label={'单位 '+(i+1)} value={line.unit} onChange={e=>updateLine(i,'unit',e.target.value)}>{units.map(x=><option key={x}>{x}</option>)}</ForgeSelectControl><button className="loan-line-remove" aria-label={'删除物品 '+(i+1)} disabled={dialog.items.length===1} onClick={()=>removeLine(i)}>×</button></div>)}<button className="fp-button loan-add-line" onClick={addLine}>＋ 添加物品</button></div><div className="fp-field"><label>借用原因 *</label><div className="loan-reasons">{reasons.map(x=><button key={x} className={'loan-choice '+(dialog?.reason===x?'active':'')} onClick={()=>setDialog(d=>({...d,reason:x,custom_reason:''}))}>{x}</button>)}</div><input className="fp-input" aria-label="自定义原因" placeholder="或输入自定义原因..." value={dialog?.custom_reason||''} onChange={e=>setDialog(d=>({...d,custom_reason:e.target.value,reason:''}))}/></div><div className="fp-detail-grid"><div className="fp-field"><label>借用日期 *</label><ForgeDateInput aria-label="借用日期" value={dialog?.start_on||''} onChange={e=>setDialog(d=>({...d,start_on:e.target.value}))}/></div><div className="fp-field"><label>预计归还日期 *</label><ForgeDateInput aria-label="预计归还日期" min={dialog?.start_on||''} value={dialog?.end_on||''} onChange={e=>setDialog(d=>({...d,end_on:e.target.value}))}/></div></div><div className="fp-field"><label>备注</label><textarea className="fp-input" rows="3" value={dialog?.remarks||''} onChange={e=>setDialog(d=>({...d,remarks:e.target.value}))}/></div><div className="loan-dialog-actions"><button className="fp-button" disabled={busy} onClick={()=>save('draft')}>保存草稿</button></div></ForgeDialog><ForgeDialog open={dialog?.kind==='view'} title={dialog?.row?.title||'借用单详情'} subtitle={dialog?.row?.code||''} confirmLabel="关闭" hideCancel onCancel={()=>setDialog(null)} onConfirm={()=>setDialog(null)}><div className="fp-detail-grid">{[['借用对象','department'],['经办人','owner_name'],['状态','status'],['借用日期','start_on'],['预计归还','end_on'],['数量','quantity']].map(([label,key])=><div className="fp-field" key={key}><label>{label}</label><div>{dialog?.row?.[key]??'—'}</div></div>)}</div></ForgeDialog></div>}
export default App;${forgeProductUiRuntime}`;

export const loanManagementSource = loanManagementSourceBase
  .replace(
    '<ForgeStatus value={x.status} label={statusText[x.status]||x.status}/>',
    '<ForgeStatus value={effectiveStatus(x)} label={statusText[effectiveStatus(x)]||effectiveStatus(x)}/>',
  )
  .replace(
    'confirmLabel="提交审批" onCancel=',
    "confirmLabel=\"提交审批\" secondaryLabel=\"保存草稿\" onSecondary={()=>save('draft')} onCancel=",
  )
  .replace(
    "<div className=\"loan-dialog-actions\"><button className=\"fp-button\" disabled={busy} onClick={()=>save('draft')}>保存草稿</button></div>",
    '',
  );
