import { forgeProductUiCss, forgeProductUiRuntime } from './product-ui.js';
import { productionDataTaskHelpers } from './production-data-task.page.js';

const subcontractCss = `
.forge-subcontract .sc-tabs{margin-bottom:14px}.forge-subcontract .sc-kpis{display:grid;grid-template-columns:repeat(5,minmax(0,1fr));gap:12px;margin-bottom:14px}.forge-subcontract .sc-kpi{padding:16px}.forge-subcontract .sc-kpi span{display:block;color:var(--fp-muted);font-size:12px}.forge-subcontract .sc-kpi strong{display:block;font-size:23px;margin:5px 0}.forge-subcontract .sc-kpi small{color:var(--fp-muted)}.forge-subcontract .sc-title h2{margin:0 0 5px;font-size:22px}.forge-subcontract .sc-title p{margin:0;color:var(--fp-muted)}.forge-subcontract .sc-actions{display:flex;gap:8px;align-items:center;flex-wrap:wrap}.forge-subcontract .sc-card-grid{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:12px}.forge-subcontract .sc-supplier{padding:17px}.forge-subcontract .sc-supplier h3{margin:0 0 3px;font-size:15px}.forge-subcontract .sc-meta{color:var(--fp-muted);font-size:12px}.forge-subcontract .sc-rate{display:grid;grid-template-columns:repeat(3,1fr);gap:8px;margin:14px 0}.forge-subcontract .sc-rate div{padding:9px;border-radius:8px;background:#f7f9fc}.forge-subcontract .sc-rate span{display:block;color:var(--fp-muted);font-size:11px}.forge-subcontract .sc-rate strong{display:block;margin-top:3px}.forge-subcontract .sc-create-top{display:flex;align-items:center;justify-content:space-between;gap:16px;margin-bottom:14px;padding:18px;border-radius:13px;background:linear-gradient(120deg,#4051b5,#245bdb);color:#fff}.forge-subcontract .sc-create-top h2{margin:0;font-size:23px}.forge-subcontract .sc-create-top p{margin:3px 0 0;color:#dbe5ff}.forge-subcontract .sc-create-top .fp-button{background:rgba(255,255,255,.96)}.forge-subcontract .sc-section{padding:19px;margin-bottom:14px}.forge-subcontract .sc-section h3{margin:0 0 15px;padding-left:10px;border-left:3px solid var(--fp-primary);font-size:15px}.forge-subcontract .sc-form{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:14px 18px}.forge-subcontract .sc-span{grid-column:1/-1}.forge-subcontract .sc-radio{display:flex;gap:8px}.forge-subcontract .sc-radio label{display:flex;align-items:center;gap:6px;flex:1;border:1px solid var(--fp-line-strong);padding:9px 12px;border-radius:8px}.forge-subcontract .sc-lines{overflow:auto}.forge-subcontract .sc-lines table{min-width:1120px}.forge-subcontract .sc-lines input,.forge-subcontract .sc-lines select{width:100%;min-width:100px;border:1px solid var(--fp-line-strong);border-radius:7px;padding:7px}.forge-subcontract .sc-note{margin-top:11px;color:var(--fp-muted);font-size:12px}.forge-subcontract .sc-prereq{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:10px}.forge-subcontract .sc-prereq-item{padding:12px;border:1px solid var(--fp-line);border-radius:9px;background:#fafbfc}.forge-subcontract .sc-prereq-item strong,.forge-subcontract .sc-prereq-item span{display:block}.forge-subcontract .sc-prereq-item span{margin-top:4px;color:var(--fp-muted);font-size:12px}.forge-subcontract .sc-money{font-variant-numeric:tabular-nums;font-weight:650}.forge-subcontract .sc-progress{font-size:11px;color:var(--fp-muted)}.forge-subcontract .sc-overview{display:flex;gap:18px;flex-wrap:wrap;margin:-5px 0 14px;color:var(--fp-muted);font-size:12px}.forge-subcontract .sc-overview strong{color:var(--fp-text);font-size:14px}.forge-subcontract .sc-dashboard-grid{display:grid;grid-template-columns:minmax(260px,.8fr) minmax(420px,1.6fr);gap:12px;margin-bottom:14px}.forge-subcontract .sc-dashboard-card{padding:17px}.forge-subcontract .sc-dashboard-card h3{margin:0 0 12px;font-size:15px}.forge-subcontract .sc-task-list,.forge-subcontract .sc-order-list{display:grid;gap:8px}.forge-subcontract .sc-task,.forge-subcontract .sc-order-row{display:flex;justify-content:space-between;align-items:center;gap:12px;padding:10px 0;border-bottom:1px solid var(--fp-line)}.forge-subcontract .sc-task:last-child,.forge-subcontract .sc-order-row:last-child{border-bottom:0}.forge-subcontract .sc-quick{display:grid;grid-template-columns:repeat(5,minmax(0,1fr));gap:8px}.forge-subcontract .sc-quick .fp-button{justify-content:center}.forge-subcontract .sc-dashboard-empty{padding:18px;text-align:center;color:var(--fp-muted)}@media(max-width:960px){.forge-subcontract .sc-kpis,.forge-subcontract .sc-card-grid,.forge-subcontract .sc-quick,.forge-subcontract .sc-prereq{grid-template-columns:repeat(2,1fr)}.forge-subcontract .sc-dashboard-grid{grid-template-columns:1fr}}@media(max-width:700px){.forge-subcontract .sc-form,.forge-subcontract .sc-kpis,.forge-subcontract .sc-card-grid,.forge-subcontract .sc-prereq{grid-template-columns:1fr}.forge-subcontract .sc-span{grid-column:auto}.forge-subcontract .sc-create-top{align-items:flex-start;flex-direction:column}}
`;

const subcontractSource = `
const {useEffect,useMemo,useState}=React;
const css=${JSON.stringify(forgeProductUiCss + subcontractCss + '.forge-subcontract .sc-kpi-button{display:block;width:100%;text-align:left;color:inherit;border:0;cursor:pointer}.forge-subcontract .sc-kpi-button:hover{box-shadow:0 0 0 2px var(--fp-primary) inset}')};
${forgeProductUiRuntime}
${productionDataTaskHelpers}
function App(){const adapter=useAdapter();
 const standaloneSupplierPage=window.location.pathname.includes('page_subcontract_suppliers'),urlParams=new URLSearchParams(window.location.search),pageView=standaloneSupplierPage?'委外供应商':window.location.pathname.includes('page_subcontract_dashboard')?'看板':'委外订单';
 const [state,setState]=useState({loading:true,error:'',data:{}}),[view,setView]=useState(pageView),[busy,setBusy]=useState(''),[toast,setToast]=useState(''),[dialog,setDialog]=useState(null),[search,setSearch]=useState(()=>urlParams.get('q')||''),[processFilter,setProcessFilter]=useState('all'),[enabledOnly,setEnabledOnly]=useState(true),[orderScope,setOrderScope]=useState('all'),[orderStatus,setOrderStatus]=useState('all'),[supplierFilter,setSupplierFilter]=useState('all'),[orderPage,setOrderPage]=useState(1),[create,setCreate]=useState(null),[selectedOrder,setSelectedOrder]=useState(null),[requestedOrderId]=useState(()=>urlParams.get('order_id')||''),[requestedNew,setRequestedNew]=useState(()=>urlParams.get('new')==='1');
 async function request(path,options={}){const r=await ForgeApiResponse(adapter,path,{credentials:'include',headers:{'Content-Type':'application/json',...(adapter?.getAuthHeaders?.()||{}),...(options.headers||{})},...options}),p=await r.json().catch(()=>({}));if(!r.ok)throw new Error((typeof p.error==='string'?p.error:p.error?.message)||(Array.isArray(p.fields)&&p.fields.length?p.fields.map(f=>f.message||f.label).filter(Boolean).join('；'):'')||p.message||'请求失败');return p}
 async function find(name){if(adapter?.find){const p=await adapter.find(name,{limit:1000});return p?.records||p?.data?.records||p?.data||[]}return(await request('/data/'+name+'?$top=1000')).records||[]}
 async function load(){setState(s=>({...s,loading:true,error:''}));try{const names=['forge_supplier','forge_subcontract_supplier_profile','forge_subcontract_processing_price','forge_subcontract_order','forge_subcontract_order_line','forge_subcontract_material_plan','forge_subcontract_issue','forge_subcontract_receipt','forge_subcontract_inbound','forge_subcontract_ncr','forge_subcontract_return','forge_subcontract_return_line','forge_subcontract_return_inbound','forge_subcontract_reconciliation','forge_subcontract_reconciliation_line','forge_accounts_payable','forge_warehouse','forge_material_sku','forge_material','forge_project','forge_subcontract_business_setting','forge_payment_condition','forge_bom','forge_bom_node','forge_sales_order','forge_assembly_order','forge_bom_shortage_analysis'],pairs=await Promise.all(names.map(async n=>[n,await find(n)]));setState({loading:false,error:'',data:Object.fromEntries(pairs)})}catch(e){setState(s=>({...s,loading:false,error:String(e.message||e)}))}}
 useEffect(()=>{load()},[]);
 const today=new Date(Date.now()+8*60*60*1000).toISOString().slice(0,10),data=state.data,suppliers=data.forge_supplier||[],profiles=data.forge_subcontract_supplier_profile||[],processingPrices=data.forge_subcontract_processing_price||[],orders=data.forge_subcontract_order||[],lines=data.forge_subcontract_order_line||[],plans=data.forge_subcontract_material_plan||[],issues=data.forge_subcontract_issue||[],receipts=data.forge_subcontract_receipt||[],inbounds=data.forge_subcontract_inbound||[],ncrs=data.forge_subcontract_ncr||[],returns=data.forge_subcontract_return||[],returnLines=data.forge_subcontract_return_line||[],returnInbounds=data.forge_subcontract_return_inbound||[],reconciliations=data.forge_subcontract_reconciliation||[],reconciliationLines=data.forge_subcontract_reconciliation_line||[],payables=data.forge_accounts_payable||[],warehouses=data.forge_warehouse||[],skus=data.forge_material_sku||[],materials=data.forge_material||[],projects=data.forge_project||[],businessSettings=data.forge_subcontract_business_setting||[],activeProcessTypes=businessSettings.filter(x=>x.category==='process_type'&&(x.enabled===true||x.enabled===1||x.enabled==='1')).sort((a,b)=>Number(a.sort_order||0)-Number(b.sort_order||0)||String(a.name).localeCompare(String(b.name),'zh-CN'));
 const supplierMap=Object.fromEntries(suppliers.map(x=>[x.id,x])),profileMap=Object.fromEntries(profiles.map(x=>[x.id,x])),materialMap=Object.fromEntries(materials.map(x=>[x.id,x])),skuMap=Object.fromEntries(skus.map(x=>[x.id,x]));
 const paymentConditions=(data.forge_payment_condition||[]).filter(x=>x.status==='active'),boms=(data.forge_bom||[]).filter(x=>x.status==='active'),bomNodes=data.forge_bom_node||[],salesOrders=(data.forge_sales_order||[]).filter(x=>['approved','active','partially_shipped'].includes(x.status)),productionOrders=(data.forge_assembly_order||[]).filter(x=>['waiting_pick','assembling'].includes(x.status)),mrpAnalyses=(data.forge_bom_shortage_analysis||[]).filter(x=>x.status==='completed'),subcontractSkus=skus.filter(s=>(s.enabled===true||s.enabled===1||s.enabled==='1')&&materialMap[s.material_id]?.status==='active'&&materialMap[s.material_id]?.source_type==='subcontracted');
 useEffect(()=>{if(requestedOrderId&&!selectedOrder){const order=orders.find(x=>x.id===requestedOrderId);if(order)setSelectedOrder(order)}},[requestedOrderId,orders.length,selectedOrder]);
 useEffect(()=>{if(!requestedNew||state.loading||create)return;setRequestedNew(false);if(view!=='委外订单')setView('委外订单');setCreate(blankCreate())},[requestedNew,state.loading,create,view]);
 const statusText={draft:'草稿',pending_approval:'待审核',approved:'已审核',rejected:'已驳回',in_progress:'进行中',completed:'已完工',reconciled:'已对账',cancelled:'已取消',active:'已开通',inactive:'已停用'},issueStatusText={draft:'草稿',pending_approval:'待审核',rejected:'已驳回',ready_to_issue:'待发料',issued:'已发出',signed:'已签收',cancelled:'已取消'},receiptStatusText={draft:'草稿',pending_inbound:'待入库',inspection_exception:'不良待处理',ncr_pending:'不良待处理',ncr_resolved:'不良已处置',stocked:'已入库',cancelled:'已取消'},inboundStatusText={pending:'待入库',stocked:'已入库',cancelled:'已取消'},returnStatusText={draft:'草稿',pending_inbound:'待入库',stocked:'已入库',cancelled:'已取消'},ncrStatusText={draft:'待提交',pending_approval:'审批中',approved:'已通过',rejected:'已驳回',executed:'已执行',cancelled:'已作废'},supplyText={customer_supplied:'甲供料',turnkey:'包工包料'},ratingText={one:'1 星',two:'2 星',three:'3 星',four:'4 星',five:'5 星'};
 const money=v=>'¥'+Number(v||0).toLocaleString('zh-CN',{minimumFractionDigits:2,maximumFractionDigits:2}),pct=(a,b)=>b?Math.min(100,Math.round(Number(a||0)/Number(b)*100))+'%':'0%';

 async function exportOrders(){try{const rows=filteredOrders.map(x=>[x.code,supplierMap[x.supplier_id]?.name||'',projects.find(p=>p.id===x.project_id)?.code||'手工新建',x.payment_term||'',Number(x.processing_amount||0),Number(x.issued_quantity||0),Number(x.issue_planned_quantity||0),Number(x.received_quantity||0),Number(x.total_quantity||0),Number(x.reconciled_amount||0),x.expected_delivery_on||'',statusText[x.status]||x.status]);const count=await saveProductionExport(request,'subcontract_order','委外订单',window.location.pathname,'委外订单.csv',['订单号','供应商','关联','付款条件','加工费','已发数量','计划发料','已回数量','订单数量','已对账金额','交期','状态'],rows);setToast('已导出 '+count+' 条委外订单，可在导入导出任务中再次下载')}catch(e){setState(s=>({...s,error:String(e.message||e)}))}}
 const supplierKey=id=>{const s=supplierMap[id]||{};return s.code||s.name||id},rawActiveProfiles=profiles.filter(x=>x.status==='active'),activeProfiles=Object.values(rawActiveProfiles.reduce((all,p)=>{const key=supplierKey(p.supplier_id),current=all[key];if(!current||String(p.updated_at||p.created_at||p.id)>String(current.updated_at||current.created_at||current.id))all[key]=p;return all},{})),ongoingOrders=orders.filter(x=>['approved','in_progress'].includes(x.status)),overdueOrders=ongoingOrders.filter(x=>x.expected_delivery_on&&x.expected_delivery_on<today&&Number(x.received_quantity||0)<Number(x.total_quantity||0)),pendingReconAmount=orders.filter(x=>['completed','reconciled'].includes(x.status)).reduce((sum,x)=>sum+Math.max(0,Number(x.processing_amount||0)-Number(x.reconciled_amount||0)),0),filteredOrders=orders.filter(x=>{const qOk=!search||[x.code,x.name,supplierMap[x.supplier_id]?.name,...lines.filter(l=>l.order_id===x.id).map(l=>l.name)].some(v=>String(v||'').toLowerCase().includes(search.toLowerCase())),scopeOk=orderScope==='all'||(orderScope==='mine'?((x.owner_name||x.created_by_name||x.created_by||'Dev Admin')==='Dev Admin'):orderScope==='ongoing'?ongoingOrders.some(o=>o.id===x.id):orderScope==='overdue'?overdueOrders.some(o=>o.id===x.id):true),statusOk=orderStatus==='all'||x.status===orderStatus,supplierOk=supplierFilter==='all'||supplierKey(x.supplier_id)===supplierFilter;return qOk&&scopeOk&&statusOk&&supplierOk}),orderPageSize=20,orderPageCount=Math.max(1,Math.ceil(filteredOrders.length/orderPageSize)),safeOrderPage=Math.min(orderPage,orderPageCount),visibleOrders=filteredOrders.slice((safeOrderPage-1)*orderPageSize,safeOrderPage*orderPageSize),processOptions=[...new Set(activeProfiles.flatMap(p=>String(p.process_capabilities||'').split(String.fromCharCode(10)).map(x=>x.trim()).filter(Boolean)))],selectedProfile=profileMap[create?.supplier_profile_id],selectedCapabilityNames=new Set(String(selectedProfile?.process_capabilities||'').split(String.fromCharCode(10)).map(x=>x.trim()).filter(Boolean)),availableProcessTypes=activeProcessTypes.filter(type=>selectedCapabilityNames.has(type.name)),supplierGroups=Object.values(suppliers.reduce((all,supplier)=>{const key=supplier.code||supplier.name||supplier.id,item=all[key]||(all[key]={...supplier,related_ids:[]});item.related_ids.push(supplier.id);if(!item.contact_name&&supplier.contact_name)item.contact_name=supplier.contact_name;return all},{})),filteredSuppliers=supplierGroups.filter(x=>{const profile=activeProfiles.find(p=>x.related_ids.includes(p.supplier_id)),enabledOk=!enabledOnly||!!profile,searchOk=!search||[x.name,x.code,x.contact_name].some(v=>String(v||'').toLowerCase().includes(search.toLowerCase())),processOk=processFilter==='all'||String(profile?.process_capabilities||'').includes(processFilter);return enabledOk&&searchOk&&processOk});
 function blankCreate(){return{code:'SC-'+new Date().toISOString().slice(0,10).replaceAll('-','')+'-001',name:'',supplier_profile_id:'',supply_mode:'customer_supplied',expected_delivery_on:'',project_id:'',source_type:'manual',source_sales_order_id:'',source_production_order_id:'',source_mrp_analysis_id:'',inspection_method:'full',sampling_ratio:100,payment_condition_id:'',payment_term:'',remarks:'',lines:[{sku_id:'',process_type_id:'',process_type:'',quantity:1,unit_name:'件',unit_price:0,expected_delivery_on:'',drawing_number:''}],plans:[]}}
 function currentPrice(profileId,processTypeId,skuId){return processingPrices.filter(x=>x.status==='active'&&x.supplier_profile_id===profileId&&x.process_type_id===processTypeId&&(!x.effective_from||x.effective_from<=today)&&(!x.effective_to||x.effective_to>=today)&&(x.sku_id===skuId||!x.sku_id)).sort((a,b)=>Number(!!b.sku_id)-Number(!!a.sku_id)||String(b.effective_from||'').localeCompare(String(a.effective_from||'')))[0]}
 function updateLine(i,key,value){setCreate(c=>{const lines=c.lines.map((x,n)=>{if(n!==i)return x;const next={...x,[key]:value};if(key==='sku_id'||key==='process_type_id'){const price=currentPrice(c.supplier_profile_id,key==='process_type_id'?value:next.process_type_id,key==='sku_id'?value:next.sku_id);if(price){next.unit_price=Number(price.unit_price||0);next.unit_name=price.unit_name||next.unit_name;next.price_source='当前价目'}else{next.unit_price=0;next.price_source='未维护价目'}}return next});return{...c,lines}})}
 function updatePlan(i,key,value){setCreate(c=>({...c,plans:c.plans.map((x,n)=>n===i?{...x,[key]:value}:x)}))}
 function expandBomPlans(){const c=create,expanded=[];for(let lineIndex=0;lineIndex<c.lines.length;lineIndex++){const line=c.lines[lineIndex],sku=skuMap[line.sku_id],bom=sku&&boms.find(x=>x.material_id===sku.material_id);if(!bom)continue;for(const node of bomNodes.filter(x=>x.bom_id===bom.id&&x.is_leaf&&x.sku_id)){const componentSku=skuMap[node.sku_id],component=componentSku&&materialMap[componentSku.material_id];if(!component||component.source_type!=='purchased')continue;const loss=Number(node.loss_rate||0),lossRate=loss>1?loss/100:loss,quantity=Math.round(Number(node.quantity||0)*Number(line.quantity||0)*(1+lossRate)*10000)/10000;if(quantity>0)expanded.push({sku_id:node.sku_id,order_line_index:lineIndex,planned_quantity:quantity,standard_quantity:quantity,unit_name:'件'})}}if(!expanded.length)return setCreate({...c,error:'所选加工件没有可用于发料的生效 BOM 采购件，请手动增加发料计划'});setCreate({...c,plans:expanded,error:''})}
 async function invoke(object,action,id,params={}){const p=await request('/actions/'+object+'/'+action+'/'+id,{method:'POST',body:JSON.stringify({params})});let r=p;for(let i=0;i<5&&r&&typeof r==='object';i++){if(r.result!==undefined){r=r.result;continue}if(r.data!==undefined){r=r.data;continue}break}return r}
 async function saveOrder(submit){const c=create;if(!activeProcessTypes.length)return setCreate({...c,error:'请先在委外配置中启用至少一个加工类型'});if(!c.lines.length||c.lines.some(x=>!x.sku_id||!x.process_type_id||!x.process_type||!(Number(x.quantity)>0)))return setCreate({...c,error:'至少需要一行完整的外协加工件'});if(!c.code.trim()||!c.supplier_profile_id)return setCreate({...c,error:'请先选择委外供应商并填写委外订单号'});if(submit&&(!c.expected_delivery_on||!c.payment_condition_id))return setCreate({...c,error:'提交审核前必须选择期望交期和付款条件'});if(submit&&c.source_type==='sales_order'&&!c.source_sales_order_id||submit&&c.source_type==='production_order'&&!c.source_production_order_id||submit&&c.source_type==='mrp'&&!c.source_mrp_analysis_id)return setCreate({...c,error:'请选择与业务来源对应的有效单据'});if(submit&&c.inspection_method==='sampling'&&(!(Number(c.sampling_ratio)>0)||Number(c.sampling_ratio)>100))return setCreate({...c,error:'请输入 0 到 100 之间的抽检比例'});if(c.lines.some(x=>!subcontractSkus.some(s=>s.id===x.sku_id)))return setCreate({...c,error:'加工件只能选择已启用的外协物料'});if(c.lines.some(x=>!availableProcessTypes.some(type=>type.id===x.process_type_id&&type.name===x.process_type)))return setCreate({...c,error:'加工类型已停用或不存在，请重新选择'});if(c.supply_mode==='customer_supplied'&&(!c.plans.length||c.plans.some(x=>!x.sku_id||!(Number(x.planned_quantity)>0)||!(Number(x.standard_quantity)>0))))return setCreate({...c,error:'甲供料订单必须完善发料计划'});setBusy('save');try{const profile=profileMap[c.supplier_profile_id],supplier=supplierMap[profile.supplier_id],payment=paymentConditions.find(x=>x.id===c.payment_condition_id),created=await request('/data/forge_subcontract_order',{method:'POST',body:JSON.stringify({name:c.name.trim()||c.code.trim(),code:c.code.trim(),supplier_profile_id:profile.id,supplier_id:supplier.id,supply_mode:c.supply_mode,expected_delivery_on:c.expected_delivery_on||null,project_id:c.project_id||null,source_type:c.source_type,source_sales_order_id:c.source_sales_order_id||null,source_production_order_id:c.source_production_order_id||null,source_mrp_analysis_id:c.source_mrp_analysis_id||null,inspection_method:c.inspection_method,sampling_ratio:c.inspection_method==='sampling'?Number(c.sampling_ratio):null,payment_condition_id:c.payment_condition_id||null,payment_term:payment?.name||'',responsible_id:supplier.responsible_id,remarks:c.remarks.trim()||null})}),orderId=created.id||created.record?.id,lineIds=[];
 for(const row of c.lines){const sku=skuMap[row.sku_id],material=materialMap[sku.material_id],saved=await request('/data/forge_subcontract_order_line',{method:'POST',body:JSON.stringify({name:material.name,order_id:orderId,sku_id:sku.id,item_code:material.code,specification:sku.name,process_type_id:row.process_type_id,process_type:row.process_type,quantity:Number(row.quantity),unit_name:row.unit_name||'件',unit_price:Number(row.unit_price||0),expected_delivery_on:row.expected_delivery_on||c.expected_delivery_on,drawing_number:row.drawing_number||null})});lineIds.push(saved.id||saved.record?.id)}
 if(c.supply_mode==='customer_supplied')for(const row of c.plans){const sku=skuMap[row.sku_id],material=materialMap[sku.material_id],lineIndex=Math.min(c.lines.length-1,Math.max(0,Number(row.order_line_index||0)));await request('/data/forge_subcontract_material_plan',{method:'POST',body:JSON.stringify({name:material.name,order_id:orderId,order_line_id:lineIds[lineIndex],sku_id:sku.id,item_code:material.code,specification:sku.name,planned_quantity:Number(row.planned_quantity),standard_quantity:Number(row.standard_quantity),unit_name:row.unit_name||'件'})})}
 if(submit)await invoke('forge_subcontract_order','subcontract_order_submit',orderId);setCreate(null);setView('委外订单');setToast(submit?'委外订单已提交审核':'委外订单草稿已保存');await load()}catch(e){setCreate(x=>({...x,error:String(e.message||e)}))}finally{setBusy('')}}
 async function activate(){const d=dialog;setBusy('activate');try{await invoke('forge_supplier','subcontract_supplier_activate',d.row.id,{process_capabilities:activeProcessTypes.filter(type=>(d.process_type_ids||[]).includes(type.id)).map(type=>type.name).join(String.fromCharCode(10)),credit_rating:d.credit_rating,default_issue_warehouse_id:d.default_issue_warehouse_id||null,default_receipt_warehouse_id:d.default_receipt_warehouse_id||null,loss_rate_limit:Number(d.loss_rate_limit||0),warranty_terms:d.warranty_terms,remarks:d.remarks});setDialog(null);setToast('委外供应商档案已开通');await load()}catch(e){setDialog(x=>({...x,error:String(e.message||e)}))}finally{setBusy('')}}
 async function submitExisting(row){setBusy(row.id);try{await invoke('forge_subcontract_order','subcontract_order_submit',row.id);setToast('委外订单已提交审核');await load()}catch(e){setState(s=>({...s,error:String(e.message||e)}))}finally{setBusy('')}}
 async function review(){const d=dialog;if(!d.comment.trim())return setDialog({...d,error:'请填写审核意见'});setBusy('review');try{const result=await invoke('forge_subcontract_order','subcontract_order_review',d.row.id,{decision:d.decision,comment:d.comment});setDialog(null);setToast((d.decision==='approve'?'审核通过 · 下一步：':'已驳回 · ')+(result.next_step||''));await load()}catch(e){setDialog(x=>({...x,error:String(e.message||e)}))}finally{setBusy('')}}
 function OrderContext(){const order=selectedOrder,orderLines=lines.filter(x=>x.order_id===order.id),orderIssues=issues.filter(x=>x.order_id===order.id),orderReceipts=receipts.filter(x=>x.order_id===order.id),orderInbounds=inbounds.filter(x=>x.order_id===order.id),orderNcrs=ncrs.filter(x=>x.order_id===order.id),orderReturns=returns.filter(x=>x.order_id===order.id),orderReturnInbounds=returnInbounds.filter(x=>orderReturns.some(r=>r.id===x.return_id)),orderRecons=reconciliations.filter(x=>reconciliationLines.some(line=>line.reconciliation_id===x.id&&line.order_id===order.id)),orderPayables=payables.filter(x=>orderRecons.some(r=>r.payable_id===x.id)),supplierSignoffIssue=orderIssues.find(x=>x.status==='issued'),supplierSignoffBlocked=!!supplierSignoffIssue,ncrBlocked=orderNcrs.some(x=>x.status!=='executed'),inboundBlocked=orderInbounds.some(x=>x.status==='pending'),returnBlocked=orderReturns.some(x=>x.status==='pending_inbound')||orderReturnInbounds.some(x=>x.status==='pending'),supplier=supplierMap[order.supplier_id],contextUrl=path=>path+'?order_id='+encodeURIComponent(order.id),contextIssueUrl=path=>path+'?order_id='+encodeURIComponent(order.id)+'&issue_id='+encodeURIComponent(supplierSignoffIssue?.id||''),stage=order.status==='pending_approval'?'待审核':supplierSignoffBlocked?'待供应商签收':ncrBlocked?'不良待处理':inboundBlocked?'待入库':returnBlocked?'退料待入库':order.supply_mode==='customer_supplied'&&Number(order.issued_quantity||0)<Number(order.issue_planned_quantity||0)?'待发料':Number(order.received_quantity||0)<Number(order.total_quantity||0)?'待回厂':Number(order.reconciled_amount||0)<Number(order.processing_amount||0)?'待对账':'已完成';return <div className="forge-product forge-subcontract"><style>{css}</style><div className="fp-shell"><div className="fp-page-header fp-list-context"><div className="sc-title"><h2>委外订单上下文</h2><p>从当前订单查看来源、执行进度、异常和可继续的岗位任务</p></div><div className="sc-actions"><button className="fp-button" onClick={()=>setSelectedOrder(null)}>返回委外订单</button><button className="fp-icon-button" aria-label="刷新" title="刷新" onClick={load}><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round"><path d="M21 12a9 9 0 1 1-3-6.7"/><path d="M21 3v6h-6"/></svg></button></div></div><section className="fp-card sc-section"><div className="fp-list-context"><div><h3 style={{margin:0}}>{order.code}</h3><div className="sc-meta">{supplier?.name||'—'} · {supplyText[order.supply_mode]} · {statusText[order.status]}</div></div><span className="fp-status">当前下一步：{stage}</span></div><div className="sc-form" style={{marginTop:16}}><div><div className="sc-meta">期望交期</div><strong>{order.expected_delivery_on||'—'}</strong></div><div><div className="sc-meta">加工费</div><strong className="sc-money">{money(order.processing_amount)}</strong></div><div><div className="sc-meta">发料进度</div><strong>{Number(order.issued_quantity||0)} / {Number(order.issue_planned_quantity||0)}</strong></div><div><div className="sc-meta">回厂进度</div><strong>{Number(order.received_quantity||0)} / {Number(order.total_quantity||0)}</strong></div></div></section><section className="fp-card sc-section"><h3>执行链与结果</h3><div className="fp-table-wrap"><table className="fp-table"><thead><tr><th>环节</th><th>当前记录</th><th>状态</th><th>结果</th><th>下一步</th></tr></thead><tbody><tr><td>委外订单</td><td>{order.code}</td><td>{statusText[order.status]}</td><td>{orderLines.length} 个加工件，{money(order.processing_amount)} 加工费</td><td>{order.status==='pending_approval'?'等待审核':'—'}</td></tr><tr><td>发料与签收</td><td>{orderIssues.length?orderIssues.map(x=>x.code).join('、'):'尚未创建'}</td><td>{orderIssues.length?orderIssues.map(x=>issueStatusText[x.status]||x.status).join('、'):'待发料'}</td><td>已发 {Number(order.issued_quantity||0)} / 计划 {Number(order.issue_planned_quantity||0)}{supplierSignoffBlocked?' · 等待供应商签收':''}</td><td>{supplierSignoffBlocked?<a className="fp-link" href={contextIssueUrl('/_console/apps/com.inoforge.forge.production/page_subcontract_issue_workspace')}>登记供应商签收</a>:order.supply_mode==='customer_supplied'&&Number(order.issued_quantity||0)<Number(order.issue_planned_quantity||0)?<a className="fp-link" href={contextUrl('/_console/apps/com.inoforge.forge.production/page_subcontract_issue_workspace')}>进入委外发料</a>:'—'}</td></tr><tr><td>回厂验收</td><td>{orderReceipts.length?orderReceipts.map(x=>x.code).join('、'):'尚未登记'}</td><td>{orderReceipts.length?orderReceipts.map(x=>receiptStatusText[x.status]||x.status).join('、'):'待回厂'}</td><td>已回 {Number(order.received_quantity||0)} / 订单 {Number(order.total_quantity||0)}</td><td>{orderReceipts.some(x=>x.status==='pending_inbound')?<a className="fp-link" href={contextUrl('/_console/apps/com.inoforge.forge.production/page_subcontract_receipt_workspace')}>确认良品入库</a>:order.status==='in_progress'?<a className="fp-link" href={contextUrl('/_console/apps/com.inoforge.forge.production/page_subcontract_receipt_workspace')}>登记回厂验收</a>:'—'}</td></tr><tr><td>良品入库</td><td>{orderInbounds.length?orderInbounds.map(x=>x.code).join('、'):'尚未生成'}</td><td>{orderInbounds.length?orderInbounds.map(x=>inboundStatusText[x.status]||x.status).join('、'):'待回厂验收'}</td><td>{orderInbounds.reduce((s,x)=>s+Number(x.total_quantity||0),0)} 件进入待入库/已入库</td><td>{orderInbounds.some(x=>x.status==='pending')?<a className="fp-link" href={contextUrl('/_console/apps/com.inoforge.forge.production/page_subcontract_receipt_workspace')}>确认入库</a>:'—'}</td></tr><tr><td>不良处置</td><td>{orderNcrs.length?orderNcrs.map(x=>x.code).join('、'):'无不良记录'}</td><td>{orderNcrs.length?orderNcrs.map(x=>ncrStatusText[x.status]||x.status).join('、'):'—'}</td><td>{orderNcrs.reduce((s,x)=>s+Number(x.defective_quantity||0),0)} 件不良</td><td>{ncrBlocked?<a className="fp-link" href={contextUrl('/_console/apps/com.inoforge.forge.production/page_subcontract_ncr_workspace')}>进入不合格处理</a>:'—'}</td></tr><tr><td>退料交接</td><td>{orderReturns.length?orderReturns.map(x=>x.code).join('、'):'无退料记录'}</td><td>{orderReturns.length?orderReturns.map(x=>returnStatusText[x.status]||x.status).join('、'):'—'}</td><td>{orderReturnInbounds.length?orderReturnInbounds.map(x=>x.code).join('、'):'尚未生成待入库单'}</td><td>{orderReturnInbounds.some(x=>x.status==='pending')?<a className="fp-link" href={contextUrl('/_console/apps/com.inoforge.forge.production/page_subcontract_return_workspace')}>仓库确认退料入库</a>:orderReturns.some(x=>x.status==='draft')||!orderReturns.length?<a className="fp-link" href={contextUrl('/_console/apps/com.inoforge.forge.production/page_subcontract_return_workspace')}>进入委外退料</a>:'—'}</td></tr><tr><td>对账结算</td><td>{orderRecons.length?orderRecons.map(x=>x.code).join('、'):'尚未建立委外对账单'}</td><td>{orderRecons.length?orderRecons.map(x=>({pending_confirmation:'待确认',confirmed:'已确认',payable_generated:'已生成应付',voided:'已作废'}[x.status]||x.status)).join('、'):'待对账'}</td><td>订单已对账 {money(order.reconciled_amount)} / 加工费 {money(order.processing_amount)} {orderPayables.length?(' · '+orderPayables.map(x=>x.code).join('、')):''}</td><td>{orderPayables.length?'已生成应付':ncrBlocked?'先完成不良处置':inboundBlocked?'先完成良品入库':orderRecons.some(x=>x.status==='confirmed')?<a className="fp-link" href={contextUrl('/_console/apps/com.inoforge.forge.production/page_subcontract_reconciliation')}>生成应付</a>:orderRecons.some(x=>x.status==='pending_confirmation')?'等待供应商确认':<a className="fp-link" href={contextUrl('/_console/apps/com.inoforge.forge.production/page_subcontract_reconciliation')}>进入委外对账</a>}</td></tr></tbody></table></div></section></div></div>}
 if(state.loading)return <div className="forge-product forge-subcontract"><style>{css}</style><div className="fp-shell"><ForgeLoading label="正在加载委外业务"/></div></div>;
 if(selectedOrder)return <OrderContext/>;
 if(create)return <CreatePage/>;
 function Header(){const title=view==='看板'?'委外业务全景':view,desc=view==='看板'?'从供应商能力、订单审批到发料、回厂和对账的委外业务入口':view==='委外订单'?'整件委外 / 甲供料 — 加工件下单、发料、回厂、对账闭环':'维护工艺能力、信用等级、加工价目表；准时率与良率由已完成单据派生，不可手工改写';return <><ForgeHero section={'生产 / 委外管理 / '+(view==='看板'?'委外看板':view)} title={view==='看板'?'委外看板':view} description={desc} icon="▤" tone={view==='看板'?'blue':view==='委外订单'?'blue':'slate'} art="flow"/><div className="fp-action-row">{view==='委外订单'&&<><button className="fp-button primary" onClick={()=>setCreate(blankCreate())}>新建委外订单</button><button className="fp-button" disabled={!filteredOrders.length||!!busy} onClick={exportOrders}>导出</button><button className="fp-button" onClick={()=>openProductionDataTasks('subcontract_order')}>导入导出任务</button></>}{!standaloneSupplierPage&&<button className="fp-icon-button" aria-label="刷新" title="刷新" onClick={load}><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round"><path d="M21 12a9 9 0 1 1-3-6.7"/><path d="M21 3v6h-6"/></svg></button>}</div>{!standaloneSupplierPage&&<nav className="fp-card fp-tabs sc-tabs"><button className={'fp-tab '+(view==='看板'?'active':'')} onClick={()=>{setView('看板');setSearch('')}}>委外看板</button><button className={'fp-tab '+(view==='委外订单'?'active':'')} onClick={()=>{setView('委外订单');setSearch('')}}>委外订单</button><button className={'fp-tab '+(view==='委外供应商'?'active':'')} onClick={()=>{setView('委外供应商');setSearch('')}}>委外供应商</button></nav>}</>}
 function Dashboard(){
  const ongoing=orders.filter(x=>['approved','in_progress'].includes(x.status)),pendingIssue=ongoing.filter(x=>x.supply_mode==='customer_supplied'&&Number(x.issued_quantity)<Number(x.issue_planned_quantity)),pendingReturn=ongoing.filter(x=>Number(x.received_quantity)<Number(x.total_quantity)),pendingRecon=orders.filter(x=>x.status==='completed'&&Number(x.reconciled_amount)<Number(x.processing_amount)),overdue=ongoing.filter(x=>x.expected_delivery_on&&x.expected_delivery_on<today&&Number(x.received_quantity)<Number(x.total_quantity)),currentYear=today.slice(0,4),yearOrders=orders.filter(x=>x.status!=='cancelled'&&String(x.created_at||x.expected_delivery_on||'').startsWith(currentYear)),unsettled=yearOrders.reduce((sum,x)=>sum+Math.max(0,Number(x.processing_amount||0)-Number(x.reconciled_amount||0)),0),validReceipts=receipts.filter(x=>x.status!=='cancelled'),receivedTotal=validReceipts.reduce((sum,x)=>sum+Number(x.total_received_quantity||0),0),qualifiedTotal=validReceipts.reduce((sum,x)=>sum+Number(x.qualified_quantity||0),0),yieldRate=receivedTotal?Math.round(qualifiedTotal/receivedTotal*100):0,go=path=>{if(path)window.location.assign(path)},kpis=[['进行中订单',ongoing.length,'审核后到对账前','/_console/apps/com.inoforge.forge.production/page_subcontract_workspace'],['待发料订单',pendingIssue.length,'甲供料 · 剩余可发','/_console/apps/com.inoforge.forge.production/page_subcontract_issue_workspace'],['待回厂订单',pendingReturn.length,'供应商在加工中','/_console/apps/com.inoforge.forge.production/page_subcontract_receipt_workspace'],['待对账订单',pendingRecon.length,'已完工 · 尚未对账','/_console/apps/com.inoforge.forge.production/page_subcontract_reconciliation'],['逾期订单',overdue.length,'超过期望交期','/_console/apps/com.inoforge.forge.production/page_subcontract_undelivered']],tasks=[['待审核订单',orders.filter(x=>x.status==='pending_approval').length,'/_console/apps/com.inoforge.forge.production/page_subcontract_workspace'],['待审核发料',issues.filter(x=>x.status==='pending_approval').length,'/_console/apps/com.inoforge.forge.production/page_subcontract_issue_workspace'],['待供应商签收',issues.filter(x=>x.status==='issued').length,'/_console/apps/com.inoforge.forge.production/page_subcontract_issue_workspace'],['回厂待入库',receipts.filter(x=>x.status==='pending_inbound').length,'/_console/apps/com.inoforge.forge.production/page_subcontract_receipt_workspace'],['不良待处理',ncrs.filter(x=>x.status!=='executed'&&x.status!=='cancelled').length,'/_console/apps/com.inoforge.forge.production/page_subcontract_ncr_workspace']],inTransit=[...pendingReturn].sort((a,b)=>String(a.expected_delivery_on||'').localeCompare(String(b.expected_delivery_on||''))).slice(0,6),recent=[...orders].sort((a,b)=>String(b.created_at||b.id).localeCompare(String(a.created_at||a.id))).slice(0,5);return <><div className="sc-overview"><span>进行中 <strong>{ongoing.length} 单</strong></span><span>{currentYear} 年未结 <strong>{money(unsettled)}</strong></span><span>整体良率 <strong>{yieldRate}%</strong></span></div><div className="sc-kpis">{kpis.map(x=><button className="fp-card sc-kpi sc-kpi-button" key={x[0]} onClick={()=>go(x[3])}><span>{x[0]}</span><strong>{x[1]}</strong><small>{x[2]}</small></button>)}</div><div className="sc-actions" style={{marginBottom:14}}><button className="fp-button primary" onClick={()=>{setView('委外订单');setCreate(blankCreate())}}>新建委外订单</button></div><div className="sc-dashboard-grid"><section className="fp-card sc-dashboard-card"><h3>我的待办</h3><div className="sc-task-list">{tasks.map(x=><button className="fp-button sc-task" key={x[0]} onClick={()=>go(x[2])}><span>{x[0]}</span><strong>{x[1]}</strong></button>)}</div></section><section className="fp-card sc-dashboard-card"><div className="fp-list-context"><h3>在途委外订单</h3><button className="fp-button small" onClick={()=>go('/_console/apps/com.inoforge.forge.production/page_subcontract_workspace')}>查看全部</button></div>{inTransit.length?<div className="sc-order-list">{inTransit.map(x=><div className="sc-order-row" key={x.id}><div><button className="fp-link" onClick={()=>go('/_console/apps/com.inoforge.forge.production/page_subcontract_workspace?order_id='+encodeURIComponent(x.id))}>{x.code}</button><div className="sc-meta">{supplierMap[x.supplier_id]?.name||'—'} · {statusText[x.status]||x.status}</div></div><div className="sc-meta">回厂 {Number(x.received_quantity||0)} / {Number(x.total_quantity||0)} · 交期 {x.expected_delivery_on||'—'}</div></div>)}</div>:<div className="sc-dashboard-empty">暂无在途委外订单</div>}</section></div><section className="fp-card sc-dashboard-card"><h3>快捷入口</h3><div className="sc-quick">{[['委外订单','page_subcontract_workspace'],['委外发料','page_subcontract_issue_workspace'],['委外回厂','page_subcontract_receipt_workspace'],['委外对账','page_subcontract_reconciliation'],['委外供应商','page_subcontract_suppliers'],['加工价目','page_subcontract_pricing']].map(x=><button className="fp-button" key={x[0]} onClick={()=>go(forgePageHref(x[1]))}>{x[0]}</button>)}</div></section><section className="fp-card sc-dashboard-card"><h3>最近创建的委外订单</h3>{recent.length?<div className="sc-order-list">{recent.map(x=><div className="sc-order-row" key={x.id}><div><button className="fp-link" onClick={()=>go('/_console/apps/com.inoforge.forge.production/page_subcontract_workspace?order_id='+encodeURIComponent(x.id))}>{x.code}</button><div className="sc-meta">{supplierMap[x.supplier_id]?.name||'—'}</div></div><span className="fp-status">{statusText[x.status]||x.status}</span></div>)}</div>:<div className="sc-dashboard-empty">暂无委外订单</div>}</section></>}

 function Orders(){const kpis=[['进行中订单',ongoingOrders.length,'审核后到对账前','ongoing'],['待审核',orders.filter(x=>x.status==='pending_approval').length,'等待审核处理','pending_approval'],['逾期未完工',overdueOrders.length,'已超过计划交期','overdue'],['待对账金额',money(pendingReconAmount),'订单金额减已对账金额','recon']];return <><div className="sc-kpis">{kpis.map(x=><button className="fp-card sc-kpi sc-kpi-button" key={x[0]} onClick={()=>{setOrderPage(1);if(x[3]==='ongoing'){setOrderScope('ongoing');setOrderStatus('all')}else if(x[3]==='pending_approval'){setOrderScope('all');setOrderStatus('pending_approval')}else if(x[3]==='overdue'){setOrderScope('overdue');setOrderStatus('all')}else {setOrderScope('all');setOrderStatus('completed')}}}><span>{x[0]}</span><strong>{x[1]}</strong><small>{x[2]}</small></button>)}</div><section className="fp-card"><div className="fp-filterbar"><div className="fp-search"><input className="fp-input" aria-label="搜索委外订单" placeholder="搜索订单号 / 供应商 / 加工件名称" value={search} onChange={e=>{setSearch(e.target.value);setOrderPage(1)}}/></div><ForgeSelectControl className="fp-select" aria-label="订单范围" value={orderScope} onChange={e=>{setOrderScope(e.target.value);setOrderPage(1)}}><option value="all">全部范围</option><option value="mine">我负责的</option><option value="ongoing">进行中订单</option><option value="overdue">逾期未完工</option></ForgeSelectControl><ForgeSelectControl className="fp-select" aria-label="订单状态" value={orderStatus} onChange={e=>{setOrderStatus(e.target.value);setOrderPage(1)}}><option value="all">全部状态</option>{Object.entries(statusText).filter(x=>!['active','inactive'].includes(x[0])).map(x=><option key={x[0]} value={x[0]}>{x[1]}</option>)}</ForgeSelectControl><ForgeSelectControl className="fp-select" aria-label="筛选供应商" value={supplierFilter} onChange={e=>{setSupplierFilter(e.target.value);setOrderPage(1)}}><option value="all">全部供应商</option>{activeProfiles.map(p=><option key={supplierKey(p.supplier_id)} value={supplierKey(p.supplier_id)}>{supplierMap[p.supplier_id]?.name}</option>)}</ForgeSelectControl>{(search||orderScope!=='all'||orderStatus!=='all'||supplierFilter!=='all')&&<button className="fp-button" onClick={()=>{setSearch('');setOrderScope('all');setOrderStatus('all');setSupplierFilter('all');setOrderPage(1)}}>重置</button>}</div><div className="fp-table-wrap"><table className="fp-table"><thead><tr><th>订单号</th><th>供应商</th><th>关联</th><th>付款条件</th><th>加工费</th><th>发料进度</th><th>回厂进度</th><th>对账进度</th><th>交期</th><th>操作</th></tr></thead><tbody>{visibleOrders.map(x=><tr key={x.id}><td><button className="fp-link" onClick={()=>setSelectedOrder(x)}>{x.code}</button><div className="sc-meta">{supplyText[x.supply_mode]} · {statusText[x.status]}</div></td><td>{supplierMap[x.supplier_id]?.name}</td><td>{projects.find(p=>p.id===x.project_id)?.code||'手工新建'}</td><td>{x.payment_term}</td><td className="sc-money">{money(x.processing_amount)}</td><td><div className="sc-progress">{Number(x.issued_quantity||0)} / {Number(x.issue_planned_quantity||0)} · {pct(x.issued_quantity,x.issue_planned_quantity)}</div></td><td><div className="sc-progress">{Number(x.received_quantity||0)} / {Number(x.total_quantity||0)} · {pct(x.received_quantity,x.total_quantity)}</div></td><td><div className="sc-progress">{money(x.reconciled_amount)} / {money(x.processing_amount)}</div></td><td>{x.expected_delivery_on}</td><td><div className="sc-actions">{['draft','rejected'].includes(x.status)&&<button className="fp-button small" disabled={!!busy} onClick={()=>submitExisting(x)}>提交审核</button>}{x.status==='pending_approval'&&<button className="fp-button small primary" onClick={()=>setDialog({kind:'review',row:x,title:'审核委外订单',decision:'approve',comment:'',error:''})}>审核</button>}</div></td></tr>)}{!visibleOrders.length&&<tr><td colSpan="10"><ForgeEmpty title="无符合条件的委外订单" description="可新建草稿并提交审核"/></td></tr>}</tbody></table></div><div className="fp-pagination"><span>共 {filteredOrders.length} 条记录 · 每页 {orderPageSize} 条</span><div className="fp-pagination-actions"><button className="fp-button small" disabled={safeOrderPage<=1} onClick={()=>setOrderPage(safeOrderPage-1)}>上一页</button><span>{safeOrderPage} / {orderPageCount}</span><button className="fp-button small" disabled={safeOrderPage>=orderPageCount} onClick={()=>setOrderPage(safeOrderPage+1)}>下一页</button></div></div></section></>}
 function Suppliers(){return <><section className="fp-card"><div className="fp-filterbar"><div className="fp-search"><input className="fp-input" aria-label="搜索委外供应商" placeholder="搜索供应商名称 / 编号" value={search} onChange={e=>setSearch(e.target.value)}/></div><ForgeSelectControl className="fp-select" aria-label="全部工艺能力" value={processFilter} onChange={e=>setProcessFilter(e.target.value)}><option value="all">全部工艺能力</option>{processOptions.map(x=><option key={x} value={x}>{x}</option>)}</ForgeSelectControl><label style={{display:'flex',alignItems:'center',gap:7}}><input aria-label="仅看已启用委外" type="checkbox" checked={enabledOnly} onChange={e=>setEnabledOnly(e.target.checked)}/>仅看已启用委外</label><span className="sc-meta">共 {filteredSuppliers.length} 家</span>{(search||processFilter!=='all'||!enabledOnly)&&<button className="fp-button" onClick={()=>{setSearch('');setProcessFilter('all');setEnabledOnly(true)}}>重置</button>}</div></section><div className="sc-card-grid" style={{marginTop:12}}>{filteredSuppliers.map(s=>{const p=activeProfiles.find(x=>s.related_ids.includes(x.supplier_id)),canActivate=s.status==='active'&&s.approval_status==='approved';return <article className="fp-card sc-supplier" key={s.id}><h3>{s.name}</h3><div className="sc-meta">{s.code||'未编号'} · {s.contact_name||'未填写联系人'}</div><div className="sc-rate"><div><span>交期准时率</span><strong>{p?.on_time_rate==null?'暂无数据':p.on_time_rate+'%'}</strong></div><div><span>历史良率</span><strong>{p?.yield_rate==null?'暂无数据':p.yield_rate+'%'}</strong></div><div><span>信用等级</span><strong>{p?ratingText[p.credit_rating]:'未评'}</strong></div></div><div className="sc-meta">{p?String(p.process_capabilities||'').split(String.fromCharCode(10)).filter(Boolean).join('、'):'未配置工艺能力，开通后才能参与委外选商'}</div><div className="sc-actions" style={{marginTop:12}}>{p?<><ForgeStatus value="active" label="已开通委外"/><button className="fp-button small" onClick={()=>navigate('/apps/com.inoforge.forge.production/page_subcontract_pricing?supplier_profile_id='+encodeURIComponent(p.id))}>维护加工价目</button><button className="fp-button small" onClick={()=>navigate('/apps/com.inoforge.forge.production/page_subcontract_workspace?q='+encodeURIComponent(s.name))}>查看委外订单</button></>:canActivate?<button className="fp-button primary" onClick={()=>setDialog({kind:'activate',row:s,title:'开通委外业务 · '+s.name,process_type_ids:[],credit_rating:'three',default_issue_warehouse_id:'',default_receipt_warehouse_id:'',loss_rate_limit:0,warranty_terms:'',remarks:'',error:''})}>开通委外</button>:<span className="sc-meta">需先完成供应商启用与审批</span>}</div></article>})}{!filteredSuppliers.length&&<section className="fp-card" style={{gridColumn:'1/-1'}}><ForgeEmpty title="没有符合条件的委外供应商" description="供应商需先在主数据建档并审批，再在这里开通委外档案"/></section>}</div></>}
 function CreatePage(){const c=create,amount=c.lines.reduce((a,x)=>a+Number(x.quantity||0)*Number(x.unit_price||0),0),prerequisites=[['委外供应商',activeProfiles.length>0,activeProfiles.length+' 家可选'],['加工类型',activeProcessTypes.length>0,activeProcessTypes.length+' 个已启用'],['付款条件',paymentConditions.length>0,paymentConditions.length+' 项已启用'],['外协加工件',subcontractSkus.length>0,subcontractSkus.length+' 个规格可选']];return <div className="forge-product forge-subcontract"><style>{css}</style><div className="fp-shell"><div className="sc-create-top"><div><h2>新建委外订单</h2><p>{c.supply_mode==='customer_supplied'?'我方提供原料，审核后进入委外发料':'供应商自行备料，审核后等待加工回厂'}</p></div><div className="sc-actions"><button className="fp-button" disabled={!!busy} onClick={()=>saveOrder(false)}>保存草稿</button><button className="fp-button" disabled={!!busy} onClick={()=>saveOrder(true)}>{busy?'提交中…':'提交审核'}</button></div></div>{c.error&&<ForgeNotice tone="error">{c.error}</ForgeNotice>}<section className="fp-card sc-section"><div className="fp-list-context"><h3 style={{margin:0}}>办理前准备</h3><button className="fp-button small" onClick={()=>navigate('/apps/com.inoforge.forge.production/page_production_prerequisites')}>查看全部准备项</button></div><div className="sc-prereq">{prerequisites.map(item=><div className="sc-prereq-item" key={item[0]}><strong>{item[0]} · {item[1]?'已具备':'待补充'}</strong><span>{item[2]}</span></div>)}</div></section><section className="fp-card sc-section"><h3>基础信息</h3><div className="sc-form"><div className="fp-field"><label>委外供应商 <span className="fp-required">*</span></label><ForgeSelectControl className="fp-select" aria-label="委外供应商" value={c.supplier_profile_id} onChange={e=>setCreate({...c,supplier_profile_id:e.target.value,lines:c.lines.map(x=>({...x,process_type_id:'',process_type:'',unit_price:0,price_source:''})),error:''})}><option value="">搜索供应商名称 / 编号 / 联系人</option>{activeProfiles.map(p=><option key={p.id} value={p.id}>{supplierMap[p.supplier_id]?.name}</option>)}</ForgeSelectControl></div><div className="fp-field"><label>料权方式 <span className="fp-required">*</span></label><div className="sc-radio"><label><input type="radio" name="supply" checked={c.supply_mode==='customer_supplied'} onChange={()=>setCreate({...c,supply_mode:'customer_supplied'})}/>甲供料</label><label><input type="radio" name="supply" checked={c.supply_mode==='turnkey'} onChange={()=>setCreate({...c,supply_mode:'turnkey',plans:[]})}/>包工包料</label></div></div><div className="fp-field"><label>委外订单号 <span className="fp-required">*</span></label><input className="fp-input" aria-label="委外订单号" value={c.code} onChange={e=>setCreate({...c,code:e.target.value})}/></div><div className="fp-field"><label>期望交期 <span className="fp-required">*</span></label><ForgeDateInput className="fp-input" aria-label="期望交期"  value={c.expected_delivery_on} onChange={e=>setCreate({...c,expected_delivery_on:e.target.value})}/></div><div className="fp-field"><label>项目</label><ForgeSelectControl className="fp-select" aria-label="项目" value={c.project_id} onChange={e=>setCreate({...c,project_id:e.target.value})}><option value="">搜索项目名称、编号...</option>{projects.map(p=><option key={p.id} value={p.id}>{p.code} · {p.name}</option>)}</ForgeSelectControl></div><div className="fp-field"><label>业务来源</label><ForgeSelectControl className="fp-select" aria-label="业务来源" value={c.source_type} onChange={e=>setCreate({...c,source_type:e.target.value,source_sales_order_id:'',source_production_order_id:'',source_mrp_analysis_id:'',error:''})}><option value="manual">手工新建</option><option value="sales_order">关联销售订单</option><option value="production_order">关联生产工单</option><option value="mrp">MRP推送</option></ForgeSelectControl></div>{c.source_type==='sales_order'&&<div className="fp-field"><label>来源销售订单 <span className="fp-required">*</span></label><ForgeSelectControl className="fp-select" aria-label="来源销售订单" value={c.source_sales_order_id} onChange={e=>setCreate({...c,source_sales_order_id:e.target.value,error:''})}><option value="">请选择已审批且仍在执行的销售订单</option>{salesOrders.map(x=><option key={x.id} value={x.id}>{x.code} · {x.name}</option>)}</ForgeSelectControl></div>}{c.source_type==='production_order'&&<div className="fp-field"><label>来源生产工单 <span className="fp-required">*</span></label><ForgeSelectControl className="fp-select" aria-label="来源生产工单" value={c.source_production_order_id} onChange={e=>setCreate({...c,source_production_order_id:e.target.value,error:''})}><option value="">请选择已下达且尚未完工的生产工单</option>{productionOrders.map(x=><option key={x.id} value={x.id}>{x.code} · {x.name}</option>)}</ForgeSelectControl></div>}{c.source_type==='mrp'&&<div className="fp-field"><label>来源 MRP 分析 <span className="fp-required">*</span></label><ForgeSelectControl className="fp-select" aria-label="来源 MRP 分析" value={c.source_mrp_analysis_id} onChange={e=>setCreate({...c,source_mrp_analysis_id:e.target.value,error:''})}><option value="">请选择已完成的 MRP 分析</option>{mrpAnalyses.map(x=><option key={x.id} value={x.id}>{x.code} · {x.name}</option>)}</ForgeSelectControl></div>}<div className="fp-field"><label>验收方式</label><ForgeSelectControl className="fp-select" aria-label="验收方式" value={c.inspection_method} onChange={e=>setCreate({...c,inspection_method:e.target.value,error:''})}><option value="full">全检</option><option value="sampling">抽检-按比例</option><option value="supplier_self">供应商自检</option></ForgeSelectControl></div>{c.inspection_method==='sampling'&&<div className="fp-field"><label>抽检比例（%） <span className="fp-required">*</span></label><input className="fp-input" aria-label="抽检比例" type="number" min="0.01" max="100" value={c.sampling_ratio} onChange={e=>setCreate({...c,sampling_ratio:e.target.value,error:''})}/></div>}<div className="fp-field"><label>付款条件 <span className="fp-required">*</span></label><ForgeSelectControl className="fp-select" aria-label="付款条件" value={c.payment_condition_id} onChange={e=>{const payment=paymentConditions.find(x=>x.id===e.target.value);setCreate({...c,payment_condition_id:e.target.value,payment_term:payment?.name||'',error:''})}}><option value="">{paymentConditions.length?'请选择已启用的付款条件':'暂无启用的付款条件，请先到采购销售设置维护'}</option>{paymentConditions.map(x=><option key={x.id} value={x.id}>{x.name}</option>)}</ForgeSelectControl>{!paymentConditions.length&&<button type="button" className="fp-link" onClick={()=>window.location.href='/_console/apps/com.inoforge.forge.finance/forge_payment_condition'}>去维护付款条件</button>}</div><div className="fp-field sc-span"><label>备注</label><textarea className="fp-textarea" aria-label="备注" value={c.remarks} onChange={e=>setCreate({...c,remarks:e.target.value})}/></div></div></section><section className="fp-card sc-section"><div className="fp-list-context"><h3 style={{margin:0}}>加工件</h3><button className="fp-button small" onClick={()=>setCreate({...c,lines:[...c.lines,{sku_id:'',process_type_id:'',process_type:'',quantity:1,unit_name:'件',unit_price:0,expected_delivery_on:'',drawing_number:''}]})}>增加加工件</button></div><div className="sc-lines"><table className="fp-table"><thead><tr><th>#</th><th>物料编号</th><th>物料名称 / 规格</th><th>加工类型</th><th>数量</th><th>单位</th><th>单价</th><th>价格来源</th><th>小计</th><th>期望交期</th><th>图纸号</th><th></th></tr></thead><tbody>{c.lines.map((x,i)=><tr key={i}><td>{i+1}</td><td><ForgeSelectControl aria-label={'加工件物料 '+(i+1)} value={x.sku_id} onChange={e=>updateLine(i,'sku_id',e.target.value)}><option value="">选择物料</option>{subcontractSkus.map(s=><option key={s.id} value={s.id}>{materialMap[s.material_id]?.code}</option>)}</ForgeSelectControl></td><td>{x.sku_id?(materialMap[skuMap[x.sku_id]?.material_id]?.name+' / '+skuMap[x.sku_id]?.name):'—'}</td><td><ForgeSelectControl aria-label={'加工类型 '+(i+1)} value={x.process_type_id} onChange={e=>{const type=availableProcessTypes.find(t=>t.id===e.target.value);const next=[...c.lines],price=currentPrice(c.supplier_profile_id,type?.id||'',next[i].sku_id);next[i]={...next[i],process_type_id:type?.id||'',process_type:type?.name||'',unit_price:price?Number(price.unit_price||0):0,unit_name:price?.unit_name||next[i].unit_name,price_source:price?'当前价目':'未维护价目'};setCreate({...c,lines:next,error:''})}}><option value="">{c.supplier_profile_id?'选择供应商工艺能力':'请先选择委外供应商'}</option>{availableProcessTypes.map(type=><option key={type.id} value={type.id}>{type.parent_id?'　↳ ':''}{type.name}</option>)}</ForgeSelectControl></td><td><input aria-label={'加工数量 '+(i+1)} type="number" min="0.0001" value={x.quantity} onChange={e=>updateLine(i,'quantity',e.target.value)}/></td><td><input aria-label={'加工单位 '+(i+1)} value={x.unit_name} onChange={e=>updateLine(i,'unit_name',e.target.value)}/></td><td><input aria-label={'加工单价 '+(i+1)} type="number" min="0" value={x.unit_price} onChange={e=>updateLine(i,'unit_price',e.target.value)}/></td><td><span className="sc-meta">{x.price_source||'手工填写'}</span></td><td>{money(Number(x.quantity||0)*Number(x.unit_price||0))}</td><td><ForgeDateInput aria-label={'加工件交期 '+(i+1)}  value={x.expected_delivery_on} onChange={e=>updateLine(i,'expected_delivery_on',e.target.value)}/></td><td><input aria-label={'图纸号 '+(i+1)} value={x.drawing_number} onChange={e=>updateLine(i,'drawing_number',e.target.value)}/></td><td><button className="fp-button small" disabled={c.lines.length===1} onClick={()=>setCreate({...c,lines:c.lines.filter((_,n)=>n!==i)})}>删除</button></td></tr>)}</tbody></table></div><div className="sc-note" style={{textAlign:'right'}}>加工费总额 <strong className="sc-money">{money(amount)}</strong></div></section>{c.supply_mode==='customer_supplied'&&<section className="fp-card sc-section"><div className="fp-list-context"><h3 style={{margin:0}}>发料计划</h3><div className="sc-actions"><button className="fp-button small" onClick={expandBomPlans}>按 BOM 展开发料</button><button className="fp-button small" onClick={()=>setCreate({...c,plans:[...c.plans,{sku_id:'',order_line_index:0,planned_quantity:1,standard_quantity:1,unit_name:'件'}]})}>手动增加</button></div></div><div className="sc-lines"><table className="fp-table"><thead><tr><th>#</th><th>物料编号</th><th>物料名称 / 规格</th><th>计划发料</th><th>标准应耗</th><th>单位</th><th>用于加工件</th><th></th></tr></thead><tbody>{c.plans.map((x,i)=><tr key={i}><td>{i+1}</td><td><ForgeSelectControl aria-label={'发料物料 '+(i+1)} value={x.sku_id} onChange={e=>updatePlan(i,'sku_id',e.target.value)}><option value="">选择物料</option>{skus.map(s=><option key={s.id} value={s.id}>{materialMap[s.material_id]?.code}</option>)}</ForgeSelectControl></td><td>{x.sku_id?(materialMap[skuMap[x.sku_id]?.material_id]?.name+' / '+skuMap[x.sku_id]?.name):'—'}</td><td><input aria-label={'计划发料 '+(i+1)} type="number" min="0.0001" value={x.planned_quantity} onChange={e=>updatePlan(i,'planned_quantity',e.target.value)}/></td><td><input aria-label={'标准应耗 '+(i+1)} type="number" min="0.0001" value={x.standard_quantity} onChange={e=>updatePlan(i,'standard_quantity',e.target.value)}/></td><td><input aria-label={'发料单位 '+(i+1)} value={x.unit_name} onChange={e=>updatePlan(i,'unit_name',e.target.value)}/></td><td><ForgeSelectControl aria-label={'用于加工件 '+(i+1)} value={x.order_line_index} onChange={e=>updatePlan(i,'order_line_index',Number(e.target.value))}>{c.lines.map((_,n)=><option key={n} value={n}>加工件 {n+1}</option>)}</ForgeSelectControl></td><td><button className="fp-button small" disabled={c.plans.length===1} onClick={()=>setCreate({...c,plans:c.plans.filter((_,n)=>n!==i)})}>删除</button></td></tr>)}</tbody></table></div><div className="sc-note">按 BOM 展开只取生效 BOM 中来源为采购的末级件，数量按单机用量 × 加工件数量 ×（1 + 损耗率）计算；没有可用 BOM 时可手动增加。</div></section>}<div className="sc-actions" style={{justifyContent:'flex-end'}}><button className="fp-button" onClick={()=>setCreate(null)}>取消</button><button className="fp-button" disabled={!!busy} onClick={()=>saveOrder(false)}>保存草稿</button><button className="fp-button primary" disabled={!!busy} onClick={()=>saveOrder(true)}>提交审核</button></div></div></div>}
 return <div className="forge-product forge-subcontract"><style>{css}</style><div className="fp-shell"><Header/>{state.error&&<ForgeNotice tone="error" onClose={()=>setState(s=>({...s,error:''}))}>{state.error}</ForgeNotice>}{view==='看板'?<Dashboard/>:view==='委外订单'?<Orders/>:<Suppliers/>}</div>{toast&&<div className="fp-toast" role="status">{toast}</div>}<ForgeDialog open={!!dialog} title={dialog?.title} error={dialog?.error} busy={!!busy} confirmLabel={dialog?.kind==='activate'?'开通':'确认审核'} onCancel={()=>!busy&&setDialog(null)} onConfirm={dialog?.kind==='activate'?activate:review}>{dialog?.kind==='activate'&&<><div className="fp-field"><label>工艺能力 <span className="fp-required">*</span></label><div className="fp-check-list">{activeProcessTypes.map(type=><label key={type.id} className="fp-checkbox"><input type="checkbox" checked={(dialog.process_type_ids||[]).includes(type.id)} onChange={e=>setDialog({...dialog,process_type_ids:e.target.checked?[...(dialog.process_type_ids||[]),type.id]:(dialog.process_type_ids||[]).filter(id=>id!==type.id),error:''})}/><span>{type.parent_id?'　↳ ':''}{type.name}</span></label>)}</div>{!activeProcessTypes.length&&<ForgeNotice tone="warning">请先在委外配置中添加并启用加工类型。</ForgeNotice>}</div><div className="fp-field"><label>信用等级</label><ForgeSelectControl className="fp-select" aria-label="信用等级" value={dialog.credit_rating} onChange={e=>setDialog({...dialog,credit_rating:e.target.value})}>{Object.entries(ratingText).map(x=><option key={x[0]} value={x[0]}>{x[1]}</option>)}</ForgeSelectControl></div><div className="fp-field"><label>默认发料来源仓</label><ForgeSelectControl className="fp-select" aria-label="默认发料来源仓" value={dialog.default_issue_warehouse_id} onChange={e=>setDialog({...dialog,default_issue_warehouse_id:e.target.value})}><option value="">不设则每次手选</option>{warehouses.map(w=><option key={w.id} value={w.id}>{w.name}</option>)}</ForgeSelectControl></div><div className="fp-field"><label>默认回厂入库仓</label><ForgeSelectControl className="fp-select" aria-label="默认回厂入库仓" value={dialog.default_receipt_warehouse_id} onChange={e=>setDialog({...dialog,default_receipt_warehouse_id:e.target.value})}><option value="">不设则每次手选</option>{warehouses.map(w=><option key={w.id} value={w.id}>{w.name}</option>)}</ForgeSelectControl></div><div className="fp-field"><label>损耗率上限（%）</label><input className="fp-input" aria-label="损耗率上限" type="number" min="0" max="100" value={dialog.loss_rate_limit} onChange={e=>setDialog({...dialog,loss_rate_limit:e.target.value})}/></div><div className="fp-field"><label>质保条款</label><textarea className="fp-textarea" aria-label="质保条款" value={dialog.warranty_terms} onChange={e=>setDialog({...dialog,warranty_terms:e.target.value})}/></div></>}{dialog?.kind==='review'&&<><div className="fp-field"><label>审核结论</label><ForgeSelectControl className="fp-select" aria-label="审核结论" value={dialog.decision} onChange={e=>setDialog({...dialog,decision:e.target.value})}><option value="approve">同意</option><option value="reject">驳回</option></ForgeSelectControl></div><div className="fp-field"><label>审核意见 <span className="fp-required">*</span></label><textarea className="fp-textarea" aria-label="审核意见" value={dialog.comment} onChange={e=>setDialog({...dialog,comment:e.target.value,error:''})}/></div></>}</ForgeDialog></div>
}
export default App;`;

export const SubcontractWorkspacePage = {
  name: 'page_subcontract_workspace', label: '委外订单', description: '委外订单草稿、提交审核与执行上下文',
  icon: 'factory', type: 'app' as const, kind: 'react' as const, source: subcontractSource,
};

export const SubcontractDashboardPage = {
  name: 'page_subcontract_dashboard', label: '委外看板', description: '按供应商、订单、发料、回厂和结算查看委外业务全景',
  icon: 'layout-dashboard', type: 'app' as const, kind: 'react' as const, source: subcontractSource,
};

export const SubcontractOrdersPage = {
  name: 'page_subcontract_orders', label: '委外订单', description: '委外订单列表、创建、提交与审核',
  icon: 'clipboard-list', type: 'app' as const, kind: 'react' as const,
  source: subcontractSource.replace("useState('看板')", "useState('委外订单')"),
};

export const SubcontractSuppliersPage = {
  name: 'page_subcontract_suppliers', label: '委外供应商', description: '维护工艺能力、信用等级、加工价目表与启用状态',
  icon: 'factory', type: 'app' as const, kind: 'react' as const, source: subcontractSource,
};
