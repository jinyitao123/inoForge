import { forgeProductUiCss, forgeProductUiRuntime } from './product-ui.js';

const source = `
const {useEffect,useMemo,useState}=React;
const css=${JSON.stringify(forgeProductUiCss + '.sc-stock-page{min-height:100%;background:#f4f6f8;color:#222831}.sc-stock-page .head{padding:18px 22px;background:#fff;border-bottom:1px solid #e1e5ea}.sc-stock-page .head h1{margin:0 0 5px;font-size:22px}.sc-stock-page .head p{margin:0;color:#76808d;font-size:12px}.sc-stock-page .body{padding:18px 22px 40px;max-width:1480px;margin:auto}.sc-stock-page .top-actions{display:flex;justify-content:space-between;gap:12px;align-items:center;flex-wrap:wrap;margin-bottom:12px}.sc-stock-page .toolbar{display:flex;gap:8px;align-items:center;flex-wrap:wrap;margin-bottom:14px}.sc-stock-page .toolbar input{min-width:290px}.sc-stock-page .segmented{display:flex;gap:4px}.sc-stock-page .segmented button,.sc-stock-page .age button,.sc-stock-page .filter-chip{border:1px solid #d6dce3;background:#fff;padding:8px 12px;border-radius:4px}.sc-stock-page .segmented button.active,.sc-stock-page .age button.active,.sc-stock-page .filter-chip.active{color:#1769e0;border-color:#1769e0;background:#edf4ff}.sc-stock-page .age{display:flex;gap:5px;flex-wrap:wrap}.sc-stock-page .kpis{display:grid;grid-template-columns:repeat(6,1fr);gap:10px;margin-bottom:14px}.sc-stock-page .card{background:#fff;border:1px solid #dfe4e9;border-radius:7px;padding:15px;margin-bottom:14px}.sc-stock-page .kpi{min-height:80px;text-align:left}.sc-stock-page .kpi.clickable{cursor:pointer}.sc-stock-page .kpi.clickable:hover{border-color:#1769e0}.sc-stock-page .kpi span,.sc-stock-page .muted{display:block;color:#76808d;font-size:12px}.sc-stock-page .kpi strong{display:block;font-size:22px;margin:5px 0}.sc-stock-page .table{overflow:auto;border:1px solid #e1e5ea;border-radius:5px}.sc-stock-page table{width:100%;border-collapse:collapse;font-size:12px}.sc-stock-page th,.sc-stock-page td{padding:9px 10px;border-bottom:1px solid #edf0f3;text-align:left;vertical-align:top}.sc-stock-page th{background:#f7f8fa;color:#68727f;font-weight:500}.sc-stock-page .link{color:#1769e0;cursor:pointer;background:none;border:0;padding:0;text-decoration:none}.sc-stock-page .btn{border:1px solid #d6dce3;background:#fff;border-radius:4px;padding:8px 12px;color:#222831;cursor:pointer}.sc-stock-page .btn.primary{background:#1769e0;border-color:#1769e0;color:#fff}.sc-stock-page .warn{color:#a14b00;font-weight:600}.sc-stock-page .actions{display:flex;gap:8px;align-items:center;flex-wrap:wrap}.sc-stock-page .empty{padding:28px;text-align:center;color:#76808d}.sc-stock-page .toast{position:fixed;right:26px;bottom:26px;background:#162033;color:#fff;padding:10px 14px;border-radius:6px;box-shadow:0 8px 30px rgba(0,0,0,.18);z-index:20}.sc-stock-page .footer{display:flex;justify-content:space-between;gap:10px;flex-wrap:wrap;margin-top:10px}@media(max-width:1100px){.sc-stock-page .kpis{grid-template-columns:repeat(3,1fr)}}@media(max-width:900px){.sc-stock-page .kpis{grid-template-columns:repeat(2,1fr)}.sc-stock-page .body{padding:10px}}@media(max-width:620px){.sc-stock-page .kpis{grid-template-columns:1fr}.sc-stock-page .toolbar input{min-width:0;width:100%}}')};
${forgeProductUiRuntime}
function App(){const adapter=useAdapter();
 const [state,setState]=useState({loading:true,error:'',rows:[],suppliers:[],orders:[],skus:[],materials:[],ledgers:[]}),[search,setSearch]=useState(''),[view,setView]=useState('supplier'),[age,setAge]=useState('all'),[overdue,setOverdue]=useState(false),[supplierFilter,setSupplierFilter]=useState(''),[toast,setToast]=useState('');
 async function request(path,options={}){const r=await fetch('/api/v1'+path,{credentials:'include',headers:{'Content-Type':'application/json',...(adapter?.getAuthHeaders?.()||{}),...(options.headers||{})},...options}),p=await r.json().catch(()=>({}));if(!r.ok)throw new Error((typeof p.error==='string'?p.error:p.error?.message)||(Array.isArray(p.fields)&&p.fields.length?p.fields.map(f=>f.message||f.label).filter(Boolean).join('；'):'')||p.message||'请求失败');return p}
 async function find(name){if(adapter?.find){const p=await adapter.find(name,{limit:1000});return p?.records||p?.data?.records||p?.data||[]}return(await request('/data/'+name+'?$top=1000')).records||[]}
 async function load(){try{const [rows,suppliers,orders,skus,materials,ledgers]=await Promise.all(['forge_subcontract_stock_balance','forge_supplier','forge_subcontract_order','forge_material_sku','forge_material','forge_subcontract_stock_ledger'].map(find));setState({loading:false,error:'',rows,suppliers,orders,skus,materials,ledgers})}catch(e){setState(s=>({...s,loading:false,error:String(e.message||e)}))}}
 useEffect(()=>{load()},[]);useEffect(()=>{if(!toast)return;const t=setTimeout(()=>setToast(''),2200);return()=>clearTimeout(t)},[toast]);
 const supplierMap=Object.fromEntries(state.suppliers.map(x=>[x.id,x])),orderMap=Object.fromEntries(state.orders.map(x=>[x.id,x])),skuMap=Object.fromEntries(state.skus.map(x=>[x.id,x])),materialMap=Object.fromEntries(state.materials.map(x=>[x.id,x]));
 const firstIssueAt=useMemo(()=>{const m={};state.ledgers.filter(x=>x.movement_type==='issue_inbound').forEach(x=>{if(!x.supplier_id||!x.order_id||!x.sku_id||!x.occurred_at)return;const key=x.supplier_id+':'+x.order_id+':'+x.sku_id;if(!m[key]||new Date(x.occurred_at)<new Date(m[key]))m[key]=x.occurred_at});return m},[state.ledgers]);
 const ageDays=timestamp=>timestamp?Math.max(0,Math.floor((Date.now()-new Date(timestamp).getTime())/86400000)):0;
 const enrichedRows=state.rows.map(row=>{const key=row.supplier_id+':'+row.order_id+':'+row.sku_id;return {...row,supplier:supplierMap[row.supplier_id],order:orderMap[row.order_id],sku:skuMap[row.sku_id],material:materialMap[skuMap[row.sku_id]?.material_id],firstIssueAt:firstIssueAt[key]||row.last_movement_at,age:ageDays(firstIssueAt[key]||row.last_movement_at)}}).filter(row=>row.supplier&&row.order&&Number(row.on_hand_quantity||0)>0);
 const supplierKey=row=>String(row.supplier?.name||'').trim().toLowerCase(),supplierOptions=[...new Map(enrichedRows.map(row=>[supplierKey(row),row.supplier])).entries()].map(([value,supplier])=>({value,label:supplier.name}));
 const stockRows=enrichedRows.filter(row=>!supplierFilter||supplierKey(row)===supplierFilter).filter(row=>!search||[row.supplier.name,row.order.code,row.material?.name,row.material?.code,row.sku?.name,row.sku?.specification,row.sku?.code].some(v=>String(v||'').toLowerCase().includes(search.toLowerCase()))).filter(row=>age==='all'||age==='7'&&row.age<=7||age==='30'&&row.age>7&&row.age<=30||age==='60'&&row.age>30&&row.age<=60||age==='over60'&&row.age>60).filter(row=>!overdue||row.age>30),groups=Object.values(stockRows.reduce((all,row)=>{const key=view==='supplier'?supplierKey(row):row.sku_id,g=all[key]||(all[key]={key,rows:[]});g.rows.push(row);return all},{}));
 const totalQuantity=stockRows.reduce((s,x)=>s+Number(x.on_hand_quantity||0),0),totalValue=stockRows.reduce((s,x)=>s+Number(x.inventory_value||0),0),maxAge=stockRows.reduce((m,x)=>Math.max(m,x.age),0),suppliersCount=new Set(stockRows.map(supplierKey)).size,skusCount=new Set(stockRows.map(x=>x.sku_id)).size,over30Count=new Set(enrichedRows.filter(x=>(!supplierFilter||supplierKey(x)===supplierFilter)&&x.age>30).map(x=>x.sku_id)).size;
 function formatQty(v){return Number(v||0).toLocaleString('zh-CN',{maximumFractionDigits:4})}
 function exportCsv(){const today=new Date().toISOString().slice(0,10),header=view==='supplier'?['供应商','订单数','物料种类','累计已发','倒冲耗用','累计退料','在外余量','在外金额']:['物料','规格','供应商','委外订单','累计已发','倒冲耗用','累计退料','在外余量','账龄'];const exportRows=view==='supplier'?groups.map(g=>g.rows):groups.flatMap(g=>g.rows),lines=[header,...(view==='supplier'?groups.map(g=>{const rows=g.rows;return[rows[0].supplier.name,new Set(rows.map(x=>x.order_id)).size,new Set(rows.map(x=>x.sku_id)).size,rows.reduce((s,x)=>s+Number(x.cumulative_issued_quantity||0),0),rows.reduce((s,x)=>s+Number(x.backflushed_quantity||0),0),rows.reduce((s,x)=>s+Number(x.returned_quantity||0),0),rows.reduce((s,x)=>s+Number(x.on_hand_quantity||0),0),rows.reduce((s,x)=>s+Number(x.inventory_value||0),0)]}):exportRows.map(x=>[x.material?.name||x.sku?.name,x.sku?.specification||'—',x.supplier.name,x.order.code,x.cumulative_issued_quantity,x.backflushed_quantity,x.returned_quantity,x.on_hand_quantity,x.age+' 天']))];const csv='\ufeff'+lines.map(row=>row.map(v=>'"'+String(v??'').replaceAll('"','""')+'"').join(',')).join('\\n');const a=document.createElement('a');a.href=URL.createObjectURL(new Blob([csv],{type:'text/csv;charset=utf-8'}));a.download='委外厂库存-'+today+'.csv';a.click();URL.revokeObjectURL(a.href);setToast('已导出 '+Math.max(0,lines.length-1)+' 条委外厂库存记录')}
 function resetFilters(){setSearch('');setAge('all');setOverdue(false);setSupplierFilter('')}
 if(state.loading)return <div className="forge-product sc-stock-page"><style>{css}</style><div className="body">正在加载委外厂库存…</div></div>;
 if(state.error)return <div className="forge-product sc-stock-page"><style>{css}</style><div className="body"><div className="card">{state.error}</div></div></div>;
 return <div className="forge-product sc-stock-page"><style>{css}</style><ForgeHero section="生产 / 委外管理 / 委外厂库存" title="委外厂库存" description="按 委外订单 + 物料 维度查询发到外协厂的在途库存" icon="▤" tone="indigo" art="stack"/><div className="body"><div className="kpis"><div className="card kpi"><span>在途供应商</span><strong>{suppliersCount}</strong><span>持有我方物料的外协厂</span></div><div className="card kpi"><span>在途物料种类</span><strong>{skusCount}</strong><span>SKU 数</span></div><div className="card kpi"><span>在途总数量</span><strong>{formatQty(totalQuantity)}</strong><span>已发 − 已耗 − 已退</span></div><div className="card kpi"><span>在外金额</span><strong>¥{totalValue.toFixed(2)}</strong><span>余量 × 材料单价</span></div><div className="card kpi"><span>最长账龄</span><strong>{maxAge} 天</strong><span>从首次发料起算</span></div><button className={'card kpi clickable '+(overdue?'active':'')} aria-pressed={overdue} onClick={()=>setOverdue(!overdue)}><span>超 30 天 SKU</span><strong>{over30Count}</strong><span>点击仅看超期</span></button></div><section className="card"><div className="top-actions"><div className="segmented" role="tablist" aria-label="库存视角"><button className={view==='supplier'?'active':''} aria-pressed={view==='supplier'} onClick={()=>setView('supplier')}>供应商视角</button><button className={view==='material'?'active':''} aria-pressed={view==='material'} onClick={()=>setView('material')}>物料视角</button></div><div className="actions"><button className="btn primary" onClick={()=>window.location.href='/_console/apps/com.inoforge.forge.production/page_subcontract_issue_workspace'}>去发料</button><button className="btn" onClick={exportCsv}>导出</button></div></div><div className="toolbar"><label className="muted">账龄</label><div className="age">{[['all','全部'],['7','0-7 天'],['30','8-30 天'],['60','31-60 天'],['over60','>60 天']].map(x=><button className={age===x[0]?'active':''} aria-pressed={age===x[0]} key={x[0]} onClick={()=>setAge(x[0])}>{x[1]}</button>)}</div><input className="fp-input" aria-label="搜索委外订单 / 物料 / 规格 / 供应商" placeholder="搜索委外订单 / 物料 / 规格 / 供应商" value={search} onChange={e=>setSearch(e.target.value)}/><ForgeSelectControl className="fp-select" aria-label="供应商" value={supplierFilter} onChange={e=>setSupplierFilter(e.target.value)}><option value="">全部</option>{supplierOptions.map(x=><option key={x.value} value={x.value}>{x.label}</option>)}</ForgeSelectControl>{(search||age!=='all'||overdue||supplierFilter)&&<button className="filter-chip" onClick={resetFilters}>清空筛选</button>}</div><div className="table"><table><thead><tr>{view==='supplier'?<><th>供应商</th><th>订单数</th><th>物料种类</th><th>累计已发</th><th>倒冲耗用</th><th>累计退料</th><th>在外余量</th><th>在外金额</th><th>操作</th></>:<><th>物料</th><th>规格</th><th>供应商</th><th>委外订单</th><th>累计已发</th><th>倒冲耗用</th><th>累计退料</th><th>在外余量</th><th>账龄</th></>}</tr></thead><tbody>{view==='supplier'?groups.map(g=>{const rows=g.rows,quantity=rows.reduce((s,x)=>s+Number(x.on_hand_quantity||0),0),value=rows.reduce((s,x)=>s+Number(x.inventory_value||0),0);return <tr key={g.key}><td>{rows[0].supplier.name}</td><td>{new Set(rows.map(x=>x.order_id)).size}</td><td>{new Set(rows.map(x=>x.sku_id)).size}</td><td>{formatQty(rows.reduce((s,x)=>s+Number(x.cumulative_issued_quantity||0),0))}</td><td>{formatQty(rows.reduce((s,x)=>s+Number(x.backflushed_quantity||0),0))}</td><td>{formatQty(rows.reduce((s,x)=>s+Number(x.returned_quantity||0),0))}</td><td>{formatQty(quantity)}</td><td>¥{value.toFixed(2)}</td><td><button className="link" onClick={()=>{setSupplierFilter(supplierKey(rows[0]));setView('material')}}>查看明细</button></td></tr>}):groups.flatMap(g=>g.rows.map(x=><tr key={x.id}><td>{x.material?.name||x.sku?.name||'—'}</td><td>{x.sku?.specification||'—'}</td><td>{x.supplier.name}</td><td><button className="link" onClick={()=>window.location.href='/_console/apps/com.inoforge.forge.production/page_subcontract_workspace?order_id='+encodeURIComponent(x.order.id)}>{x.order.code}</button></td><td>{formatQty(x.cumulative_issued_quantity)}</td><td>{formatQty(x.backflushed_quantity)}</td><td>{formatQty(x.returned_quantity)}</td><td className={x.age>30?'warn':''}>{formatQty(x.on_hand_quantity)}</td><td className={x.age>30?'warn':''}>{x.age} 天</td></tr>))}{!groups.length&&<tr><td colSpan={view==='supplier'?9:9} className="empty">暂无符合条件的在途库存</td></tr>}</tbody></table></div><div className="footer muted"><span>共 {view==='supplier'?groups.length:stockRows.length} 条记录 · 每页 20 条 · 1 / 1</span><span>在外余量 = 累计已发 − 倒冲消耗 − 累计已退料。回厂入库时按 BOM 标准用量自动倒冲核销，委外订单完工后余量趋近 0。</span></div></section>{toast&&<div className="toast" role="status">{toast}</div>}</div></div>;
}
export default App;
`;
export const SubcontractStockPage = { name: 'page_subcontract_stock', label: '委外库存', description: '按供应商、订单和物料查询委外厂在途库存', icon: 'warehouse', type: 'app' as const, kind: 'react' as const, source };
