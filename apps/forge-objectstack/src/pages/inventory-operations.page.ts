import { forgeProductUiCss, forgeProductUiRuntime } from "./product-ui.js";

const inventoryOperationsCss = `
.forge-inventory-ops .io-kpi{display:flex;align-items:center;gap:12px}.forge-inventory-ops .io-kpi-icon{flex:0 0 auto;width:34px;height:34px;border-radius:8px;background:var(--fp-primary-soft);color:var(--fp-primary);display:flex;align-items:center;justify-content:center}.forge-inventory-ops .io-kpi-icon svg{width:17px;height:17px}.forge-inventory-ops .fp-filterbar.io-lock-filter{grid-template-columns:minmax(200px,1.3fr) minmax(120px,.5fr) minmax(120px,.5fr) minmax(140px,.7fr) auto auto}.forge-inventory-ops .io-total{justify-self:end;align-self:center;color:var(--fp-muted);font-size:12px}.forge-inventory-ops .fp-tab{display:inline-flex;align-items:center;gap:6px}.forge-inventory-ops .fp-tab svg{width:15px;height:15px}
.forge-inventory-ops .fp-shell{max-width:1520px}.forge-inventory-ops .fp-filterbar{grid-template-columns:minmax(280px,1fr) minmax(150px,.35fr) auto}.forge-inventory-ops .fp-table{min-width:1120px}.forge-inventory-ops .fp-form-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:14px 16px}.forge-inventory-ops .fp-field+.fp-field{margin-top:12px}.forge-inventory-ops .io-actions{display:flex;gap:6px;flex-wrap:wrap}.forge-inventory-ops .io-title{font-weight:650;color:var(--fp-primary)}.forge-inventory-ops .io-sub{font-size:12px;color:var(--fp-muted);margin-top:3px}.forge-inventory-ops .alert-card{border-left:4px solid #d92d20}.forge-inventory-ops .alert-card.ok{border-left-color:#16845b}.forge-inventory-ops .verify-result{padding:14px;border:1px solid var(--fp-line);border-radius:8px;background:#f8fafc;line-height:1.8}.forge-inventory-ops .io-alert-switch{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:12px;margin:16px 0}.forge-inventory-ops .io-alert-switch button{display:flex;justify-content:space-between;align-items:center;text-align:left;border:1px solid var(--fp-line);border-radius:8px;background:var(--fp-surface);padding:16px;color:var(--fp-text)}.forge-inventory-ops .io-alert-switch button.active{border-color:var(--fp-primary);box-shadow:0 0 0 1px var(--fp-primary)}.forge-inventory-ops .io-alert-switch strong{display:block;font-size:15px}.forge-inventory-ops .io-alert-switch span{font-size:12px;color:var(--fp-muted)}.forge-inventory-ops .io-alert-switch em{font-style:normal;font-size:18px;font-weight:700}.forge-inventory-ops .io-alert-tabs{display:flex;gap:4px;margin-bottom:12px}.forge-inventory-ops .io-alert-tabs button{border:0;border-bottom:2px solid transparent;background:transparent;padding:8px 12px;color:var(--fp-muted)}.forge-inventory-ops .io-alert-tabs button.active{border-bottom-color:var(--fp-primary);color:var(--fp-primary);font-weight:650}.forge-inventory-ops .io-role-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:8px}.forge-inventory-ops .io-role-grid label{display:flex;gap:8px;align-items:center;border:1px solid var(--fp-line);border-radius:6px;padding:9px 10px}.forge-inventory-ops.delivery-active .fp-metrics,.forge-inventory-ops.delivery-active .fp-metrics+section,.forge-inventory-ops:has(.io-alert-switch button:nth-child(2).active) .fp-metrics,.forge-inventory-ops:has(.io-alert-switch button:nth-child(2).active) .fp-metrics+section{display:none}@media(max-width:760px){.forge-inventory-ops .fp-form-grid,.forge-inventory-ops .fp-filterbar,.forge-inventory-ops .io-alert-switch,.forge-inventory-ops .io-role-grid{grid-template-columns:1fr}}
`;

function source(mode: string) {
  return `
const MODE=${JSON.stringify(mode)};
function App(){
 const today=new Date().toISOString().slice(0,10),defs={lock:{title:'库存锁定',description:'业务占用、人工预留与释放记录统一管理。',add:'新增业务锁库'},count:{title:'库存盘点',description:'制定盘点计划、执行盘点与盘亏盘盈处理。',add:'新建盘点'},transfer:{title:'调拨与借出',description:'跨仓库调拨、借出归还与外协领用单据管理。',add:'新建单据'},alerts:{title:'库存预警',description:'低库存、超期、滞销与异常预警的统一中心。',add:'物料预警配置'},damage:{title:'报损单',description:'登记损坏、丢失和盘亏，审批执行后扣减库存并保留报损流水。',add:'新建报损'},sn:{title:'SN码管理',description:'登记和验证供应商序列号，追踪产品当前出入库状态。',add:'登记SN码'}},def=defs[MODE];
 const [state,setState]=React.useState({loading:true,operations:[],balances:[],warehouses:[],skus:[],materials:[],suppliers:[],serials:[],users:[],orders:[],orderLines:[],ledgers:[],alertPolicies:[],error:''}),[query,setQuery]=React.useState(''),[status,setStatus]=React.useState(''),[dialog,setDialog]=React.useState(null),[busy,setBusy]=React.useState(false),[toast,setToast]=React.useState(''),[verify,setVerify]=React.useState(''),[alertScope,setAlertScope]=React.useState('inventory'),[alertKind,setAlertKind]=React.useState('all'),[lockTab,setLockTab]=React.useState('business'),[lockSource,setLockSource]=React.useState(''),[lockWarehouse,setLockWarehouse]=React.useState(''),[lockSelected,setLockSelected]=React.useState([]);
 async function request(path,options){const response=await fetch('/api/v1'+path,{credentials:'include',headers:{'Content-Type':'application/json'},...options}),payload=await response.json().catch(()=>({}));if(!response.ok)throw new Error((typeof payload.error==='string'?payload.error:payload.error?.message)||(Array.isArray(payload.fields)&&payload.fields.length?payload.fields.map(f=>f.message||f.label).filter(Boolean).join('；'):'')||payload.message||'请求失败');return payload}
 async function find(object){return(await request('/data/'+object+'?$top=500')).records||[]}
 async function load(){setState(s=>({...s,loading:true,error:''}));try{const [operations,balances,warehouses,skus,materials,suppliers,serials,users,orders,orderLines,ledgers,alertPolicies]=await Promise.all(['forge_inventory_operation','forge_inventory_balance','forge_warehouse','forge_material_sku','forge_material','forge_supplier','forge_inventory_serial_number','sys_user','forge_purchase_order','forge_purchase_order_line','forge_inventory_ledger','forge_inventory_alert_policy'].map(find));setState({loading:false,operations,balances,warehouses,skus,materials,suppliers,serials,users,orders,orderLines,ledgers,alertPolicies,error:''})}catch(error){setState(s=>({...s,loading:false,error:String(error.message||error)}))}}
 React.useEffect(()=>{load()},[]);
 const by=(rows,id)=>rows.find(x=>x.id===id),warehouse=id=>by(state.warehouses,id)?.name||'—',sku=id=>by(state.skus,id),material=id=>by(state.materials,sku(id)?.material_id),item=id=>(material(id)?.name||sku(id)?.name||'—')+' · '+(sku(id)?.code||'—'),user=id=>by(state.users,id)?.name||'Dev Admin',countdown=row=>{if(!row.due_at)return '—';const days=Math.ceil((new Date(row.due_at).getTime()-Date.now())/86400000);return days>=0?days+' 天':'已超期 '+Math.abs(days)+' 天'},qty=v=>Number(v||0).toLocaleString('zh-CN',{maximumFractionDigits:4}),money=v=>'¥'+Number(v||0).toLocaleString('zh-CN',{minimumFractionDigits:2,maximumFractionDigits:2});
 const typeForMode={lock:['lock'],count:['count'],transfer:['transfer','loan'],damage:['damage']},statusText={draft:'草稿',pending_approval:'待审批',active:'生效中',completed:'已完成',released:'已释放',returned:'已归还',rejected:'已驳回',voided:'已作废',in_stock:'在库',outbound:'已出库'},typeText={lock:'库存锁定',count:'库存盘点',transfer:'仓库调拨',loan:'物料借出',damage:'库存报损'};
 const opRows=state.operations.filter(x=>(typeForMode[MODE]||[]).includes(x.operation_type)).filter(x=>!status||x.status===status).filter(x=>!query.trim()||[x.code,x.name,x.source_code,x.counterpart,item(x.sku_id),warehouse(x.source_warehouse_id),warehouse(x.target_warehouse_id)].join(' ').toLowerCase().includes(query.trim().toLowerCase()));
 const serialRows=state.serials.filter(x=>(!status||x.status===status)&&(!query.trim()||[x.code,x.inbound_code,x.outbound_code,item(x.sku_id)].join(' ').toLowerCase().includes(query.trim().toLowerCase())));
 const policy=state.alertPolicies[0]||null,policyValue=(field,fallback)=>policy&&policy[field]!==null&&policy[field]!==undefined?policy[field]:fallback,lowEnabled=policyValue('low_stock_enabled',true)!==false,lowLeadDays=Number(policyValue('low_stock_lead_days',7)),overstockEnabled=policyValue('overstock_enabled',true)!==false,overstockPercent=Number(policyValue('overstock_threshold_percent',120)),slowEnabled=policyValue('slow_stock_enabled',true)!==false,globalSlowDays=Number(policyValue('slow_stock_days',180)),deliveryEnabled=policyValue('delivery_alert_enabled',true)!==false,deliveryDays=Number(policyValue('delivery_warning_days',7)),deliveryUrgentDays=Number(policyValue('delivery_urgent_days',2));
 const ageDays=date=>date?Math.floor((Date.now()-new Date(date).getTime())/86400000):null,recentCutoff=Date.now()-30*86400000,monthlyOutbound=skuId=>state.ledgers.filter(x=>x.sku_id===skuId&&x.direction==='outbound'&&new Date(x.occurred_at).getTime()>=recentCutoff).reduce((sum,x)=>sum+Number(x.quantity||0),0),alerts=state.balances.flatMap(b=>{const result=[],minimum=Number(b.minimum_quantity||0),maximum=Number(b.maximum_quantity||0),dailyUse=monthlyOutbound(b.sku_id)/30,projected=Number(b.available_quantity||0)-dailyUse*lowLeadDays;if(lowEnabled&&minimum>0&&projected<minimum)result.push({kind:'low',label:'低库存',severity:Number(b.available_quantity||0)<minimum?'紧急':'中等',threshold:minimum,balance:b});if(overstockEnabled&&maximum>0&&Number(b.on_hand_quantity||0)>maximum*overstockPercent/100)result.push({kind:'high',label:'超储预警',severity:'中等',threshold:maximum*overstockPercent/100,balance:b});const age=ageDays(b.last_movement_at),slow=Number(b.slow_days||globalSlowDays);if(slowEnabled&&Number(b.on_hand_quantity||0)>0&&age!==null&&age>=slow)result.push({kind:'slow',label:'呆滞库存',severity:'提醒',threshold:slow,balance:b});return result}).filter(x=>(alertKind==='all'||x.kind===alertKind)&&(!query.trim()||[x.label,item(x.balance.sku_id),warehouse(x.balance.warehouse_id)].join(' ').toLowerCase().includes(query.trim().toLowerCase())));
 const orderById=id=>by(state.orders,id),dayDiff=date=>date?Math.ceil((new Date(date+'T23:59:59').getTime()-Date.now())/86400000):99999,deliveryAlerts=(deliveryEnabled?state.orderLines.flatMap(line=>{const order=orderById(line.order_id),remaining=Math.max(0,Number(line.quantity||0)-Number(line.arrived_quantity||0)),due=line.expected_arrival_on||(order&&order.expected_arrival_on),days=dayDiff(due);if(!order||!remaining||['completed','cancelled','rejected'].includes(order.status)||days>deliveryDays)return[];return[{kind:days<0?'overdue':'due',label:days<0?'交期已逾期':'临近交期',severity:days<0||days<=deliveryUrgentDays?'紧急':'提醒',line,order,remaining,due,days}] }):[]).filter(x=>!query.trim()||[x.label,x.order.code,x.line.item_code,x.line.name,by(state.suppliers,x.order.supplier_id)?.name].join(' ').toLowerCase().includes(query.trim().toLowerCase()));
 function toggleLockRow(id){setLockSelected(c=>c.includes(id)?c.filter(x=>x!==id):[...c,id])}function toggleLockAll(){setLockSelected(c=>c.length===lockViewRows.length&&lockViewRows.length>0?[]:lockViewRows.map(x=>x.id))}
function exportLock(){const src=lockSelected.length?lockViewRows.filter(r=>lockSelected.includes(r.id)):lockViewRows,head=['锁定单号','物料名称','型号','规格/SKU','批次号','业务来源','来源单号','锁定数量','已释放','状态','锁定日期'],body=src.map(r=>[r.code,item(r.sku_id),material(r.sku_id)?.model||'',sku(r.sku_id)?.name||sku(r.sku_id)?.code||'',r.batch_number||'',r.source_type||'',r.source_code||'',r.quantity,r.released_quantity,statusText[r.status]||r.status,r.planned_on||'']),csv=[head,...body].map(row=>row.map(v=>'"'+String(v==null?'':v).replace(/"/g,'""')+'"').join(',')).join('\\n'),a=document.createElement('a');a.href=URL.createObjectURL(new Blob(['\\ufeff'+csv],{type:'text/csv'}));a.download='库存锁定.csv';a.click();setToast('已导出 '+body.length+' 条锁定记录')}
 function openCreate(){if(MODE==='alerts')return setDialog({kind:'alert',step:'form',balance_id:'',minimum_quantity:'0',maximum_quantity:'0',slow_days:String(globalSlowDays),error:''});if(MODE==='sn')return setDialog({kind:'sn',step:'form',code:'',sku_id:'',inbound_code:'',batch_number:'',supplier_id:'',remarks:'',error:''});setDialog({kind:'create',step:'form',operation_type:MODE==='transfer'?'transfer':MODE,document_name:'',balance_id:'',target_warehouse_id:'',quantity:'',actual_quantity:'',source_type:MODE==='lock'?'手动锁库':MODE==='count'?'全盘':MODE==='transfer'?'普通调拨':MODE==='damage'?'损坏':'日常作业',source_code:'',counterpart:'',reason:'',planned_on:today,due_at:'',lock_accounting:false,error:''})}
 function openPolicy(){const roles=String(policyValue('notification_positions','warehouse,purchasing')).split(',').filter(Boolean);setDialog({kind:'policy',step:'form',low_stock_enabled:lowEnabled,low_stock_lead_days:String(lowLeadDays),overstock_enabled:overstockEnabled,overstock_threshold_percent:String(overstockPercent),slow_stock_enabled:slowEnabled,slow_stock_days:String(globalSlowDays),delivery_alert_enabled:deliveryEnabled,delivery_warning_days:String(deliveryDays),delivery_urgent_days:String(deliveryUrgentDays),notification_positions:roles,remarks:String(policyValue('remarks','')),error:''})}
 const selectedBalance=dialog&&by(state.balances,dialog.balance_id);
 function validateCreate(){if(dialog?.kind==='policy'){if(Number(dialog.low_stock_lead_days)<0||Number(dialog.overstock_threshold_percent)<100||Number(dialog.slow_stock_days)<1||Number(dialog.delivery_warning_days)<0||Number(dialog.delivery_urgent_days)<0)throw new Error('请填写有效的预警阈值');if(Number(dialog.delivery_urgent_days)>Number(dialog.delivery_warning_days))throw new Error('紧急预警天数不能大于临近预警天数');return}if(MODE==='alerts'){if(!selectedBalance)throw new Error('请选择仓库物料');return}if(MODE==='sn'){if(!dialog.code.trim()||!dialog.sku_id||!dialog.inbound_code.trim())throw new Error('请填写SN码、产品和入库单号');return}if(!selectedBalance)throw new Error('请选择仓库物料');const type=dialog.operation_type,quantity=type==='count'?Number(dialog.actual_quantity):Number(dialog.quantity);if(type!=='count'&&!(quantity>0))throw new Error('数量必须大于0');if(type==='count'&&!(quantity>=0))throw new Error('实盘数量不能小于0');if(type==='count'&&!dialog.document_name.trim())throw new Error('请填写盘点名称');if(!dialog.reason.trim())throw new Error('请填写办理原因');if(type==='transfer'&&!dialog.target_warehouse_id)throw new Error('请选择调入仓库');if(type==='loan'&&!dialog.counterpart.trim())throw new Error('请填写借用方')}
 function nextCreate(){try{validateCreate();setDialog(d=>({...d,step:'confirm',error:''}))}catch(error){setDialog(d=>({...d,error:String(error.message||error)}))}}
 function createImpact(){if(dialog?.kind==='policy')return '保存后将立即按新规则重新计算库存和采购交期预警。';if(MODE==='alerts')return '保存后将按新的最低库存、最高库存和呆滞天数更新预警结果。';if(MODE==='sn')return '登记后可通过该序列号查询产品的入库、出库和验证状态。';if(dialog.operation_type==='lock')return '创建后先保存为锁库草稿，不会立即占用库存；确认锁库后才会减少可用数量。';if(dialog.operation_type==='count')return '创建后先保存为盘点草稿，不会立即调整库存；完成盘点后才会形成盘盈或盘亏。';if(dialog.operation_type==='transfer')return '创建后先保存为调拨草稿，不会立即移动库存；审批执行后才会完成仓库间调拨。';if(dialog.operation_type==='loan')return '创建后先保存为借出草稿，不会立即扣减库存；审批执行后才会办理借出。';return '创建后先保存为报损草稿，不会立即扣减库存；审批执行后才会完成报损。'}
 function confirmTitle(){if(dialog?.kind==='policy')return '确认保存预警设置';if(MODE==='alerts')return '确认保存库存预警';if(MODE==='sn')return '确认登记SN码';return '确认'+def.add}
 async function create(){setBusy(true);try{validateCreate();if(dialog?.kind==='policy'){const body={name:'默认库存预警设置',code:'INVENTORY-ALERT-DEFAULT',low_stock_enabled:!!dialog.low_stock_enabled,low_stock_lead_days:Number(dialog.low_stock_lead_days),overstock_enabled:!!dialog.overstock_enabled,overstock_threshold_percent:Number(dialog.overstock_threshold_percent),slow_stock_enabled:!!dialog.slow_stock_enabled,slow_stock_days:Number(dialog.slow_stock_days),delivery_alert_enabled:!!dialog.delivery_alert_enabled,delivery_warning_days:Number(dialog.delivery_warning_days),delivery_urgent_days:Number(dialog.delivery_urgent_days),notification_positions:dialog.notification_positions.join(','),remarks:dialog.remarks.trim()||null};await request('/data/forge_inventory_alert_policy'+(policy?'/'+policy.id:''),{method:policy?'PATCH':'POST',body:JSON.stringify(body)});setToast('预警设置已保存并重新计算')}else if(MODE==='alerts'){if(!selectedBalance)throw new Error('请选择仓库物料');await request('/data/forge_inventory_balance/'+selectedBalance.id,{method:'PATCH',body:JSON.stringify({minimum_quantity:Number(dialog.minimum_quantity||0),maximum_quantity:Number(dialog.maximum_quantity||0),slow_days:Number(dialog.slow_days||globalSlowDays)})});setToast('物料预警参数已保存')}else if(MODE==='sn'){if(!dialog.code.trim()||!dialog.sku_id||!dialog.inbound_code.trim())throw new Error('请填写SN码、产品和入库单号');await request('/data/forge_inventory_serial_number',{method:'POST',body:JSON.stringify({name:dialog.code.trim(),code:dialog.code.trim(),sku_id:dialog.sku_id,inbound_code:dialog.inbound_code.trim(),batch_number:dialog.batch_number.trim()||null,supplier_id:dialog.supplier_id||null,status:'in_stock',responsible_id:state.users[0]?.id,remarks:dialog.remarks.trim()||null})});setToast('SN码已登记')}else{if(!selectedBalance)throw new Error('请选择仓库物料');const type=dialog.operation_type,quantity=type==='count'?Number(dialog.actual_quantity):Number(dialog.quantity);if(type!=='count'&&!(quantity>0))throw new Error('数量必须大于0');if(type==='count'&&!(quantity>=0))throw new Error('实盘数量不能小于0');if(type==='count'&&!dialog.document_name.trim())throw new Error('请填写盘点名称');if(!dialog.reason.trim())throw new Error('请填写办理原因');if(type==='transfer'&&!dialog.target_warehouse_id)throw new Error('请选择调入仓库');if(type==='loan'&&!dialog.counterpart.trim())throw new Error('请填写借用方');const code=(type==='lock'?'LOCK':type==='count'?'CHK':type==='transfer'?'TRF':type==='loan'?'LOAN':'DMG')+'-'+Date.now();await request('/data/forge_inventory_operation',{method:'POST',body:JSON.stringify({name:(dialog.document_name.trim()||code+' '+item(selectedBalance.sku_id)),code,operation_type:type,source_warehouse_id:selectedBalance.warehouse_id,target_warehouse_id:dialog.target_warehouse_id||null,sku_id:selectedBalance.sku_id,quantity:type==='count'?Number(selectedBalance.on_hand_quantity):quantity,system_quantity:Number(selectedBalance.on_hand_quantity),actual_quantity:type==='count'?quantity:0,source_type:dialog.source_type,source_code:dialog.source_code.trim()||null,counterpart:dialog.counterpart.trim()||null,reason:dialog.reason.trim(),planned_on:dialog.planned_on,due_at:dialog.due_at||null,lock_accounting:!!dialog.lock_accounting,status:'draft',responsible_id:state.users[0]?.id})});setToast(def.title+'草稿已创建，请在列表继续办理')};setDialog(null);await load()}catch(error){setDialog(d=>({...d,error:String(error.message||error)}))}finally{setBusy(false)}}
 async function invoke(row,action){setBusy(true);try{await request('/actions/forge_inventory_operation/'+action+'/'+row.id,{method:'POST',body:JSON.stringify({params:{}})});setDialog(null);setToast(action==='inventory_operation_submit'?typeText[row.operation_type]+'已提交审批':action==='inventory_lock_activate'?'库存已锁定':action==='inventory_lock_release'?'库存已释放':action==='inventory_count_complete'?'库存盘点已完成':action==='inventory_loan_return'?'借出物料已归还':typeText[row.operation_type]+'已执行');await load()}catch(error){setDialog(d=>({...d,error:String(error.message||error)}))}finally{setBusy(false)}}
 function ask(row,action,title,impact,danger=false){setDialog({kind:'confirm',row,action,title,impact,danger,error:''})}
 async function verifySn(){const code=verify.trim(),row=state.serials.find(x=>x.code===code);if(!row){setToast('未找到该SN码');return}setBusy(true);try{await request('/actions/forge_inventory_serial_number/inventory_serial_verify/'+row.id,{method:'POST',body:JSON.stringify({params:{}})});setDialog({kind:'verify',row});await load()}catch(error){setToast(String(error.message||error))}finally{setBusy(false)}}
 const lockOps=state.operations.filter(x=>x.operation_type==='lock'),lockManualRows=opRows.filter(x=>String(x.source_type||'')==='人工操作'),lockBusinessRows=opRows.filter(x=>String(x.source_type||'')!=='人工操作'),lockViewRows=(MODE==='lock'&&lockTab==='manual'?lockManualRows:lockBusinessRows).filter(x=>(!lockSource||String(x.source_type||'')===lockSource)&&(!lockWarehouse||x.source_warehouse_id===lockWarehouse)),lockRemaining=lockOps.reduce((sum,x)=>sum+Math.max(0,Number(x.quantity||0)-Number(x.released_quantity||0)),0),lockActive=lockOps.filter(x=>['draft','pending_approval','active'].includes(x.status)),lockMaterialIds=[...new Set(lockOps.map(x=>x.sku_id).filter(Boolean))],lockSummary=lockMaterialIds.map(id=>{const rs=lockOps.filter(x=>x.sku_id===id),locked=rs.reduce((sum,x)=>sum+Math.max(0,Number(x.quantity||0)-Number(x.released_quantity||0)),0),onHand=state.balances.filter(b=>b.sku_id===id).reduce((sum,b)=>sum+Number(b.on_hand_quantity||0),0);return{id,name:item(id),locked,records:rs.length,ratio:onHand>0?Math.round(locked/onHand*100):0}}).sort((a,b)=>b.locked-a.locked),lockHighCount=lockSummary.filter(x=>x.ratio>=50).length,lockLogRows=lockOps.slice().sort((a,b)=>String(b.created_at||'').localeCompare(String(a.created_at||'')));
 const rows=MODE==='sn'?serialRows:opRows,total=rows.length;
 function actions(row){return <div className="io-actions"><button className="fp-button small" onClick={()=>setDialog({kind:'view',row})}>查看</button>{row.operation_type==='lock'&&row.status==='draft'&&<button className="fp-button small primary" onClick={()=>ask(row,'inventory_lock_activate','确认锁定库存','确认后将增加占用数量并减少可用库存。')}>确认锁库</button>}{row.operation_type==='lock'&&row.status==='active'&&<button className="fp-button small danger" onClick={()=>ask(row,'inventory_lock_release','释放库存锁定','确认后将释放剩余锁定数量并恢复可用库存。',true)}>释放库存</button>}{row.operation_type==='count'&&row.status==='draft'&&<button className="fp-button small primary" onClick={()=>ask(row,'inventory_count_complete','完成库存盘点','确认后按照实盘数量调整库存，盘盈或盘亏将写入库存流水。')}>完成盘点</button>}{['transfer','loan','damage'].includes(row.operation_type)&&row.status==='draft'&&<button className="fp-button small primary" onClick={()=>ask(row,'inventory_operation_submit','提交'+typeText[row.operation_type]+'审批','提交后库存不会立即变化，需审批执行。')}>提交审批</button>}{row.status==='pending_approval'&&<button className="fp-button small primary" onClick={()=>ask(row,'inventory_operation_approve','审批并执行'+typeText[row.operation_type],'确认后会立即变更库存并写入可追溯流水。')}>审批并执行</button>}{row.operation_type==='loan'&&row.status==='active'&&<button className="fp-button small primary" onClick={()=>ask(row,'inventory_loan_return','确认借出归还','确认后物料将回到原仓库并生成归还流水。')}>确认归还</button>}</div>}
 return <div className={"forge-product forge-inventory-ops "+(MODE==="alerts"&&alertScope==="delivery"?"delivery-active":"")}><style>{${JSON.stringify(forgeProductUiCss + inventoryOperationsCss)}}</style><div className="fp-shell"><ForgeHero section={'供应链 / 库存管理 / '+def.title} title={def.title} description={def.description} icon="▤" tone="blue" art="stack"/>{MODE==='lock'&&<div className="fp-metrics">{[["剩余锁定数量",qty(lockRemaining),"#1d4ed8","#e8efff","M6 4h12v16H6zM9 9h6M9 13h6"],["活跃锁定明细",lockActive.length,"#15803d","#e7f6ee","M4 7h16M4 12h16M4 17h10"],["涉及物料",lockMaterialIds.length,"#6d28d9","#f1e9fe","M12 3l8 4.5v9L12 21l-8-4.5v-9z"],["高锁定率物料",lockHighCount,"#b45309","#fdf1e3","M12 4l9 16H3z"]].map(([label,value,color,bg,d])=><div className="fp-metric io-kpi" key={label}><span className="io-kpi-icon" aria-hidden="true" style={{color,background:bg}}><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round"><path d={d}/></svg></span><div><div className="fp-metric-label">{label}</div><div className="fp-metric-value">{value}</div></div></div>)}</div>}{MODE!=='lock'&&<div className="fp-action-row">{MODE==='sn'&&<><input className="fp-input" aria-label="输入SN码验证" placeholder="输入SN码验证" value={verify} onChange={e=>setVerify(e.target.value)}/><button className="fp-button" disabled={!verify.trim()||busy} onClick={verifySn}>验证SN码</button></>}{MODE==='lock'&&<><button className="fp-button" disabled={!lockViewRows.length} onClick={exportLock}>导出</button><button className="fp-button" onClick={()=>setToast('库存锁定导入使用平台导入能力；本页导出按钮按当前筛选结果导出 CSV。')}>导入/导出</button></>}<ForgeListSettings/><button className="fp-icon-button" aria-label="刷新" title="刷新" onClick={load}><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round"><path d="M21 12a9 9 0 1 1-3-6.7"/><path d="M21 3v6h-6"/></svg></button><button className="fp-button" onClick={openCreate}>{def.add}</button>{MODE==='alerts'&&<button className="fp-button primary" onClick={openPolicy}>预警设置</button>}</div>}{MODE==='lock'&&<div className="fp-tabs">{[['business','业务锁库','M7 11V8a5 5 0 0 1 10 0v3','M5 11h14v9H5z'],['manual','手动锁库','M9 11V7a3 3 0 0 1 6 0v4','M7 11h10v9H7z'],['summary','物料锁定概况','M4 6h16M4 12h16M4 18h16','M8 4v16'],['log','锁库操作日志','M12 7v5l3 2','M21 12a9 9 0 1 1-9-9 9 9 0 0 1 9 9z']].map(x=><button key={x[0]} className={'fp-tab '+(lockTab===x[0]?'active':'')} onClick={()=>setLockTab(x[0])}><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round" aria-hidden="true"><path d={x[2]}/>{x[3]&&<path d={x[3]}/>}</svg>{x[1]}</button>)}</div>}{state.error&&<ForgeNotice tone="error">{state.error}</ForgeNotice>}{toast&&<div className="fp-toast" role="status">{toast}</div>}
 {MODE==='alerts'&&<><div className="io-alert-switch"><button className={alertScope==='inventory'?'active':''} onClick={()=>setAlertScope('inventory')}><span><strong>库存预警</strong><span>{alerts.length?'低库存、超储与呆滞提醒':'暂无预警'}</span></span><em>{alerts.length||''}</em></button><button className={alertScope==='delivery'?'active':''} onClick={()=>setAlertScope('delivery')}><span><strong>交期预警</strong><span>{deliveryAlerts.length?deliveryAlerts.filter(x=>x.kind==='due').length+' 临近':'暂无预警'}</span></span><em>{deliveryAlerts.length||''}</em></button></div>{alertScope==='inventory'?<div className="io-alert-tabs">{[['all','全部'],['low','低库存'],['high','超储预警'],['slow','呆滞库存']].map(([value,label])=><button key={value} className={'fp-tab '+(alertKind===value?'active':'')} onClick={()=>setAlertKind(value)}>{label}</button>)}</div>:<section className="fp-card"><div className="fp-filterbar"><input className="fp-input" aria-label="搜索交期预警" placeholder="搜索物料名称、编号或采购订单" value={query} onChange={e=>setQuery(e.target.value)}/><span></span><button className="fp-button" onClick={()=>setQuery('')}>清空筛选</button></div><div className="fp-table-wrap"><table className="fp-table"><thead><tr><th>预警类型</th><th>采购订单</th><th>物料</th><th>供应商</th><th>未到货数量</th><th>预计到货</th><th>剩余天数</th><th>等级</th></tr></thead><tbody>{deliveryAlerts.map(x=><tr key={x.line.id}><td><strong>{x.label}</strong></td><td>{x.order.code}</td><td>{x.line.name}<div className="io-sub">{x.line.item_code||'—'}</div></td><td>{by(state.suppliers,x.order.supplier_id)?.name||'—'}</td><td>{qty(x.remaining)} {x.line.unit_name||''}</td><td>{x.due}</td><td>{x.days<0?'逾期 '+Math.abs(x.days)+' 天':x.days+' 天'}</td><td>{x.severity}</td></tr>)}{!deliveryAlerts.length&&<tr><td colSpan="8" className="fp-empty-cell"><ForgeEmpty title="交期状态良好" description="当前没有临近或逾期的采购交期预警。"/></td></tr>}</tbody></table></div></section>}</>}
 {MODE==='alerts'?<>{MODE==='lock'&&lockTab==='summary'&&<section className="fp-card"><div className="fp-report-title">物料锁定概况</div><div className="fp-table-wrap"><table className="fp-table"><thead><tr><th>物料</th><th>锁定记录</th><th>剩余锁定数量</th><th>占在库比例</th></tr></thead><tbody>{lockSummary.map(x=><tr key={x.id}><td><strong>{x.name}</strong></td><td>{x.records}</td><td>{qty(x.locked)}</td><td>{x.ratio}%</td></tr>)}{!lockSummary.length&&<tr><td colSpan="4" className="fp-empty-cell"><ForgeEmpty title="暂无锁定物料" description="业务锁库或手动锁库后，会按物料汇总在这里。"/></td></tr>}</tbody></table></div></section>}{MODE==='lock'&&lockTab==='log'&&<section className="fp-card"><div className="fp-report-title">锁库操作日志</div><div className="fp-table-wrap"><table className="fp-table"><thead><tr><th>时间</th><th>锁定单号</th><th>业务来源</th><th>物料</th><th>数量</th><th>状态</th></tr></thead><tbody>{lockLogRows.map(x=><tr key={x.id}><td>{String(x.created_at||'').slice(0,16).replace('T',' ')||'—'}</td><td><strong>{x.code}</strong></td><td>{x.source_type||'—'}</td><td>{item(x.sku_id)}</td><td>{qty(x.quantity)}</td><td><ForgeStatus value={x.status} label={statusText[x.status]||x.status}/></td></tr>)}{!lockLogRows.length&&<tr><td colSpan="6" className="fp-empty-cell"><ForgeEmpty title="暂无锁库日志" description="确认锁库或释放后，操作会记录在这里。"/></td></tr>}</tbody></table></div></section>}<div className="fp-metrics"><div className="fp-metric"><div className="fp-metric-label">紧急预警</div><div className="fp-metric-value">{alerts.filter(x=>x.severity==='紧急').length}</div></div><div className="fp-metric"><div className="fp-metric-label">中等预警</div><div className="fp-metric-value">{alerts.filter(x=>x.severity==='中等').length}</div></div><div className="fp-metric"><div className="fp-metric-label">提醒</div><div className="fp-metric-value">{alerts.filter(x=>x.severity==='提醒').length}</div></div><div className="fp-metric"><div className="fp-metric-label">预警资产额</div><div className="fp-metric-value">{money(alerts.reduce((s,x)=>s+Number(x.balance.inventory_value||0),0))}</div></div></div><section className="fp-card"><div className="fp-filterbar"><input className="fp-input" aria-label="搜索库存预警" placeholder="搜索物料或仓库" value={query} onChange={e=>setQuery(e.target.value)}/><span></span><button className="fp-button" onClick={()=>setQuery('')}>清空筛选</button></div><div className="fp-table-wrap"><table className="fp-table"><thead><tr><th>预警类型</th><th>物料</th><th>仓库</th><th>当前库存</th><th>可用库存</th><th>阈值</th><th>最近变动</th><th>等级</th><th>操作</th></tr></thead><tbody>{alerts.map((x,i)=><tr key={x.balance.id+':'+x.kind}><td><strong>{x.label}</strong></td><td>{item(x.balance.sku_id)}</td><td>{warehouse(x.balance.warehouse_id)}</td><td>{qty(x.balance.on_hand_quantity)}</td><td>{qty(x.balance.available_quantity)}</td><td>{x.kind==='low'?'最低 '+qty(x.balance.minimum_quantity):x.kind==='high'?'最高 '+qty(x.balance.maximum_quantity):qty(x.balance.slow_days)+' 天'}</td><td>{String(x.balance.last_movement_at||'—').slice(0,10)}</td><td>{x.severity}</td><td><button className="fp-button small" onClick={()=>setDialog({kind:'alert',balance_id:x.balance.id,minimum_quantity:String(x.balance.minimum_quantity||0),maximum_quantity:String(x.balance.maximum_quantity||0),slow_days:String(x.balance.slow_days||90),error:''})}>调整参数</button></td></tr>)}{!alerts.length&&<tr><td colSpan="9" className="fp-empty-cell"><ForgeEmpty title="库存状态良好" description="当前没有匹配的低库存、超储或呆滞库存预警。"/></td></tr>}</tbody></table></div></section></>:<>{MODE==='lock'&&lockTab==='summary'&&<section className="fp-card"><div className="fp-report-title">物料锁定概况</div><div className="fp-table-wrap"><table className="fp-table"><thead><tr><th>物料</th><th>锁定记录</th><th>剩余锁定数量</th><th>占在库比例</th></tr></thead><tbody>{lockSummary.map(x=><tr key={x.id}><td><strong>{x.name}</strong></td><td>{x.records}</td><td>{qty(x.locked)}</td><td>{x.ratio}%</td></tr>)}{!lockSummary.length&&<tr><td colSpan="4" className="fp-empty-cell"><ForgeEmpty title="暂无锁定物料" description="业务锁库或手动锁库后，会按物料汇总在这里。"/></td></tr>}</tbody></table></div></section>}{MODE==='lock'&&lockTab==='log'&&<section className="fp-card"><div className="fp-report-title">锁库操作日志</div><div className="fp-table-wrap"><table className="fp-table"><thead><tr><th>时间</th><th>锁定单号</th><th>业务来源</th><th>物料</th><th>数量</th><th>状态</th></tr></thead><tbody>{lockLogRows.map(x=><tr key={x.id}><td>{String(x.created_at||'').slice(0,16).replace('T',' ')||'—'}</td><td><strong>{x.code}</strong></td><td>{x.source_type||'—'}</td><td>{item(x.sku_id)}</td><td>{qty(x.quantity)}</td><td><ForgeStatus value={x.status} label={statusText[x.status]||x.status}/></td></tr>)}{!lockLogRows.length&&<tr><td colSpan="6" className="fp-empty-cell"><ForgeEmpty title="暂无锁库日志" description="确认锁库或释放后，操作会记录在这里。"/></td></tr>}</tbody></table></div></section>}{MODE!=='lock'&&<div className="fp-metrics"><div className="fp-metric"><div className="fp-metric-label">{MODE==='lock'?'剩余锁定数量':def.title+'记录'}</div><div className="fp-metric-value">{MODE==='lock'?qty(lockRemaining):total}</div></div><div className="fp-metric"><div className="fp-metric-label">{MODE==='lock'?'活跃锁定明细':'待办理'}</div><div className="fp-metric-value">{MODE==='lock'?lockActive.length:rows.filter(x=>['draft','pending_approval','active'].includes(x.status)).length}</div></div><div className="fp-metric"><div className="fp-metric-label">{MODE==='lock'?'涉及物料':'已完成'}</div><div className="fp-metric-value">{MODE==='lock'?lockMaterialIds.length:rows.filter(x=>['completed','released','returned'].includes(x.status)).length}</div></div>{MODE==='lock'&&<div className="fp-metric"><div className="fp-metric-label">高锁定率物料</div><div className="fp-metric-value">{lockHighCount}</div></div>}</div>}{!(MODE==='lock'&&(lockTab==='summary'||lockTab==='log'))&&<section className="fp-card">{MODE==='lock'&&<div className="fp-card-toolbar"><button className="fp-button primary" onClick={openCreate}>新增业务锁库</button><button className="fp-button" disabled={!lockViewRows.length} onClick={exportLock}>导出 ▾</button><span className="fp-grow"/>{lockSelected.length>0&&<span className="io-total">已选 {lockSelected.length} 条</span>}<ForgeListSettings/><button className="fp-icon-button" aria-label="刷新" title="刷新" onClick={load}><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round"><path d="M21 12a9 9 0 1 1-3-6.7"/><path d="M21 3v6h-6"/></svg></button><button className="fp-icon-button" aria-label="导入/导出任务" title="导入/导出任务" onClick={()=>setToast('库存锁定导入使用平台导入能力；本页导出按钮按当前筛选或勾选结果导出 CSV。')}><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round"><path d="M12 3v12"/><path d="M7 10l5 5 5-5"/><path d="M5 21h14"/></svg></button></div>}<div className={"fp-filterbar"+(MODE==='lock'?' io-lock-filter':'')}><input className="fp-input" aria-label={'搜索'+def.title} placeholder="搜索单号、物料、仓库或来源" value={query} onChange={e=>setQuery(e.target.value)}/>{MODE==='lock'&&<ForgeSelectControl className="fp-select" aria-label="业务类型" value={lockSource} onChange={e=>setLockSource(e.target.value)}><option value="">全部业务类型</option>{[...new Set(lockOps.map(x=>x.source_type).filter(Boolean))].map(v=><option key={v} value={v}>{v}</option>)}</ForgeSelectControl>}<ForgeSelectControl className="fp-select" aria-label="状态" value={status} onChange={e=>setStatus(e.target.value)}><option value="">全部状态</option>{Object.entries(statusText).map(([value,label])=><option key={value} value={value}>{label}</option>)}</ForgeSelectControl>{MODE==='lock'&&<><ForgeSelectControl className="fp-select" aria-label="仓库" value={lockWarehouse} onChange={e=>setLockWarehouse(e.target.value)}><option value="">全部仓库</option>{state.warehouses.map(w=><option key={w.id} value={w.id}>{w.name}</option>)}</ForgeSelectControl></>}<button className="fp-button" onClick={()=>{setQuery('');setStatus('');setLockSource('');setLockWarehouse('')}}>清空筛选</button>{MODE==='lock'&&<span className="io-total">共 {lockViewRows.length} 条</span>}</div>{state.loading?<ForgeLoading label={'正在加载'+def.title}/>:<div className="fp-table-wrap"><table className="fp-table"><thead>{MODE==='lock'?<tr><th><input type="checkbox" aria-label="选择当前页" checked={lockViewRows.length>0&&lockSelected.length===lockViewRows.length} onChange={toggleLockAll}/></th><th>锁定单号</th><th>物料名称</th><th>型号</th><th>规格 / SKU</th><th>批次号</th><th>业务来源</th><th>来源单号</th><th>锁定数量</th><th>已释放</th><th>状态</th><th>业务状态</th><th>锁定日期</th><th>到期释放时间</th><th>剩余倒计时</th><th>操作</th></tr>:MODE==='sn'?<tr><th>#</th><th>供应商序列号</th><th>产品名称</th><th>入库单号</th><th>批次号</th><th>供应商</th><th>出库状态</th><th>出库单号</th><th>最近验证</th></tr>:<tr><th>单号</th><th>业务类型</th><th>物料</th><th>发出/所在仓库</th><th>接收方</th><th>数量</th><th>金额/差异</th><th>日期</th><th>状态</th><th>经手人</th><th>操作</th></tr>}</thead><tbody>{MODE==='lock'?lockViewRows.map(row=><tr key={row.id}><td><input type="checkbox" aria-label={'选择 '+row.code} checked={lockSelected.includes(row.id)} onChange={()=>toggleLockRow(row.id)}/></td><td><span className="io-title">{row.code}</span></td><td>{material(row.sku_id)?.name||sku(row.sku_id)?.name||'—'}</td><td>{material(row.sku_id)?.model||'—'}</td><td>{sku(row.sku_id)?.name||sku(row.sku_id)?.code||'—'}</td><td>{row.batch_number||'—'}</td><td>{row.source_type||'—'}</td><td>{row.source_code||'—'}</td><td>{qty(row.quantity)}</td><td>{qty(row.released_quantity)}</td><td><ForgeStatus value={row.status} label={statusText[row.status]||row.status}/></td><td>{typeText[row.operation_type]}</td><td>{row.planned_on||'—'}</td><td>{row.due_at?String(row.due_at).slice(0,16).replace('T',' '):'—'}</td><td>{countdown(row)}</td><td>{actions(row)}</td></tr>):MODE==='sn'?rows.map((row,i)=><tr key={row.id}><td>{i+1}</td><td><strong>{row.code}</strong></td><td>{item(row.sku_id)}</td><td>{row.inbound_code}</td><td>{row.batch_number||'—'}</td><td>{by(state.suppliers,row.supplier_id)?.name||'—'}</td><td><ForgeStatus value={row.status} label={statusText[row.status]||row.status}/></td><td>{row.outbound_code||'—'}</td><td>{row.verified_at?String(row.verified_at).slice(0,19).replace('T',' '):'—'}</td></tr>):rows.map(row=><tr key={row.id}><td><span className="io-title">{row.code}</span><div className="io-sub">{row.source_code||row.reason}</div></td><td>{typeText[row.operation_type]}</td><td>{item(row.sku_id)}</td><td>{warehouse(row.source_warehouse_id)}{row.target_warehouse_id&&<div className="io-sub">→ {warehouse(row.target_warehouse_id)}</div>}</td><td>{row.counterpart||'—'}</td><td>{qty(row.quantity)}{row.operation_type==='count'&&<div className="io-sub">实盘 {qty(row.actual_quantity)}</div>}</td><td>{row.operation_type==='count'?'差异 '+qty(row.variance_quantity):money(row.amount)}</td><td>{row.planned_on}</td><td><ForgeStatus value={row.status} label={statusText[row.status]||row.status}/></td><td>{user(row.responsible_id)}</td><td>{actions(row)}</td></tr>)}{!(MODE==='lock'?lockViewRows.length:rows.length)&&<tr><td colSpan={MODE==='lock'?16:MODE==='sn'?9:11} className="fp-empty-cell"><ForgeEmpty title={'暂无'+def.title+'记录'} description={'点击“'+def.add+'”开始办理。'} action={<button className="fp-button primary" onClick={openCreate}>{def.add}</button>}/></td></tr>}</tbody></table></div>}<div className="fp-pagination"><span>共 {(MODE==='lock'?lockViewRows.length:rows.length)} 条记录</span><span>每页 20 条 · 1 / 1</span></div></section>}</>}</div>
 <ForgeDialog open={dialog?.kind==='policy'} title="预警设置" subtitle="库存、交期与通知规则" error={dialog?.error} busy={busy} confirmLabel={dialog?.step==='confirm'?'确认保存':'保存设置'} onCancel={()=>!busy&&setDialog(null)} onConfirm={()=>dialog?.step==='confirm'?create():nextCreate()}>{dialog?.step==='confirm'?<div><div className="fp-dialog-impact">{createImpact()}</div><div className="fp-confirm-list"><p>低库存提前 {dialog.low_stock_lead_days} 天 · 超储阈值 {dialog.overstock_threshold_percent}% · 呆滞 {dialog.slow_stock_days} 天</p><p>交期临近 {dialog.delivery_warning_days} 天 · 紧急 {dialog.delivery_urgent_days} 天</p><p>通知角色 {(dialog?.notification_positions||[]).length?(dialog?.notification_positions||[]).join('、'):'未选择'}</p></div></div>:<><h4>库存预警规则</h4><div className="fp-field"><label className="fp-check-label"><input type="checkbox" checked={dialog?.low_stock_enabled||false} onChange={e=>setDialog({...dialog,low_stock_enabled:e.target.checked})}/> 低库存预警</label><div className="io-sub">当预计库存低于安全库存线时触发预警</div></div><div className="fp-field"><label>预警提前天数</label><input className="fp-input" aria-label="低库存预警提前天数" inputMode="numeric" value={dialog?.low_stock_lead_days||''} onChange={e=>setDialog({...dialog,low_stock_lead_days:e.target.value})}/><div className="io-sub">基于最近 30 天平均出库量计算</div></div><div className="fp-field"><label className="fp-check-label"><input type="checkbox" checked={dialog?.overstock_enabled||false} onChange={e=>setDialog({...dialog,overstock_enabled:e.target.checked})}/> 超储预警</label></div><div className="fp-field"><label>超储阈值（相对最大库存 %）</label><input className="fp-input" aria-label="超储阈值" inputMode="decimal" value={dialog?.overstock_threshold_percent||''} onChange={e=>setDialog({...dialog,overstock_threshold_percent:e.target.value})}/></div><div className="fp-field"><label className="fp-check-label"><input type="checkbox" checked={dialog?.slow_stock_enabled||false} onChange={e=>setDialog({...dialog,slow_stock_enabled:e.target.checked})}/> 呆滞库存预警</label></div><div className="fp-field"><label>呆滞天数</label><input className="fp-input" aria-label="呆滞天数设置" inputMode="numeric" value={dialog?.slow_stock_days||''} onChange={e=>setDialog({...dialog,slow_stock_days:e.target.value})}/></div><h4>交期预警规则</h4><div className="fp-field"><label className="fp-check-label"><input type="checkbox" checked={dialog?.delivery_alert_enabled||false} onChange={e=>setDialog({...dialog,delivery_alert_enabled:e.target.checked})}/> 采购交期预警</label></div><div className="fp-form-grid"><div className="fp-field"><label>临近预警天数</label><input className="fp-input" aria-label="交期临近预警天数" inputMode="numeric" value={dialog?.delivery_warning_days||''} onChange={e=>setDialog({...dialog,delivery_warning_days:e.target.value})}/></div><div className="fp-field"><label>紧急预警天数</label><input className="fp-input" aria-label="交期紧急预警天数" inputMode="numeric" value={dialog?.delivery_urgent_days||''} onChange={e=>setDialog({...dialog,delivery_urgent_days:e.target.value})}/></div></div><h4>通知设置</h4><div className="io-role-grid">{[['warehouse','仓库管理员'],['purchasing','采购员'],['production','生产计划员'],['manager','供应链负责人']].map(([value,label])=><label key={value}><input type="checkbox" checked={(dialog?.notification_positions||[]).includes(value)} onChange={e=>setDialog({...dialog,notification_positions:e.target.checked?[...dialog.notification_positions,value]:dialog.notification_positions.filter(x=>x!==value)})}/>{label}</label>)}</div><div className="fp-field"><label>备注</label><textarea className="fp-textarea" aria-label="预警设置备注" value={dialog?.remarks||''} onChange={e=>setDialog({...dialog,remarks:e.target.value})}/></div></>}</ForgeDialog>
 <ForgeDialog open={dialog?.kind==='view'} title={'查看'+def.title} subtitle={dialog?.row?dialog.row.code:''} confirmLabel="关闭" onCancel={()=>setDialog(null)} onConfirm={()=>setDialog(null)}>{dialog?.row&&<div className="fp-detail-grid"><div><span>单号</span><strong>{dialog.row.code}</strong></div><div><span>业务类型</span><strong>{typeText[dialog.row.operation_type]||dialog.row.operation_type}</strong></div><div><span>物料</span><strong>{item(dialog.row.sku_id)}</strong></div><div><span>批次</span><strong>{dialog.row.batch_number||'—'}</strong></div><div><span>发出/所在仓库</span><strong>{warehouse(dialog.row.source_warehouse_id)}</strong></div><div><span>接收方</span><strong>{dialog.row.counterpart||'—'}</strong></div><div><span>数量</span><strong>{qty(dialog.row.quantity)}</strong></div><div><span>已释放</span><strong>{qty(dialog.row.released_quantity)}</strong></div><div><span>金额</span><strong>{money(dialog.row.amount)}</strong></div><div><span>状态</span><strong>{statusText[dialog.row.status]||dialog.row.status}</strong></div><div><span>业务日期</span><strong>{dialog.row.planned_on||'—'}</strong></div><div><span>到期时间</span><strong>{dialog.row.due_at?String(dialog.row.due_at).slice(0,16).replace('T',' '):'—'}</strong></div><div><span>原因</span><strong>{dialog.row.reason||'—'}</strong></div><div><span>经手人</span><strong>{user(dialog.row.responsible_id)}</strong></div></div>}</ForgeDialog><ForgeDialog open={!!dialog&&dialog?.kind!=='policy'} title={dialog?.kind==='confirm'?dialog.title:dialog?.kind==='verify'?'SN码验证结果':dialog?.step==='confirm'?confirmTitle():def.add} subtitle={dialog?.kind==='confirm'?dialog.row.code:dialog?.step==='confirm'?(selectedBalance?warehouse(selectedBalance.warehouse_id)+' · '+item(selectedBalance.sku_id):MODE==='sn'?(dialog.code||'待登记序列号'):def.title):def.description} error={dialog?.error} busy={busy} danger={!!dialog?.danger} confirmLabel={dialog?.kind==='confirm'?'确认执行':dialog?.kind==='verify'?'关闭':dialog?.step==='confirm'?(MODE==='alerts'?'确认保存':MODE==='sn'?'确认登记':'确认创建'):'下一步'} onCancel={()=>!busy&&setDialog(null)} onConfirm={()=>dialog?.kind==='confirm'?invoke(dialog.row,dialog.action):dialog?.kind==='verify'?setDialog(null):dialog?.step==='confirm'?create():nextCreate()}>{dialog?.kind==='confirm'?<div className="fp-dialog-impact">{dialog.impact}<br/>单据 {dialog.row.code}<br/>物料 {item(dialog.row.sku_id)}<br/>数量 {qty(dialog.row.quantity)}</div>:dialog?.kind==='verify'?<div className="verify-result"><strong>{dialog.row.code}</strong><br/>{item(dialog.row.sku_id)}<br/>入库单：{dialog.row.inbound_code}<br/>状态：{statusText[dialog.row.status]||dialog.row.status}</div>:dialog?.step==='confirm'?<div><div className="fp-dialog-impact">{createImpact()}</div>{MODE==='alerts'?<div className="fp-confirm-list"><p><strong>{warehouse(selectedBalance?.warehouse_id)} · {item(selectedBalance?.sku_id)}</strong></p><p>最低库存 {qty(dialog.minimum_quantity)} · 最高库存 {qty(dialog.maximum_quantity)} · 呆滞 {qty(dialog.slow_days)} 天</p></div>:MODE==='sn'?<div className="fp-confirm-list"><p><strong>{dialog.code}</strong></p><p>{item(dialog.sku_id)} · 入库单 {dialog.inbound_code}</p></div>:<div className="fp-confirm-list"><p><strong>{typeText[dialog.operation_type]} · {item(selectedBalance?.sku_id)}</strong></p><p>{warehouse(selectedBalance?.warehouse_id)}{dialog.target_warehouse_id?' → '+warehouse(dialog.target_warehouse_id):''} · 数量 {qty(dialog.operation_type==='count'?dialog.actual_quantity:dialog.quantity)}</p><p>业务日期 {dialog.planned_on} · 原因 {dialog.reason}</p></div>}</div>:dialog?.kind==='alert'?<div className="fp-form-grid"><div className="fp-field"><label>仓库物料 *</label><ForgeSelectControl className="fp-select" aria-label="预警物料库存" value={dialog.balance_id} onChange={e=>{const b=by(state.balances,e.target.value);setDialog({...dialog,balance_id:e.target.value,minimum_quantity:String(b?.minimum_quantity||0),maximum_quantity:String(b?.maximum_quantity||0),slow_days:String(b?.slow_days||90),error:''})}}><option value="">请选择</option>{state.balances.map(b=><option key={b.id} value={b.id}>{warehouse(b.warehouse_id)} · {item(b.sku_id)} · 可用 {qty(b.available_quantity)}</option>)}</ForgeSelectControl></div><div className="fp-field"><label>最低库存</label><input className="fp-input" aria-label="最低库存" inputMode="decimal" value={dialog.minimum_quantity} onChange={e=>setDialog({...dialog,minimum_quantity:e.target.value})}/></div><div className="fp-field"><label>最高库存</label><input className="fp-input" aria-label="最高库存" inputMode="decimal" value={dialog.maximum_quantity} onChange={e=>setDialog({...dialog,maximum_quantity:e.target.value})}/></div><div className="fp-field"><label>呆滞天数</label><input className="fp-input" aria-label="呆滞天数" inputMode="numeric" value={dialog.slow_days} onChange={e=>setDialog({...dialog,slow_days:e.target.value})}/></div></div>:dialog?.kind==='sn'?<div className="fp-form-grid"><div className="fp-field"><label>供应商序列号 *</label><input className="fp-input" aria-label="供应商序列号" value={dialog.code} onChange={e=>setDialog({...dialog,code:e.target.value})}/></div><div className="fp-field"><label>产品 *</label><ForgeSelectControl className="fp-select" aria-label="SN产品" value={dialog.sku_id} onChange={e=>setDialog({...dialog,sku_id:e.target.value})}><option value="">请选择</option>{state.skus.map(s=><option key={s.id} value={s.id}>{item(s.id)}</option>)}</ForgeSelectControl></div><div className="fp-field"><label>入库单号 *</label><input className="fp-input" aria-label="SN入库单号" value={dialog.inbound_code} onChange={e=>setDialog({...dialog,inbound_code:e.target.value})}/></div><div className="fp-field"><label>批次号</label><input className="fp-input" aria-label="SN批次号" value={dialog.batch_number} onChange={e=>setDialog({...dialog,batch_number:e.target.value})}/></div><div className="fp-field"><label>供应商</label><ForgeSelectControl className="fp-select" aria-label="SN供应商" value={dialog.supplier_id} onChange={e=>setDialog({...dialog,supplier_id:e.target.value})}><option value="">请选择</option>{state.suppliers.map(s=><option key={s.id} value={s.id}>{s.name}</option>)}</ForgeSelectControl></div><div className="fp-field"><label>备注</label><input className="fp-input" aria-label="SN备注" value={dialog.remarks} onChange={e=>setDialog({...dialog,remarks:e.target.value})}/></div></div>:dialog?.kind==='create'&&<><div className="fp-form-grid"><div className="fp-field"><label>业务类型 *</label>{MODE==='transfer'?<ForgeSelectControl className="fp-select" aria-label="库存作业类型" value={dialog.operation_type} onChange={e=>setDialog({...dialog,operation_type:e.target.value,source_type:e.target.value==='loan'?'借出管理':'普通调拨'})}><option value="transfer">普通调拨</option><option value="loan">借出管理</option></ForgeSelectControl>:<input className="fp-input" value={typeText[dialog.operation_type]} readOnly/>}</div>{MODE==='lock'&&<div className="fp-field"><label>锁库方式 *</label><ForgeSelectControl className="fp-select" aria-label="锁库方式" value={dialog.source_type} onChange={e=>setDialog({...dialog,source_type:e.target.value})}><option value="订单锁库">订单锁库</option><option value="手动锁库">手动锁库</option></ForgeSelectControl></div>}{MODE==='count'&&<><div className="fp-field"><label>盘点名称 *</label><input className="fp-input" aria-label="盘点名称" value={dialog.document_name} onChange={e=>setDialog({...dialog,document_name:e.target.value})}/></div><div className="fp-field"><label>盘点类型 *</label><ForgeSelectControl className="fp-select" aria-label="盘点类型" value={dialog.source_type} onChange={e=>setDialog({...dialog,source_type:e.target.value})}><option value="全盘">全盘</option><option value="抽盘">抽盘</option><option value="清盘">清盘</option></ForgeSelectControl></div></>}{MODE==='damage'&&<div className="fp-field"><label>报损类型 *</label><ForgeSelectControl className="fp-select" aria-label="报损类型" value={dialog.source_type} onChange={e=>setDialog({...dialog,source_type:e.target.value})}><option value="损坏">损坏</option><option value="丢失">丢失</option><option value="盘亏">盘亏</option></ForgeSelectControl></div>}<div className="fp-field"><label>仓库物料 *</label><ForgeSelectControl className="fp-select" aria-label="库存作业物料" value={dialog.balance_id} onChange={e=>setDialog({...dialog,balance_id:e.target.value,error:''})}><option value="">请选择</option>{state.balances.filter(b=>MODE==='count'||Number(b.available_quantity)>0).map(b=><option key={b.id} value={b.id}>{warehouse(b.warehouse_id)} · {item(b.sku_id)} · 现存 {qty(b.on_hand_quantity)} · 可用 {qty(b.available_quantity)}</option>)}</ForgeSelectControl></div>{dialog.operation_type==='transfer'&&<div className="fp-field"><label>接收仓库 *</label><ForgeSelectControl className="fp-select" aria-label="接收仓库" value={dialog.target_warehouse_id} onChange={e=>setDialog({...dialog,target_warehouse_id:e.target.value})}><option value="">请选择</option>{state.warehouses.filter(w=>w.id!==selectedBalance?.warehouse_id).map(w=><option key={w.id} value={w.id}>{w.name}</option>)}</ForgeSelectControl></div>}{dialog.operation_type==='count'?<div className="fp-field"><label>实盘数量 *</label><input className="fp-input" aria-label="实盘数量" inputMode="decimal" value={dialog.actual_quantity} onChange={e=>setDialog({...dialog,actual_quantity:e.target.value})}/></div>:<div className="fp-field"><label>数量 *</label><input className="fp-input" aria-label="库存作业数量" inputMode="decimal" value={dialog.quantity} onChange={e=>setDialog({...dialog,quantity:e.target.value})}/></div>}{dialog.operation_type==='loan'&&<div className="fp-field"><label>借用方 *</label><input className="fp-input" aria-label="借用方" value={dialog.counterpart} onChange={e=>setDialog({...dialog,counterpart:e.target.value})}/></div>}<div className="fp-field"><label>业务日期 *</label><ForgeDateInput className="fp-input" aria-label="库存作业日期" value={dialog.planned_on} onChange={e=>setDialog({...dialog,planned_on:e.target.value})}/></div>{MODE==='lock'&&<div className="fp-field"><label>计划释放日期</label><ForgeDateInput className="fp-input" aria-label="计划释放日期" value={dialog.due_at} onChange={e=>setDialog({...dialog,due_at:e.target.value})}/></div>}{MODE==='count'&&<div className="fp-field"><label className="fp-check-label"><input type="checkbox" checked={dialog.lock_accounting} onChange={e=>setDialog({...dialog,lock_accounting:e.target.checked})}/> 盘点期间锁定库存账</label></div>}<div className="fp-field"><label>来源单号</label><input className="fp-input" aria-label="来源单号" value={dialog.source_code} onChange={e=>setDialog({...dialog,source_code:e.target.value})}/></div></div><div className="fp-field"><label>原因 *</label><textarea className="fp-textarea" aria-label="库存作业原因" value={dialog.reason} onChange={e=>setDialog({...dialog,reason:e.target.value})}/></div></>}</ForgeDialog></div>}
export default App;
${forgeProductUiRuntime}
`;
}

const page = (name: string, label: string, icon: string, mode: string) => ({
  name,
  label,
  description: label,
  icon,
  type: "app" as const,
  kind: "react" as const,
  source: source(mode),
});
export const InventoryLocksPage = page(
  "page_inventory_locks",
  "库存锁定",
  "lock",
  "lock",
);
export const InventoryAlertsPage = page(
  "page_inventory_alerts",
  "库存预警",
  "bell-ring",
  "alerts",
);
