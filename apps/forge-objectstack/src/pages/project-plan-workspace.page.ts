import { forgeProductUiCss, forgeProductUiRuntime } from './product-ui.js';

const projectPlanCss = `
.forge-plan .fp-plan-list{overflow:hidden}.forge-plan .fp-plan-grid{display:grid;grid-template-columns:minmax(330px,1.15fr) minmax(480px,1.85fr);gap:14px}.forge-plan .fp-section{padding:16px}.forge-plan .fp-section-head{display:flex;align-items:center;justify-content:space-between;gap:12px;margin-bottom:12px}.forge-plan .fp-section-head h2{font-size:14px;margin:0}.forge-plan .fp-progress{height:7px;border-radius:99px;background:#e9edf4;overflow:hidden;min-width:90px}.forge-plan .fp-progress>span{display:block;height:100%;background:var(--fp-primary);border-radius:inherit}.forge-plan .fp-tree-row.phase td{background:#f8faff;font-weight:620}.forge-plan .fp-tree-row.delayed td{background:#fffaf2}.forge-plan .fp-gantt{display:grid;grid-template-columns:250px minmax(620px,1fr);min-width:870px}.forge-plan .fp-gantt-head,.forge-plan .fp-gantt-name,.forge-plan .fp-gantt-track{height:42px;border-bottom:1px solid #edf0f5}.forge-plan .fp-gantt-head{background:#f8f9fc;color:var(--fp-muted);font-size:11px;padding:12px}.forge-plan .fp-gantt-name{padding:11px 12px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.forge-plan .fp-gantt-track{position:relative;background-image:linear-gradient(to right,#edf0f5 1px,transparent 1px);background-size:calc(100% / 10) 100%}.forge-plan .fp-gantt-bar{position:absolute;top:11px;height:20px;min-width:6px;border-radius:5px;background:#7aa2f7;color:#fff;font-size:10px;padding:2px 6px;overflow:hidden;white-space:nowrap}.forge-plan .fp-gantt-bar.phase{background:#245bdb}.forge-plan .fp-gantt-bar.milestone{width:14px!important;min-width:14px;transform:rotate(45deg);border-radius:2px;background:#b86500;padding:0}.forge-plan .fp-gantt-bar.delayed{background:#c53b32}.forge-plan .fp-form-grid{display:grid;grid-template-columns:1fr 1fr;gap:13px}.forge-plan .fp-checkbox{display:flex;gap:8px;align-items:center;color:#475467;margin-top:11px}.forge-plan .fp-report{padding:12px;border:1px solid var(--fp-line);border-radius:9px;margin-bottom:9px}.forge-plan .fp-report-head{display:flex;justify-content:space-between;gap:12px;margin-bottom:5px}.forge-plan .fp-report-body{white-space:pre-wrap}.forge-plan .fp-report-meta{margin-top:7px;color:var(--fp-muted);font-size:12px}.forge-plan .fp-inline-actions{display:flex;gap:6px;align-items:center}.forge-plan .fp-tab-panel{padding:16px}.forge-plan .fp-context-link{border:0;background:transparent;color:var(--fp-primary);padding:0;font-weight:600}.forge-plan .fp-keyline{display:flex;gap:12px;align-items:center;flex-wrap:wrap;color:var(--fp-muted);font-size:11px;margin:8px 0 0}.forge-plan .fp-keyline i{width:14px;height:7px;border-radius:3px;background:#7aa2f7;display:inline-block;margin-right:4px}.forge-plan .fp-keyline i.phase{background:#245bdb}.forge-plan .fp-keyline i.milestone{background:#b86500}.forge-plan .fp-keyline i.delayed{background:#c53b32}@media(max-width:980px){.forge-plan .fp-plan-grid{grid-template-columns:1fr}}@media(max-width:760px){.forge-plan .fp-form-grid{grid-template-columns:1fr}.forge-plan .fp-section{padding:12px}}
.forge-plan .fp-baseline{border-left:3px solid #7a5af8}.forge-plan .fp-baseline-head{display:flex;align-items:flex-start;justify-content:space-between;gap:12px;margin-bottom:10px}.forge-plan .fp-baseline-grid{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:10px}.forge-plan .fp-baseline-cell{padding:10px 12px;background:#faf9ff;border:1px solid #e8e2ff;border-radius:6px}.forge-plan .fp-baseline-label{font-size:11px;color:#6f668d}.forge-plan .fp-baseline-value{margin-top:5px;font-weight:600;color:#282238}.forge-plan .fp-baseline-note{margin-top:10px;color:#6f668d;font-size:12px;line-height:1.6}@media(max-width:980px){.forge-plan .fp-baseline-grid{grid-template-columns:repeat(2,minmax(0,1fr))}}@media(max-width:760px){.forge-plan .fp-baseline-grid{grid-template-columns:1fr}}
`;

const projectPlanPageSource = `
const css=${JSON.stringify(forgeProductUiCss + projectPlanCss)};
function App(){const adapter=useAdapter();
  const initialId=new URLSearchParams(window.location.search).get('id');
  const [view,setView]=React.useState(initialId?'detail':'list');
  const [state,setState]=React.useState({loading:true,plans:[],projects:[],customers:[],orders:[],contracts:[],links:[],items:[],reports:[],users:[],evidence:[],identityMappings:[],plan:null,project:null,error:''});
  const [filters,setFilters]=React.useState({search:'',status:''});const [tab,setTab]=React.useState('计划表');const [busy,setBusy]=React.useState(false);const [dialog,setDialog]=React.useState(null);const [toast,setToast]=React.useState('');const [collapsed,setCollapsed]=React.useState({});
  async function request(path,options){const response=await ForgeApiResponse(adapter,path,{credentials:'include',headers:{'Content-Type':'application/json'},...options});const payload=await response.json().catch(()=>({}));if(!response.ok)throw new Error((typeof payload.error==='string'?payload.error:payload.error?.message)||(Array.isArray(payload.fields)&&payload.fields.length?payload.fields.map(f=>f.message||f.label).filter(Boolean).join('；'):'')||payload.message||'请求失败');return payload}
  async function fetchAll(object,filter){const rows=[];let skip=0;for(let guard=0;guard<100;guard++){const params=new URLSearchParams({$top:'100',$skip:String(skip)});if(filter)params.set('$filter',JSON.stringify(filter));const payload=await request('/data/'+object+'?'+params);const batch=payload.records||[];rows.push(...batch);if(batch.length<100)break;skip+=batch.length}return rows}
  function routeTo(id,replace=false){const url=id?window.location.pathname+'?id='+encodeURIComponent(id):window.location.pathname;window.history[replace?'replaceState':'pushState']({},'',url);if(id){setView('detail');setTab('计划表');loadDetail(id)}else{setView('list');loadList()}}
  async function loadList(){setState(s=>({...s,loading:true,error:'',plan:null}));try{const [plans,projects]=await Promise.all([fetchAll('forge_project_plan'),fetchAll('forge_project')]);setState(s=>({...s,loading:false,plans,projects,error:''}))}catch(error){setState(s=>({...s,loading:false,error:String(error.message||error)}))}}
  async function loadDetail(id){setState(s=>({...s,loading:true,error:''}));try{const detail=await request('/data/forge_project_plan/'+id),plan=detail.record;if(!plan)throw new Error('未找到指定项目计划，记录可能已删除或当前账号无权访问');const [plans,projects,customers,orders,contracts,links,items,reports,users,evidence,identityMappings]=await Promise.all([fetchAll('forge_project_plan'),fetchAll('forge_project'),fetchAll('forge_customer'),fetchAll('forge_sales_order'),fetchAll('forge_sales_contract'),fetchAll('forge_project_sales_link'),fetchAll('forge_project_work_item',{plan_id:id}),fetchAll('forge_project_daily_report',{plan_id:id}),fetchAll('sys_user').catch(()=>[]),fetchAll('forge_project_plan_evidence',{project_id:plan.project_id}).catch(()=>[]),fetchAll('forge_project_identity_mapping',{project_id:plan.project_id}).catch(()=>[])]);items.sort((a,b)=>Number(a.sort_order||0)-Number(b.sort_order||0));reports.sort((a,b)=>String(b.report_on||'').localeCompare(String(a.report_on||''))||String(b.created_at||'').localeCompare(String(a.created_at||'')));setState({loading:false,plans,projects,customers,orders,contracts,links,items,reports,users,evidence,identityMappings,plan,project:projects.find(x=>x.id===plan.project_id)||null,error:''})}catch(error){setState(s=>({...s,loading:false,plan:null,error:String(error.message||error)}))}}
  React.useEffect(()=>{const onPop=()=>{const id=new URLSearchParams(window.location.search).get('id');if(id){setView('detail');loadDetail(id)}else{setView('list');loadList()}};window.addEventListener('popstate',onPop);if(initialId)loadDetail(initialId);else loadList();return()=>window.removeEventListener('popstate',onPop)},[]);React.useEffect(()=>{if(!toast)return;const timer=setTimeout(()=>setToast(''),3200);return()=>clearTimeout(timer)},[toast]);
  function openCreate(){setDialog({kind:'create',title:'手工创建项目计划',subtitle:'选择一个进行中或已暂停且尚无执行中计划的项目',project_id:'',phase_name:'',owner_id:'',planned_start_on:'',planned_end_on:'',weight:'20',critical_path:false,planned_deliverable:'',error:''})}
  function openAdd(type='task'){const phase=state.items.find(x=>x.item_type==='phase');setDialog({kind:'add',title:'新增阶段、里程碑或任务',subtitle:'请填写工作项安排',item_type:type,name:'',parent_id:type==='phase'?'':phase?.id||'',owner_id:state.project?.manager_id||'',planned_start_on:'',planned_end_on:'',predecessor_ids:[],weight:'20',critical_path:false,planned_deliverable:'',error:''})}
  function openProgress(item){setDialog({kind:'progress',title:'更新工作项进度',subtitle:item.name,item,progress:String(item.progress||0),status:item.status||'pending',actual_start_on:item.actual_start_on||'',actual_end_on:item.actual_end_on||'',error:''})}
  function openReport(item){const today=new Date(Date.now()+8*60*60*1000).toISOString().slice(0,10);setDialog({kind:'report',title:'提交项目日报',subtitle:item.name,item,reporter_id:state.project?.manager_id||'',report_on:today,completed_today:'',completion_percent:String(item.progress||0),blockage:'',assistance_needed:'',expected_finish_changed:false,expected_finish_on:'',error:''})}
  async function invoke(object,action,id,params,success){setBusy(true);try{const payload=await request('/actions/'+object+'/'+action+'/'+id,{method:'POST',body:JSON.stringify({params})});const result=payload?.result||payload?.data?.result||payload?.data||payload;setDialog(null);setToast(success);if(action==='project_create_manual_plan'&&result?.id)routeTo(result.id,true);else if(state.plan)await loadDetail(state.plan.id);else await loadList()}catch(error){setDialog(d=>d?({...d,error:String(error.message||error)}):d)}finally{setBusy(false)}}
  function confirmDialog(){if(dialog.kind==='create'){if(!dialog.project_id||!dialog.phase_name.trim()||!dialog.planned_start_on||!dialog.planned_end_on)return setDialog({...dialog,error:'请选择项目并填写首个阶段名称和计划日期'});if(dialog.step!=='confirm')return setDialog({...dialog,step:'confirm',error:''});return invoke('forge_project','project_create_manual_plan',dialog.project_id,{phase_name:dialog.phase_name.trim(),owner_id:dialog.owner_id||null,planned_start_on:dialog.planned_start_on,planned_end_on:dialog.planned_end_on,weight:Number(dialog.weight),critical_path:dialog.critical_path,planned_deliverable:dialog.planned_deliverable||null},'项目计划已创建')}
    if(dialog.kind==='add'){if(!dialog.name.trim()||!dialog.planned_start_on||!dialog.planned_end_on)return setDialog({...dialog,error:'请填写工作项名称和计划日期'});if(dialog.step!=='confirm')return setDialog({...dialog,step:'confirm',error:''});return invoke('forge_project_plan','project_plan_add_work_item',state.plan.id,{item_type:dialog.item_type,name:dialog.name.trim(),parent_id:dialog.item_type==='phase'?null:(dialog.parent_id||null),owner_id:dialog.owner_id||null,planned_start_on:dialog.planned_start_on,planned_end_on:dialog.planned_end_on,predecessor_ids:dialog.predecessor_ids,weight:Number(dialog.weight),critical_path:dialog.critical_path,planned_deliverable:dialog.planned_deliverable||null},'计划工作项已添加')}
    if(dialog.kind==='progress'){if(dialog.step!=='confirm')return setDialog({...dialog,step:'confirm',error:''});return invoke('forge_project_work_item','project_work_item_update_progress',dialog.item.id,{progress:Number(dialog.progress),status:dialog.status,actual_start_on:dialog.actual_start_on||null,actual_end_on:dialog.actual_end_on||null},'工作项进度已更新')}
    if(dialog.kind==='report'){if(!dialog.reporter_id||!dialog.completed_today.trim())return setDialog({...dialog,error:'请填写填报人和今日完成内容'});if(dialog.expected_finish_changed&&!dialog.expected_finish_on)return setDialog({...dialog,error:'预计完成日期变化时请填写调整后的日期'});if(dialog.step!=='confirm')return setDialog({...dialog,step:'confirm',error:''});return invoke('forge_project_plan','project_plan_submit_daily_report',state.plan.id,{work_item_id:dialog.item.id,reporter_id:dialog.reporter_id,report_on:dialog.report_on,completed_today:dialog.completed_today.trim(),completion_percent:Number(dialog.completion_percent),blockage:dialog.blockage||null,assistance_needed:dialog.assistance_needed||null,expected_finish_changed:dialog.expected_finish_changed,expected_finish_on:dialog.expected_finish_changed?dialog.expected_finish_on:null},'项目日报已提交')}
  }
  const statusText={active:'执行中',superseded:'已替代',archived:'已归档',pending:'未开始',in_progress:'进行中',completed:'已完成',delayed:'已延期',cancelled:'已取消'};const typeText={phase:'阶段',milestone:'里程碑',task:'任务'};const sourceText={manual:'手工创建',system_template:'系统模板',custom_template:'我的模板',copied_project:'从项目复制'};const normalized=v=>String(v||'').toLowerCase();const userName=id=>{const user=state.users.find(x=>x.id===id),label=user?.display_name||user?.username||(user?.name!==id?user?.name:'');return label||(id&&id===state.project?.manager_id?'项目负责人':'未分配')||'—'};const projectById=Object.fromEntries(state.projects.map(x=>[x.id,x])),sourceLinks=state.links.filter(x=>x.project_id===state.project?.id).map(x=>{const order=state.orders.find(o=>o.id===x.order_id),contract=state.contracts.find(c=>c.id===x.contract_id);return{...x,order,contract}});
  function treeRows(){const byParent={};state.items.forEach(item=>{const key=item.parent_id||'__root__';(byParent[key]||(byParent[key]=[])).push(item)});const rows=[],seen=new Set();function walk(item,depth){if(seen.has(item.id))return;seen.add(item.id);const children=byParent[item.id]||[];rows.push({item,depth,hasChildren:children.length>0});if(!collapsed[item.id])children.forEach(child=>walk(child,depth+1))}(byParent.__root__||[]).forEach(item=>walk(item,0));state.items.filter(x=>!seen.has(x.id)).forEach(item=>walk(item,0));return rows}
  function List(){const eligibleProjects=state.projects.filter(project=>['in_progress','paused'].includes(project.status)&&!state.plans.some(plan=>plan.project_id===project.id&&plan.status==='active'));const filtered=state.plans.filter(plan=>(!filters.search||(normalized(plan.name).includes(normalized(filters.search))||normalized(plan.plan_key).includes(normalized(filters.search))||normalized(projectById[plan.project_id]?.name).includes(normalized(filters.search))))&&(!filters.status||plan.status===filters.status));return <div className="fp-shell"><div className="fp-list-context"><div className="fp-phase"><span className="fp-phase-index">03</span><strong>计划与方案设计</strong></div><div className="fp-toolbar"><ForgeListSettings/><button className="fp-icon-button" aria-label="刷新" title="刷新" disabled={state.loading} onClick={loadList}><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round"><path d="M21 12a9 9 0 1 1-3-6.7"/><path d="M21 3v6h-6"/></svg></button><button className="fp-button primary" disabled={!eligibleProjects.length} onClick={openCreate}>创建计划</button></div></div>{state.error&&<ForgeNotice tone="error">{state.error}</ForgeNotice>}<section className="fp-card fp-plan-list"><div className="fp-filterbar"><div className="fp-search"><input className="fp-input" aria-label="搜索项目计划" placeholder="搜索计划、编号或项目" value={filters.search} onChange={e=>setFilters({...filters,search:e.target.value})}/></div><ForgeSelectControl className="fp-select" aria-label="计划状态" value={filters.status} onChange={e=>setFilters({...filters,status:e.target.value})}><option value="">全部状态</option><option value="active">执行中</option><option value="superseded">已替代</option><option value="archived">已归档</option></ForgeSelectControl></div>{state.loading?<ForgeLoading label="正在加载项目计划"/>:filtered.length?<div className="fp-table-wrap"><table className="fp-table"><thead><tr><th>计划</th><th>项目</th><th>期间</th><th>工作项</th><th>进度</th><th>状态</th></tr></thead><tbody>{filtered.map(plan=><tr key={plan.id}><td><button className="fp-link-button" onClick={()=>routeTo(plan.id)}>{plan.name}</button><div className="fp-secondary">计划版本 V{plan.revision||1} · {sourceText[plan.source]||'其他来源'}</div></td><td>{projectById[plan.project_id]?.code||'—'}<div className="fp-secondary">{projectById[plan.project_id]?.name||'未关联项目'}</div></td><td>{plan.planned_start_on} 至 {plan.planned_end_on}</td><td>{plan.item_count||0}</td><td><div className="fp-progress"><span style={{width:Math.max(0,Math.min(100,Number(plan.progress||0)))+'%'}}/></div><div className="fp-secondary">{Number(plan.progress||0)}%</div></td><td><ForgeStatus value={plan.status} label={statusText[plan.status]}/></td></tr>)}</tbody></table></div>:<ForgeEmpty title="没有符合条件的项目计划" description={eligibleProjects.length?'可以从进行中的项目手工创建第一版计划':'请先在项目中心启动项目，或调整筛选条件'}/>}</section><ForgeDialog open={dialog?.kind==='create'} title={dialog?.step==='confirm'?'确认创建项目计划':dialog?.title} subtitle={dialog?.subtitle} error={dialog?.error} busy={busy} confirmLabel={dialog?.step==='confirm'?'确认创建':'下一步'} onCancel={()=>setDialog(null)} onConfirm={confirmDialog}>{dialog&&<><div className="fp-field"><label>项目 <span className="fp-required">*</span></label><ForgeSelectControl className="fp-select" aria-label="计划所属项目" value={dialog.project_id} onChange={e=>{const project=eligibleProjects.find(x=>x.id===e.target.value);setDialog({...dialog,project_id:e.target.value,owner_id:project?.manager_id||'',planned_start_on:project?.planned_start_on||'',planned_end_on:project?.planned_end_on||''})}}><option value="">请选择</option>{eligibleProjects.map(x=><option key={x.id} value={x.id}>{x.code} · {x.name}</option>)}</ForgeSelectControl></div><div className="fp-field"><label>首个阶段名称 <span className="fp-required">*</span></label><input className="fp-input" aria-label="首个阶段名称" value={dialog.phase_name} onChange={e=>setDialog({...dialog,phase_name:e.target.value})}/></div><div className="fp-form-grid"><div className="fp-field"><label>计划开始 <span className="fp-required">*</span></label><ForgeDateInput className="fp-input"  aria-label="计划开始" value={dialog.planned_start_on} onChange={e=>setDialog({...dialog,planned_start_on:e.target.value})}/></div><div className="fp-field"><label>计划结束 <span className="fp-required">*</span></label><ForgeDateInput className="fp-input"  aria-label="计划结束" value={dialog.planned_end_on} onChange={e=>setDialog({...dialog,planned_end_on:e.target.value})}/></div></div><div className="fp-field"><label>计划产出物</label><textarea className="fp-textarea" aria-label="首阶段计划产出物" value={dialog.planned_deliverable} onChange={e=>setDialog({...dialog,planned_deliverable:e.target.value})}/></div></>}</ForgeDialog></div>}
  if(view==='list')return <div className="forge-product forge-plan"><style>{css}</style><List/>{toast&&<div className="fp-toast">{toast}</div>}</div>;if(state.loading)return <div className="forge-product forge-plan"><style>{css}</style><ForgeLoading label="正在加载项目计划"/></div>;if(!state.plan)return <div className="forge-product forge-plan"><style>{css}</style><div className="fp-shell">{state.error&&<ForgeNotice tone="error">{state.error}</ForgeNotice>}<ForgeEmpty title="无法打开项目计划" action={<button className="fp-button" onClick={()=>routeTo(null)}>返回计划列表</button>}/></div></div>;
  const plan=state.plan,rows=treeRows(),leaves=state.items.filter(x=>x.item_type!=='phase'),phases=state.items.filter(x=>x.item_type==='phase'),start=Date.parse(plan.planned_start_on),end=Date.parse(plan.planned_end_on),span=Math.max(86400000,end-start+86400000),riseMapBaseline=state.evidence.find(x=>x.source_system==='risemap'),identityMapping=state.identityMappings.find(x=>x.source_system==='risemap'&&x.source_display_name===riseMapBaseline?.owner_display_name);function barStyle(item){const left=Math.max(0,Math.min(100,(Date.parse(item.planned_start_on)-start)/span*100)),width=Math.max(1,Math.min(100-left,(Date.parse(item.planned_end_on)-Date.parse(item.planned_start_on)+86400000)/span*100));return{left:left+'%',width:width+'%'}}
  function PlanTable(){return state.items.length?<div className="fp-table-wrap"><table className="fp-table"><thead><tr><th>工作项</th><th>负责人</th><th>计划日期</th><th>工期</th><th>前置关系</th><th>进度</th><th>状态</th><th>操作</th></tr></thead><tbody>{rows.map(({item,depth,hasChildren})=><tr key={item.id} className={'fp-tree-row '+item.item_type+' '+(item.status==='delayed'?'delayed':'')}><td><div className="fp-tree-name"><span className="fp-tree-indent" style={{width:depth*22}}/>{hasChildren?<button className="fp-tree-toggle" aria-label={(collapsed[item.id]?'展开':'收起')+item.name} onClick={()=>setCollapsed({...collapsed,[item.id]:!collapsed[item.id]})}>{collapsed[item.id]?'›':'⌄'}</button>:<span className="fp-tree-spacer"/>}<span>{item.name}</span>{item.critical_path&&<span className="fp-key">关键</span>}</div><div className="fp-secondary">{typeText[item.item_type]}</div></td><td>{userName(item.owner_id)}</td><td>{item.planned_start_on} 至 {item.planned_end_on}<div className="fp-secondary">实际 {item.actual_start_on||'—'} 至 {item.actual_end_on||'—'}</div></td><td>{item.item_type==='milestone'?'—':item.duration_days+' 天'}</td><td>{(item.predecessor_ids||[]).map(id=>state.items.find(x=>x.id===id)?.name||'未命名工作项').join('、')||'—'}</td><td><div className="fp-progress"><span style={{width:Number(item.progress||0)+'%'}}/></div><div className="fp-secondary">{Number(item.progress||0)}%</div></td><td><ForgeStatus value={item.status} label={statusText[item.status]}/></td><td><div className="fp-inline-actions">{item.item_type!=='phase'&&<><button className="fp-button small" onClick={()=>openProgress(item)}>更新进度</button><button className="fp-button small" onClick={()=>openReport(item)}>填日报</button></>}</div></td></tr>)}</tbody></table></div>:<ForgeEmpty title="计划中还没有工作项" description="添加阶段后，再在阶段下安排里程碑和任务" action={<button className="fp-button primary" onClick={()=>openAdd('phase')}>新增阶段</button>}/>}
  function Gantt(){return state.items.length?<div className="fp-table-wrap"><div className="fp-gantt"><div className="fp-gantt-head">工作项</div><div className="fp-gantt-head">{plan.planned_start_on} 至 {plan.planned_end_on}</div>{rows.map(({item,depth})=><React.Fragment key={item.id}><div className="fp-gantt-name" style={{paddingLeft:12+depth*20}}>{item.name}</div><div className="fp-gantt-track"><span title={item.name+' '+item.planned_start_on+' 至 '+item.planned_end_on} className={'fp-gantt-bar '+item.item_type+' '+(item.status==='delayed'?'delayed':'')} style={barStyle(item)}>{item.item_type==='milestone'?'':item.name}</span></div></React.Fragment>)}</div><div className="fp-keyline"><span><i className="phase"/>阶段</span><span><i/>任务</span><span><i className="milestone"/>里程碑</span><span><i className="delayed"/>已延期</span><span>日期请在工作项中调整</span></div></div>:<ForgeEmpty title="暂无可显示的计划工作项"/>}
  function Progress(){return <div className="fp-plan-grid"><section className="fp-card fp-section"><div className="fp-section-head"><h2>任务进度</h2><span className="fp-secondary">阶段进度自动汇总</span></div>{leaves.length?<div className="fp-table-wrap"><table className="fp-table"><thead><tr><th>工作项</th><th>完成度</th><th>状态</th><th>操作</th></tr></thead><tbody>{leaves.map(item=><tr key={item.id}><td>{item.name}<div className="fp-secondary">{typeText[item.item_type]}</div></td><td>{item.progress||0}%</td><td><ForgeStatus value={item.status} label={statusText[item.status]}/></td><td><div className="fp-inline-actions"><button className="fp-button small" onClick={()=>openProgress(item)}>更新</button><button className="fp-button small" onClick={()=>openReport(item)}>填日报</button></div></td></tr>)}</tbody></table></div>:<ForgeEmpty title="暂无任务或里程碑"/>}</section><section className="fp-card fp-section"><div className="fp-section-head"><h2>日报回读</h2><span className="fp-secondary">{state.reports.length} 条</span></div>{state.reports.length?state.reports.map(report=><div className="fp-report" key={report.id}><div className="fp-report-head"><strong>{state.items.find(x=>x.id===report.work_item_id)?.name||report.name}</strong><span>{report.report_on}</span></div><div className="fp-report-body">{report.completed_today}</div><div className="fp-report-meta">完成度 {report.completion_percent}%{report.blockage?' · 阻塞：'+report.blockage:''}{report.assistance_needed?' · 协助：'+report.assistance_needed:''}{report.expected_finish_changed?' · 调整至 '+report.expected_finish_on:''}</div></div>):<ForgeEmpty title="还没有日报" description="可从左侧任务提交当天进展"/>}</section></div>}
  return <div className="forge-product forge-plan"><style>{css}</style><div className="fp-shell"><div className="fp-phase"><span className="fp-phase-index">03</span><button className="fp-context-link" onClick={()=>routeTo(null)}>项目计划与执行</button><span>/</span><strong>{plan.name}</strong></div><header className="fp-page-header"><div className="fp-heading"><div className="fp-eyebrow">计划版本 V{plan.revision||1} · {sourceText[plan.source]||'其他来源'}</div><div className="fp-title-row"><h1 className="fp-title">{plan.name}</h1><ForgeStatus value={plan.status} label={statusText[plan.status]}/></div><div className="fp-record-summary"><span>{state.project?.code||'—'} · {state.project?.name||'项目名称未填写'}</span><span>{state.customers.find(x=>x.id===state.project?.customer_id)?.name||'未关联客户'}</span><span>{sourceLinks.length?sourceLinks.map(x=>(x.contract?.code||'合同')+' / '+(x.order?.code||'订单')).join('、'):'未关联来源订单'}</span><span>{plan.planned_start_on} 至 {plan.planned_end_on}</span><span>版本 V{plan.revision}</span></div></div><div className="fp-toolbar"><button className="fp-button" onClick={()=>routeTo(null)}>返回列表</button>{plan.status==='active'&&<button className="fp-button primary" onClick={()=>openAdd('task')}>新增工作项</button>}</div></header>{state.error&&<ForgeNotice tone="error">{state.error}</ForgeNotice>}<div className="fp-metrics"><div className="fp-metric"><div className="fp-metric-label">计划进度</div><div className="fp-metric-value">{Number(plan.progress||0)}%</div></div><div className="fp-metric"><div className="fp-metric-label">阶段</div><div className="fp-metric-value">{phases.length}</div></div><div className="fp-metric"><div className="fp-metric-label">任务与里程碑</div><div className="fp-metric-value">{leaves.length}</div></div><div className="fp-metric warning"><div className="fp-metric-label">已延期</div><div className="fp-metric-value">{leaves.filter(x=>x.status==='delayed').length}</div></div><div className="fp-metric"><div className="fp-metric-label">日报</div><div className="fp-metric-value">{state.reports.length}</div></div></div>{riseMapBaseline&&<section className="fp-card fp-baseline"><div className="fp-baseline-head"><div><h2>计划参考</h2><div className="fp-secondary">来源外部计划，用于核对项目计划安排</div></div><ForgeStatus value={riseMapBaseline.verification_status||'observed'} label={riseMapBaseline.verification_status==='pending_review'?'待确认':'已更新'}/></div><div className="fp-baseline-grid"><div className="fp-baseline-cell"><div className="fp-baseline-label">阶段</div><div className="fp-baseline-value">{riseMapBaseline.phase_name}</div></div><div className="fp-baseline-cell"><div className="fp-baseline-label">负责人</div><div className="fp-baseline-value">{riseMapBaseline.owner_display_name}</div></div><div className="fp-baseline-cell"><div className="fp-baseline-label">计划日期</div><div className="fp-baseline-value">{riseMapBaseline.planned_start_on} 至 {riseMapBaseline.planned_end_on}</div></div><div className="fp-baseline-cell"><div className="fp-baseline-label">工期 / 权重</div><div className="fp-baseline-value">{riseMapBaseline.duration_days} 天 / {riseMapBaseline.weight}%</div></div><div className="fp-baseline-cell"><div className="fp-baseline-label">完成进度 / 状态</div><div className="fp-baseline-value">{riseMapBaseline.progress}% / {statusText[riseMapBaseline.status]||'其他状态'}</div></div><div className="fp-baseline-cell"><div className="fp-baseline-label">任务管理总数</div><div className="fp-baseline-value">{riseMapBaseline.task_count}</div></div><div className="fp-baseline-cell"><div className="fp-baseline-label">交付物</div><div className="fp-baseline-value">{riseMapBaseline.planned_deliverable}</div></div></div><div className="fp-baseline-note">{riseMapBaseline.evidence_note} 更新日期 {riseMapBaseline.observed_on}。{identityMapping?'账号对应关系：'+(identityMapping.status==='pending_review'?'待确认':'已确认')+'。':''} 下方显示当前项目中的任务、里程碑和日报。</div></section>}<section className="fp-card"><div className="fp-tabs">{['计划表','甘特图','进度与日报'].map(name=><button key={name} className={'fp-tab '+(tab===name?'active':'')} onClick={()=>setTab(name)}>{name}</button>)}</div><div className="fp-tab-panel">{tab==='计划表'?<PlanTable/>:tab==='甘特图'?<Gantt/>:<Progress/>}</div></section></div><PlanDialogs/>{toast&&<div className="fp-toast">{toast}</div>}</div>
  function PlanDialogs(){const d=dialog;if(!d)return null;const phases=state.items.filter(x=>x.item_type==='phase'),predecessors=state.items.filter(x=>x.item_type!=='phase'),users=state.users.length?state.users:(state.project?.manager_id?[{id:state.project.manager_id,name:'项目负责人'}]:[]);return <ForgeDialog open={!!d} title={d.step==='confirm'?'确认'+d.title:d.title} subtitle={d.subtitle} error={d.error} busy={busy} confirmLabel={d.step==='confirm'?'确认提交':d.kind==='add'?'下一步':d.kind==='progress'?'下一步':'下一步'} onCancel={()=>setDialog(null)} onConfirm={confirmDialog}>{d.step==='confirm'&&<div className="fp-dialog-impact">请确认当前项目、日期、负责人和产出物已核对无误；确认后将写入项目计划。</div>}{d.kind==='add'&&<><div className="fp-form-grid"><div className="fp-field"><label>类型 <span className="fp-required">*</span></label><ForgeSelectControl className="fp-select" aria-label="工作项类型" value={d.item_type} onChange={e=>setDialog({...d,item_type:e.target.value,parent_id:e.target.value==='phase'?'':d.parent_id})}><option value="phase">阶段</option><option value="milestone">里程碑</option><option value="task">任务</option></ForgeSelectControl></div><div className="fp-field"><label>名称 <span className="fp-required">*</span></label><input className="fp-input" aria-label="工作项名称" value={d.name} onChange={e=>setDialog({...d,name:e.target.value})}/></div>{d.item_type!=='phase'&&<div className="fp-field"><label>所属阶段</label><ForgeSelectControl className="fp-select" aria-label="所属阶段" value={d.parent_id} onChange={e=>setDialog({...d,parent_id:e.target.value})}><option value="">不指定</option>{phases.map(x=><option value={x.id} key={x.id}>{x.name}</option>)}</ForgeSelectControl></div>}<div className="fp-field"><label>负责人</label><ForgeSelectControl className="fp-select" aria-label="工作项负责人" value={d.owner_id} onChange={e=>setDialog({...d,owner_id:e.target.value})}><option value="">不指定</option>{users.map(x=><option value={x.id} key={x.id}>{x.name||x.username||x.id}</option>)}</ForgeSelectControl></div><div className="fp-field"><label>计划开始 <span className="fp-required">*</span></label><ForgeDateInput className="fp-input" aria-label="工作项计划开始"  value={d.planned_start_on} onChange={e=>setDialog({...d,planned_start_on:e.target.value})}/></div><div className="fp-field"><label>计划结束 <span className="fp-required">*</span></label><ForgeDateInput className="fp-input" aria-label="工作项计划结束"  value={d.planned_end_on} onChange={e=>setDialog({...d,planned_end_on:e.target.value})}/></div><div className="fp-field"><label>权重</label><input className="fp-input" aria-label="工作项权重" type="number" min="0" max="100" value={d.weight} onChange={e=>setDialog({...d,weight:e.target.value})}/></div></div>{d.item_type!=='phase'&&<div className="fp-field"><label>前置任务或里程碑</label><ForgeSelectControl className="fp-select" aria-label="前置任务" multiple size={Math.min(4,Math.max(2,predecessors.length))} value={d.predecessor_ids} onChange={e=>setDialog({...d,predecessor_ids:Array.from(e.target.selectedOptions).map(x=>x.value)})}>{predecessors.map(x=><option value={x.id} key={x.id}>{x.name}</option>)}</ForgeSelectControl></div>}<div className="fp-field"><label>计划产出物</label><textarea className="fp-textarea" aria-label="工作项计划产出物" value={d.planned_deliverable} onChange={e=>setDialog({...d,planned_deliverable:e.target.value})}/></div><label className="fp-checkbox"><input type="checkbox" checked={d.critical_path} onChange={e=>setDialog({...d,critical_path:e.target.checked})}/>标记为关键路径项</label></>}{d.kind==='progress'&&<><div className="fp-form-grid"><div className="fp-field"><label>完成度 <span className="fp-required">*</span></label><input className="fp-input" aria-label="工作项完成度" type="number" min="0" max="100" value={d.progress} onChange={e=>{const progress=Number(e.target.value),status=d.status==='delayed'||d.status==='cancelled'?d.status:(progress===0?'pending':progress===100?'completed':'in_progress');setDialog({...d,progress:e.target.value,status})}}/></div><div className="fp-field"><label>状态</label><ForgeSelectControl className="fp-select" aria-label="工作项状态" value={d.status} onChange={e=>setDialog({...d,status:e.target.value})}><option value="pending">未开始</option><option value="in_progress">进行中</option><option value="completed">已完成</option><option value="delayed">已延期</option><option value="cancelled">已取消</option></ForgeSelectControl></div><div className="fp-field"><label>实际开始</label><ForgeDateInput className="fp-input" aria-label="实际开始"  value={d.actual_start_on} onChange={e=>setDialog({...d,actual_start_on:e.target.value})}/></div><div className="fp-field"><label>实际完成</label><ForgeDateInput className="fp-input" aria-label="实际完成"  value={d.actual_end_on} onChange={e=>setDialog({...d,actual_end_on:e.target.value})}/></div></div></>}{d.kind==='report'&&<><div className="fp-form-grid"><div className="fp-field"><label>填报人 <span className="fp-required">*</span></label><ForgeSelectControl className="fp-select" aria-label="日报填报人" value={d.reporter_id} onChange={e=>setDialog({...d,reporter_id:e.target.value})}><option value="">请选择</option>{users.map(x=><option value={x.id} key={x.id}>{x.name||x.username||x.id}</option>)}</ForgeSelectControl></div><div className="fp-field"><label>日报日期 <span className="fp-required">*</span></label><ForgeDateInput className="fp-input" aria-label="日报日期"  value={d.report_on} onChange={e=>setDialog({...d,report_on:e.target.value})}/></div><div className="fp-field"><label>完成度 <span className="fp-required">*</span></label><input className="fp-input" aria-label="日报完成度" type="number" min="0" max="100" value={d.completion_percent} onChange={e=>setDialog({...d,completion_percent:e.target.value})}/></div></div><div className="fp-field"><label>今日完成内容 <span className="fp-required">*</span></label><textarea className="fp-textarea" aria-label="今日完成内容" value={d.completed_today} onChange={e=>setDialog({...d,completed_today:e.target.value})}/></div><div className="fp-field"><label>阻塞问题</label><textarea className="fp-textarea" aria-label="日报阻塞问题" value={d.blockage} onChange={e=>setDialog({...d,blockage:e.target.value})}/></div><div className="fp-field"><label>需要协助</label><textarea className="fp-textarea" aria-label="日报需要协助" value={d.assistance_needed} onChange={e=>setDialog({...d,assistance_needed:e.target.value})}/></div><label className="fp-checkbox"><input type="checkbox" checked={d.expected_finish_changed} onChange={e=>setDialog({...d,expected_finish_changed:e.target.checked})}/>预计完成日期发生变化</label>{d.expected_finish_changed&&<div className="fp-field"><label>调整后的预计完成日期 <span className="fp-required">*</span></label><ForgeDateInput className="fp-input" aria-label="调整后的预计完成日期"  value={d.expected_finish_on} onChange={e=>setDialog({...d,expected_finish_on:e.target.value})}/></div>}</>}</ForgeDialog>}
}export default App;
${forgeProductUiRuntime}
`;

export const ProjectPlanWorkspacePage = { name: 'page_project_plan_workspace', label: '项目计划与执行', description: '项目计划、日期安排、进度更新与日报回读', icon: 'calendar-range', type: 'app' as const, kind: 'react' as const, source: projectPlanPageSource };
