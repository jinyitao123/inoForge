import { forgeProductUiCss, forgeProductUiRuntime, forgeProcurementUiCss } from './product-ui.js';

const inboundCss = forgeProductUiCss + forgeProcurementUiCss;

const purchaseInboundPageSource = `
function App(){
 const query=new URLSearchParams(window.location.search),inboundId=query.get('id'),orderId=query.get('order');
 const [state,setState]=React.useState({loading:true,inbounds:[],inbound:null,lines:[],order:null,orderLines:{},supplier:null,warehouses:[],suppliers:{},inspections:[],logs:[],invoices:[],error:''}),[form,setForm]=React.useState({inbound_on:new Date(Date.now()+8*60*60*1000).toISOString().slice(0,10),remarks:''}),[lines,setLines]=React.useState([]),[approval,setApproval]=React.useState(''),[invoiceDialog,setInvoiceDialog]=React.useState(null),[confirm,setConfirm]=React.useState(null),[busy,setBusy]=React.useState(false);
 async function request(path,options){const response=await fetch('/api/v1'+path,{credentials:'include',headers:{'Content-Type':'application/json'},...options});const payload=await response.json().catch(()=>({}));if(!response.ok)throw new Error(payload.error?.message||payload.message||'请求失败');return payload;}
 async function find(object,where){const filter=where?('&$filter='+encodeURIComponent(JSON.stringify(where))):'';return (await request('/data/'+object+'?$top=100'+filter)).records||[];}async function read(object,id){return (await request('/data/'+object+'/'+id)).record;}
 async function load(){try{if(!inboundId&&!orderId){const [inbounds,suppliers,warehouses,users]=await Promise.all([find('forge_purchase_inbound'),find('forge_supplier'),find('forge_warehouse'),find('sys_user')]);setState({loading:false,inbounds,inbound:null,lines:[],order:null,orderLines:{},supplier:null,warehouses,suppliers:Object.fromEntries(suppliers.map(x=>[x.id,x])),users:Object.fromEntries(users.map(x=>[x.id,x])),inspections:[],logs:[],invoices:[],error:''});return;}if(inboundId){const inbound=await read('forge_purchase_inbound',inboundId),[savedLines,order,supplier,warehouses,logs,invoices]=await Promise.all([find('forge_purchase_inbound_line',{inbound_id:inboundId}),read('forge_purchase_order',inbound.order_id),read('forge_supplier',inbound.supplier_id),find('forge_warehouse'),find('forge_purchase_inbound_approval_log',{inbound_id:inboundId}),find('forge_purchase_invoice',{inbound_id:inboundId})]);setState({loading:false,inbounds:[],inbound,lines:savedLines,order,orderLines:{},supplier,warehouses,suppliers:{},inspections:[],logs,invoices,error:''});return;}const order=await read('forge_purchase_order',orderId),[supplier,warehouses,inspections,existingLines,orderLineList]=await Promise.all([read('forge_supplier',order.supplier_id),find('forge_warehouse'),find('forge_purchase_inspection',{order_id:orderId}),find('forge_purchase_inbound_line'),find('forge_purchase_order_line',{order_id:orderId})]);const eligible=inspections.filter(i=>i.status==='completed'&&Number(i.accepted_quantity||0)>0).map(i=>{const used=existingLines.filter(line=>line.inspection_id===i.id&&line.status!=='cancelled').reduce((sum,line)=>sum+Number(line.quantity||0),0);return {...i,remaining:Math.max(0,Number(i.accepted_quantity||0)-used)}}).filter(i=>i.remaining>0);setLines(eligible.map(i=>({inspection_id:i.id,quantity:i.remaining,warehouse_id:i.warehouse_id,warehouse_location:'',remarks:''})));setState({loading:false,inbounds:[],inbound:null,lines:[],order,orderLines:Object.fromEntries(orderLineList.map(line=>[line.id,line])),supplier,warehouses,suppliers:{},inspections:eligible,logs:[],invoices:[],error:''});}catch(error){setState(s=>({...s,loading:false,error:String(error.message||error)}));}}
 React.useEffect(()=>{load();},[]);function setLine(index,field,value){setLines(current=>current.map((line,i)=>i===index?{...line,[field]:value}:line));}
 function confirmTitle(action,mode){return {create:mode==='submit'?'确认提交采购入库':'确认保存采购入库',purchase_inbound_submit:'确认提交入库审批',purchase_inbound_approve:'确认审批通过',purchase_inbound_stock:'确认执行入库'}[action]||'确认采购入库操作';}
 function confirmImpact(action,mode){if(action==='create')return mode==='submit'?'确认后将生成采购入库单并提交审批。':'确认后将保存采购入库草稿，后续仍可提交审批。';if(action==='purchase_inbound_submit')return '确认后该入库单进入审批流程。';if(action==='purchase_inbound_approve')return '确认后该入库单可由仓库执行入库。';if(action==='purchase_inbound_stock')return '确认后将增加仓库库存并回写采购入库状态。';return '确认后将推进采购入库办理状态。';}
 async function runConfirmed(task){setBusy(true);setState(s=>({...s,error:''}));try{if(task.action==='create'){const mode=task.mode;const params={mode,inbound_on:form.inbound_on,remarks:form.remarks,lines_json:JSON.stringify(lines.map(line=>({...line,quantity:Number(line.quantity||0)})))};const payload=await request('/actions/forge_purchase_order/purchase_order_create_inbound/'+state.order.id,{method:'POST',body:JSON.stringify({params})});const result=payload.result||payload.data?.result||payload.data||payload;if(!result.id)throw new Error('采购入库单创建后未返回记录ID');window.location.href=window.location.pathname+'?id='+encodeURIComponent(result.id);return;}await request('/actions/forge_purchase_inbound/'+task.action+'/'+state.inbound.id,{method:'POST',body:JSON.stringify({params:task.params||{}})});setConfirm(null);await load();}catch(error){setConfirm(c=>c?({...c,error:String(error.message||error)}):c);setState(s=>({...s,error:String(error.message||error)}));}finally{setBusy(false);}}
 function create(mode){setConfirm({action:'create',mode,params:{},title:confirmTitle('create',mode),impact:confirmImpact('create',mode),subtitle:state.order?.code||'采购入库',error:''});}
 function action(name,params={}){setConfirm({action:name,params,title:confirmTitle(name),impact:confirmImpact(name),subtitle:state.inbound?.code||'采购入库',error:''});}
 async function registerInvoice(){const note=invoiceDialog||{},missing=!String(note.code||'').trim()?'请填写登记编号':!String(note.invoice_number||'').trim()?'请填写发票号码':!note.invoice_on?'请选择开票日期':!note.due_on?'请选择应付日期':'';if(missing){setInvoiceDialog({...note,error:missing});return;}setBusy(true);try{await request('/actions/forge_purchase_inbound/purchase_inbound_register_invoice/'+state.inbound.id,{method:'POST',body:JSON.stringify({params:{code:String(note.code).trim(),invoice_number:String(note.invoice_number).trim(),invoice_on:note.invoice_on,due_on:note.due_on,remarks:String(note.remarks||'').trim()||null}})});setInvoiceDialog(null);await load();}catch(error){setInvoiceDialog(value=>({...value,error:String(error.message||error)}));}finally{setBusy(false);}}
 const money=v=>'¥'+Number(v||0).toLocaleString('zh-CN',{minimumFractionDigits:2,maximumFractionDigits:2}),statusText={draft:'草稿',pending_approval:'待审批',approved:'已审批',stocked:'已入库',cancelled:'已取消'},sourceText={purchase_order:'采购订单',purchase_replacement:'采购换货补货'},inboundTypeText={purchase:'采购入库'},warehouseName=id=>state.warehouses.find(w=>w.id===id)?.name||'—',userName=id=>state.users?.[id]?.display_name||state.users?.[id]?.name||state.users?.[id]?.username||'Dev Admin';
 const total=lines.reduce((sum,line)=>sum+Number(line.quantity||0),0),taxed=lines.reduce((sum,line)=>{const i=state.inspections.find(x=>x.id===line.inspection_id),orderLine=i&&state.orderLines[i.order_line_id];return sum+Math.round((Number(line.quantity||0)*Number(orderLine?.taxed_unit_price||0)+Number.EPSILON)*100)/100;},0);
 const css=${JSON.stringify(inboundCss)};

 if(state.loading)return <div className="forge-product forge-procurement forge-inbound"><style>{css}</style><div className="body"><ForgeLoading label="正在加载采购入库"/></div></div>;
 if(!inboundId&&!orderId)return <div className="forge-product forge-procurement forge-inbound"><style>{css}</style><div className="crumb">库存管理 / 入库管理 / 采购入库</div><div className="head"><div><h1>采购入库</h1><div className="muted">检验合格物料的入库审批与库存落账</div></div><div className="actions"><button className="btn" onClick={()=>window.location.href='/_console/apps/forge/page/page_purchase_inspection_workspace'}>检验单</button><button className="btn" onClick={load}>刷新</button></div></div><div className="body"><div className="card"><div className="table"><table><thead><tr><th>入库单号</th><th>类型</th><th>来源</th><th>入库类型</th><th>批次号</th><th>供应商/客户</th><th>仓库</th><th>入库日期</th><th>含税金额</th><th>状态</th><th>创建人</th><th>操作</th></tr></thead><tbody>{state.inbounds.map(x=><tr key={x.id}><td>{x.code}</td><td>{inboundTypeText[x.inbound_type]||'采购入库'}</td><td>{sourceText[x.source_type]||x.source_type||'采购订单'}</td><td>{inboundTypeText[x.inbound_type]||'采购入库'}</td><td>{x.batch_number||'—'}</td><td>{state.suppliers[x.supplier_id]?.name||'—'}</td><td>{warehouseName(x.warehouse_id)}</td><td>{x.inbound_on}</td><td>{money(x.taxed_amount)}</td><td><span className="pill">{statusText[x.status]}</span></td><td>{userName(x.submitted_by||x.responsible_id)}</td><td><button className="btn" onClick={()=>window.location.href=window.location.pathname+'?id='+encodeURIComponent(x.id)}>查看</button></td></tr>)}</tbody></table></div></div></div></div>;
 if(inboundId){const inbound=state.inbound;return <div className="forge-product forge-procurement forge-inbound"><style>{css}</style><div className="crumb">库存管理 / 采购入库 / {inbound.code}</div><div className="head"><div><span className="pill">{statusText[inbound.status]}</span><h1>{inbound.code}</h1><div className="muted">来源 {state.order.code} · {state.supplier.name}</div></div><div className="actions"><button className="btn" onClick={()=>window.location.href=window.location.pathname}>返回列表</button>{inbound.status==='draft'&&<button className="btn primary" disabled={busy} onClick={()=>action('purchase_inbound_submit')}>提交审批</button>}{inbound.status==='pending_approval'&&<><input className="approval" aria-label="审批意见" value={approval} onChange={e=>setApproval(e.target.value)}/><button className="btn primary" disabled={busy} onClick={()=>String(approval||'').trim()?action('purchase_inbound_approve',{approval_note:approval.trim()}):setState(s=>({...s,error:'请填写审批意见'}))}>审批通过</button></>}{inbound.status==='approved'&&<button className="btn primary" disabled={busy} onClick={()=>action('purchase_inbound_stock')}>执行入库</button>}{inbound.status==='stocked'&&!state.invoices.some(x=>x.status==='normal')&&<button className="btn primary" disabled={busy} onClick={()=>setInvoiceDialog({code:'PI-'+inbound.code,invoice_number:'INV-'+inbound.code,invoice_on:inbound.inbound_on,due_on:inbound.inbound_on,remarks:'',error:''})}>登记采购发票</button>}{state.invoices.some(x=>x.status==='normal')&&<span className="pill">已登记进项发票</span>}</div></div><div className="flow"><span>草稿</span><span>待审批</span><span>已审批</span><span className={inbound.status==='stocked'?'active':''}>已入库</span></div><div className="body">{state.error&&<div className="notice">{state.error}</div>}<div className="card"><div className="details"><div className="value"><small>入库类型</small>采购入库</div><div className="value"><small>入库日期</small>{inbound.inbound_on}</div><div className="value"><small>供应商</small>{state.supplier.name}</div><div className="value"><small>采购订单</small>{state.order.code}</div><div className="value"><small>物料行数</small>{inbound.line_count}</div><div className="value"><small>入库数量</small>{inbound.total_quantity}</div><div className="value"><small>含税金额</small>{money(inbound.taxed_amount)}</div><div className="value"><small>审批意见</small>{inbound.approval_note||'—'}</div></div></div><div className="card"><h2>发票与应付</h2>{state.invoices.length?<div className="table"><table><thead><tr><th>登记编号</th><th>发票号码</th><th>开票日期</th><th>应付日期</th><th>金额</th><th>状态</th></tr></thead><tbody>{state.invoices.map(inv=><tr key={inv.id}><td>{inv.code}</td><td>{inv.invoice_number}</td><td>{inv.invoice_on}</td><td>{inv.due_on}</td><td>{money(inv.total_amount)}</td><td><span className="pill">{inv.status==='normal'?'正常':inv.status}</span></td></tr>)}</tbody></table></div>:<div className="muted">入库完成后可登记采购发票，系统按入库明细生成进项发票和应付账款。</div>}</div><div className="card"><h2>入库物料明细</h2><div className="table"><table><thead><tr><th>物料编码</th><th>物料名称</th><th>规格型号</th><th>单位</th><th>数量</th><th>含税单价</th><th>不含税单价</th><th>税率</th><th>含税金额</th><th>仓库</th><th>库位</th><th>批次</th><th>状态</th></tr></thead><tbody>{state.lines.map(line=><tr key={line.id}><td>{line.item_code}</td><td>{line.name}</td><td>{line.specification} / {line.model}</td><td>{line.unit_name}</td><td>{line.quantity}</td><td>{money(line.taxed_unit_price)}</td><td>{money(line.untaxed_unit_price)}</td><td>{line.tax_rate}%</td><td>{money(line.taxed_amount)}</td><td>{state.warehouses.find(w=>w.id===line.warehouse_id)?.name||'—'}</td><td>{line.warehouse_location||'—'}</td><td>{line.batch_number||'—'}</td><td>{statusText[line.status]}</td></tr>)}</tbody></table></div></div></div><ForgeDialog open={!!invoiceDialog} title="登记采购发票" subtitle={inbound.code} error={invoiceDialog?.error} busy={busy} confirmLabel="确认登记" onCancel={()=>!busy&&setInvoiceDialog(null)} onConfirm={registerInvoice}><div className="field"><label>登记编号 *</label><input aria-label="进项发票登记编号" value={invoiceDialog?.code||''} onChange={e=>setInvoiceDialog({...invoiceDialog,code:e.target.value,error:''})}/></div><div className="field"><label>发票号码 *</label><input aria-label="进项发票号码" value={invoiceDialog?.invoice_number||''} onChange={e=>setInvoiceDialog({...invoiceDialog,invoice_number:e.target.value,error:''})}/></div><div className="field"><label>开票日期 *</label><ForgeDateInput aria-label="进项发票开票日期" value={invoiceDialog?.invoice_on||''} onChange={e=>setInvoiceDialog({...invoiceDialog,invoice_on:e.target.value,error:''})}/></div><div className="field"><label>应付日期 *</label><ForgeDateInput aria-label="进项发票应付日期" value={invoiceDialog?.due_on||''} onChange={e=>setInvoiceDialog({...invoiceDialog,due_on:e.target.value,error:''})}/></div><div className="field"><label>备注</label><textarea aria-label="进项发票备注" value={invoiceDialog?.remarks||''} onChange={e=>setInvoiceDialog({...invoiceDialog,remarks:e.target.value,error:''})}/></div></ForgeDialog></div>}
 return <div className="forge-product forge-procurement forge-inbound"><style>{css}</style><div className="crumb">库存管理 / 入库管理 / 新建入库单</div><div className="head"><div><h1>新建入库单</h1><div className="muted">入库单号将在保存时生成，提交审批后进入工作流</div></div><div className="actions"><button className="btn" disabled={busy} onClick={()=>window.location.href='/_console/apps/forge/page/page_purchase_inspection_workspace'}>返回检验单</button><button className="btn" disabled={busy} onClick={()=>create('draft')}>保存草稿</button><button className="btn primary" disabled={busy} onClick={()=>create('submit')}>提交审批</button></div></div><div className="flow"><span className="active">草稿</span><span>待审批</span><span>已审批</span><span>已入库</span></div><div className="body">{state.error&&<div className="notice">{state.error}</div>}<div className="card"><h2>基本信息</h2><div className="grid"><div className="field"><label>入库单号</label><input aria-label="入库单号" value="保存时自动生成" disabled/></div><div className="field"><label>入库类型 *</label><input value="采购入库" disabled/></div><div className="field"><label>入库日期 *</label><ForgeDateInput  value={form.inbound_on} onChange={e=>setForm({...form,inbound_on:e.target.value})}/></div><div className="field"><label>供应商 *</label><input value={state.supplier.name} disabled/></div><div className="field"><label>采购订单 *</label><input value={state.order.code} disabled/></div></div></div><div className="card"><h2>入库物料明细 <span className="muted">{state.inspections.length} 种</span></h2><div className="table"><table><thead><tr><th>#</th><th>物料编码</th><th>物料名称</th><th>规格型号</th><th>检验合格</th><th>入库数量 *</th><th>含税单价</th><th>仓库 *</th><th>库位</th></tr></thead><tbody>{state.inspections.map((inspection,index)=>{const line=lines[index]||{},orderLine=state.orderLines[inspection.order_line_id]||{};return <tr key={inspection.id}><td>{index+1}</td><td>{inspection.item_code}</td><td>{inspection.name.replace(/^IQC-\\d{4}-\\d+ PIN-\\d{4}-\\d+ /,'')}</td><td>{inspection.specification} / {inspection.model}</td><td>{inspection.remaining}</td><td><input aria-label={inspection.item_code+' 入库数量'} type="number" min="0" max={inspection.remaining} value={line.quantity??0} onChange={e=>setLine(index,'quantity',e.target.value)}/></td><td>{money(orderLine.taxed_unit_price)}</td><td><ForgeSelectControl aria-label={inspection.item_code+' 入库仓库'} value={line.warehouse_id||''} onChange={e=>setLine(index,'warehouse_id',e.target.value)}><option value="">请选择</option>{state.warehouses.map(w=><option key={w.id} value={w.id}>{w.name}</option>)}</ForgeSelectControl></td><td><input aria-label={inspection.item_code+' 入库库位'} value={line.warehouse_location||''} onChange={e=>setLine(index,'warehouse_location',e.target.value)}/></td></tr>})}</tbody></table>{!state.inspections.length&&<div className="muted" style={{padding:32,textAlign:'center'}}>当前订单没有可入库的检验合格物料</div>}</div><div className="summary"><span>入库数量 <strong>{total}</strong></span><span>含税金额 <strong>{money(taxed)}</strong></span></div></div><div className="card"><h2>备注与附件</h2><div className="field"><label>备注</label><textarea value={form.remarks} onChange={e=>setForm({...form,remarks:e.target.value})}/></div></div></div></div>;
}
export default App;
${forgeProductUiRuntime}
`;

export const PurchaseInboundWorkspacePage={name:'page_purchase_inbound_workspace',label:'采购入库',description:'承接逐物料检验合格数量的采购入库审批与库存落账',icon:'package-plus',type:'app' as const,kind:'react' as const,source:purchaseInboundPageSource};
