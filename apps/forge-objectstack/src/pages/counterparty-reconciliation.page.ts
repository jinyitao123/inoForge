import { forgeProductUiCss, forgeProductUiRuntime } from './product-ui.js';

const counterpartyReconciliationSource = `
function App(){const adapter=useAdapter(),nav=new URLSearchParams(location.search).get('nav')||'',initialSupplier=nav==='supplier_reconciliation',initialStatements=initialSupplier||nav==='customer_reconciliation';const [data,setData]=React.useState({loading:true,customers:[],suppliers:[],statements:[],lines:[],logs:[],receivables:[],payables:[],allocations:[],writeoffs:[],customerOffsets:[],supplierOffsets:[],receipts:[],payments:[],outbounds:[],inbounds:[],error:''}),[tab,setTab]=React.useState(initialSupplier?'supplier':'customer'),[view,setView]=React.useState(initialStatements?'statements':'pool'),[dimension,setDimension]=React.useState('party'),[query,setQuery]=React.useState(''),[period,setPeriod]=React.useState('all'),[selectedKeys,setSelectedKeys]=React.useState([]),[busy,setBusy]=React.useState(false),[form,setForm]=React.useState({customer_id:'',supplier_id:'',customer_code:'',supplier_code:'',period_start:'',period_end:'',recipient:'',confirmed_balance:'',confirmation_note:'',resolution_note:''});
async function request(path,options={}){const headers={'Content-Type':'application/json',...(adapter?.getAuthHeaders?.()||{}),...(options.headers||{})},rawBase=String(adapter?.baseUrl||''),base=rawBase.endsWith('/')?rawBase.slice(0,-1):rawBase,transport=adapter?.fetchImpl||fetch;const r=await transport(base+'/api/v1'+path,{credentials:'include',...options,headers}),p=await r.json().catch(()=>({}));if(!r.ok)throw new Error(p.error?.message||p.message||'请求失败');return p;}async function find(o){return(await request('/data/'+o+'?$top=500')).records||[];}async function load(){try{const v=await Promise.all(['forge_customer','forge_supplier','forge_counterparty_statement','forge_counterparty_statement_line','forge_counterparty_statement_log','forge_accounts_receivable','forge_accounts_payable','forge_collection_allocation','forge_payment_writeoff','forge_customer_prepayment_offset','forge_supplier_prepayment_offset','forge_cash_receipt','forge_cash_payment','forge_sales_outbound','forge_purchase_inbound'].map(find));setData({loading:false,customers:v[0],suppliers:v[1],statements:v[2],lines:v[3],logs:v[4],receivables:v[5],payables:v[6],allocations:v[7],writeoffs:v[8],customerOffsets:v[9],supplierOffsets:v[10],receipts:v[11],payments:v[12],outbounds:v[13],inbounds:v[14],error:''});setForm(f=>({...f,customer_id:v[0].some(x=>x.id===f.customer_id)?f.customer_id:'',supplier_id:v[1].some(x=>x.id===f.supplier_id)?f.supplier_id:''}));}catch(e){setData(d=>({...d,loading:false,error:String(e.message||e)}));}}React.useEffect(()=>{load();},[]);async function invoke(o,a,id,params){setBusy(true);setData(d=>({...d,error:''}));try{await request('/actions/'+o+'/'+a,{method:'POST',body:JSON.stringify({recordId:id,params})});await load();return true;}catch(e){setData(d=>({...d,error:String(e.message||e)}));return false;}finally{setBusy(false);}}
const money=v=>'¥'+Number(v||0).toLocaleString('zh-CN',{minimumFractionDigits:2,maximumFractionDigits:2}),status={draft:'草稿',sent:'已发送待确认',confirmed:'已确认',disputed:'有差异',closed:'已关闭'},party=x=>x.party_type==='customer'?(data.customers.find(c=>c.id===x.customer_id)?.name||x.customer_id):(data.suppliers.find(s=>s.id===x.supplier_id)?.name||x.supplier_id),css=\`${forgeProductUiCss}.forge-product .body{width:min(1380px,100%);max-width:1380px;margin:0 auto;padding:20px 0 40px}.forge-product .cards{gap:11px}.forge-product .card{border:1px solid var(--fp-line);border-radius:10px;box-shadow:none}.forge-product .btn{height:34px;border-radius:8px;border-color:var(--fp-line-strong);padding:0 12px}.forge-product .tabs button{border-radius:8px}.forge-product .table{border-radius:9px;border-color:var(--fp-line)}@media(max-width:760px){.forge-product .body{padding:12px 0 28px}.forge-product .cards{grid-template-columns:1fr}}div:has(>.forge-reconcile){max-width:none!important;margin:0!important}.forge-reconcile+*{display:none}main:has(.forge-reconcile)>div{padding:0!important}.forge-reconcile{min-height:100%;background:#f4f6f8;color:#222831;font-family:-apple-system,BlinkMacSystemFont,Segoe UI,PingFang SC,sans-serif}.crumb{height:38px;padding:0 22px;display:flex;align-items:center;color:#76808d;font-size:12px;background:#fff;border-bottom:1px solid #e3e7eb}.head{padding:16px 22px;background:#fff;border-bottom:1px solid #e3e7eb}.head h1{font-size:21px;margin:2px 0 5px}.body{padding:18px 22px 40px;max-width:1380px;margin:auto}.tabs{display:flex;gap:4px;margin-bottom:14px;flex-wrap:wrap}.tabs button{padding:9px 18px;border:0;border-bottom:2px solid transparent;background:transparent}.tabs .active{color:#1769e0;border-color:#1769e0}.cards,.form{display:grid;grid-template-columns:repeat(auto-fit,minmax(178px,1fr));gap:10px}.card{background:#fff;border:1px solid #dfe4e9;border-radius:10px;padding:16px;margin-bottom:14px}.card h2{font-size:15px;margin:0 0 12px}.kpi{font-size:23px;font-weight:650;margin:7px 0}.muted,label{font-size:12px;color:#76808d}.field label{display:block;margin-bottom:5px}.field input,.field select{width:100%;box-sizing:border-box;border:1px solid #d4dae1;border-radius:8px;padding:8px}.btn{height:34px;border:1px solid #ccd3da;border-radius:8px;background:#fff;padding:0 10px}.primary{background:#1769e0;color:#fff;border-color:#1769e0}.danger{color:#b42318}.btn:disabled{opacity:.4}.actions{display:flex;gap:6px;flex-wrap:wrap}.table{overflow:auto;border:1px solid #e1e5ea;border-radius:8px}.table table{width:100%;border-collapse:collapse;font-size:12px;min-width:1120px}.table th,.table td{padding:9px 10px;border-bottom:1px solid #edf0f3;text-align:left}.table th{background:#f7f8fa;color:#68727f;font-weight:500}.pill{font-size:11px;padding:3px 8px;border-radius:10px;background:#edf4ff;color:#1769e0}.pill.warn{background:#fff1e8;color:#a14b00}.notice{padding:10px 13px;background:#fff3cd;color:#745600;border-radius:8px;margin-bottom:14px}.rule{padding:11px 13px;background:#eef5ff;color:#31527a;border-radius:8px;margin-bottom:14px}.empty{text-align:center;color:#76808d;padding:28px!important}@media(max-width:900px){.cards,.form{grid-template-columns:1fr}.body{padding:10px}}\`;
if(data.loading)return <div className="forge-product forge-reconcile"><style>{css}</style><div className="body">正在加载往来对账数据…</div></div>;const today=new Date().toISOString().slice(0,10),occupied=new Set(data.lines.map(x=>x.source_key)),pool=[];
for(const x of data.receivables.filter(x=>x.status!=='red_reversed')){const outbound=data.outbounds.find(o=>o.order_id===x.order_id),amount=Number(x.original_amount||0)-Number(x.red_reversed_amount||0);if(amount>0)pool.push({party_type:'customer',party_id:x.customer_id,date:String(x.recognized_on||''),type:'应收',code:x.code,key:'receivable:'+x.id,amount,contract:x.contract_id||'',order:x.order_id||'',shipment:outbound?.id||'',invoice:x.invoice_id||'',cash:''});}
for(const x of data.allocations.filter(x=>x.status==='approved')){const outbound=data.outbounds.find(o=>o.order_id===x.order_id);pool.push({party_type:'customer',party_id:x.customer_id,date:String(x.allocated_on||''),type:'收款核销',code:x.code,key:'collection:'+x.id,amount:Number(x.amount||0),contract:x.contract_id||'',order:x.order_id||'',shipment:outbound?.id||'',invoice:x.invoice_id||'',cash:x.receipt_id||''});}
for(const x of data.customerOffsets.filter(x=>x.status==='approved'))pool.push({party_type:'customer',party_id:x.customer_id,date:String(x.offset_on||''),type:'预收冲抵',code:x.code,key:'customer-offset:'+x.id,amount:Number(x.amount||0),contract:x.contract_id||'',order:x.order_id||'',shipment:'',invoice:x.invoice_id||'',cash:'prepayment:'+x.prepayment_id});
for(const x of data.payables.filter(x=>x.status!=='red_reversed')){const amount=Number(x.original_amount||0)-Number(x.red_reversed_amount||0);if(amount>0)pool.push({party_type:'supplier',party_id:x.supplier_id,date:String(x.recognized_on||''),type:'应付',code:x.code,key:'payable:'+x.id,amount,contract:'',order:x.order_id||'',shipment:x.inbound_id||'',invoice:x.invoice_id||'',cash:''});}
for(const x of data.writeoffs.filter(x=>x.status==='approved')){const payment=data.payments.find(p=>p.id===x.payment_id),payable=data.payables.find(p=>p.id===x.payable_id);pool.push({party_type:'supplier',party_id:x.supplier_id,date:String(payment?.paid_on||x.reviewed_at||'').slice(0,10),type:'付款核销',code:x.code,key:'payment-writeoff:'+x.id,amount:Number(x.amount||0),contract:'',order:payable?.order_id||'',shipment:payable?.inbound_id||'',invoice:payable?.invoice_id||'',cash:x.payment_id||''});}
for(const x of data.supplierOffsets.filter(x=>x.status==='approved'))pool.push({party_type:'supplier',party_id:x.supplier_id,date:String(x.offset_on||''),type:'预付冲抵',code:x.code,key:'supplier-offset:'+x.id,amount:Number(x.amount||0),contract:'',order:x.order_id||'',shipment:'',invoice:'',cash:'prepayment:'+x.prepayment_id});
const eligible=pool.filter(x=>x.party_type===tab&&x.date&&x.date<today&&x.amount>0&&!occupied.has(x.key)&&(!query.trim()||[x.code,x.party_id,x.contract,x.order,x.invoice,x.shipment,x.cash].some(v=>String(v||'').toLowerCase().includes(query.trim().toLowerCase())))&&(period==='all'||String(x.date).slice(0,7)===period)),dimKey=x=>dimension==='party'?x.party_id:dimension==='contract'?x.contract:dimension==='order'?x.order:dimension==='shipment_receipt'?x.shipment:dimension==='invoice'?x.invoice:x.cash,dimText={party:'往来单位',contract:'合同',order:'订单',shipment_receipt:'发货/收货',invoice:'发票',cash:'收付款'},periods=[...new Set(pool.filter(x=>x.party_type===tab&&x.date).map(x=>String(x.date).slice(0,7)))].sort().reverse(),groups=Object.values(eligible.reduce((m,x)=>{const k=x.party_id+'|'+(dimKey(x)||'未关联');if(!m[k])m[k]={key:k,party_id:x.party_id,dimension_key:dimKey(x)||'未关联',rows:[],amount:0};m[k].rows.push(x);m[k].amount+=x.amount;return m;},{})),current=data.statements.filter(x=>x.party_type===tab&&(!query.trim()||[x.code,x.customer_id,x.supplier_id].some(v=>String(v||'').toLowerCase().includes(query.trim().toLowerCase())))),closing=current.reduce((s,x)=>s+Number(x.closing_balance||0),0),disputed=current.filter(x=>x.status==='disputed').length;
function prepare(g){const dates=g.rows.map(x=>x.date).sort(),field=tab==='customer'?'customer_id':'supplier_id';setSelectedKeys(g.rows.map(x=>x.key));setForm(f=>({...f,[field]:g.party_id,period_start:dates[0],period_end:dates[dates.length-1]}));setView('statements');}
async function generate(){const isCustomer=tab==='customer',id=isCustomer?form.customer_id:form.supplier_id;const ok=await invoke(isCustomer?'forge_customer':'forge_supplier',isCustomer?'customer_generate_statement':'supplier_generate_statement',id,{code:isCustomer?form.customer_code:form.supplier_code,period_start:form.period_start,period_end:form.period_end,dimension,basis:'balance',audited_only:true,include_prepayment:true,source_keys:selectedKeys,remarks:selectedKeys.length?'由待对账池生成':'往来余额对账'});if(ok)setSelectedKeys([]);}
return <div className="forge-product forge-reconcile"><style>{css}</style><div className="body"><ForgePageHeader section="业务确认" title={view==='pool'?'待对账池':tab==='customer'?'客户对账':'供应商对账'} description={view==='pool'?'汇集已结束期间、已审核生效且尚未占用的往来来源':tab==='customer'?'生成客户对账单并跟踪发送、确认和差异处理':'生成供应商对账单并跟踪发送、确认和差异处理'}/>{data.error&&<div className="notice">{data.error}</div>}<div className="tabs"><button className={view==='pool'?'active':''} onClick={()=>setView('pool')}>待对账池</button><button className={view==='statements'?'active':''} onClick={()=>setView('statements')}>对账单</button></div><div className="tabs"><button aria-label="客户对账" className={tab==='customer'?'active':''} onClick={()=>{setTab('customer');setSelectedKeys([])}}>客户{view==='pool'?'待对账':'对账单'}</button><button aria-label="供应商对账" className={tab==='supplier'?'active':''} onClick={()=>{setTab('supplier');setSelectedKeys([])}}>供应商{view==='pool'?'待对账':'对账单'}</button></div><section className="card reconcile-filter"><div className="form"><div className="field"><label>搜索来源或往来单位</label><input aria-label="搜索对账来源" placeholder="编号、客户/供应商、订单或发票" value={query} onChange={e=>setQuery(e.target.value)}/></div><div className="field"><label>业务期间</label><ForgeSelectControl aria-label="对账期间" value={period} onChange={e=>setPeriod(e.target.value)}><option value="all">全部期间</option>{periods.map(x=><option key={x} value={x}>{x}</option>)}</ForgeSelectControl></div><div className="field"><label>当前待对账金额</label><strong className="kpi">{money(eligible.reduce((s,x)=>s+x.amount,0))}</strong></div><div className="field"><label>待对账来源</label><strong className="kpi">{eligible.length}</strong></div></div></section>{view==='pool'?<><div className="rule">仅展示业务日期早于今天、来源已审核生效且未被有效对账单占用的数据。</div><div className="tabs">{Object.entries(dimText).map(([k,v])=><button key={k} className={dimension===k?'active':''} onClick={()=>{setDimension(k);setSelectedKeys([])}}>按{v} {new Set(eligible.map(x=>k==='party'?x.party_id:k==='contract'?x.contract:k==='order'?x.order:k==='shipment_receipt'?x.shipment:k==='invoice'?x.invoice:x.cash).filter(Boolean)).size}</button>)}</div><div className="card"><h2>{tab==='customer'?'客户':'供应商'}待对账清单</h2><div className="table"><table><thead><tr><th>往来单位</th><th>待对账期间</th><th>待对账来源</th><th>{dimText[dimension]}值</th><th>待对账金额</th><th>最近业务日期</th><th>操作</th></tr></thead><tbody>{groups.map(g=>{const dates=g.rows.map(x=>x.date).sort();return <tr key={g.key}><td>{tab==='customer'?(data.customers.find(x=>x.id===g.party_id)?.name||g.party_id):(data.suppliers.find(x=>x.id===g.party_id)?.name||g.party_id)}</td><td>{dates[0]} 至 {dates[dates.length-1]}</td><td>{g.rows.map(x=>x.type+' '+x.code).join('；')}</td><td>{g.dimension_key}</td><td>{money(g.amount)}</td><td>{dates[dates.length-1]}</td><td><button className="btn primary" onClick={()=>prepare(g)}>选择生成</button></td></tr>})}{!groups.length&&<tr><td className="empty" colSpan="7">当前筛选条件下暂无可对账数据</td></tr>}</tbody></table></div></div></>:<><div className="cards"><div className="card"><span className="pill">对账单</span><div className="kpi">{current.length}</div><div className="muted">当前方向全部快照</div></div><div className="card"><span className="pill">期末余额</span><div className="kpi">{money(closing)}</div><div className="muted">对账单账面余额合计</div></div><div className="card"><span className="pill warn">有差异</span><div className="kpi">{disputed}</div><div className="muted">等待处理</div></div><div className="card"><span className="pill">已确认</span><div className="kpi">{current.filter(x=>x.status==='confirmed').length}</div><div className="muted">余额一致</div></div></div>
<div className="card"><h2>生成{tab==='customer'?'客户':'供应商'}对账单{selectedKeys.length?' · 已从待对账池选择 '+selectedKeys.length+' 项':''}</h2><div className="form"><div className="field"><label>往来单位 *</label><ForgeSelectControl aria-label="往来单位" value={tab==='customer'?form.customer_id:form.supplier_id} onChange={e=>setForm({...form,[tab==='customer'?'customer_id':'supplier_id']:e.target.value})}><option value="">请选择</option>{(tab==='customer'?data.customers:data.suppliers).map(x=><option key={x.id} value={x.id}>{x.name}</option>)}</ForgeSelectControl></div><div className="field"><label>对账单号 *</label><input aria-label="对账单号" value={tab==='customer'?form.customer_code:form.supplier_code} onChange={e=>setForm({...form,[tab==='customer'?'customer_code':'supplier_code']:e.target.value})}/></div><div className="field"><label>期间开始 *</label><ForgeDateInput aria-label="期间开始"  value={form.period_start} onChange={e=>setForm({...form,period_start:e.target.value})}/></div><div className="field"><label>期间结束 *</label><ForgeDateInput aria-label="期间结束"  value={form.period_end} onChange={e=>setForm({...form,period_end:e.target.value})}/></div><div className="field"><label>生成维度 *</label><ForgeSelectControl aria-label="生成维度" value={dimension} onChange={e=>setDimension(e.target.value)}>{Object.entries(dimText).map(([k,v])=><option key={k} value={k}>按{v}</option>)}</ForgeSelectControl></div><div className="field"><label>&nbsp;</label><button className="btn primary" disabled={busy||!(tab==='customer'?form.customer_id:form.supplier_id)||!(tab==='customer'?form.customer_code:form.supplier_code).trim()||!form.period_start||!form.period_end} onClick={generate}>生成对账单</button></div></div></div>
<div className="card"><h2>确认参数</h2><div className="form"><div className="field"><label>发送对象 *</label><input aria-label="发送对象" value={form.recipient} onChange={e=>setForm({...form,recipient:e.target.value})}/></div><div className="field"><label>对方确认余额 *</label><input aria-label="对方确认余额" value={form.confirmed_balance} onChange={e=>setForm({...form,confirmed_balance:e.target.value})}/></div><div className="field"><label>确认 / 差异说明</label><input aria-label="确认差异说明" value={form.confirmation_note} onChange={e=>setForm({...form,confirmation_note:e.target.value})}/></div><div className="field"><label>差异处理说明</label><input aria-label="差异处理说明" value={form.resolution_note} onChange={e=>setForm({...form,resolution_note:e.target.value})}/></div></div></div>
<div className="card"><h2>{tab==='customer'?'客户':'供应商'}对账单</h2><div className="table"><table><thead><tr><th>对账单号</th><th>往来单位</th><th>期间</th><th>期初</th><th>本期发生</th><th>本期收付</th><th>期末</th><th>明细</th><th>状态</th><th>差异</th><th>操作</th></tr></thead><tbody>{current.map(x=><tr key={x.id}><td>{x.code}</td><td>{party(x)}</td><td>{x.period_start} 至 {x.period_end}</td><td>{money(x.opening_balance)}</td><td>{money(x.period_charge)}</td><td>{money(x.period_settlement)}</td><td>{money(x.closing_balance)}</td><td>{x.line_count}</td><td><span className={'pill '+(x.status==='disputed'?'warn':'')}>{status[x.status]||x.status}</span></td><td>{money(x.discrepancy_amount)}</td><td><div className="actions">{x.status==='draft'&&<button className="btn primary" disabled={busy||!form.recipient.trim()} onClick={()=>invoke('forge_counterparty_statement','counterparty_statement_send',x.id,{recipient:form.recipient})}>登记发送</button>}{x.status==='sent'&&<button className="btn primary" disabled={busy||form.confirmed_balance===''||!form.confirmation_note.trim()} onClick={()=>invoke('forge_counterparty_statement','counterparty_statement_confirm',x.id,{confirmed_balance:Number(form.confirmed_balance),confirmation_note:form.confirmation_note})}>登记确认</button>}{x.status==='disputed'&&<button className="btn danger" disabled={busy||!form.resolution_note.trim()} onClick={()=>invoke('forge_counterparty_statement','counterparty_statement_close',x.id,{resolution_note:form.resolution_note})}>关闭差异</button>}{['confirmed','closed'].includes(x.status)&&<button className="btn" disabled={busy||!form.resolution_note.trim()} onClick={()=>invoke('forge_counterparty_statement','counterparty_statement_reopen',x.id,{resolution_note:form.resolution_note})}>重新发起确认</button>}</div></td></tr>)}</tbody></table></div></div>
<div className="card"><h2>明细与审计</h2><div className="muted">本页已加载 {data.lines.length} 条冻结明细和 {data.logs.length} 条操作记录；来源进入有效对账单后不再回到待对账池。</div></div></>}</div></div>}export default App;
${forgeProductUiRuntime}
`;

const withReconciliationActions = (source:string) => source.replace(/(<ForgePageHeader\b[^>]*description="[^"]*")\/>/, (_, prefix) => prefix + ' actions={<button className="fp-button" onClick={load}>刷新</button>} />');
export const CounterpartyReconciliationPage = { name: 'page_counterparty_reconciliation', label: '往来对账中心', description: '客户应收与供应商应付期间对账、发送确认及差异处理', icon: 'file-check-2', type: 'app' as const, kind: 'react' as const, source: withReconciliationActions(counterpartyReconciliationSource) };

export const ReconciliationPoolPage = { ...CounterpartyReconciliationPage, name: 'page_reconciliation_pool', label: '待对账池', description: '按往来单位、合同、订单、发货收货、发票和收付款维度汇集待对账来源' };
export const CustomerReconciliationPage = { ...CounterpartyReconciliationPage, name: 'page_customer_reconciliation', label: '客户对账', description: '客户应收对账单生成、发送、确认和差异处理' };
export const SupplierReconciliationPage = { ...CounterpartyReconciliationPage, name: 'page_supplier_reconciliation', label: '供应商对账', description: '供应商应付对账单生成、发送、确认和差异处理' };
