import { forgeProductUiCss, forgeProductUiRuntime } from './product-ui.js';

const createCss = `
.forge-contract-create .contract-create-shell{max-width:1180px;margin:0 auto;padding-bottom:84px}.forge-contract-create .contract-create-title{display:flex;align-items:center;gap:12px;margin-bottom:14px}.forge-contract-create .contract-create-title h1{margin:0;font-size:22px}.forge-contract-create .draft-pill{padding:4px 10px;border-radius:999px;background:#e9f2ff;color:#0c66e4;font-size:12px;font-weight:700}.forge-contract-create .contract-section{margin-bottom:12px;overflow:visible}.forge-contract-create .contract-section-head{display:flex;align-items:center;justify-content:space-between;gap:12px;padding:13px 16px;border-bottom:1px solid #ebedf0}.forge-contract-create .contract-section-head h2{margin:0;font-size:15px}.forge-contract-create .contract-section-body{padding:16px}.forge-contract-create .contract-grid{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:14px 16px}.forge-contract-create .span-2{grid-column:span 2}.forge-contract-create .span-all{grid-column:1/-1}.forge-contract-create .contract-toolbar{display:flex;align-items:center;gap:8px;flex-wrap:wrap}.forge-contract-create .contract-lines{overflow:auto;border:1px solid #dfe1e6;border-radius:6px}.forge-contract-create .contract-lines table{min-width:1040px}.forge-contract-create .contract-lines .fp-input,.forge-contract-create .contract-lines .fp-picker-trigger{min-height:30px;height:30px}.forge-contract-create .contract-empty{padding:28px;text-align:center;color:#6b778c}.forge-contract-create .contract-checks{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:12px}.forge-contract-create .contract-check{display:flex;align-items:flex-start;gap:10px;padding:12px;border:1px solid #dfe1e6;border-radius:6px}.forge-contract-create .contract-check strong{display:block;font-size:13px}.forge-contract-create .contract-check span{display:block;margin-top:3px;color:#6b778c;font-size:12px}.forge-contract-create .contract-fee{display:grid;grid-template-columns:1fr 1fr 140px 40px;gap:8px;margin-top:8px}.forge-contract-create .contract-footer{position:fixed;z-index:20;right:0;bottom:0;left:0;display:flex;align-items:center;justify-content:flex-end;gap:8px;padding:12px 28px;border-top:1px solid #dfe1e6;background:#fff;box-shadow:0 -4px 18px rgba(9,30,66,.08)}.forge-contract-create .contract-total{margin-right:14px;color:#6b778c}.forge-contract-create .contract-total strong{margin-left:8px;color:#172b4d;font-size:19px}.forge-contract-create .contract-preview{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:10px}.forge-contract-create .contract-preview div{padding:10px;border-radius:5px;background:#f7f8fa}.forge-contract-create .contract-preview span{display:block;color:#6b778c;font-size:12px}.forge-contract-create .contract-preview strong{display:block;margin-top:3px}.forge-contract-create .contract-import-note{margin-top:10px;color:#6b778c;font-size:12px}.forge-contract-create .contract-file-list{display:grid;gap:8px;margin-top:10px}.forge-contract-create .contract-file{display:flex;align-items:center;justify-content:space-between;gap:12px;padding:9px 11px;border:1px solid #dfe1e6;border-radius:6px;background:#fafbfc}.forge-contract-create .contract-file span{min-width:0;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}.forge-contract-create .contract-bundles{display:grid;gap:8px;max-height:360px;overflow:auto}.forge-contract-create .contract-bundle{display:flex;align-items:flex-start;gap:10px;padding:11px;border:1px solid #dfe1e6;border-radius:6px}.forge-contract-create .contract-bundle div{min-width:0}.forge-contract-create .contract-bundle strong,.forge-contract-create .contract-bundle span{display:block}.forge-contract-create .contract-bundle span{margin-top:3px;color:#6b778c;font-size:12px}
@media(max-width:900px){.forge-contract-create .contract-grid{grid-template-columns:repeat(2,minmax(0,1fr))}.forge-contract-create .span-all{grid-column:1/-1}}
@media(max-width:760px){.forge-contract-create .contract-create-shell{padding-bottom:110px}.forge-contract-create .contract-grid,.forge-contract-create .contract-checks{grid-template-columns:1fr}.forge-contract-create .span-2,.forge-contract-create .span-all{grid-column:auto}.forge-contract-create .contract-section-body{padding:12px}.forge-contract-create .contract-fee{grid-template-columns:1fr 1fr}.forge-contract-create .contract-footer{flex-wrap:wrap;padding:10px 16px}.forge-contract-create .contract-total{width:100%;margin:0;text-align:right}}
`;

const source = `
const css=${JSON.stringify(forgeProductUiCss + createCss)};
function App(){
 const today=new Date().toISOString().slice(0,10),nextYear=String(Number(today.slice(0,4))+1)+today.slice(4),listUrl=forgeBase+'/page/page_sales_contract_workspace';
 const blankLine=()=>({key:Math.random().toString(36).slice(2),sku_id:'',name:'',item_code:'',model:'',specification:'',unit_name:'',quantity_limit:'1',taxed_unit_price:'0',tax_rate:'13',discount_rate:'0'}),blankFee=()=>({key:Math.random().toString(36).slice(2),fee_item:'',bearing_type:'customer',total_amount:'0'});
 const [data,setData]=React.useState({loading:true,error:'',customers:[],contacts:[],users:[],types:[],quotes:[],quoteLines:[],skus:[],materials:[],units:[],accounts:[],bundles:[],bundleLines:[]});
 const [form,setForm]=React.useState({name:'',code:'',customer_po_number:'',contract_type_id:'',customer_id:'',contact_id:'',company_account_id:'',delivery_address:'',delivery_contact:'',delivery_phone:'',responsible_id:'',collaborator_ids:[],quotation_id:'',project_name:'',signed_on:today,starts_on:today,ends_on:nextYear,remarks:'',has_order_amount_limit:false,order_amount_limit:'',allow_affiliate_orders:false,affiliate_company_names:'',outside_item_requires_approval:true,all_orders_require_approval:false,revenue_trigger:'shipment',payment_term:'',delivery_cycle_days:'21',warranty_months:'',business_terms:'',attachment_note:''});
 const [lines,setLines]=React.useState([]),[fees,setFees]=React.useState([]),[attachments,setAttachments]=React.useState([]),[dialog,setDialog]=React.useState(null),[busy,setBusy]=React.useState(false),[toast,setToast]=React.useState('');
 async function request(path,options){const token=localStorage.getItem('auth-session-token'),response=await fetch('/api/v1'+path,{credentials:'include',headers:{'Content-Type':'application/json',...(token?{Authorization:'Bearer '+token}:{})},...options}),payload=await response.json().catch(()=>({}));if(!response.ok)throw new Error((typeof payload.error==='string'?payload.error:payload.error?.message)||payload.message||'请求失败');return payload}
 async function find(name){return (await request('/data/'+name+'?'+new URLSearchParams({$top:'500'}))).records||[]}
 async function load(){try{const names=['forge_customer','forge_contact','sys_user','forge_contract_type','forge_quotation','forge_quotation_line','forge_material_sku','forge_material','forge_unit','forge_fund_account','forge_product_bundle','forge_product_bundle_line'],sets=await Promise.all(names.map(find)),d=Object.fromEntries(names.map((name,index)=>[name,sets[index]])),session=await request('/auth/get-session').catch(()=>({})),current=session?.user?.id||d.sys_user[0]?.id||'';setData({loading:false,error:'',customers:d.forge_customer,contacts:d.forge_contact,users:d.sys_user,types:d.forge_contract_type,quotes:d.forge_quotation,quoteLines:d.forge_quotation_line,skus:d.forge_material_sku,materials:d.forge_material,units:d.forge_unit,accounts:d.forge_fund_account,bundles:d.forge_product_bundle,bundleLines:d.forge_product_bundle_line});setForm(value=>({...value,responsible_id:value.responsible_id||current}))}catch(error){setData(value=>({...value,loading:false,error:String(error.message||error)}))}}
 React.useEffect(()=>{load()},[]);React.useEffect(()=>{if(!toast)return;const timer=setTimeout(()=>setToast(''),2600);return()=>clearTimeout(timer)},[toast]);
 const byId=(items,id)=>items.find(item=>item.id===id),display=(items,id)=>{const item=byId(items,id);return item?(item.display_name||item.name||item.code||'—'):'—'},money=value=>'¥'+Number(value||0).toLocaleString('zh-CN',{minimumFractionDigits:2,maximumFractionDigits:2});
 const contacts=data.contacts.filter(item=>!form.customer_id||item.customer_id===form.customer_id),total=lines.reduce((sum,line)=>sum+Number(line.quantity_limit||0)*Number(line.taxed_unit_price||0)*(1-Number(line.discount_rate||0)/100),0)+fees.reduce((sum,fee)=>sum+Number(fee.total_amount||0),0);
 function change(name,value){setForm(current=>({...current,[name]:value,...(name==='customer_id'?{contact_id:''}:{})}))}
 function changeLine(key,name,value){setLines(items=>items.map(item=>item.key===key?({...item,[name]:value}):item))}
 function selectSku(key,skuId){const sku=byId(data.skus,skuId),material=byId(data.materials,sku?.material_id),unit=byId(data.units,material?.unit_id);setLines(items=>items.map(item=>item.key===key?({...item,sku_id:skuId,name:material?.name||sku?.name||'',item_code:material?.code||sku?.code||'',model:material?.model||'',specification:sku?.name||'',unit_name:unit?.name||''}):item))}
 function importQuote(){const rows=data.quoteLines.filter(item=>item.quotation_id===form.quotation_id).map(item=>({...blankLine(),quotation_line_id:item.id,sku_id:item.sku_id||'',name:item.name,item_code:item.item_code||'',model:item.model||'',specification:item.specification||'',unit_name:item.unit_name||'',quantity_limit:String(item.quantity||1),taxed_unit_price:String(item.taxed_unit_price||0),tax_rate:String(item.tax_rate??13),discount_rate:String(item.discount_rate||0)}));if(!rows.length)return setToast('所选报价单没有可导入明细');setLines(rows);setToast('已从报价单导入 '+rows.length+' 项物料')}
 function importText(text){const rows=String(text||'').split(/\\r?\\n/).map(row=>row.trim()).filter(Boolean),parsed=[];for(const row of rows){const cells=row.split(/\\t|,/).map(value=>value.trim()),code=cells[0],sku=data.skus.find(item=>item.code===code),material=byId(data.materials,sku?.material_id),unit=byId(data.units,material?.unit_id);if(!sku)continue;parsed.push({...blankLine(),sku_id:sku.id,name:material?.name||sku.name||'',item_code:material?.code||sku.code||'',model:material?.model||'',specification:sku.name||'',unit_name:unit?.name||'',quantity_limit:String(Number(cells[1])>0?Number(cells[1]):1),taxed_unit_price:String(Number(cells[2])>=0?Number(cells[2]):Number(sku.sale_price||0)),tax_rate:String(Number(cells[3])>=0?Number(cells[3]):13),discount_rate:String(Number(cells[4])>=0?Number(cells[4]):0)})}if(!parsed.length)throw new Error('未识别到有效 SKU；请按 SKU编码、数量、含税单价、税率、折扣率粘贴');setLines(items=>[...items,...parsed]);setDialog(null);setToast('已导入 '+parsed.length+' 项物料')}
 function importBundle(bundleId){const rows=data.bundleLines.filter(item=>item.bundle_id===bundleId).map(item=>{const sku=byId(data.skus,item.sku_id),material=byId(data.materials,item.material_id||sku?.material_id),unit=byId(data.units,material?.unit_id);return {...blankLine(),sku_id:item.sku_id,name:item.name||material?.name||sku?.name||'',item_code:material?.code||sku?.code||'',model:item.model||material?.model||'',specification:item.specification||sku?.name||'',unit_name:item.unit_name||unit?.name||'',quantity_limit:String(item.quantity||1),taxed_unit_price:String(item.sale_price||sku?.sale_price||0)}});if(!rows.length)return setDialog(current=>({...current,error:'该物料组合没有可导入明细'}));setLines(items=>[...items,...rows]);setDialog(null);setToast('已导入物料组合 '+rows.length+' 项')}
 async function uploadFile(file){const signed=await request('/storage/upload/presigned',{method:'POST',body:JSON.stringify({filename:file.name,mimeType:file.type||'application/octet-stream',size:file.size,scope:'user'})}),descriptor=signed.data||signed,url=new URL(descriptor.uploadUrl,window.location.origin),uploaded=await fetch(url,{method:descriptor.method||'PUT',headers:descriptor.headers||{},body:file});if(!uploaded.ok)throw new Error('上传 '+file.name+' 失败');const completed=await request('/storage/upload/complete',{method:'POST',body:JSON.stringify({fileId:descriptor.fileId})});return (completed.data||completed).fileId||descriptor.fileId}
 function validate(){if(!form.customer_id||!form.name.trim()||!form.contract_type_id||!form.signed_on||!form.responsible_id)throw new Error('请填写客户、合同标题、合同类型、签订日期和负责人');if(!lines.length)throw new Error('请至少添加一项合同物料');if(lines.some(line=>!line.sku_id||!(Number(line.quantity_limit)>0)||Number(line.taxed_unit_price)<0))throw new Error('物料明细必须选择物料并填写正数数量和有效价格');if(form.has_order_amount_limit&&!(Number(form.order_amount_limit)>0))throw new Error('启用下单金额限制后必须填写正数上限');if(!form.payment_term.trim())throw new Error('请填写付款条件')}
 async function save(submit){setBusy(true);try{validate();const attachmentIds=[];for(const file of attachments)attachmentIds.push(await uploadFile(file));const code=form.code.trim()||'SC-'+new Date().toISOString().replace(/[-:TZ.]/g,'').slice(0,14),payload={...form,name:form.name.trim(),code,customer_po_number:form.customer_po_number||null,contact_id:form.contact_id||null,company_account_id:form.company_account_id||null,quotation_id:form.quotation_id||null,attachment_ids:attachmentIds,order_amount_limit:form.has_order_amount_limit?Number(form.order_amount_limit):0,total_amount:Number(total.toFixed(2)),delivery_cycle_days:Number(form.delivery_cycle_days||0),warranty_months:form.warranty_months===''?null:Number(form.warranty_months),affiliate_company_names:form.allow_affiliate_orders?form.affiliate_company_names||null:null,status:'draft'},created=await request('/data/forge_sales_contract',{method:'POST',body:JSON.stringify(payload)}),record=created.record||created.data||created,contractId=record.id||created.id;if(!contractId)throw new Error('保存响应没有合同标识');for(const line of lines)await request('/data/forge_sales_contract_line',{method:'POST',body:JSON.stringify({name:line.name,contract_id:contractId,quotation_line_id:line.quotation_line_id||null,sku_id:line.sku_id,item_code:line.item_code||null,model:line.model||null,specification:line.specification||null,unit_name:line.unit_name||null,quantity_limit:Number(line.quantity_limit),ordered_quantity:0,taxed_unit_price:Number(line.taxed_unit_price||0),tax_rate:Number(line.tax_rate||0),discount_rate:Number(line.discount_rate||0),taxed_subtotal:Number((Number(line.quantity_limit)*Number(line.taxed_unit_price)*(1-Number(line.discount_rate||0)/100)).toFixed(2))})});for(const [index,fee] of fees.entries())if(fee.fee_item.trim()&&Number(fee.total_amount)>0)await request('/data/forge_sales_additional_fee',{method:'POST',body:JSON.stringify({name:fee.fee_item.trim()+' '+code,code:code+'-F'+String(index+1).padStart(2,'0'),source_type:'sales_contract',contract_id:contractId,customer_id:form.customer_id,bearing_type:fee.bearing_type,fee_item:fee.fee_item.trim(),occurred_on:form.signed_on,total_amount:Number(fee.total_amount),document_status:'draft',finance_status:fee.bearing_type==='customer'?'pending_invoice':'pending_payment',responsible_id:form.responsible_id})});if(submit)await request('/actions/forge_sales_contract/contract_submit/'+contractId,{method:'POST',body:JSON.stringify({params:{}})});setToast(submit?'合同已保存并提交审批':'合同草稿已保存');setTimeout(()=>window.location.href=listUrl,500)}catch(error){setDialog({kind:'error',error:String(error.message||error)})}finally{setBusy(false)}}
 if(data.loading)return <div className="forge-product forge-contract-create"><style>{css}</style><ForgeLoading label="加载新建合同页面"/></div>;
 const field=(labelText,control,wide)=><div className={'fp-field '+(wide?'span-2':'')}><label>{labelText}</label>{control}</div>;
 return <div className="forge-product forge-contract-create"><style>{css}</style><main className="contract-create-shell"><div className="contract-create-title"><button className="fp-icon-button" aria-label="返回合同列表" onClick={()=>window.location.href=listUrl}>←</button><div><div className="fp-secondary">销售管理 / 框架销售合同 / 新建合同</div><h1>新建框架销售合同</h1></div><span className="draft-pill">草稿</span></div>{data.error&&<ForgeNotice tone="error">{data.error}</ForgeNotice>}
 <section className="fp-card contract-section"><div className="contract-section-head"><h2>客户信息</h2></div><div className="contract-section-body contract-grid">{field('客户 *',<ForgeSelect label="客户" value={form.customer_id} onChange={value=>change('customer_id',value)} options={data.customers.map(item=>({value:item.id,label:item.name}))} placeholder="搜索客户名称..."/>)}{field('联系人 / 联系电话',<ForgeSelect label="联系人" value={form.contact_id} onChange={value=>change('contact_id',value)} options={contacts.map(item=>({value:item.id,label:item.name+(item.phone?' · '+item.phone:'')}))} placeholder={form.customer_id?'请选择联系人':'请先选择客户'} disabled={!form.customer_id}/>)}{field('收货信息',<input className="fp-input" aria-label="收货地址" placeholder={form.customer_id?'请输入收货地址':'请先选择客户'} value={form.delivery_address} onChange={event=>change('delivery_address',event.target.value)} disabled={!form.customer_id}/>)}<div className="fp-field span-all"><label>合同备注</label><textarea className="fp-textarea" aria-label="合同备注" placeholder="请输入合同备注" value={form.remarks} onChange={event=>change('remarks',event.target.value)}/></div></div></section>
 <section className="fp-card contract-section"><div className="contract-section-head"><h2>销售人员</h2><span className="fp-secondary">负责人承担合同业绩，协同销售参与合同协作</span></div><div className="contract-section-body contract-grid">{field('负责人 *',<ForgeSelect label="负责人" value={form.responsible_id} onChange={value=>change('responsible_id',value)} options={data.users.map(item=>({value:item.id,label:display(data.users,item.id)}))} placeholder="请选择负责人"/>)}<div className="fp-field span-2"><label>协同销售 · 已选 {form.collaborator_ids.length} 人</label><div className="contract-toolbar">{data.users.filter(item=>item.id!==form.responsible_id).map(item=><label className="fp-chip" key={item.id}><input type="checkbox" checked={form.collaborator_ids.includes(item.id)} onChange={()=>change('collaborator_ids',form.collaborator_ids.includes(item.id)?form.collaborator_ids.filter(id=>id!==item.id):[...form.collaborator_ids,item.id])}/>{display(data.users,item.id)}</label>)}</div></div></div></section>
 <section className="fp-card contract-section"><div className="contract-section-head"><h2>合同基本信息</h2></div><div className="contract-section-body contract-grid">{field('合同标题 *',<input className="fp-input" aria-label="合同标题" placeholder="请输入合同标题" value={form.name} onChange={event=>change('name',event.target.value)}/>,true)}{field('公司账户',<ForgeSelect label="公司账户" value={form.company_account_id} onChange={value=>change('company_account_id',value)} options={data.accounts.map(item=>({value:item.id,label:item.name}))} placeholder="搜索允许打印的公司账户..."/>)}{field('合同类型 *',<ForgeSelect label="合同类型" value={form.contract_type_id} onChange={value=>change('contract_type_id',value)} options={data.types.map(item=>({value:item.id,label:item.name}))} placeholder="请选择合同类型"/>)}{field('合同编号',<input className="fp-input" aria-label="合同编号" placeholder="留空则按编码规则自动生成" value={form.code} onChange={event=>change('code',event.target.value)}/>)}{field('客户单号',<input className="fp-input" aria-label="客户单号" placeholder="请输入客户单号" value={form.customer_po_number} onChange={event=>change('customer_po_number',event.target.value)}/>)}{field('签订日期 *',<ForgeDateInput className="fp-input" aria-label="签订日期" value={form.signed_on} onChange={event=>change('signed_on',event.target.value)}/>)}{field('关联项目',<input className="fp-input" aria-label="关联项目" placeholder={form.customer_id?'请输入关联项目':'请先选择客户'} value={form.project_name} onChange={event=>change('project_name',event.target.value)} disabled={!form.customer_id}/>)}{field('来源报价单',<ForgeSelect label="来源报价单" value={form.quotation_id} onChange={value=>change('quotation_id',value)} options={data.quotes.filter(item=>!form.customer_id||item.customer_id===form.customer_id).map(item=>({value:item.id,label:item.code+' · '+item.name}))} placeholder="选择报价单后可导入物料"/>)}</div></section>
 <section className="fp-card contract-section"><div className="contract-section-head"><h2>物料明细 · {lines.length} 项</h2><div className="contract-toolbar"><button className="fp-button" onClick={()=>setLines(items=>[...items,blankLine()])}>添加行</button><button className="fp-button" disabled={!form.quotation_id} onClick={importQuote}>报价单导入</button><button className="fp-button" onClick={()=>setDialog({kind:'paste',text:'',error:''})}>粘贴导入</button><label className="fp-button">Excel/CSV 导入<input hidden type="file" accept=".csv,.tsv,.txt,text/csv,text/tab-separated-values" onChange={event=>{const file=event.target.files?.[0];if(file)file.text().then(importText).catch(error=>setDialog({kind:'error',error:String(error.message||error)}));event.target.value=''}}/></label><button className="fp-button" onClick={()=>setDialog({kind:'bundle',error:''})}>物料组合</button></div></div><div className="contract-section-body"><div className="contract-lines"><table className="fp-table"><thead><tr><th>物料/规格 *</th><th>型号</th><th>单位</th><th>数量上限 *</th><th>协议含税单价</th><th>税率%</th><th>折扣率%</th><th>含税小计</th><th>操作</th></tr></thead><tbody>{lines.map(line=><tr key={line.key}><td><ForgeSelect label="物料规格" value={line.sku_id} onChange={value=>selectSku(line.key,value)} options={data.skus.map(item=>{const material=byId(data.materials,item.material_id);return {value:item.id,label:(material?.code||item.code||'')+' · '+(material?.name||item.name||'物料')+' / '+(item.name||'默认规格')}})} placeholder="选择物料"/><div className="fp-secondary">{line.name||'—'} · {line.item_code||'—'}</div></td><td>{line.model||line.specification||'—'}</td><td>{line.unit_name||'—'}</td><td><input className="fp-input" aria-label={'数量 '+(line.name||line.key)} type="number" min="0.0001" value={line.quantity_limit} onChange={event=>changeLine(line.key,'quantity_limit',event.target.value)}/></td><td><input className="fp-input" aria-label={'含税单价 '+(line.name||line.key)} type="number" min="0" value={line.taxed_unit_price} onChange={event=>changeLine(line.key,'taxed_unit_price',event.target.value)}/></td><td><input className="fp-input" type="number" min="0" max="100" value={line.tax_rate} onChange={event=>changeLine(line.key,'tax_rate',event.target.value)}/></td><td><input className="fp-input" type="number" min="0" max="100" value={line.discount_rate} onChange={event=>changeLine(line.key,'discount_rate',event.target.value)}/></td><td>{money(Number(line.quantity_limit||0)*Number(line.taxed_unit_price||0)*(1-Number(line.discount_rate||0)/100))}</td><td><button className="fp-icon-button" aria-label={'删除物料 '+(line.name||line.key)} onClick={()=>setLines(items=>items.filter(item=>item.key!==line.key))}>×</button></td></tr>)}{!lines.length&&<tr><td colSpan="9" className="contract-empty">暂未添加物料。点击“添加行”从物料库选择，或从报价单导入。</td></tr>}</tbody></table></div><div className="contract-import-note">特价与询价单当前没有可追溯的 Forge 来源对象，因此不显示不可用入口；待建立真实来源关系后再接入。</div></div></section>
 <section className="fp-card contract-section"><div className="contract-section-head"><h2>订货约束与授权</h2></div><div className="contract-section-body contract-checks"><label className="contract-check"><input type="checkbox" checked={form.has_order_amount_limit} onChange={event=>change('has_order_amount_limit',event.target.checked)}/><div><strong>限制合同总下单金额</strong><span>订单累计含税金额不得超过上限</span>{form.has_order_amount_limit&&<input className="fp-input" aria-label="累计下单金额上限" type="number" min="0" value={form.order_amount_limit} onChange={event=>change('order_amount_limit',event.target.value)}/>}</div></label><label className="contract-check"><input type="checkbox" checked={form.allow_affiliate_orders} onChange={event=>change('allow_affiliate_orders',event.target.checked)}/><div><strong>分/子公司或关联公司受用</strong><span>授权关联公司引用本合同下单</span>{form.allow_affiliate_orders&&<textarea className="fp-textarea" aria-label="受用关联公司" value={form.affiliate_company_names} onChange={event=>change('affiliate_company_names',event.target.value)}/>}</div></label><label className="contract-check"><input type="checkbox" checked={form.outside_item_requires_approval} onChange={event=>change('outside_item_requires_approval',event.target.checked)}/><div><strong>容许添加协议外物料下单</strong><span>协议外物料订单必须进入审批</span></div></label><label className="contract-check"><input type="checkbox" checked={form.all_orders_require_approval} onChange={event=>change('all_orders_require_approval',event.target.checked)}/><div><strong>框架和额度内下单需要审批</strong><span>开启后额度和清单范围内订单也进入审批</span></div></label></div></section>
 <section className="fp-card contract-section"><div className="contract-section-head"><h2>附加费用 · {fees.length} 项</h2><button className="fp-button" onClick={()=>setFees(items=>[...items,blankFee()])}>添加费用</button></div><div className="contract-section-body">{fees.map(fee=><div className="contract-fee" key={fee.key}><input className="fp-input" aria-label="费用项目" placeholder="费用项目" value={fee.fee_item} onChange={event=>setFees(items=>items.map(item=>item.key===fee.key?({...item,fee_item:event.target.value}):item))}/><ForgeSelect label="承担方" value={fee.bearing_type} onChange={value=>setFees(items=>items.map(item=>item.key===fee.key?({...item,bearing_type:value}):item))} options={[{value:'customer',label:'客户承担'},{value:'company',label:'公司承担'}]}/><input className="fp-input" aria-label="费用金额" type="number" min="0" value={fee.total_amount} onChange={event=>setFees(items=>items.map(item=>item.key===fee.key?({...item,total_amount:event.target.value}):item))}/><button className="fp-icon-button" aria-label="删除费用" onClick={()=>setFees(items=>items.filter(item=>item.key!==fee.key))}>×</button></div>)}{!fees.length&&<div className="contract-empty">暂无附加费用</div>}</div></section>
 <section className="fp-card contract-section"><div className="contract-section-head"><h2>收入确认规则</h2></div><div className="contract-section-body">{field('触发方式',<ForgeSelect label="收入确认方式" value={form.revenue_trigger} onChange={value=>change('revenue_trigger',value)} options={[{value:'shipment',label:'按发货出库'},{value:'acceptance',label:'按客户验收'},{value:'invoice',label:'按开票'},{value:'manual',label:'手工确认'}]}/>)}</div></section>
 <section className="fp-card contract-section"><div className="contract-section-head"><h2>合同条款</h2></div><div className="contract-section-body contract-grid">{field('合同开始日期',<ForgeDateInput className="fp-input" aria-label="合同开始日期" value={form.starts_on} onChange={event=>change('starts_on',event.target.value)}/>)}{field('合同结束日期',<ForgeDateInput className="fp-input" aria-label="合同结束日期" value={form.ends_on} onChange={event=>change('ends_on',event.target.value)}/>)}{field('付款条件 *',<input className="fp-input" aria-label="付款条件" placeholder="请输入付款条件" value={form.payment_term} onChange={event=>change('payment_term',event.target.value)}/>)}{field('交货周期（天）',<input className="fp-input" aria-label="交货周期" type="number" min="0" value={form.delivery_cycle_days} onChange={event=>change('delivery_cycle_days',event.target.value)}/>)}{field('质保期（月）',<input className="fp-input" aria-label="质保期" type="number" min="0" value={form.warranty_months} onChange={event=>change('warranty_months',event.target.value)}/>)}<div className="fp-field span-all"><label>条款内容</label><textarea className="fp-textarea" aria-label="合同条款内容" placeholder="请输入合同条款内容" value={form.business_terms} onChange={event=>change('business_terms',event.target.value)}/></div></div></section>
 <section className="fp-card contract-section"><div className="contract-section-head"><h2>附件 · {attachments.length} 个</h2><label className="fp-button">选择文件<input hidden multiple type="file" onChange={event=>{setAttachments(items=>[...items,...Array.from(event.target.files||[])]);event.target.value=''}}/></label></div><div className="contract-section-body"><div className="fp-field"><label>附件说明</label><textarea className="fp-textarea" aria-label="附件说明" placeholder="记录合同附件、附加协议或商务事故附件的补充说明" value={form.attachment_note} onChange={event=>change('attachment_note',event.target.value)}/></div>{attachments.length>0&&<div className="contract-file-list">{attachments.map((file,index)=><div className="contract-file" key={file.name+file.size+index}><span>{file.name} · {(file.size/1024).toFixed(1)} KB</span><button className="fp-icon-button" aria-label={'移除附件 '+file.name} onClick={()=>setAttachments(items=>items.filter((_,i)=>i!==index))}>×</button></div>)}</div>}</div></section></main>
 <footer className="contract-footer"><div className="contract-total">合同金额<strong>{money(total)}</strong></div><button className="fp-button" onClick={()=>setDialog({kind:'preview'})}>预览</button><button className="fp-button" disabled={busy} onClick={()=>save(false)}>保存</button><button className="fp-button primary" disabled={busy} onClick={()=>setDialog({kind:'submit'})}>提交</button></footer>
 {dialog?.kind==='preview'&&<ForgeDialog open title="合同预览" subtitle={form.name||'未命名框架销售合同'} confirmLabel="关闭" onCancel={()=>setDialog(null)} onConfirm={()=>setDialog(null)}><div className="contract-preview"><div><span>客户</span><strong>{display(data.customers,form.customer_id)}</strong></div><div><span>合同类型</span><strong>{display(data.types,form.contract_type_id)}</strong></div><div><span>负责人</span><strong>{display(data.users,form.responsible_id)}</strong></div><div><span>签订日期</span><strong>{form.signed_on||'—'}</strong></div><div><span>物料明细</span><strong>{lines.length} 项</strong></div><div><span>合同金额</span><strong>{money(total)}</strong></div></div></ForgeDialog>}
 {dialog?.kind==='submit'&&<ForgeDialog open title="确认提交框架销售合同" subtitle={form.name||'未命名合同'} busy={busy} confirmLabel="确认保存并提交" onCancel={()=>!busy&&setDialog(null)} onConfirm={()=>save(true)}><div className="fp-dialog-impact">提交后合同进入待审批状态；物料明细、订货约束、附加费用和条款将一并保存。</div></ForgeDialog>}
 {dialog?.kind==='paste'&&<ForgeDialog open title="粘贴导入合同物料" subtitle="每行一项，支持制表符或逗号分隔" error={dialog.error} confirmLabel="应用导入" onCancel={()=>setDialog(null)} onConfirm={()=>{try{importText(dialog.text)}catch(error){setDialog(current=>({...current,error:String(error.message||error)}))}}}><div className="fp-field"><label>SKU编码、数量、含税单价、税率、折扣率</label><textarea className="fp-textarea" rows="8" aria-label="粘贴导入合同物料" placeholder={'SKU-001\\t2\\t128000\\t13\\t5'} value={dialog.text} onChange={event=>setDialog(current=>({...current,text:event.target.value,error:''}))}/></div></ForgeDialog>}
 {dialog?.kind==='bundle'&&<ForgeDialog open title="从物料组合导入" subtitle="选择一个已启用且包含明细的组合" error={dialog.error} confirmLabel="关闭" onCancel={()=>setDialog(null)} onConfirm={()=>setDialog(null)}><div className="contract-bundles">{data.bundles.filter(item=>item.status!=='inactive').map(item=><label className="contract-bundle" key={item.id}><input type="radio" name="contract-bundle" onChange={()=>importBundle(item.id)}/><div><strong>{item.code} · {item.name}</strong><span>{data.bundleLines.filter(line=>line.bundle_id===item.id).length} 项物料 · {money(item.taxed_sale_price)}</span></div></label>)}{!data.bundles.length&&<ForgeEmpty title="暂无可用物料组合" description="请先在物料组合中建立真实组合明细。"/>}</div></ForgeDialog>}
 {dialog?.kind==='error'&&<ForgeDialog open title="合同保存失败" error={dialog.error} confirmLabel="返回修改" onCancel={()=>setDialog(null)} onConfirm={()=>setDialog(null)}><div className="fp-dialog-impact">请根据错误提示修正表单；未看到成功反馈前不要重复提交。</div></ForgeDialog>}{toast&&<div className="fp-toast" role="status">{toast}</div>}</div>}
export default App;
${forgeProductUiRuntime}`;

export const SalesContractCreatePage = { name: 'page_sales_contract_create', label: '新建框架销售合同', description: '按客户、物料、订货约束、费用和条款完整创建框架销售合同', icon: 'file-plus-2', type: 'app' as const, kind: 'react' as const, source };
