import { defineAction } from '@objectstack/spec';

const locations = ['record_header', 'record_more'] as const;

export const PurchaseOrderCreateReplacement = defineAction({
  name:'purchase_order_create_replacement',label:'新建换货补货',objectName:'forge_purchase_order',icon:'repeat-2',locations:[...locations],order:56,
  visible:`record.status == 'approved' || record.status == 'partially_arrived' || record.status == 'arrived' || record.status == 'completed'`,refreshAfter:true,
  params:[{field:'code',objectOverride:'forge_purchase_return',required:true},{field:'order_line_id',objectOverride:'forge_purchase_return_line',required:true},{field:'warehouse_id',objectOverride:'forge_purchase_return',required:true},{field:'requested_quantity',objectOverride:'forge_purchase_return_line',required:true},{field:'return_on',objectOverride:'forge_purchase_return',required:true},{field:'expected_replenishment_on',objectOverride:'forge_purchase_return',required:true},{field:'reason',objectOverride:'forge_purchase_return',required:true},{field:'return_address',objectOverride:'forge_purchase_return'},{field:'contact_name',objectOverride:'forge_purchase_return'},{field:'contact_phone',objectOverride:'forge_purchase_return'},{field:'remarks',objectOverride:'forge_purchase_return'}],
  onSuccess:{navigate:'/_console/apps/forge/page/page_purchase_return?return=${result.id}'},
  body:{language:'js',capabilities:['api.read','api.write'],source:`
const id=ctx.recordId||(ctx.record&&ctx.record.id),order=ctx.record,actor=ctx.session&&ctx.session.userId,reason=String(ctx.input.reason||'').trim();if(ctx.recordLoadDenied===true||!id||!order)throw new Error('采购订单不存在或不可访问');if(!actor)throw new Error('无法识别当前申请人');if(!['approved','partially_arrived','arrived','completed'].includes(order.status))throw new Error('仅已审核且未取消采购订单可以换货');if(!reason)throw new Error('换货原因不能为空');if(!ctx.input.expected_replenishment_on)throw new Error('预计补货日期不能为空');const quantity=Number(ctx.input.requested_quantity||0);if(!(quantity>0))throw new Error('换货数量必须大于0');const line=await ctx.api.object('forge_purchase_order_line').findOne({where:{id:ctx.input.order_line_id}});if(!line||line.order_id!==id)throw new Error('换货物料必须来自当前采购订单');const existing=await ctx.api.object('forge_purchase_return_line').find({where:{order_line_id:line.id}}),committed=existing.filter(x=>!['rejected','cancelled'].includes(x.status)).reduce((sum,x)=>sum+Number(x.requested_quantity||0),0),round4=v=>Math.round((Number(v)+Number.EPSILON)*10000)/10000,returnable=round4(Number(line.inbound_quantity||0)+Number(line.replenished_quantity||0)-committed);if(quantity>returnable)throw new Error('换货数量超过该订单明细已入库未退数量');const balances=await ctx.api.object('forge_inventory_balance').find({where:{balance_key:ctx.input.warehouse_id+':'+line.sku_id}});if(balances.length!==1||quantity>Number(balances[0].available_quantity||0))throw new Error('所选仓库可用库存不足');const amount=round4(quantity*Number(line.taxed_unit_price||0)),created=await ctx.api.object('forge_purchase_return').insert({name:ctx.input.code+' '+line.name,code:ctx.input.code,order_id:id,supplier_id:order.supplier_id,processing_type:'replacement',return_on:ctx.input.return_on,expected_replenishment_on:ctx.input.expected_replenishment_on,refund_method:null,currency:order.currency||'cny',exchange_rate:Number(order.exchange_rate||1),reason,warehouse_id:ctx.input.warehouse_id,return_address:ctx.input.return_address||null,contact_name:ctx.input.contact_name||null,contact_phone:ctx.input.contact_phone||null,line_count:1,total_quantity:quantity,reference_amount:amount,actual_refund_amount:0,status:'draft',finance_status:'not_required',warehouse_status:'pending',outbound_status:'pending',refund_status:'not_required',replacement_status:'pending_return',replenishment_receipt_id:null,replenishment_inspection_id:null,replenishment_inbound_id:null,replenishment_arrived_quantity:0,replenishment_accepted_quantity:0,replenished_quantity:0,account_id:null,bank_reference:null,submitted_by:null,submitted_at:null,approved_by:null,approved_at:null,approval_comment:null,warehouse_confirmed_by:null,warehouse_confirmed_at:null,outbounded_by:null,outbounded_at:null,responsible_id:order.responsible_id,remarks:ctx.input.remarks||null}),returnId=typeof created==='string'?created:created&&(created.id||(created.record&&created.record.id));if(!returnId)throw new Error('采购换货单创建后未返回ID');const lineCreated=await ctx.api.object('forge_purchase_return_line').insert({name:line.name,return_id:returnId,order_id:id,order_line_id:line.id,supplier_id:order.supplier_id,warehouse_id:ctx.input.warehouse_id,sku_id:line.sku_id,item_code:line.item_code||null,model:line.model||null,specification:line.specification||null,unit_name:line.unit_name||null,requested_quantity:quantity,confirmed_quantity:0,outbounded_quantity:0,replenished_quantity:0,accepted_replenishment_quantity:0,taxed_unit_price:Number(line.taxed_unit_price||0),reference_amount:amount,outbound_unit_cost:0,outbound_amount:0,status:'draft',remarks:ctx.input.remarks||null}),lineId=typeof lineCreated==='string'?lineCreated:lineCreated&&(lineCreated.id||(lineCreated.record&&lineCreated.record.id));if(!lineId)throw new Error('采购换货明细创建后未返回ID');return{id:returnId,line_id:lineId,order_id:id,status:'draft',quantity,reference_amount:amount,returnable_after:round4(returnable-quantity)};
`},
});

export const PurchaseReplacementSubmit = defineAction({
  name:'purchase_replacement_submit',label:'提交换货申请',objectName:'forge_purchase_return',icon:'send',locations:[...locations],order:11,visible:`record.status == 'draft' && record.processing_type == 'replacement'`,refreshAfter:true,
  body:{language:'js',capabilities:['api.read','api.write'],source:`const id=ctx.recordId||(ctx.record&&ctx.record.id),record=ctx.record,actor=ctx.session&&ctx.session.userId;if(ctx.recordLoadDenied===true||!id||!record)throw new Error('采购换货单不存在或不可访问');if(!actor)throw new Error('无法识别当前提交人');if(record.status!=='draft'||record.processing_type!=='replacement')throw new Error('仅换货补货草稿可以提交');if(!record.expected_replenishment_on)throw new Error('预计补货日期不能为空');const lines=await ctx.api.object('forge_purchase_return_line').find({where:{return_id:id}});if(lines.length!==1||!(Number(lines[0].requested_quantity)>0))throw new Error('换货单必须有一条有效原订单物料');const now=new Date().toISOString();await ctx.api.object('forge_purchase_return').update({id,status:'pending_warehouse',finance_status:'not_required',replacement_status:'pending_return',submitted_by:actor,submitted_at:now});await ctx.api.object('forge_purchase_return_line').update({id:lines[0].id,status:'pending_review'});await ctx.api.object('forge_purchase_return_approval_log').insert({name:record.code+' 提交换货',event_key:record.code+'-SUBMIT',return_id:id,action:'submitted',from_status:'draft',to_status:'pending_warehouse',comment:record.remarks||'提交采购换货申请',occurred_at:now,operator_id:actor});return{id,status:'pending_warehouse',finance_status:'not_required'};`},
});

export const PurchaseReplacementRegisterArrival = defineAction({
  name:'purchase_replacement_register_arrival',label:'登记供应商补货',objectName:'forge_purchase_return',icon:'package-check',locations:[...locations],order:61,visible:`record.processing_type == 'replacement' && (record.status == 'pending_replenishment' || record.status == 'replenishment_arrived')`,refreshAfter:true,
  params:[{field:'code',objectOverride:'forge_purchase_receipt',required:true},{field:'arrived_on',objectOverride:'forge_purchase_receipt',required:true},{field:'quantity',objectOverride:'forge_purchase_receipt_line',required:true},{field:'batch_number',objectOverride:'forge_purchase_receipt_line'},{field:'external_sn',objectOverride:'forge_purchase_receipt_line'},{field:'carrier',objectOverride:'forge_purchase_receipt'},{field:'logistics_number',objectOverride:'forge_purchase_receipt'},{field:'remarks',objectOverride:'forge_purchase_receipt'}],
  body:{language:'js',capabilities:['api.read','api.write'],source:`
const id=ctx.recordId||(ctx.record&&ctx.record.id),record=ctx.record,actor=ctx.session&&ctx.session.userId;if(ctx.recordLoadDenied===true||!id||!record)throw new Error('采购换货单不存在或不可访问');if(!actor)throw new Error('无法识别当前登记人');if(record.processing_type!=='replacement'||record.outbound_status!=='outbounded'||!['pending_replenishment','replenishment_arrived'].includes(record.status))throw new Error('仅已退货出库的换货单可以登记供应商补货');const lines=await ctx.api.object('forge_purchase_return_line').find({where:{return_id:id}});if(lines.length!==1)throw new Error('换货单必须且只能包含一条原订单物料');const line=lines[0],quantity=Number(ctx.input.quantity||0),round4=v=>Math.round((Number(v)+Number.EPSILON)*10000)/10000;if(!(quantity>0))throw new Error('补货到货数量必须大于0');const existingReceiptLines=await ctx.api.object('forge_purchase_receipt_line').find({where:{purchase_return_id:id}});let held=0;for(const receiptLine of existingReceiptLines){const inspections=await ctx.api.object('forge_purchase_inspection').find({where:{receipt_line_id:receiptLine.id}});if(!inspections.length||inspections.some(x=>x.status!=='completed'))held=round4(held+Number(receiptLine.quantity||0));}const remaining=round4(Number(line.outbounded_quantity||0)-Number(line.accepted_replenishment_quantity||0)-held);if(quantity>remaining)throw new Error('补货到货数量超过换货独立可到额度');const orderLine=await ctx.api.object('forge_purchase_order_line').findOne({where:{id:line.order_line_id}}),now=new Date().toISOString(),taxedAmount=round4(quantity*Number(line.taxed_unit_price||0)),untaxedAmount=round4(quantity*Number(orderLine&&orderLine.untaxed_unit_price||0)),noticeCreated=await ctx.api.object('forge_purchase_arrival_notice').insert({name:ctx.input.code+' 换货补货通知',code:ctx.input.code+'-NOTICE',order_id:record.order_id,order_line_id:line.order_line_id,sku_id:line.sku_id,item_code:line.item_code||null,notice_type:'purchase_replacement',purchase_return_id:id,supplier_id:record.supplier_id,warehouse_id:line.warehouse_id,expected_arrival_on:record.expected_replenishment_on,line_count:1,planned_quantity:quantity,arrived_quantity:quantity,status:'arrived',responsible_id:record.responsible_id,remarks:'由换货单 '+record.code+' 的独立补货额度生成'}),noticeId=typeof noticeCreated==='string'?noticeCreated:noticeCreated&&(noticeCreated.id||(noticeCreated.record&&noticeCreated.record.id));if(!noticeId)throw new Error('换货补货通知创建后未返回ID');const noticeLineCreated=await ctx.api.object('forge_purchase_arrival_notice_line').insert({name:line.name,notice_id:noticeId,purchase_return_id:id,purchase_return_line_id:line.id,order_id:record.order_id,order_line_id:line.order_line_id,sku_id:line.sku_id,item_code:line.item_code,model:line.model,specification:line.specification,unit_name:line.unit_name,planned_quantity:quantity,arrived_quantity:quantity,status:'arrived'}),noticeLineId=typeof noticeLineCreated==='string'?noticeLineCreated:noticeLineCreated&&(noticeLineCreated.id||(noticeLineCreated.record&&noticeLineCreated.record.id));if(!noticeLineId)throw new Error('换货补货通知明细创建后未返回ID');const receiptCreated=await ctx.api.object('forge_purchase_receipt').insert({name:ctx.input.code+' 换货补货到货',code:ctx.input.code,notice_id:noticeId,order_id:record.order_id,order_line_id:line.order_line_id,sku_id:line.sku_id,item_code:line.item_code||null,quantity,batch_number:ctx.input.batch_number||null,taxed_unit_price:Number(line.taxed_unit_price||0),supplier_id:record.supplier_id,purchase_return_id:id,customer_id:null,warehouse_id:line.warehouse_id,arrival_type:'supplier_replacement',arrived_on:ctx.input.arrived_on,contact_name:record.contact_name||null,contact_phone:record.contact_phone||null,carrier:ctx.input.carrier||null,logistics_number:ctx.input.logistics_number||null,line_count:1,total_quantity:quantity,untaxed_amount:untaxedAmount,taxed_amount:taxedAmount,status:'pending_inspection',submitted_at:now,submitted_by:actor,responsible_id:record.responsible_id,remarks:ctx.input.remarks||('供应商为 '+record.code+' 等量补货')}),receiptId=typeof receiptCreated==='string'?receiptCreated:receiptCreated&&(receiptCreated.id||(receiptCreated.record&&receiptCreated.record.id));if(!receiptId)throw new Error('换货补货到货单创建后未返回ID');const receiptLineCreated=await ctx.api.object('forge_purchase_receipt_line').insert({name:line.name,receipt_id:receiptId,notice_id:noticeId,notice_line_id:noticeLineId,purchase_return_id:id,purchase_return_line_id:line.id,order_id:record.order_id,order_line_id:line.order_line_id,sku_id:line.sku_id,item_code:line.item_code,model:line.model,specification:line.specification,unit_name:line.unit_name,quantity,warehouse_id:line.warehouse_id,warehouse_location:null,external_sn:ctx.input.external_sn||null,batch_number:ctx.input.batch_number||null,taxed_unit_price:Number(line.taxed_unit_price||0),untaxed_unit_price:Number(orderLine&&orderLine.untaxed_unit_price||0),tax_rate:Number(orderLine&&orderLine.tax_rate||0),untaxed_amount:untaxedAmount,taxed_amount:taxedAmount,status:'pending_inspection',remarks:ctx.input.remarks||null}),receiptLineId=typeof receiptLineCreated==='string'?receiptLineCreated:receiptLineCreated&&(receiptLineCreated.id||(receiptLineCreated.record&&receiptLineCreated.record.id));if(!receiptLineId)throw new Error('换货补货到货明细创建后未返回ID');const allPending=await ctx.api.object('forge_pending_inspection').find({where:{}}),year=String(ctx.input.arrived_on||'').slice(0,4),pendingCode='PIN-REP-'+year+'-'+String(allPending.length+1).padStart(4,'0'),pendingCreated=await ctx.api.object('forge_pending_inspection').insert({name:line.name,code:pendingCode,receipt_id:receiptId,receipt_line_id:receiptLineId,purchase_return_id:id,purchase_return_line_id:line.id,order_id:record.order_id,order_line_id:line.order_line_id,supplier_id:record.supplier_id,customer_id:null,warehouse_id:line.warehouse_id,sku_id:line.sku_id,item_code:line.item_code,model:line.model,specification:line.specification,unit_name:line.unit_name,arrival_quantity:quantity,batch_number:ctx.input.batch_number||null,external_sn:ctx.input.external_sn||null,arrived_on:ctx.input.arrived_on,status:'pending',responsible_id:actor,remarks:'由换货补货到货 '+ctx.input.code+' 生成'}),pendingId=typeof pendingCreated==='string'?pendingCreated:pendingCreated&&(pendingCreated.id||(pendingCreated.record&&pendingCreated.record.id));if(!pendingId)throw new Error('换货补货待检记录创建后未返回ID');const arrived=round4(Number(record.replenishment_arrived_quantity||0)+quantity);await ctx.api.object('forge_purchase_return').update({id,status:'replenishment_arrived',replacement_status:'arrived',replenishment_receipt_id:receiptId,replenishment_arrived_quantity:arrived});await ctx.api.object('forge_purchase_return_approval_log').insert({name:record.code+' 补货到货',event_key:record.code+'-ARRIVAL-'+receiptId,return_id:id,action:'replacement_arrived',from_status:record.status,to_status:'replenishment_arrived',comment:ctx.input.remarks||'供应商补货已到并进入待检',occurred_at:now,operator_id:actor});return{id:receiptId,line_id:receiptLineId,pending_inspection_id:pendingId,return_id:id,status:'pending_inspection',quantity,remaining_arrival:round4(remaining-quantity)};
`},
});

export const PurchaseReplacementInspect = defineAction({
  name:'purchase_replacement_inspect',label:'完成补货检验',objectName:'forge_purchase_return',icon:'clipboard-check',locations:[...locations],order:62,visible:`record.processing_type == 'replacement' && record.status == 'replenishment_arrived'`,refreshAfter:true,
  params:[{field:'pending_inspection_id',objectOverride:'forge_purchase_inspection',required:true},{field:'inspected_on',objectOverride:'forge_purchase_inspection',required:true},{field:'accepted_quantity',objectOverride:'forge_purchase_inspection',required:true},{field:'inspection_method',objectOverride:'forge_purchase_inspection',required:true},{field:'inspection_note',objectOverride:'forge_purchase_inspection',required:true}],
  body:{language:'js',capabilities:['api.read','api.write'],source:`
const id=ctx.recordId||(ctx.record&&ctx.record.id),record=ctx.record,actor=ctx.session&&ctx.session.userId,note=String(ctx.input.inspection_note||'').trim();if(ctx.recordLoadDenied===true||!id||!record)throw new Error('采购换货单不存在或不可访问');if(!actor)throw new Error('无法识别当前检验人');if(record.processing_type!=='replacement'||record.status!=='replenishment_arrived')throw new Error('仅补货已到的换货单可以检验');if(!note)throw new Error('检验结论不能为空');if(!['full','sampling'].includes(ctx.input.inspection_method))throw new Error('检验方式必须为全检或抽检');const pending=await ctx.api.object('forge_pending_inspection').findOne({where:{id:ctx.input.pending_inspection_id}});if(!pending||pending.purchase_return_id!==id||pending.status!=='pending')throw new Error('待检记录不属于当前换货单或状态已变化');const accepted=Number(ctx.input.accepted_quantity),total=Number(pending.arrival_quantity||0);if(accepted<0||accepted>total)throw new Error('合格数量必须在0和到货数量之间');const rejected=Math.round((total-accepted+Number.EPSILON)*10000)/10000,result=accepted===total?'passed':accepted===0?'rejected':'partial',all=await ctx.api.object('forge_purchase_inspection').find({where:{}}),year=String(ctx.input.inspected_on||'').slice(0,4),code='IQC-REP-'+year+'-'+String(all.length+1).padStart(4,'0'),now=new Date().toISOString(),created=await ctx.api.object('forge_purchase_inspection').insert({name:pending.name,code,receipt_id:pending.receipt_id,receipt_line_id:pending.receipt_line_id,pending_inspection_id:pending.id,purchase_return_id:id,purchase_return_line_id:pending.purchase_return_line_id,order_id:pending.order_id,order_line_id:pending.order_line_id,supplier_id:pending.supplier_id,warehouse_id:pending.warehouse_id,sku_id:pending.sku_id,item_code:pending.item_code,model:pending.model,specification:pending.specification,unit_name:pending.unit_name,batch_number:pending.batch_number||null,inspection_method:ctx.input.inspection_method,total_quantity:total,accepted_quantity:accepted,rejected_quantity:rejected,inspected_on:ctx.input.inspected_on,result,status:'completed',inspector_id:actor,inspection_note:note,remarks:'采购换货补货检验'}),inspectionId=typeof created==='string'?created:created&&(created.id||(created.record&&created.record.id));if(!inspectionId)throw new Error('换货补货检验单创建后未返回ID');await ctx.api.object('forge_pending_inspection').update({id:pending.id,status:'inspected',inspection_id:inspectionId});await ctx.api.object('forge_purchase_receipt_line').update({id:pending.receipt_line_id,status:'inspected'});await ctx.api.object('forge_purchase_receipt').update({id:pending.receipt_id,status:'inspected'});const line=await ctx.api.object('forge_purchase_return_line').findOne({where:{id:pending.purchase_return_line_id}}),round4=v=>Math.round((Number(v)+Number.EPSILON)*10000)/10000,lineAccepted=round4(Number(line.accepted_replenishment_quantity||0)+accepted),totalAccepted=round4(Number(record.replenishment_accepted_quantity||0)+accepted),complete=totalAccepted>=Number(record.total_quantity||0),nextStatus=complete?'replenishment_inspected':'pending_replenishment',replacementStatus=complete?'inspected':'pending_replenishment';await ctx.api.object('forge_purchase_return_line').update({id:line.id,accepted_replenishment_quantity:lineAccepted});await ctx.api.object('forge_purchase_return').update({id,status:nextStatus,replacement_status:replacementStatus,replenishment_inspection_id:inspectionId,replenishment_accepted_quantity:totalAccepted});await ctx.api.object('forge_purchase_return_approval_log').insert({name:record.code+' 补货检验',event_key:record.code+'-INSPECT-'+inspectionId,return_id:id,action:'replacement_inspected',from_status:'replenishment_arrived',to_status:nextStatus,comment:note,occurred_at:now,operator_id:actor});return{id:inspectionId,return_id:id,status:'completed',inspection_result:result,total_quantity:total,accepted_quantity:accepted,rejected_quantity:rejected,replacement_status:nextStatus,remaining_accepted:round4(Number(record.total_quantity||0)-totalAccepted)};
`},
});

export const PurchaseReplacementStock = defineAction({
  name:'purchase_replacement_stock',label:'完成补货入库',objectName:'forge_purchase_return',icon:'package-plus',locations:[...locations],order:63,visible:`record.processing_type == 'replacement' && record.status == 'replenishment_inspected'`,refreshAfter:true,
  params:[{field:'code',objectOverride:'forge_purchase_inbound',required:true},{field:'inbound_on',objectOverride:'forge_purchase_inbound',required:true},{field:'approval_note',objectOverride:'forge_purchase_inbound',required:true}],
  body:{language:'js',capabilities:['api.read','api.write'],source:`
const id=ctx.recordId||(ctx.record&&ctx.record.id),record=ctx.record,actor=ctx.session&&ctx.session.userId,note=String(ctx.input.approval_note||'').trim();
if(ctx.recordLoadDenied===true||!id||!record)throw new Error('采购换货单不存在或不可访问');
if(!actor)throw new Error('无法识别当前入库人');
if(record.processing_type!=='replacement'||record.status!=='replenishment_inspected')throw new Error('仅补货检验全部合格的换货单可以入库');
if(!note)throw new Error('入库意见不能为空');
const returnLines=await ctx.api.object('forge_purchase_return_line').find({where:{return_id:id}});
if(returnLines.length!==1)throw new Error('换货单必须且只能包含一条原订单物料');
const returnLine=returnLines[0],inspections=await ctx.api.object('forge_purchase_inspection').find({where:{purchase_return_id:id}}),accepted=inspections.filter(x=>x.status==='completed').reduce((sum,x)=>sum+Number(x.accepted_quantity||0),0),round4=v=>Math.round((Number(v)+Number.EPSILON)*10000)/10000;
if(round4(accepted)!==round4(Number(returnLine.outbounded_quantity||0)))throw new Error('补货检验合格数量必须与退货出库数量完全一致');
const existing=await ctx.api.object('forge_purchase_inbound').find({where:{purchase_return_id:id}});
if(existing.length)throw new Error('当前换货单已经生成补货入库单');
const primaryInspection=inspections.find(x=>Number(x.accepted_quantity||0)>0);
if(!primaryInspection)throw new Error('换货补货没有可入库的合格检验记录');
const receiptLine=await ctx.api.object('forge_purchase_receipt_line').findOne({where:{id:primaryInspection.receipt_line_id}}),balanceKey=returnLine.warehouse_id+':'+returnLine.sku_id,balances=await ctx.api.object('forge_inventory_balance').find({where:{balance_key:balanceKey}});
if(balances.length!==1)throw new Error('换货物料库存余额不存在或重复');
const now=new Date().toISOString(),unitCost=Number(returnLine.outbound_unit_cost||0),amount=round4(accepted*unitCost),receiptIds=[...new Set(inspections.map(x=>x.receipt_id))],balance=balances[0],before=Number(balance.on_hand_quantity||0),reserved=Number(balance.reserved_quantity||0),beforeAvailable=Number(balance.available_quantity||0),beforeValue=Number(balance.inventory_value||0),after=round4(before+accepted),afterAvailable=round4(after-reserved),afterValue=round4(beforeValue+amount),average=after>0?round4(afterValue/after):0;
const created=await ctx.api.object('forge_purchase_inbound').insert({name:ctx.input.code+' 换货补货入库',code:ctx.input.code,inbound_type:'purchase',source_type:'purchase_replacement',order_id:record.order_id,inspection_id:primaryInspection.id,order_line_id:returnLine.order_line_id,sku_id:returnLine.sku_id,item_code:returnLine.item_code,batch_number:primaryInspection.batch_number||null,quantity:accepted,unit_cost:unitCost,inventory_amount:amount,before_on_hand:before,after_on_hand:after,purchase_return_id:id,receipt_id:receiptIds.length===1?receiptIds[0]:null,supplier_id:record.supplier_id,warehouse_id:returnLine.warehouse_id,inbound_on:ctx.input.inbound_on,line_count:1,total_quantity:accepted,untaxed_amount:amount,taxed_amount:amount,status:'stocked',submitted_at:now,submitted_by:actor,approved_at:now,approved_by:actor,stocked_at:now,stocked_by:actor,approval_note:note,responsible_id:record.responsible_id,remarks:'采购换货补货检验后入库'}),inboundId=typeof created==='string'?created:created&&(created.id||(created.record&&created.record.id));
if(!inboundId)throw new Error('换货补货入库单创建后未返回ID');
const lineCreated=await ctx.api.object('forge_purchase_inbound_line').insert({name:returnLine.name,inbound_id:inboundId,inspection_id:primaryInspection.id,receipt_id:receiptIds.length===1?receiptIds[0]:primaryInspection.receipt_id,receipt_line_id:primaryInspection.receipt_line_id,purchase_return_id:id,purchase_return_line_id:returnLine.id,order_id:record.order_id,order_line_id:returnLine.order_line_id,supplier_id:record.supplier_id,warehouse_id:returnLine.warehouse_id,warehouse_location:receiptLine&&receiptLine.warehouse_location||null,sku_id:returnLine.sku_id,item_code:returnLine.item_code,model:returnLine.model,specification:returnLine.specification,unit_name:returnLine.unit_name,batch_number:primaryInspection.batch_number||null,external_sn:receiptLine&&receiptLine.external_sn||null,quantity:accepted,taxed_unit_price:unitCost,untaxed_unit_price:unitCost,tax_rate:0,untaxed_amount:amount,taxed_amount:amount,before_on_hand:before,after_on_hand:after,status:'stocked',remarks:'采购换货等量补货入库'}),inboundLineId=typeof lineCreated==='string'?lineCreated:lineCreated&&(lineCreated.id||(lineCreated.record&&lineCreated.record.id));
if(!inboundLineId)throw new Error('换货补货入库明细创建后未返回ID');
await ctx.api.object('forge_inventory_balance').update({id:balance.id,on_hand_quantity:after,reserved_quantity:reserved,available_quantity:afterAvailable,average_cost:average,inventory_value:afterValue,last_movement_at:now});
await ctx.api.object('forge_inventory_ledger').insert({name:record.code+' '+returnLine.name+' 换货补货入库',code:record.code+'-REPL-IN',warehouse_id:returnLine.warehouse_id,sku_id:returnLine.sku_id,direction:'inbound',movement_type:'purchase_replacement_inbound',quantity:accepted,before_on_hand:before,after_on_hand:after,before_available:beforeAvailable,after_available:afterAvailable,unit_cost:unitCost,amount,occurred_at:now,source_object:'forge_purchase_inbound',source_id:inboundId,source_line_id:inboundLineId,responsible_id:actor,remarks:note});
for(const inspection of inspections){await ctx.api.object('forge_purchase_receipt_line').update({id:inspection.receipt_line_id,status:'stocked'});await ctx.api.object('forge_purchase_receipt').update({id:inspection.receipt_id,status:'stocked'});}
await ctx.api.object('forge_purchase_return_line').update({id:returnLine.id,replenished_quantity:accepted,status:'replenished'});
const orderLine=await ctx.api.object('forge_purchase_order_line').findOne({where:{id:returnLine.order_line_id}}),order=await ctx.api.object('forge_purchase_order').findOne({where:{id:record.order_id}});
await ctx.api.object('forge_purchase_order_line').update({id:orderLine.id,replenished_quantity:round4(Number(orderLine.replenished_quantity||0)+accepted)});
await ctx.api.object('forge_purchase_order').update({id:order.id,replenished_quantity:round4(Number(order.replenished_quantity||0)+accepted)});
await ctx.api.object('forge_purchase_return').update({id,status:'completed',replacement_status:'stocked',replenishment_inbound_id:inboundId,replenished_quantity:accepted});
await ctx.api.object('forge_purchase_inbound_approval_log').insert({name:ctx.input.code+' 换货补货入库',inbound_id:inboundId,action:'stocked',from_status:'replenishment_inspected',to_status:'stocked',comment:note,occurred_at:now,operator_id:actor});
await ctx.api.object('forge_purchase_return_approval_log').insert({name:record.code+' 补货入库',event_key:record.code+'-STOCK',return_id:id,action:'replacement_stocked',from_status:'replenishment_inspected',to_status:'completed',comment:note,occurred_at:now,operator_id:actor});
return{id:inboundId,line_id:inboundLineId,return_id:id,status:'completed',quantity:accepted,inventory_amount:amount,before_on_hand:before,after_on_hand:after};
`},
});
