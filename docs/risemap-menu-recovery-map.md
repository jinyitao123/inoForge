# RISEMAP 菜单复刻挽回台账

更新于 2026-09-12。基线为 Forge `main` 提交 `7ba6d14` 与已保存的 RISEMAP 181 个菜单入口清单。本轮只整理 Forge 主导航与信息架构，不删除已有对象、Action、页面或数据，不修改线上 RISEMAP 数据，也不把入口可见写成业务闭环通过。当前主线冻结审计见 `docs/forge-main-product-audit-20260912.md`。

## 事实

- RISEMAP 当前保存证据中的一级菜单顺序为：工作台、供应链、销售、生产、项目、行政、财务、报表、系统。
- 九类菜单分别包含 4、41、19、29、5、32、18、11、22 个入口，完整编号见 `docs/risemap-feature-inventory.md`。
- Forge 已有采购、库存、销售、生产、委外、项目和财务的部分对象、Action 与专用页面；这些能力此前按 OTC 阶段组织，现已按 RISEMAP 菜单重新归位，但仍需按页面和岗位任务逐项审查。
- 当前委外已形成订单、发料、供应商签收、回厂、良品入库、NCR、委外对账和应付生成的受控样板；委外退料、供应商外部确认、部分结算和岗位待办仍缺或待 RISEMAP 复核。

## 决策

1. Forge 一级菜单及顺序固定对齐 RISEMAP 九类入口。
2. 二级分组优先使用 RISEMAP 已观察名称；只有存在真实可达页面或对象时才挂三级入口。
3. 现有底层对象、Action、库存与财务台账全部保留。明细、日志、审批记录等内部对象从常规导航收回，后续放入主单据详情页。
4. OTC 九阶段保留为跨菜单任务旅程，不再承担主导航。
5. 每个功能以后独立经历页面合同、岗位任务、状态动作、上下游、异常阻断、同库重启和浏览器验收，不按对象数量计算进度。

## 一级菜单映射

| 一级菜单 | RISEMAP 分组 | Forge 本轮可见入口 | 状态与缺口 |
| --- | --- | --- | --- |
| 工作台 | 工作台 | 复刻状态页 | RM-001 至 RM-004 未实现；状态页只用于保留菜单结构，不冒充待办与工作台 |
| 供应链 | 到货检验、基础资料、采购管理、入库管理、库存管理、出库管理 | 到货通知/登记、待检验、检验单、物料、BOM、供应商、仓库、采购订单/退换货、采购/期初入库、库存总览/流水、销售出库 | 可复用能力已归位；检验规则、采购申请/待办/询价、通用 NCR、盘点调拨、生产及其他出入库等仍缺 |
| 销售 | 销售业务、CRM客户管理 | 销售合同、订单、发货、客户、联系人、报价 | 页面与动作需逐项按 RM-046 至 RM-064 重构；退货、价格、目标、CRM 过程和销售发票入口仍缺 |
| 生产 | 组装业务管理、图纸管理、委外管理 | 组装及领补退料、拆解/换件、图纸工作台、委外看板、委外订单、发料、回厂、NCR、供应商、库存、未交、批次追溯、进货 | 委外看板与委外订单已拆为两个导航入口并复用同一执行底座；委外库存、未交、批次追溯和进货已恢复为独立业务入口；委外退料仍缺，未交、追溯和进货真实样本待补证 |
| 项目 | 项目管理 | 项目中心、任务、工时、交付验收、费用与成本 | 现有页面可复用但未证明与 RM-094 至 RM-098 页面合同一致；项目分析与配置入口待补 |
| 行政 | 审批中心、行政管理、人力资源、考勤假期、流程中心 | 复刻状态页 | RM-099 至 RM-130 全部保持缺口；状态页只用于保留菜单结构，不用系统审批日志冒充行政产品 |
| 财务 | 资金管理、业务确认、发票管理 | 资金流水、期初往来与对冲、收付款、退款、收入确认、财务冲销、开票与结算、往来及银行对账 | 均为 Forge 局部能力；资金账户、完整应收应付、成本/费用/报销、发票总览及进销项发票等仍缺 |
| 报表 | 报表、财务统计 | 项目经营分析 | 不是 RISEMAP“项目统计”的等价页；RM-149 至 RM-159 均需独立页面合同和口径验证 |
| 系统 | 系统设置及业务配置 | 物料、客户、采购销售、库存、项目的部分字典 | 账号角色、部门、会话、审计、其他业务配置、打印、插件、字段等仍缺 |

## 假设

- ObjectStack Console 的区域、分组和标准页面足以承载 RISEMAP 的一级、二级导航语义；若实际浏览器验证发现空区域不可见或页面上下文不足，再扩展平台元数据或共享组件。
- 同一业务能力在主菜单只保留一个权威入口，跨业务查阅通过主单据关联和上下游跳转完成，而不是复制多个语义不同的菜单项。
- RISEMAP 现有试用空间的菜单结构可作为当前复刻基线；厂商版本差异仍需后续复核。

## 未执行实验

- 尚未逐个点击本轮全部入口核对路由、空状态、权限和返回路径。
- 尚未验证角色权限变化后九类菜单的隐藏规则。
- 尚未与 RISEMAP 当前线上页面重新并排截图；本轮使用的是已保存证据。

## 本轮验证

- `pnpm typecheck`、`pnpm validate`、`pnpm build` 均通过；当前验证统计为 154 个对象、44 个页面和 191 个 Action。该统计只作工程盘点，不作为产品完成度。
- 在端口 `4398` 使用独立 `.objectstack/menu-ia-20260912.sqlite` 启动 Console，登录后侧栏按顺序显示九类一级菜单。
- 首次浏览器检查发现空区域会被 Console 隐藏；加入明确标注“待复刻”的状态页后，完整停服并从同一 SQLite 重启，工作台和行政均可见。
- 浏览器已展开生产下的组装业务管理、图纸管理、委外管理；委外看板与委外订单分别可加载，订单页列表职责独立可见。
- 本轮只证明菜单结构、入口路由和状态页承载，不证明任何业务功能新增或业务闭环。

## 逐功能修补队列

首个业务修补单元为“生产 / 委外管理”，按页面和岗位连续任务推进，不再并行堆孤立对象：

1. 委外看板与订单上下文，明确来源、当前状态、结果、异常和下一步；两个入口已在导航层拆开。
2. 委外发料与供应商签收，补批次来源、岗位阻断和状态反馈。
3. 委外回厂、良品入库和不良处置，统一物理、质量和订单状态。
4. 委外退料、对账单、应付生成和付款关联。
5. 同一 SQLite 全链停服重启、真实浏览器操作及独立业务验收；当前已证明回厂批次到应付的页面闭环，但订单整体仍可能因计划数量未完成而保持进行中。

在第 5 项通过前，不把委外称为业务闭环，也不自动迁移到采购、生产、销售或财务的其他功能。
