# 委外样板独立验收审查

审查日期：2026-09-12  
审查基线：Forge `main`，提交 `ae6b5d4`
验收范围：`SC-ORDER-001` 的一条回厂批次，从异常处置到委外对账和应付生成。  
验收原则：真实页面操作、真实来源关系、同一 SQLite、完整停服重启；健康检查、构建和孤立 API 只作为辅助证据。

## 判定

**当前样板在“回厂批次 → 不良处置 → 对账 → 应付”的范围内条件通过。**

条件是：该结论只覆盖已实际办理的批次和动作，不代表整张订单完成，也不代表 RISEMAP 尚未成功办理的行为已经复刻。样板可以作为后续 RISEMAP 复核和委外剩余功能修补的基线，但暂不推广到采购、生产其他功能、销售或财务其他功能。

## 门禁结果

| 门禁 | 证据 | 结果 | 不能外推 |
| --- | --- | --- | --- |
| 岗位连续任务 | 内置浏览器从订单上下文进入 NCR，完成方案、审批、执行，再进入委外对账 | 通过 | 尚未证明真实多账号权限和待办分派 |
| 关键阻断 | 未完成 NCR 时订单显示“先完成不良处置”，对账入口被阻断；NCR 执行后解锁 | 通过 | 未覆盖拒收、复检和部分质量处置 |
| 来源关系 | 页面持续显示回厂单 `SR-API-20260912-001`、订单 `SC-ORDER-001`、NCR 和对账来源行 | 通过 | 未证明多回厂、多订单复杂合并 |
| 正常结算 | 生成 `REC-UI-20260912-001`，确认锁定，生成 `AP-REC-UI-20260912-001` | 通过 | 供应商外部确认和部分结算待复核 |
| 退料交接 | 发料签收后的甲供料订单登记 `RET-CHAIN-20260912-001`，确认生成 `RIN-RET-CHAIN-20260912-001`，仓库入库后余额由 6 降为 5 | 通过 | 仅证明 Forge 本地交接；RISEMAP 写入、外部确认和赔偿扣款关系待复核 |
| 金额口径 | 页面显示加工费 `¥71.00`、补料 `¥0.00`、扣款 `¥0.00`、应付 `¥71.00` | 通过 | 补料、退料、赔偿、税额和差异分配待复核 |
| 持久化 | 同一 SQLite 的 API 和停服重启回读脚本通过；页面在重启后回读对账结果 | 通过 | 顺序写入尚未升级为事务或补偿保证 |
| 幂等性 | 重复生成应付已有 API 验收，页面首次生成后显示唯一应付 | 通过 | 并发竞争和失败中断恢复待补 |
| 页面结构 | RM-082 看板、RM-084 发料、RM-089 委外库存、RM-087 待对账池、RM-093 对账单结果页均有独立入口；看板及发料状态指标可进入或筛选对应岗位页，库存页可切换供应商/物料视角、按账龄筛选并去发料 | 通过 | 像素级视觉差异不作为阻断；RISEMAP 写入动作与库存账龄起算口径仍待复核 |
| 独立材料 | 委外链使用独立 SQLite、独立端口和独立 worktree 验收材料，未复用线上 RISEMAP 数据 | 通过 | 尚未取得业务负责人签字确认 |

## 实际办理记录

1. 订单上下文显示 `SC-ORDER-001`、供应商“锐联钣金委外厂”、发料 `6 / 20`、回厂 `2 / 10`。
2. 未完成 NCR 时，对账行显示“先完成不良处置”，没有错误的对账入口。
3. NCR `NCR-SR-API-20260912-001-01` 配置为退货 1 件，提交审批、审核通过并执行。
4. 订单上下文回读 NCR“已执行”，对账入口恢复。
5. 对账池选择 `SR-API-20260912-001`，填写账期 `2026-09-01 至 2026-09-12`，生成 `REC-UI-20260912-001`。
6. 对账单确认锁定后状态为“已确认”，累计确认金额为 `¥71.00`。
7. 生成应付后页面显示 `AP-REC-UI-20260912-001`，状态为“已生成应付”。
8. 最终订单上下文显示对账来源、金额和应付单号；订单本身仍为“进行中”，下一步为“待发料”，因为计划履约量尚未完成。
9. 在主线端口 `4443` 的内置浏览器打开“委外退料”，选择 `SC-ORDER-001`，填写 `RET-BROWSER-20260912-001`、数量 `1` 和退料原因，保存为草稿；页面显示草稿记录并提示“退料草稿已保存”。
10. 点击“确认退料”并确认弹窗；页面显示待入库 KPI 为 `1`，生成 `RIN-RET-BROWSER-20260912-001`，且弹窗明确确认退料阶段不扣减库存。
11. 点击“确认入库”并确认弹窗；页面显示待入库 KPI 清零、已入库 KPI 为 `1`，退料结果为“已入库”，并提示委外库存已回写。完整停服后从同一 SQLite 重启，再运行 `pnpm acceptance:subcontract-return-browser-readback`，回读退料单、待入库单、明细、委外库存和退料流水均一致。
12. 在当前 `main` 上用独立端口 `4450` 和全新 SQLite `/tmp/forge-subcontract-main-rerun-20260912-v2.sqlite` 重跑 `pnpm acceptance:subcontract-one-db-chain`，连续阶段 `order → issue → supplier_signoff → receipt → inbound_confirm → ncr_disposition → reconciliation → payable` 全部通过；随后完整停服、同库重启，运行 `pnpm acceptance:subcontract-one-db-restart` 按原单据 ID 回读通过。该次复跑只更新 Forge 本地证据，不改变 RISEMAP 数据。
13. 在分支 `codex/subcontract-signoff-context` 的独立端口 `4453` 和独立 SQLite 上构造真实本地材料 `SC-ORDER-001 / SI-SIGNOFF-CONTEXT-001`，订单上下文显示“当前下一步：待供应商签收”，发料行显示“等待供应商签收”，入口携带具体 `issue_id` 进入签收列表并只显示该单据。
14. 使用内置浏览器打开“登记签收”对话框，填写“供应商已确认收货 6 张，包装完好”并提交；页面反馈“供应商签收已登记，未自动开始加工”。API 回读为发料单 `signed`、委外订单仍为 `in_progress` 且已发数量为 `6`；完整停服后使用同一 SQLite 重启，API 再次回读相同状态。
15. 在独立端口 `4454` 和独立 SQLite 上完成回厂验收样本 `SR-API-20260912-001`。订单上下文同时存在待入库和待处理 NCR 时，点击“确认良品入库”直接打开当前待入库确认对话框，而不是新建回厂表单；对话框显示回厂记录、订单、合格数量和待入库单。
16. 内置浏览器确认入库后，页面 KPI 从“待入库 1”变为“已入库 1”，回厂记录和关联入库单均显示已入库。API 回读为回厂单和入库单 `stocked`、订单累计回厂 `3`、材料倒冲累计 `6`，同一回厂来源的 NCR 仍为 `draft`；停服后使用同一 SQLite 重启，以上状态按原 ID 回读一致。
17. 在独立端口 `4455` 和独立 SQLite 上生成回厂不良样本 `NCR-SR-API-20260912-001-01`。从 `SC-ORDER-001` 订单上下文点击“进入不合格处理”后，质量页按 `order_id` 仅显示该订单的 1 张 NCR，状态为“待提交”，可继续设置处置方案。
18. 在独立端口 `4457` 的退料样本上追加 `RET-CONTEXT-20260912-001` 并只确认到待入库状态；内置浏览器打开 `page_subcontract_return_workspace?order_id=9qt7f-LnrgHaGK-Q` 后，页面仅显示 `SC-ORDER-001` 的退料记录，待入库 KPI 为 `1`，自动打开“确认退料入库”对话框。退料服务端链此前已通过同类材料完成入库后的库存余额、累计退料和 `material_return` 流水验证。

## RISEMAP 对照边界

已使用的 RISEMAP 事实材料：

- RM-087 委外对账入口、待对账池、供应商/订单/回厂批次聚合、状态和阻断呈现。
- RM-093 委外对账单的账期、费用构成、应付、关联应付、来源行和状态字段。
- 应付金额首版按“加工费 + 补料 − 扣款”呈现。

仍需 RISEMAP 复核：供应商确认、内部审核与供应商确认的分离关系、部分结算/重开/拆分、差异关闭、赔偿扣款来源、付款关联和订单关闭条件。以上行为在 Forge 中只能称为工程实现或待复核，不能称为 RISEMAP 已复刻。

## RM-086 三方互审增补

产品审查：RM-086 是“委外退料”独立岗位入口，不应直接暴露库存余额对象；页面必须显示关联订单、供应商、退料原因、退料结果和下一步。确认退料与仓库入库是两个业务节点，不能合并成一次立即扣账。

工程审查：复用 `forge_subcontract_stock_balance` 和 `forge_subcontract_stock_ledger` 作为执行底座，新增退料头、明细、待入库单和日志保持来源可追溯；退料仅允许甲供料订单，数量按当前在外余量校验。当前顺序写入未升级为事务或补偿，供应商外部账号确认未实现。

质量审查：已覆盖草稿不动库存、超量阻断、确认后待入库、入库后余额/累计/流水、同一 SQLite 停服重启回读，以及内置浏览器列表和新建表单。尚未覆盖 RISEMAP 线上成功写入、真实多角色账号和赔偿扣款联动。

三方结论：**RM-086 可作为 Forge 受控本地样板纳入委外基线，但不能宣称 RISEMAP 行为已完全复刻，也不能单独触发向其他业务域推广。**

## RM-0481 供应商签收上下文修补

产品审查：已发出但尚未签收的发料单是委外订单的当前岗位任务，订单上下文必须把它排在回厂验收之前，并显示当前单据、状态、结果和下一步。入口不能只带订单而重新打开“新建发料单”，否则用户会丢失当前办理对象。

工程审查：复用现有 `subcontract_issue_sign` Action 和 `issued → signed` 状态；订单上下文增加 `issued` 派生阻断与具体 `issue_id` 跳转，发料页按该 ID 聚焦当前发料单。没有新增对象、库存写入或线上数据迁移。

质量审查：已覆盖订单上下文、具体发料单列表、签收对话框、真实浏览器提交、API 状态回读及同一 SQLite 停服重启回读。RISEMAP 当前线上签收写入、真实外部供应商账号和多角色权限仍未证明。

三方结论：**该修补通过本地产品连续性门禁，可合并到委外样板；供应商签收的 RISEMAP 对齐仍保持待复核，不得扩展为权限或跨域推广结论。**

## RM-0472 / RM-0481 回厂入库上下文修补

产品审查：订单上下文中的“确认良品入库”是仓库岗位对既有待入库单的办理，不应重新进入回厂登记。待入库和不良处置可以并行存在，确认良品入库不能隐式关闭 NCR。

工程审查：回厂页复用现有 `subcontract_inbound_confirm` Action；当 `order_id` 上下文存在 `pending_inbound` 回厂记录时，直接打开该记录的确认对话框，只有不存在待入库记录时才保留原有“登记回厂”建单逻辑。未新增对象、Action 或数据迁移。

质量审查：已覆盖回厂 API 正常/异常材料、订单上下文、入库确认对话框、内置浏览器提交、KPI 与列表结果、API 状态回读、同一 SQLite 停服重启回读，以及 NCR 未被误关闭的边界。RISEMAP 当前线上入库写入、混合批次、拒收/复检和真实多角色权限仍未证明。

三方结论：**该修补通过本地回厂到入库的岗位连续性门禁，可纳入委外样板；不良处置和 RISEMAP 行为复核仍保持独立待办。**

## RM-0474 质量任务上下文修补

产品审查：订单上下文中的“不良处置”是当前订单的质量岗位任务，进入后应保留订单范围和来源回厂单，不能把用户带到所有订单的混合 NCR 列表。

工程审查：复用 `forge_subcontract_ncr`、现有配置/提交/审核/执行 Action；质量页读取 `order_id` 上下文并在列表查询阶段过滤当前订单。未新增对象、Action 或数据迁移。

质量审查：已用真实本地回厂不良材料完成订单上下文跳转、当前订单 NCR 唯一显示和“设置方案”下一步验证；NCR 后续处置动作、同库重启及 RISEMAP 同材料写入仍按原验收记录保持独立边界。

三方结论：**该修补通过质量岗位入口连续性门禁，可合并到委外样板；四类处置语义、赔偿扣款和真实角色权限仍不得提前宣称完成。**

## RM-0475 对账岗位上下文修补

产品审查：订单上下文中的“对账结算”应继续围绕当前委外订单办理，进入后不能把用户丢回全量待对账池；当前订单的可结算批次、阻断原因和既有对账单必须保持同一来源链。

工程审查：复用现有对账池、阻断计算和对账确认/应付生成 Action；对账页读取 `order_id` 上下文，并按回厂明细关联订单过滤待对账批次、阻断批次和对账单。无新增对象、Action 或数据迁移；不带上下文的财务入口保持全量视图。

质量审查：已完成代码门禁；在同一 SQLite 的多订单材料上完成无上下文全量视图与订单上下文视图的内置浏览器对比，`SC-ORDER-001` 上下文只显示该订单的对账单和阻断批次，没有混入 `SC-ORDER-002`。停服重启后按原订单和对账单 ID 回读，两张对账单及各自来源行仍保持正确关联。RISEMAP 线上结算写入、供应商确认和差异/赔偿关系仍待复核。

三方结论：**对账上下文多订单隔离的 Forge 本地页面与持久化门禁通过；RISEMAP 线上结算写入、供应商确认和差异/赔偿关系仍保持待复核。**

## RM-0476 退料岗位上下文修补

产品审查：订单上下文中的“退料交接”要区分登记退料和仓库确认退料入库；仓库岗位进入时应直接看到当前订单已有的待入库单，不能回到全量退料列表后重新判断，也不能误打开新建退料表单。

工程审查：复用现有退料头、明细、待入库单和 `subcontract_return_inbound_confirm` Action；退料页读取 `order_id`，过滤当前订单记录，并在存在当前订单的待入库退料单时自动打开确认对话框。未新增对象、Action 或数据迁移；无上下文入口继续保留原列表行为。

质量审查：已完成代码门禁；在同一 SQLite 的混合材料上完成全量入口与订单上下文的内置浏览器对比。全量入口同时显示 `SC-ORDER-001` 的 `RET-MULTI-CURRENT-001` 待入库和 `SC-ORDER-003` 的 `RET-MULTI-OTHER-003` 草稿；进入 `SC-ORDER-001` 上下文后只显示当前退料单，并自动打开仓库确认入库对话框。停服重启后回读当前退料仍为 `pending_inbound`、待入库单仍为 `pending`，另一订单仍为 `draft`。

三方结论：**退料上下文多订单隔离、仓库下一步聚焦和同库持久化门禁通过；RISEMAP 线上退料写入、供应商确认和赔偿关系仍保持待复核。**

## RM-0477 质量处置完整浏览器链

产品审查：委外回厂不良进入质量岗位后，当前单据必须连续呈现来源回厂单、缺陷数量、处置方式、审批状态、执行结果和下一步。真实页面链路为“设置返工方案 → 提交审批 → 审核通过 → 执行处置”，执行完成后 NCR 进入“已执行”，来源回厂单进入“已解决”，不良不进入正式库存。

工程审查：本次没有新增对象或状态，复用 `subcontract_ncr_configure`、`subcontract_ncr_submit`、`subcontract_ncr_review` 和 `subcontract_ncr_execute`。验收断言修正为分别核对持久化的 `disposition_reason`、`execution_result` 和配置/执行日志，避免把方案说明与执行结果误当成同一字段。

质量审查：在独立端口 `4458` 的内置浏览器订单上下文中，使用真实生成的 `NCR-SR-NCR-BROWSER-001-01` 完成四步操作；页面从“待提交”变为“已执行”，待审批和待执行清零，已执行变为 5。随后停服，用同一 SQLite 重启并运行 `pnpm acceptance:subcontract-ncr-ui-readback`，回读 `executed / rework / 1`、来源回厂单 `ncr_resolved / 1`、5 条 NCR 操作记录，并分别验证方案说明与执行说明。此前一次失败是验收脚本字段断言错误，修正后回读通过。

三方结论：**Forge 本地质量岗位的 NCR 页面连续操作和同库重启持久化门禁通过；RISEMAP 当前线上写入、四类处置的逐项业务语义、真实多角色权限和多订单隔离仍待复核。**

## RM-0478 对账确认与应付生成浏览器链

产品审查：财务岗位从当前对账单继续办理，确认动作必须只作用于当前单据，并在结果区显示来源批次、金额、状态和下一步。真实页面对 `REC-MULTI-ORDER-004` 完成“确认锁定 → 生成应付”，另一订单的 `REC-MULTI-ORDER-004B` 保持待确认，未被批量推进。

工程审查：复用 `subcontract_reconciliation_confirm` 和 `subcontract_reconciliation_generate_payable`；确认按对账明细回写对应订单累计，生成应付保持幂等关联。未新增对象、Action 或迁移。

质量审查：无上下文全量页面显示两张不同订单对账单；内置浏览器完成当前对账单确认和应付生成，页面显示 `AP-REC-MULTI-ORDER-004`、`¥71.00` 和“已生成应付”。停服后使用同一 SQLite 重启，回读当前对账单为 `payable_generated`、应付金额为 `71`、来源行 1 条，另一张对账单仍为 `pending_confirmation`。

三方结论：**Forge 本地对账确认、应付生成、当前单据隔离和同库持久化门禁通过；RISEMAP 供应商确认关系、付款衔接、差异/赔偿和真实权限仍待复核。**

## 推广判定

| 目标域 | 当前结论 |
| --- | --- |
| 委外剩余功能 | 可以继续按同一门禁修补，优先补退料、供应商确认、差异和权限边界 |
| 采购 | 暂不推广；需独立菜单、岗位任务和真实采购闭环验收 |
| 生产 | 暂不推广；委外样板通过不等于组装、图纸或生产入库通过 |
| 销售 | 暂不推广；销售链有独立残余缺口和 RISEMAP 待复核行为 |
| 财务 | 暂不推广；委外应付只证明来源生成，不证明完整资金、发票和付款闭环 |

下一次实施必须从当前 `main` 重新建立独立分支和 SQLite，并在编码前更新产品、工程、质量三方待决策项。若新动作无法保持当前单据上下文、来源可追溯、异常阻断和重启回读，停止扩展并先修正基础语义。
