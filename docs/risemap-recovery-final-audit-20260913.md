# RISEMAP 产品结构复刻挽回最终审查

审查日期：2026-09-12
Forge 基线：`main`，提交 `d1cfd61`
审查范围：菜单与信息架构、当前主线盘点、委外样板的产品/工程/质量门禁及推广边界。

## 最终判定

当前结果是：**菜单与信息架构已按 RISEMAP 证据完成第一轮重整；委外订单已新增一条从空白页面建单到审核通过的浏览器子链，委外样板仍达到 Forge 本地受控样板门槛，但尚未达到 RISEMAP 等价复刻或独立业务验收通过。**

因此下一步仍只修补委外，不自动迁移采购、生产、销售或财务其他模块。

## 已证明的产品能力

| 能力 | 已证明范围 | 证据边界 |
| --- | --- | --- |
| RISEMAP 菜单骨架 | 生产、供应链、销售、项目、财务、报表、系统等一级区域已重排；委外库存、待办池、结果报表保留独立业务入口 | 系统菜单的完整浏览器展开仍待复核 |
| 委外订单上下文 | 订单可显示供应商、供料方式、计划/已办数量、来源单据、异常、结果和下一步 | 订单整体关闭条件仍待 RISEMAP 复核 |
| 发料与供应商签收 | 发料草稿、提交、审核、出库、签收以及“签收不自动开始加工”已在页面和 API 中验证 | 真实外部供应商身份、多账号权限未证明 |
| 回厂与入库 | 回厂先形成待入库结果，确认入库后才写正式库存和材料倒冲；订单上下文可直接进入当前待入库单 | 拒收、复检、混合批次和 RISEMAP 写入未证明 |
| NCR 质量处置 | 订单上下文聚焦当前 NCR；页面完成方案、提交、审核、执行，来源回厂单同步为已解决 | 四类处置在 RISEMAP 同材料的字段和岗位语义未全部复核 |
| 退料交接 | 退料登记与仓库入库分为两个岗位节点；确认前不扣库存，入库后回写余额、累计和流水 | 供应商确认、赔偿和扣款关系未证明 |
| 对账与应付 | 当前订单上下文隔离待办和来源；确认锁定后幂等生成应付，另一订单不被推进 | 供应商确认、付款、部分结算、差异关闭未证明 |
| 持久化 | 同一 SQLite 的 API 连续链、停服重启回读，以及重启后对账页面回读均已通过 | 订单创建和库存前置由 API 准备；事务级原子性和失败补偿未证明 |
| 订单建单子链 | 内置浏览器完成供应商、甲供料、加工件、发料计划、金额、交期、图纸号填写，提交审核和审核通过；重启后同库回读一致 | `subcontract-order-ui-readback` 仍有旧审核意见硬编码，因文本不一致失败；该脚本契约需修补，不能把它记为业务闭环通过 |

## 分类审计

| 类别 | 当前结论 |
| --- | --- |
| RISEMAP 已观察事实 | 菜单区域、委外入口、RM-082/RM-084/RM-086/RM-087/RM-089/RM-091/RM-092/RM-093 的页面结构、字段和部分状态证据可引用 |
| Forge 工程推断 | NCR 阻断、退料库存回写、对账资格、应付公式、订单下一步排序和上下文过滤中的未被 RISEMAP 同材料验证部分，继续标为工程实现/待复核 |
| 可复用服务能力 | ObjectStack Console、对象元数据、Action、库存流水、审计日志、SQLite 持久化和通用页面组件保留，不因复刻外观而另建前端壳 |
| 需要重构的产品界面 | 岗位待办聚合、主单据详情、来源穿透、下一步和异常结果优先级仍是后续修补重点；孤立对象页不再作为完成度指标 |
| 语义风险实现 | 供应商外部确认、部分结算/重开/拆分、赔偿扣款、权限边界、事务级原子性和订单关闭条件，未经复核不得推广 |

## 仍待证明的门禁

1. 使用同一干净 SQLite，由真实多角色账号连续完成业务员、仓库、质量、财务四个岗位的完整链路；当前只证明单管理员下从已有订单/库存前置材料开始的连续浏览器链。
2. 当前样本的执行链关键结果已完成同库停服重启 API 与对账页面回读；订单创建页面子链也已纳入，但库存准备仍由 API 前置，尚不能称为订单端到端浏览器闭环。
3. 用真实多角色账号证明待办分派、动作权限、数据范围和不可越权阻断。
4. 以保存的 RISEMAP 当前页面和可成功办理材料复核签收、回厂、四类 NCR、退料、供应商确认、差异结算和付款关系。
5. 取得独立业务验收人的正常路径、异常路径和推广签字；没有签字时只能称为工程样板。

## 推广结论

| 目标域 | 结论 |
| --- | --- |
| 委外剩余功能 | 继续修补；优先完成全程浏览器门禁、真实多角色、RISEMAP 写入复核和独立业务验收 |
| 采购 | 不推广 |
| 生产 | 不推广 |
| 销售 | 不推广 |
| 财务 | 不推广；委外应付只证明来源生成，不证明完整财务闭环 |

## 停止规则

出现来源丢失、状态可绕过、异常仍可结算、金额重复、页面无法进入下一岗位、同库重启结果不一致或 RISEMAP 事实与实现冲突时，停止新增功能，先修正产品语义、持久化边界或证据记录。

## 当前门禁材料

- `docs/risemap-menu-recovery-map.md`
- `docs/risemap-product-structure-recovery-plan.md`
- `docs/forge-main-product-audit-20260912.md`
- `docs/forge-subcontract-acceptance-review-20260912.md`
- `docs/forge-subcontract-risemap-parity-20260912.md`
- `docs/forge-verification-matrix.md`

代码门禁：`pnpm typecheck`、`pnpm validate`、`pnpm build` 均通过。上述通过只证明当前代码可构建和元数据可验证，不替代业务闭环验收。
