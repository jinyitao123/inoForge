# 销售管理页面合同（RISEMAP 当前结构对照）

本轮范围：销售业务下的业绩银行、价格策略、Goodwill 订单、销售团队、销售目标。对照账号：RISEMAP `金一涛` 与 Forge `Dev Admin` 按同一业务用户对照。

## 实时入口

- RISEMAP 业绩银行：`https://risemap.cn/sales/performance-bank`
- RISEMAP 价格策略：`https://risemap.cn/sales/pricing`
- RISEMAP Goodwill订单：`https://risemap.cn/sales/goodwill`
- RISEMAP 销售团队：`https://risemap.cn/sales/organization`
- RISEMAP 销售目标：`https://risemap.cn/sales/target`
- Forge 对应页面：`page_sales_performance_bank`、`page_sales_pricing`、`page_goodwill_orders`、`page_sales_teams`、`page_sales_targets`

## RISEMAP 当前事实

业绩银行标题为“业绩银行”，说明为“按已完成销售订单统计业绩，统一沉淀确认、Rebook 与分析结果”。首屏包含所有、我负责的、下属负责的、我关注的范围按钮；指标为累计确认业绩、累计毛利、业绩笔数、待审批；页签为我的业绩银行、业绩流水、Rebook 记录、销售业绩确认、业绩分析；期间按钮为全部、本年、Q1、Q2、Q3、Q4、本月；表头为方向、销售订单号、客户、销售订单额、毛利率、业绩金额、计收比例、时间、操作。

价格策略标题为“价格策略”，说明为“统一管理商品定价、折扣策略、框架协议及特价审批”。首屏功能页签包含物料定价、价格组管理、框架协议价、特价审批、价格调整记录、价格历史。物料定价表头为物料编码、物料名称、规格型号、单位、部门、分类、价格组、成本价、最低售价、建议售价、目录价、状态、操作。当前页面有 5 条控制柜交付物价格记录，状态均显示“待生效”。

Goodwill订单标题为“Goodwill订单”，说明为“客情维护、补偿赠送等无偿订单的审批与发货跟踪”。首屏动作包含新建 Goodwill 订单、刷新、类型、状态、导出、导出任务；表头为 Goodwill单号、客户名称、赠送类型、申请原因、物品种类/总数、状态、负责人、创建日期、创建人、操作。当前列表为空。

销售团队标题为“销售组织管理”，说明为“管理销售团队结构、成员分配和团队负责人”。首屏动作为新建团队和搜索，当前为空态“暂无销售团队，请点击新建团队”。

销售目标标题为“销售目标管理”，说明为“设置和调整销售目标，实时计算完成进度与动态调整建议”。首屏包含新建目标、个人目标、团队目标和 2026 年筛选，当前为空态“暂无数据”。

## Forge 当前表现

Forge 已将上述五个销售业务入口改为独立业务页面。业绩银行读取本地销售订单和客户，按 RISEMAP 的指标、范围、页签、期间和表头展示收入确认后的业绩记录。价格策略读取物料规格、物料和报价明细，按 RISEMAP 的物料定价列结构展示成本价、最低售价、建议售价、目录价和待生效状态。

Goodwill订单、销售团队和销售目标已建立对应业务对象，并按 RISEMAP 当前空态页面展示入口、首屏说明、搜索、表头、刷新和空态。新增按钮保留为当前页面主动作，但本轮没有在 RISEMAP 办理新增流程，因此 Forge 不把新增、审批或发货流转写成已完成路径。

## 证据材料

- RISEMAP：`docs/references/risemap-capture/live/20260913-sales-full/risemap-sales-performance-bank-menu.*`、`risemap-sales-pricing-menu.*`、`risemap-sales-goodwill-orders-menu.*`、`risemap-sales-teams-menu.*`、`risemap-sales-targets-menu.*`
- Forge：`docs/references/risemap-capture/live/20260913-sales-full/forge-sales-performance-bank.*`、`forge-sales-pricing.*`、`forge-sales-goodwill-orders.*`、`forge-sales-teams.*`、`forge-sales-targets.*`

## 验收结论

当前可验收范围是销售管理五个入口的实时首屏结构、指标、表头、筛选/页签、空态或本地只读记录展示。Goodwill 新增审批、销售团队成员维护和销售目标计算仍需在 RISEMAP 有同材料办理后继续复核。

## 2026-09-13 同材料补证：销售团队、销售目标、Goodwill订单

本轮已按当前线上 RISEMAP 页面重新补证，仍以 RISEMAP 当前页面为首要事实来源。新增 RISEMAP 数据均使用 `RISEMAP 对照…20260913` 标识，只做新增与读取。

### 销售团队

- RISEMAP 当前事实：入口 `https://risemap.cn/sales/organization`，页面标题为“销售组织管理”，说明为“管理销售团队结构、成员分配和团队负责人”，动作包含“新建团队”和搜索。已新增团队 `RISEMAP 对照销售团队 20260913`，负责人 `金一涛`，成员 `1人`，创建日期 `2026-09-13`。
- Forge 当前表现：入口 `page_sales_teams`，页面标题、说明、搜索、表头和新建团队动作对齐；已新增 `RISEMAP 对照销售团队 20260913`，负责人显示 `Dev Admin`，成员 `1 人`，状态 `启用`。
- 处理结论：`金一涛` 与 `Dev Admin` 按同一业务用户对照，不作为差异。Forge 以表格承载团队记录，属于 Console 统一呈现差异。

### 销售目标

- RISEMAP 当前事实：入口 `https://risemap.cn/sales/target`，页面标题为“销售目标管理”，说明为“设置和调整销售目标，实时计算完成进度与动态调整建议”，动作包含“新建目标”、个人目标、团队目标和 `2026年`。新建个人目标字段包含目标类型、选择人员、合同额目标、回款目标、平均分配、12 个月合同额/回款字段和备注。已新增 `金一涛 · RISEMAP 对照销售团队 20260913`，合同额目标 `¥1,000,000.00`，回款目标 `¥800,000.00`，状态 `草稿`，完成进度 `0%`。
- Forge 当前表现：入口 `page_sales_targets`，页面按个人/团队和年度展示目标；已新增 `Dev Admin` 个人目标，合同额 `¥ 1,000,000.00`，回款 `¥ 800,000.00`，完成进度 `0 %`，状态 `草稿`。月度平均分配以备注记录 `83333...83337` 与 `66666...66674`，用于保持 API 回读材料。
- 处理结论：人员名称按业务用户映射处理；Forge 目前没有展开 12 个月可编辑网格，先以月度分配摘要保留计算结果，这是页面交互待继续细化项，不影响本轮目标字段与状态对照。

### Goodwill订单

- RISEMAP 当前事实：当前可用入口为 `https://risemap.cn/sales/goodwill`，此前尝试的 `/sales/goodwill-orders` 不是有效 Goodwill 列表入口。列表包含新建、刷新、类型、状态、导出、导出任务，表头为 Goodwill单号、客户名称、赠送类型、申请原因、物品种类/总数、状态、负责人、创建日期、创建人、操作。已新增详情 `GW-2026-0001`，状态 `草稿`，客户 `苏州澄岳自动化装备有限公司`，联系人 `周启明 / —`，负责人 `金一涛`，赠送类型 `RISEMAP 对照赠送类型 20260913`，申请原因和备注为同材料文本，物品种类 `1`，物品总数 `2`。
- Forge 当前表现：入口 `page_goodwill_orders`，页面标题、说明、搜索、表头和空态对齐；新建弹窗包含客户名称、联系人、负责人、赠送类型、申请原因、物品清单、备注。当前本地库无客户时，保存会先补建同材料客户分类与客户档案，再创建 `GW-FORGE-20260913`。创建后列表展示客户、赠送类型、申请原因、物品 `800型柔性线控制柜 / 2 台`、负责人 `Dev Admin`、创建人 `Dev Admin`。
- Forge 产品决策：`提交审批` 使用两层 ObjectStack 标准弹窗，第一层核对订单、客户、当前状态，第二层明确“提交后订单进入待审批，后续才能继续发货。”确认后状态变为 `待审批`。
- 处理结论：RISEMAP 当前详情停留在 `草稿`，Forge 的 `待审批` 是本地可写执行扩展，不能写成 RISEMAP 已提交审批事实。删除、打印、发货后续流转未在本轮对 RISEMAP 执行。

### 新增证据材料

- RISEMAP：`docs/references/risemap-capture/live/20260913-sales-data/risemap-sales-team-current-before-submit.*`、`risemap-sales-team-created.*`、`risemap-sales-target-form-filled.*`、`risemap-sales-target-created.*`、`risemap-goodwill-form-filled-selected-item.*`、`risemap-goodwill-created.*`、`risemap-goodwill-detail-current-recheck.*`。
- Forge：`docs/references/risemap-capture/live/20260913-sales-data/forge-sales-team-created-recheck.*`、`forge-sales-target-created-draft-recheck.*`、`forge-goodwill-empty-after-fix.*`、`forge-goodwill-form-official.*`、`forge-goodwill-created-official.*`、`forge-goodwill-submit-review.*`、`forge-goodwill-submit-confirm.*`、`forge-goodwill-pending-approval.*`。

## 2026-09-13 Goodwill 审批发货补充

RISEMAP 当前事实：`https://risemap.cn/sales/goodwill/2099141151628992514` 中 `GW-2026-0001` 已处于 `发货中`，详情页显示客户、联系人、负责人、赠送类型、申请原因、收货地址、物品种类、物品总数、发货单数、已发数量，并提供 `确认完成`、`反审核`、`一键反审核`、`打印`。本轮只读取并保存证据，没有继续确认完成线上单据。

Forge 当前表现：`page_goodwill_orders` 已支持 Goodwill 列表、新建、提交审批、同意审批、创建发货、发货中记录和确认完成弹窗。`GW-FORGE-20260913` 通过内置浏览器实际操作到 `发货中`，列表显示审批通过、`DN-GW-20260913-001`、发货记录 1、已发 2 台、收件人 `周启明 13800002609`、地址 `苏州市工业园区澄岳路9号`、无物流信息。`金一涛` 与 `Dev Admin` 按同一业务用户对照。

明确差异与结论：Forge 的完成态已实现并通过脚本验收，但本轮未执行 RISEMAP `确认完成`，因此完成态标为 Forge 可写扩展，待 RISEMAP 后续同材料复核。页面可见文案已改为业务语言，不再显示内部验收标语。

## 2026-09-20 业绩银行实时重构

### RISEMAP 当前事实

- 实时入口 `https://risemap.cn/sales/performance-bank` 使用紫色业务标题区，说明仍为“按已完成销售订单统计业绩，统一沉淀确认、Rebook 与分析结果”。
- 范围入口为所有、我负责的、下属负责的、我关注的；总览指标为累计确认业绩、累计毛利、业绩笔数、待审批。
- 业务页签为我的业绩银行、业绩流水、Rebook 记录、销售业绩确认、业绩分析；筛选包含业绩人和全部、本年、Q1、Q2、Q3、Q4、本月。
- 我的业绩银行包含业绩人身份条、业绩净额、原始确认、业绩分入、业绩分出、月度业绩趋势和业绩明细。当前账号的实时结果为零记录、零金额。

### Forge 当前表现与交互复核

- `page_sales_performance_bank` 已由普通标题和单表重构为紫色 Hero、四个范围入口、四项总览指标、五个可切换页签、业绩人选择、期间筛选、个人银行身份条、四项个人净额指标、月度趋势和明细表。
- 页面通过 Bearer 会话令牌读取本地销售订单、客户和用户。Forge 当前有两笔历史收入确认，合计 `¥486,400.00`，因此与 RISEMAP 零数据不构成同材料对照。
- 内置浏览器已实际点击 Rebook 记录并回读零记录空态；点击业绩分析并显示年度趋势与业绩结构；点击下属负责的并将指标归零，同时说明需要建立销售组织上下级关系；返回所有和我的业绩银行后数据恢复。
- 已在 `1280×900` 与 `760×900` 视口检查标题、范围入口、指标、页签、人员选择、期间筛选、身份条、指标卡和趋势图，没有发现按钮、输入框或页签互相挤压。该结果只证明本轮几何缺陷已处理，不等于像素验收通过。

### 当前状态

RISEMAP 与 Forge 的数据条件不同，同视口原图与差异图、主要元素几何误差、同一 SQLite 停服重启和独立复核尚未齐全，因此业绩银行继续保持 `review_required`，不得描述为 accepted。

## 2026-09-20 价格策略实时重构

### RISEMAP 当前事实

- 实时入口 `https://risemap.cn/sales/pricing` 使用紫色到青色业务标题区，页签依次为物料定价、价格组管理、框架协议价、特价审批、价格调整记录、价格历史。
- 物料定价左侧只有“全部物料 5”和“控制柜交付物 5”；工具栏依次提供批量调价、批量配置价格关系、分配价格组、批量导入、导入/导出任务、导出、刷新、密度和列设置。
- 筛选包含“搜索物料编码、物料名称、规格型号...”和全部部门。表格包含选择框以及物料编码、物料名称、规格型号、单位、部门、分类、价格组、成本价、最低售价、建议售价、目录价、状态、操作。
- 当前 5 条记录为 `RM-CAB-800`、`RM-PSU-24V10A`、`RM-HMI-700`、`RM-PLC-1215C`、`FG-RM-CAB-800`，分类均为控制柜交付物，状态均为待生效。当前成本价和目录价与 Forge 本地数据不同。

### Forge 当前表现与交互复核

- 修复了旧页面没有携带 Bearer 会话令牌导致 8 条真实物料规格被错误展示为 0 条的缺陷。
- `page_sales_pricing` 已重构为业务 Hero、六个真实可切换页签、物料分组、工具栏、搜索和部门筛选、带选择框的 14 列价格表。分组按名称去重，当前显示全部物料 8、控制柜交付物 5、委外加工件 2、图纸关联成品 1。
- 行编辑可真实更新 `forge_material_sku.cost_price` 与 `forge_material_sku.sale_price`；批量调价可按所选物料的当前目录价或成本价计算并持久化，且 `-100%` 被页面阻断。内置浏览器仅验证弹窗、字段和阻断，没有提交价格变更。
- 导出会生成当前筛选结果 CSV。价格关系、价格组、框架协议价、特价审批、调价流水、价格历史和导入任务因缺少对应持久对象，页面明确展示能力缺口，不保留假按钮、假记录或假成功提示。
- 已在 `1280×900` 检查标题区、页签、左侧分组、工具栏、筛选和横向滚动表格；在 `760×900` 检查页签横向承载、分组横排、工具栏换行、筛选行、表格内部横向滚动和编辑弹窗。按钮、输入框、选择器、表头和弹窗动作未出现互相覆盖、文字挤成不可读或越出容器。

### 明确差异与当前状态

- RISEMAP 当前只展示 5 条控制柜交付物，Forge 本地库有 8 条跨分类物料规格；同名 5 条的成本价和目录价也不相同，因此本轮不是同材料数值验收。
- Forge 当前 SKU 模型只有成本价与含税售价，没有最低售价、建议售价、价格组、价格关系、价格版本、生效区间和调价流水。页面保留对应列与页签并如实说明缺口，没有把零值或本地推算包装成 RISEMAP 事实。
- 同视口原图与差异图、主要元素几何误差、同一 SQLite 停服重启、同材料价格变更回读和独立复核尚未齐全，价格策略继续保持 `review_required`，不得描述为 accepted。

## 2026-09-20 Goodwill 列表几何压力返修

- RISEMAP 实时入口 `https://risemap.cn/sales/goodwill` 当前有 1 条 `GW-2026-0001`，状态为发货中。首屏为橙红色业务标题区；工具栏包含新建、搜索、类型、状态、导出、导出任务和刷新；表格为 Goodwill单号、客户名称、赠送类型、申请原因、物品种类/总数、状态、负责人、创建日期、创建人、操作。
- Forge 共用销售管理请求层此前没有携带 Bearer 会话令牌，导致页面显示 `UNAUTHENTICATED` 和 0 条假空态。本轮已修复，共用该请求层的 Goodwill、销售团队和销售目标会读取当前登录会话。
- Forge 当前回读 9 条历史 Goodwill 订单，其中 1 条发货中、8 条已完成；数量和状态与 RISEMAP 当前 1 条不属于同材料数据。
- 页面已改为橙色 Hero、新建、搜索、类型、状态、导出任务和刷新；长编号、长客户名、申请原因、发货物品、发货记录与收件信息采用固定列宽、单行省略和表格内部横向滚动，不再通过无限换行挤高行、挤压相邻列。
- 内置浏览器已在 `1280×900` 和 `760×900` 实看真实 9 条长文本数据。窄屏时新建与搜索占首行，类型、状态、导出任务和刷新进入下一行；表格保持自身横向滚动，未发现按钮、输入框、选择器或列内容互相覆盖。
- “导出任务”会打开单一 ObjectStack 标准说明弹窗；本轮发现并修复了该说明弹窗与新建弹窗同时打开的状态冲突。由于 Forge 尚无可回读导出任务对象，该入口只说明能力边界，不展示假任务或假成功。
- 页面数据与 RISEMAP 不同，创建日期/创建人列尚未按 RISEMAP 列序重排，同视口差异图、停服重启回读和独立复核也未完成，因此继续保持 `review_required`。

## 2026-09-20 销售团队实时重构与几何压力检查

### RISEMAP 当前事实

- 实时入口 `https://risemap.cn/sales/organization` 当前为普通标题区，标题“销售组织管理”，说明“管理销售团队结构、成员分配和团队负责人”，主动作为“新建团队”。
- 页面采用左右双栏结构：左侧为团队搜索和团队列表，右侧为所选团队详情与成员列表；不是普通数据表。
- 当前团队为 `RISEMAP 对照销售团队 20260913`，成员 `1 人`。团队详情展示说明、负责人 `金一涛`、成员数、创建日期 `2026-09-13`，并提供编辑与删除；成员区提供添加成员并显示负责人记录。

### Forge 当前表现与交互复核

- `page_sales_teams` 已从单表重构为同职责的双栏页面：左侧搜索和团队列表，右侧团队说明、负责人、成员数、创建日期、状态、编辑入口及当前可回读负责人。
- 页面已修复共用请求层的会话令牌，当前真实回读团队 `项目客户销售团队`、负责人 `Dev Admin`、成员 `1 人`、创建日期 `2026-09-13`、状态启用；`金一涛` 与 `Dev Admin` 仍按同一业务用户对照。
- 编辑弹窗真实回读团队名称、编码、负责人、状态和团队说明，可通过 ObjectStack API 保存；本轮仅打开检查字段与布局，没有提交数据变更。
- Forge 当前销售团队对象只有负责人和成员总数，没有独立的团队成员明细对象，因此成员区只展示可回读负责人，并明确说明能力边界；不提供无法持久化的“添加成员”假按钮。删除具有破坏性且尚未完成同材料与二次确认验证，本轮不复制删除入口。
- 内置浏览器已在 `1280×900` 检查双栏、搜索、团队卡、详情元数据、成员卡和编辑弹窗；在 `760×900` 检查单栏重排、全宽主按钮、详情元数据换行和成员卡。按钮、输入框、选择器、状态与长说明未出现互相覆盖、被挤出容器或不可读。

### 明确差异与当前状态

- RISEMAP 与 Forge 的团队名称和数据不是同一条记录；Forge 缺少独立成员对象，因此尚不能复刻添加成员、成员分配和成员明细回读。
- 同视口原图与像素差异图、主要元素几何误差、同一 SQLite 停服重启、成员维护同材料办理和独立复核尚未齐全，销售团队继续保持 `review_required`，不得描述为 accepted。

## 2026-09-20 销售目标实时重构与几何压力检查

### RISEMAP 当前事实

- 实时入口 `https://risemap.cn/sales/target` 当前为普通标题区，标题“销售目标管理”，说明“设置和调整销售目标，实时计算完成进度与动态调整建议”。
- 首屏提供新建目标、个人目标、团队目标和年度筛选。当前个人目标卡为 `金一涛`、`草稿`、`进度滞后`，合同额 `¥1,000,000.00`，回款额 `¥800,000.00`，两项进度均为 `0%`，剩余目标 `¥1,000,000.00`，建议月均 `¥333,333.33`；操作包含提交审核、编辑和删除。
- 页面按目标卡而不是普通表格呈现，卡内同时承载身份、状态、合同额/回款额进度、剩余目标、建议月均和行操作。

### Forge 当前表现与交互复核

- `page_sales_targets` 已从横向数据表重构为目标卡列表，提供新建目标、个人/团队切换、年度筛选和刷新；当前真实回读 `Dev Admin` 的 2026 年个人目标，合同额、回款额、草稿状态、零进度、剩余目标和建议月均与 RISEMAP 当前材料一致。人员名称按同一业务用户映射，不作为差异。
- 编辑弹窗真实回读目标类型、人员、合同额、回款额和月度平均分配备注，可 PATCH 保存；新建使用同一表单并可 POST 创建。本轮打开编辑弹窗检查字段和布局，没有保存数据变更。
- 草稿目标提供“提交审核”并使用 ObjectStack 标准确认弹窗；确认后只把可回读业务状态更新为执行中。Forge 尚无独立审核任务对象，弹窗明确说明边界，本轮只检查确认内容，没有提交状态变更。
- 页面保留展开详情入口，展示目标备注和月度平均分配；当前对象没有独立回款完成额与 12 个月目标明细对象，因此回款进度保持 0%，月度分配暂存于备注，未包装成已完整复刻。
- 内置浏览器已在 `1280×900` 实看标题、目标切换、年度选择、目标卡、双进度、剩余目标、建议月均和操作；在 `760×900` 发现原四列卡片仍会压缩后，将响应式断点提高到 `820px`，复查后标题动作、筛选、进度和操作按单列重排，未再出现按钮、输入框或目标数据互相挤压。编辑弹窗和提交确认弹窗均完成实看。

### 明确差异与当前状态

- RISEMAP 有删除入口，Forge 本轮没有复制：删除属于破坏性动作，尚未完成同材料与二次确认验证。RISEMAP 的审核任务、回款完成额和月度目标明细也没有对应 Forge 持久对象。
- 同视口原图与像素差异图、主要元素几何误差、同一 SQLite 停服重启、提交后的同材料状态回读和独立复核尚未齐全，销售目标继续保持 `review_required`，不得描述为 accepted。

## 2026-09-20 销售发票实时重构与几何压力检查

### RISEMAP 当前事实

- 实时入口 `https://risemap.cn/sales/invoices` 使用蓝色业务 Hero，标题“销售发票”，说明“开票申请的发起记录与流转状态跟踪；正式发票由财务在「发票管理」登记回写”。
- 状态页签为全部、待审批、已审批、已开票、已驳回、已撤回；工具栏包含导出、导出任务、刷新、密度和列设置；搜索提示为“搜索申请号 / 客户 / 订单号...”。
- 表格包含选择框、申请号、客户/销售方、模式、合并订单、价税合计、状态、申请时间、申请人、关联发票、操作。当前全部状态均为 0，空态提示“暂无开票申请记录，可在销售订单发起开票”。

### Forge 当前表现与交互复核

- 修复了销售发票请求未携带 Bearer 会话令牌而显示 `UNAUTHENTICATED` 和假空态的问题。Forge 当前真实回读 8 笔已开票记录，价税合计 `¥504,400.00`，均关联销售订单、客户、申请人和应收余额。
- 页面改为蓝色业务 Hero、六个状态页签、导出、导出任务、列表设置、刷新、搜索和固定列宽表格。真实长发票号、客户名和订单号使用省略显示，表格自身横向滚动，不再挤压相邻列。
- “导出”真实生成当前筛选结果 CSV；“导出任务”打开单一标准说明弹窗。Forge 尚无可回读的销售发票导出任务对象，因此不展示假任务或假成功。
- 内置浏览器已在 `1280×900` 实看 8 条真实记录的 Hero、页签、工具栏、搜索、表头和行操作；在 `760×900` 实看页签横向承载、工具栏换行、搜索与按钮分行以及表格内部横向滚动，未发现按钮、输入框、页签、列或弹窗动作互相覆盖。

### 明确差异与当前状态

- RISEMAP 当前为 0 条申请，Forge 为 8 条已开票历史记录，不能作为同材料数据验收；Forge 当前记录来自正式销售发票对象，尚未建立独立开票申请对象，待审批、驳回和撤回状态只保留页面结构。
- 同视口原图与像素差异图、主要元素几何误差、同一 SQLite 停服重启、从销售订单发起开票的双侧同材料办理和独立复核尚未齐全，销售发票继续保持 `review_required`，不得描述为 accepted。

## 2026-09-20 销售业务全量控件几何回归

### 回归范围与阻断规则

- 本轮重新检查销售业务 12 个入口：框架销售合同、销售订单、附加费用、收款流水、销售发货单、销售退货、业绩银行、价格策略、Goodwill 订单、销售团队、销售目标、销售发票。
- 每页分别使用 `1440×900`、`1280×900`、`760×900` 三档视口；检查对象不仅是页面是否可打开，还包含按钮与按钮、按钮与输入框、选择器与重置动作、日期区间、页签、工具栏、行操作、弹窗表单和弹窗底部动作。
- 几何判定同时检查可见控件矩形是否相交、控件是否被 `overflow` 父容器裁切、工具栏实际内容宽度是否超过容器宽度。宽表只允许在自己的 `.fp-table-wrap` 内横向滚动，不允许把页面本身撑宽，也不允许把固定操作区挤出可视区。
- 任一档视口出现交叠、裁切、不可读压缩或操作按钮越界，即阻断该页继续验收并保持 `review_required`；不得用桌面大屏正常、代码存在响应式规则或表格可滚动代替实际通过。

### 本轮发现与修复

- 框架销售合同在 `1440×900` 与 `1280×900` 下仍按单行八项筛选计算，范围、状态、订单状态、开票、发货、收款和负责人连续互压，末项被卡片裁切。已把 Console 左侧导航占用后的内容宽度纳入断点，提前重排为四列筛选。
- 销售订单在 `1440×900` 下负责人筛选被裁切，计划交货开始日期、结束日期与清空筛选互压；已同步提前进入四列与两列筛选布局。
- 附加费用在 `1440×900` 与 `1280×900` 下筛选和导出、新增动作总宽度超过卡片，右端按钮被压缩；已提前换成四列分行布局。
- 收款流水在 `1440×900` 下结束日期越出卡片；已把搜索、客户、对方流水号、金额和日期区间提前重排为两列，并让日期区间独占整行。
- 价格策略在 `760×900` 下部门选择器与“重置筛选”发生实际覆盖；已将单列筛选断点由 `680px` 提高至 `820px`。

### 修复后页面与弹窗结果

- 12 个入口在 `1440×900`、`1280×900`、`760×900` 共 36 个首屏组合重新扫描后，可见控件交叠、非预期父容器裁切、工具栏溢出均为 0。
- 在 `1280×900` 与 `760×900` 实际打开合同导入/导出、订单导入/导出、附加费用新建、发货单新建、退货新建、价格关系说明、Goodwill 新建、销售团队新建、销售目标新建和销售发票导出任务，共 20 个弹窗组合；可见控件交叠与底部动作溢出均为 0。
- 收款流水和业绩银行当前数据行只提供查看动作；本轮核对了实际行操作文本和表格内部承载，没有执行资金分配、业绩确认或其他会改变业务状态的动作。
- 本节只证明本轮控件几何回归已通过。RISEMAP/Forge 同视口原图与差异图、同材料全流程、停服重启回读和独立复核仍未全部完成，因此 12 页继续保持 `review_required`。
