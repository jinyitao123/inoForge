# Forge 供应链 · 出库管理页面合同（2026-09-17）

RISEMAP 事实来源：`docs/references/risemap-capture/deep/supply-chain/outbound/`
（rm-039 至 rm-045）与 `docs/risemap-page-contracts.json`。

## 范围与结果

| 页面 | RISEMAP 列 | 本批结果 |
| --- | --- | --- |
| 出库单列表 | 出库单号、关联单号、销售单号、客户单号、往来单位、物料明细、仓库、出库日期、状态、物流信息、收入确认、操作员、操作 | 13 列一致（含勾选），品牌标识区 + 导出/刷新 |
| 生产出库 | 同出库单列表 13 列 + 下一步操作 生产入库 | 13 列一致 |
| 其他出库 | 出库单号、出库类型、物料明细、出库日期、状态、出库原因、创建人、操作 | 8 列一致；新建其他出库 + 打印条码 + 导出 + 刷新 |
| 销售直接出库 | 直接出库单号、客户单号、客户、出库进度、状态、创建时间、操作 | 7 列一致 |
| 待出库发货单 | 发货单号、客户单号、客户、收货人、收货地址、含税金额、出库进度、状态、日期、操作 | 列一致（另有勾选列） |
| 采购退换货出库 | 退货单号、供应商、退货地址、退货金额、出库进度、状态、日期、操作 | 8 列一致 |
| 出库明细 | 21 列（序号…物流单号、状态） | 21 列一致；保留业务类型/状态与五个查询条件 |

## 验证

- 浏览器回读：`docs/evidence/supply-chain-outbound/`（7/7 hero、7/7 列匹配、0 重叠）。
- 门禁：typecheck / validate / build 通过；本组页面仍为 `review_required`。
