# Forge 生产组装、领退补料与完工入库验证

更新于 2026-09-10。实现位于隔离分支 `codex/otc-production-assembly`，运行端口 `4356`，验收数据库 `.objectstack/otc-production-assembly.sqlite`。该数据库由上一阶段已验收的 `.objectstack/otc-multiline-inbound-final.sqlite` 通过 SQLite 在线备份形成独立快照，源库未被修改，快照完整性检查为 `ok`。

## RISEMAP 已观察行为

- 组装单选择成品后会带出已生效 BOM 与版本，按末级物料展开单台用量、损耗率、需求数量、当前库存、缺口和预计物料成本。
- 当前控制柜 BOM 展开 `RM-PLC-1215C × 1`、`RM-HMI-700 × 1`、`RM-PSU-24V10A × 2`、`RM-CAB-800 × 1`，零库存时齐套率 `0%`、缺料 `4`、预计物料成本 `14442.48`。
- 缺料待办说明在产组装单按计划完工日期优先分配库存。
- 领料只选择“待领料”组装单；补料只选择“组装中”组装单；退料要求组装单已有净领料数量。
- 组装支持分批登记入库，入库数量与完工状态独立维护。
- 拆解会减少成品库存，并要求每项理论拆出量在回收与报废之间完整分配；换件保持成品库存不变，旧件可回收入库或报废，新件领用出库。

上述内容来自已保存页面证据及本次登录态只读选料核对。本次没有在 RISEMAP 保存、下达或确认任何生产单据，因此 RISEMAP 的审批结果、库存过账、成本归集和批次追溯仍待同材料成功记录复核。

## Forge 已完成闭环

1. 生效 BOM 可创建草稿或直接下达组装单，并保存四条独立物料需求。
2. 待领料单按计划完工日期和单号顺序分配同仓库可用库存。API 验收第一张两台单齐套 `100%`；第二张两台单得到 `80%`，PLC 与柜体各缺 `1`。
3. 领料单生成后处于审批中，此时库存不变。确认时再次校验四项可用库存，逐行扣减余额并写四条来源可追溯的生产领料流水。
4. 补料确认继续出库；退料数量不能超过净领用，确认后按原组装投入成本入库。验收用触摸屏补 `1`、退 `1`，数量和库存价值往返一致。
5. 生产入库允许分批登记。API 单分两批各入库 `1` 台，组装单仍为组装中；两台生产结果全部登记后，单独执行“确认完工”才进入已完工。
6. 成品入库按已确认净物料投入归集库存成本，保存入库前后数量、批次和来源流水。人工与制造费用尚未加入。
7. 真实浏览器另行办理 `ASM-2026-0003`：保存并下达、生成四行领料单 `MAT-2026-0002`、确认出库 5 件、登记 `WIN-2026-0003` 入库 1 台、最后确认完工。完整停服重启后，页面仍显示已完工、四条需求、领料单、批次及生产入库。

## 验证结果与边界

- `production-assembly.integration.mjs` 六组业务验收通过。
- `production-assembly-restart-readback.mjs` 回读 API 组装单、四条需求、领料/补料/退料、八条库存流水、两批生产入库和成品余额通过。
- `production-assembly-ui-readback.mjs --restart` 回读浏览器组装单、四行领料、五条库存流水、成品余额和完工状态通过。
- `pnpm typecheck`、`pnpm validate`、`pnpm build` 通过。保留项目既有的页面 `className` 指导提示和当前 Node `25.6.1` 不满足包声明 `<25` 的引擎提示。

多行库存写入仍由 ObjectStack 动作顺序执行，没有数据库事务级原子回滚；单号仍为数量加一，不具备并发安全。岗位审批、驳回/作废、工时与制造费用、批次/SN 谱系、拆解和换件库存闭环不属于本切片，后两项按下一顺序阶段实现。

## 2026-09-14 当前页面控件可用性复核

本轮按已登录内置浏览器重新打开 RISEMAP `https://risemap.cn/production/assembly` 与 Forge `http://localhost:4321/_console/apps/forge/page/page_production_assembly_workspace` 对照，不再只按历史合同或脚本判断。

RISEMAP 当前组装单页面显示左侧入口为生产 / 组装业务管理 / 组装单，首屏包含说明、四个指标、状态筛选、新建组装单、导出、导入/导出任务、刷新、搜索框和表格列：组装单号、成品、BOM、计划数量、合格数量、不合格数量、物料种数、物料成本、来源订单、创建日期、创建人、操作。当前 RISEMAP 租户该列表为空，因此本轮只作为页面结构和操作入口事实，不写成远端业务闭环。

Forge 当前组装单列表沿用同一信息结构，并在当前 SQLite 中显示 17 张组装单。列表中的创建人已经由技术 ID 改为业务用户 `Dev Admin`，按项目约定与 RISEMAP 的 `金一涛` 作为同一业务用户对照。列表页可点击状态筛选、搜索、刷新和查看，不保留无反馈的假按钮。

详情页复核了 `ASM-2026-0006`。该单处于待领料，页面显示刷新齐套和生成领料单两个写操作、物料需求与执行表、关联领退补料单表和生产入库记录。原实现中点击生成领料单会立即写入并跳转，已暴露为不符合人类操作习惯的产品缺口。本轮已改为业务确认弹窗：点击生成领料单后停留在当前单据，弹出“确认生成领料单”，显示单号 `ASM-2026-0006`、业务影响说明、取消和确认提交按钮；未点击确认时不会继续创建领料单。保存草稿、保存并下达、下达组装、刷新齐套、提交补料、提交退料、登记生产入库、确认完工也统一接入标准确认弹窗。

本轮证据保存于 `docs/references/risemap-capture/live/20260914-production-assembly-usability/`，包含 RISEMAP 组装单页面 AX 与截图、Forge 组装单确认弹窗 AX 与截图。本轮仅证明组装单页面结构与关键写操作可用性修正；RISEMAP 当前没有同材料可办理数据，远端审批、库存过账和批次结果仍保持待复核。

## 2026-09-14 缺料待办与按钮可用性补充

本轮继续在已登录内置浏览器打开 RISEMAP `https://risemap.cn/production/shortages`。RISEMAP 当前页面为生产 / 组装业务管理 / 缺料待办，说明为“对在产组装单实时做齐套分析：库存按计划完工日期优先分配后，不满足的物料自动汇成缺料待办”。当前指标为缺料单据 0、缺料物料 0 种、缺口总额 ¥0.00、最早计划完工 -，页面给出“当前没有缺料，所有在产组装单均已齐套”的空状态。

Forge `page_production_shortage_workspace` 在当前 SQLite 中显示真实缺料数据：缺料单据 4、缺料物料 16 种、缺口总量 37、最早计划完工 2026-09-12，并按组装单、计划完工、物料编码、物料名称、需求、可分配、缺口和操作展示。点击“查看组装单”已从缺料行跳转到 `ASM-2026-0017` 详情，页面显示齐套率 0%、缺料项 4 和四行物料缺口，因此该入口不是占位按钮。

详情页对缺料状态下的“生成领料单”保持禁用，但已补充可读原因：“当前仍有缺料，先补齐库存或刷新齐套后再生成领料单”。后续复刻同类页面时，禁用按钮必须给出业务原因和可执行下一步；可写按钮必须先出现业务确认，再执行动作并给出结果反馈。

本轮证据保存于 `docs/references/risemap-capture/live/20260914-production-shortage-usability/`，包含 RISEMAP 缺料待办页面 AX 与截图、Forge 缺料行跳转后的组装详情 AX 与截图。

## 2026-09-14 领料、补料、退料页面布局与可用性复核

本轮按已登录内置浏览器重新打开 RISEMAP `https://risemap.cn/production/requisitions`、`https://risemap.cn/production/material-supplies`、`https://risemap.cn/production/material-returns`，并与 Forge `page_production_material_workspace`、`page_production_supply_workspace`、`page_production_return_workspace` 使用当前同一 SQLite 页面回读对照。

RISEMAP 当前领料单页面职责为组装领料、拆解领料、补料、退料出入库单据汇总，首屏指标为领料单、物料种类、物料数量、净领用金额，状态筛选为全部、审批中、已确认、已驳回、已作废，列表列为领料单号、类型、成品、来源单、物料种类、数量、金额、经手人、日期、操作。RISEMAP 当前补料单页面职责为组装单超 BOM 用量补领，首屏指标为补料单总数、补领物料种数、补料金额（出库）、涉及来源单，列表列为补料单号、成品、来源单、物料种数、数量、金额、经手人、日期、操作。RISEMAP 当前退料单页面职责为已领物料退回库存，首屏指标为退料单总数、退回物料种数、退回金额（入库）、涉及来源单，列表列比补料单多仓库列。三个 RISEMAP 页面当前租户均为空，因此本轮只作为页面结构、字段、指标、入口和空状态事实，不写成远端出入库闭环。

Forge 本轮已将三页从同一套通用表格收敛为按单据类型呈现的业务页面。领料页显示领料单 14、物料种类 56、物料数量 135、净领用金额 ¥167,700.00；补料页显示补料单总数 9、补领物料种数 9、补料金额（出库）¥16,400.00、涉及来源单 9；退料页显示退料单总数 9、退回物料种数 9、退回金额（入库）¥16,400.00、涉及来源单 9。列表均显示业务成品、来源组装单 `ASM-*`、经手人 `Dev Admin`、经手日期和状态；退料页保留仓库列，领料与补料页不再显示退料专属的仓库列。

页面控件按实际办理路径复核：列表主按钮为“新建领料单”，会进入可办理组装单选择页；导出会下载当前筛选结果并保存到统一的“导入/导出任务”；刷新可重新读取当前库；状态筛选、搜索、清空筛选和查看均为真实可用动作。审批中的领料单 `MAT-2026-0001` 详情页点击“确认并过账”后没有直接改状态，而是打开标准确认弹窗“确认领料单过账”，弹窗显示单据号、来源组装单、库存过账影响、取消和确认提交按钮；检查结果 `hasNativeDialog=false`，证明不是浏览器原生弹框。未点击确认提交，因此本轮没有额外改变当前业务数据。

本轮证据保存于 `docs/references/risemap-capture/live/20260914-production-material-usability/`，包含 RISEMAP 领料、补料、退料当前页面 AX 与截图，以及 Forge 领料、补料、退料最终布局、审批中详情和二次确认弹窗 AX 与截图。本轮证明生产物料三页的页面结构、控件位置、按钮可用性和二次确认已达到当前切片要求；RISEMAP 当前无同材料办理数据，远端审批与库存过账仍标为待同材料复核。

## 2026-09-14 拆解单与换件单页面布局、弹窗和回读复核

本轮没有猜 URL，而是在已登录内置浏览器中从 RISEMAP 生产 / 组装业务管理菜单点击进入当前页面。RISEMAP 拆解单真实入口为 `https://risemap.cn/production/disassembly`，换件单真实入口为 `https://risemap.cn/production/rework`。两个页面当前租户均为空，因此本轮只记录页面结构、字段、状态和入口事实，不写成 RISEMAP 已办理闭环。

RISEMAP 拆解单页面说明为“将成品按 BOM 还原为物料，支持审批、回收入库和报废处理”。首屏指标为拆解单总数、拆解成品数、回收物料价值、报废/损耗；状态标签为全部、草稿、审批中、待领料、拆解中、已完工、已入库、已驳回；工具区包含新建拆解单、导出、导入/导出任务、刷新；列表列为拆解单号、成品、BOM、数量、原因、回收价值、报废、经手人、日期。RISEMAP 换件单页面说明为“成品不离库，仅更换内部零部件，支持审批、领新件和旧件回收”。首屏指标为换件单总数、累计换件处数、新件领用成本、旧件回收价值；状态标签为全部、草稿、审批中、待领料、换件中、已完工、已入库、已驳回；工具区包含新建换件单、导出、导入/导出任务、刷新；列表列为换件单号、成品、换件内容、改制数量、原因、新件成本、回收价值、成本变化、经手人、日期。

Forge 本轮将 `page_production_disassembly_workspace` 与 `page_production_replacement_workspace` 收敛为当前业务布局：补齐 RISEMAP 对应说明、四项指标、状态标签、刷新、搜索、清空筛选、业务成品、原因、经手人和真实分页。列表行操作只保留真实可达的“查看”。新建页分别提供“保存草稿”和“确认拆解/确认换件”，仓库在保存草稿时可留空，确认动作在仓库未选时禁用；详情页补仓库后才能提交审批。

按钮可用性已在内置浏览器用同一控制柜 BOM 实际办理。拆解单 `DIS-2026-0011` 无仓库保存为草稿，补仓库后通过“提交拆解确认”标准弹窗进入审批中，再通过“确认拆解并过账”弹窗完成过账；成品库存 `11 → 10`，释放成本 `16816.1399`，回收价值 `16320.00`，报废损失 `496.1399`，产生一条成品出库和四条回收入库流水。换件单 `REP-2026-0011` 同样先无仓库保存草稿，再补仓库提交并确认过账；整机库存保持 `10 → 10`，新件成本 `3200.00`，旧件回收 `860.00`，成本变化 `2340.00`，产生一条新件出库和一条旧件回收入库流水。所有提交与过账均使用 Console 标准确认弹窗。

生产拆换 API 验收现为七组，新增覆盖无仓库草稿、草稿不产生库存流水、空仓库提交阻断、补仓库后状态与明细进入审批中；原有匿名拒绝、拆解完整分配、成品扣减、回收入库、换件理论上限、回收/报废、整机不变和重复确认阻断继续通过。物理数据库已通过 `os migrate apply` 放宽两张主单的 `warehouse_id`，迁移前备份完整性为 `ok`，迁移后 `os migrate plan` 为 0。

本轮新证据保存于 `docs/references/risemap-capture/live/20260914-production-transform-draft-submit/`，包含 RISEMAP 拆解、换件实时首屏，以及 Forge 新建草稿表单、已入库详情和重启回读 AX 与截图。已通过 `acceptance:production-transform`、`acceptance:production-transform-restart`、`acceptance:production-transform-ui-readback`。RISEMAP 当前原因字典没有可选值，远端同材料保存、审批和过账仍标为待复核；Forge 的三个拆解原因与三个换件原因属于本地可写执行扩展，不写成 RISEMAP 已观察事实。

## 2026-09-14 生产原因配置与拆换联动复核

本轮在已登录内置浏览器实时打开 RISEMAP `https://risemap.cn/settings/business/production`。当前生产配置明确分为“拆解原因”和“改制原因”两个页签；拆解原因用于新建拆解单，改制原因用于新建换件单。两类新增弹窗均包含原因名称、描述、标识颜色和添加动作；停用的原因不进入后续新建单据下拉，已保存单据仍保留原原因。RISEMAP 当前改制原因弹窗已使用同材料填写“质量返修”和“成品保留库存，仅更换不合格内部零部件”，尚未点击最终添加，因此远端数据仍未改变。

Forge 已增加真实的“系统 / 生产配置”页面和两类原因对象，支持搜索、新增、标识颜色、启用和停用。停用与重新启用均使用标准二次确认弹窗。内置浏览器已创建拆解原因“质量返工”和改制原因“质量返修”，验证停用后换件单原因下拉为空、重新启用后恢复；拆件单只列出启用的“质量返工”，换件单只列出启用的“质量返修”。页面不再使用硬编码原因选项。

拆件单和换件单继续保存原因名称快照，同时关联对应原因配置。Action 对布尔值及 SQLite `0/1` 返回统一判断，已配置但停用的原因会在创建单据前阻断。`acceptance:production-transform` 新增“停用原因阻断”和两类单据 `reason_id` 持久关联断言，八组业务验收全部通过。完整停服后从同一 `.objectstack/data/objectstack.db` 重启，`acceptance:production-transform-restart`、`acceptance:production-transform-ui-readback` 再次通过；数据库完整性为 `ok`，迁移计划为 `0`，重启后两条配置保持启用，拆件和换件各有 2 张验收单据关联原因配置。

当前边界仍是 RISEMAP 最终添加及同材料拆解、换件办理尚未提交。该部分继续标为“待 RISEMAP 同材料复核”，不把 Forge 结果写成远端闭环。

## 2026-09-14 生产导入导出任务对照与回读

本轮在已登录内置浏览器实时打开 RISEMAP `https://risemap.cn/production/assembly`，点击“导入/导出任务”。当前抽屉列为任务、来源、状态、进度、结果、失败原因、提交时间和操作，包含刷新、任务总数与分页；当前租户为空，因此这里只记录页面结构和入口事实。

Forge 已建立生产域统一“导入/导出任务”页面。组装单、拆解单、换件单、领料单、补料单、退料单、生产入库和生产出库的列表导出都会保留任务名称、来源、状态、进度、结果条数、结果文件、失败原因和提交时间。用户关闭来源页面后仍可回到任务页再次下载结果；筛选、清空、刷新、分页、查看全部来源和返回来源均为真实控件。任务中心允许展示导入任务状态，但本轮没有新增未取证的批量导入入口。

内置浏览器实际从 Forge 组装单导出 46 条，任务 `PDT-1789392211938-2AT2` 显示“已完成 / 100% / 46 条”并出现“下载结果”；随后从生产出库导出 58 条，任务 `PDT-1789392308281-NP9M` 显示“已完成 / 100% / 58 条”。一次在字段只读修正前产生的未完成任务已如实转为“失败”，失败原因为“页面升级期间任务未完成，请重新导出”，没有伪装成成功记录。`acceptance:production-data-tasks` 已验证结果内容、结果文件、来源入口以及八类生产页面的任务接入。

## 2026-09-14 组装草稿编辑、取消与业务确认文案

本轮在已登录内置浏览器实时打开 RISEMAP `https://risemap.cn/production/assembly` 与 `https://risemap.cn/production/assembly/new`，并使用 Forge `http://localhost:4356/_console/apps/forge/page/page_production_assembly_workspace` 对照。RISEMAP 当前列表仍为空；新建页已观察到成品、BOM、销售订单、仓库、数量、计划完工日期、备注以及“保存草稿 / 保存并下达”入口。本轮没有向 RISEMAP 写入生产单据，因此远端审批与取消规则继续标为待同材料复核。

同材料取证已继续推进到新建页的实时齐套计算。RISEMAP 可选择 `FG-RM-CAB-800 · 800型柔性线控制柜`，随后自动带出 `BOM-RM-CAB-800-V1 · 800型柔性线控制柜标准BOM` 与已生效版本 `V1.0`。数量为 1 台时，页面展开 PLC、触摸屏、电源和柜体四种末级物料，显示齐套率 `0%`、缺料项 `4`、预计物料成本 `¥14,442`；四项当前库存均为 0。该结果与 Forge 当前控制柜 BOM 结构一致，严格同材料下达与后续领料的当前远端阻断明确为 RISEMAP 物料库存不足。尚未点击保存草稿或保存并下达，因此不把表单可填写写成远端单据闭环。

Forge 已补齐草稿编辑和取消：草稿可修改数量、仓库、计划完工日期和备注，保持原 BOM 并重新计算物料需求；已下达单据不能再编辑或取消。取消必须填写业务原因，并使用标准二次确认弹窗说明单据、状态变化和不可恢复影响。内置浏览器实际办理 `ASM-2026-0045`：数量由 `2` 改为 `3`，四行需求重算为 `3 / 3 / 6 / 3`；空取消原因时弹窗保持打开并提示“请填写取消原因”，填写“草稿计划调整后不再生产”后转为“已取消”，详情不再显示编辑、取消或下达动作。

新增和取消确认弹窗不展示对象名、Action、参数、内部字段或记录 ID 等工程语言，只展示单据、物料、仓库、数量、日期、原因、状态与库存影响。库存锁定页同时对照 RISEMAP `https://risemap.cn/inventory/lock`：Forge 新增业务锁库确认显示仓库、物料、数量、日期、原因，并明确“草稿不占用库存，确认锁库后减少可用数量”。

`acceptance:production-assembly-draft-safety` 四组通过；完整停服并从同一 `.objectstack/data/objectstack.db` 重启后，`acceptance:production-assembly-draft-safety-restart` 通过，内置浏览器仍回读 `ASM-2026-0045` 为“已取消”。`pnpm typecheck`、`pnpm validate`、`pnpm build` 与 `acceptance:page-visible-language` 通过。当前对照缺口仍包括 RISEMAP 新建页的可选销售订单字段，以及 RISEMAP 页面出现的“审批中”状态与 Forge 当前草稿直接下达为“待领料”的差异；未取得远端实际办理证据前不自行补写规则。

本轮继续用内置浏览器创建 `ASM-2026-0050`，从详情进入“编辑草稿”，将计划数量从 `2` 调整为 `3`、再调整为 `4`。四行 BOM 需求随数量重算为 `4 / 4 / 4 / 8`。走查发现原编辑动作会把仓库可用量快照错误清零，现已改为按草稿所选仓库重新读取库存；页面回读四项快照为 `1004 / 1009 / 1004 / 1008`。对应 API 验收增加了逐行库存快照断言，四组安全用例、三项工程门禁和同一 SQLite 停服重启回读均通过。RISEMAP 登录态在本轮重启后失效，新的实时页面复核仍需登录后补齐，因此本段只描述 Forge 当前可验证结果，不替代已有 RISEMAP 页面证据。

## 2026-09-15 P0/P1 当前复核

本轮在已登录内置浏览器重新打开 RISEMAP `https://risemap.cn/production/assembly` 与 Forge `http://localhost:4356/_console/apps/forge/page/page_production_assembly_workspace`。RISEMAP 当前列表仍为空，但新建表单实时显示成品物料选择、BOM、BOM版本、组装数量、入库仓库（选填）、关联销售订单、计划完工日期、备注，以及按成品展开的四条物料需求、齐套率、缺料项和预计物料成本。选择 `FG-RM-CAB-800 · 800型柔性线控制柜` 后自动带出 `BOM-RM-CAB-800-V1`、`V1.0`，数量 1 台时显示四种末级物料、齐套率 0%、缺料项 4、预计成本 `¥14,442`；本轮未在 RISEMAP 保存或下达。

Forge 页面已修正 ObjectStack 外壳与自定义页面重复标题，列表首屏只保留一组业务标题；草稿列表仍按状态只给草稿显示“编辑”，已下达、组装中和已完工单据不出现编辑或取消。内置浏览器实际将 `ASM-2026-0050` 数量从 4 改为 5，点击“保存草稿”进入二次确认，确认后回到详情并回读计划数量 5、四条需求 `5 / 5 / 5 / 10` 和仓库可用快照 `1004 / 1009 / 1004 / 1008`。点击“取消组装单”会打开二次确认，显示单号、保留已取消状态、不可恢复及不能继续下达/领料/入库的影响，并要求取消原因；本轮未点击最终破坏性确认。

本轮 `acceptance:production-assembly-draft-safety` 与 `acceptance:production-assembly-draft-safety-restart` 通过，`pnpm typecheck`、`pnpm validate`、`pnpm build` 通过。当前仍有两项页面差异待后续修补：Forge 新建表单把成品、BOM和版本合并为一个 BOM 选择器，且仓库文案仍显示为必填；Forge 已通过数据层扩展了成品与预览数据，但表单展示尚未完全收敛到 RISEMAP 的三步选择结构。RISEMAP 当前无可办理列表数据，远端取消与库存过账继续标为待同材料复核。
