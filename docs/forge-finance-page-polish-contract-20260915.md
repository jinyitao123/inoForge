# 财务页面精修与 RISEMAP 对照合同（2026-09-15）

## 范围和判定

本轮覆盖财务导航下 21 个页面。RISEMAP 当前页面决定入口、标题、首屏信息结构、筛选、列、空态和已观察弹窗；Forge 继续使用 ObjectStack Console 与 `product-ui.ts`。RISEMAP 当前没有可办理数据的页面，本轮只确认首屏和空态，不据此推断审批、记账、撤销等后续规则。

## 本轮实时打开的 RISEMAP 页面

| RISEMAP 页面 | 当前观察事实 | Forge 处理 |
| --- | --- | --- |
| `/finance/bank-accounts` | 标题、账户指标、搜索/类型/状态、卡片/表格切换、新建账户弹窗；弹窗含账户类型、银行资料、币种、期初余额和日期、负责人、可见范围 | 新增 `page_fund_accounts`，读取真实资金账户；新建使用共享弹窗、选择器和日期控件并持久化 |
| `/finance/accounts` | 应收、应付、往来对冲、期初应收、期初应付页签；搜索、状态、来源、日期、余额和开票筛选 | 新增 `page_receivables_payables`，统一读取真实应收和应付账；对冲与期初办理继续进入已存在专页 |
| `/finance/credit-management` | 授信客户、待审批授信；额度、占用、可用、使用率、超期金额、冻结和负责人列；当前为空 | 新增 `page_credit_management`，由客户授信额度和应收余额计算占用、可用和逾期金额 |
| `/finance/loans` | 员工借款、银行贷款；申请、导出、刷新、搜索、状态；申请表含标题、用途、分类、金额、项目、预计还款日、备注和附件 | 新增借款持久对象及 `page_finance_loans`；本轮开放草稿保存。审批、计息、放款和还款规则待同材料办理后补齐 |
| `/finance/cost-center` | 成本归集和结果看板；总额、已归集、待归集、处理中、已完成、归集率；来源和状态筛选；当前为空 | 新增 `page_finance_cost_center`，读取项目成本池并呈现 RISEMAP 的首屏维度 |
| `/finance/expense-center` | 企业支出和费用统计；新建付款申请；日期、类别、状态和搜索；当前为空 | 新增 `page_finance_expense_center`，读取付款任务；主按钮进入已有付款办理页 |
| `/finance/reimbursement` | 日期区间、费用归属、本人/代人、状态筛选；报销指标；新建报销；当前为空 | 新增 `page_finance_reimbursement`，读取项目费用报销；主按钮进入已有报销办理页 |
| `/finance/invoices/overview` | 月/季/半年/全年；销项、进项、预计税额、未开票指标；趋势、税额构成和三个业务入口 | 新增 `page_invoice_overview`，用真实销项、进项和往来账形成总览 |
| `/finance/invoices/sales` | 时间、状态、搜索；发票号、日期、客户、金额、税率、关联单据和项目；当前为空 | 新增 `page_output_invoices`，读取真实销项发票；申请开票进入已有申请页 |
| `/finance/invoices/purchase` | 时间、状态、类型、抵扣状态、供应商和关联单据筛选；当前为空 | 新增 `page_input_invoices`，读取真实进项发票；登记入口进入已有采购发票页 |
| `/finance/invoices/tasks` | 待处理任务、待开票金额、全部申请；状态筛选；申请号、合同/订单、客户、金额、类型、税率、申请人和时间；当前为空 | 新增 `page_invoice_tasks`，先承接已经形成的销售发票记录；独立的待开票申请对象尚未建立，不把正式发票倒推成未办理申请 |
| `/finance/reconciliations` | 客户/供应商、对账维度、仅展示已结束期间和有效来源、搜索/期间、待对账列；客户为空、订单维度有 1 条 | 已有往来对账页统一标题区并保留待对账池及生成逻辑 |

## 已有财务页面统一处理

资金流水、银行余额对账、期初往来账与对冲、收款管理、付款管理、退款申请、销售收入确认、往来对账、财务冲销、开票收款与项目结算，均改用共享标题区并继续注入 `product-ui.ts`。原有真实按钮、筛选、状态变化、标准确认弹窗和持久业务动作保持不变。

## 明确差异和未决项

- RISEMAP 的贷款申请当前只观察到申请表，没有用同一材料跑通审批、放款、还款和银行贷款明细。本轮只实现持久草稿，不把后续规则写进产品。
- RISEMAP 的授信、成本、费用、报销和三类发票列表当前为空，因此首屏结构已有当前证据，行级动作仍需有可办理数据后逐项对照。
- Forge 当前销售订单动作会直接形成正式销项发票，没有独立的“待开票申请”持久对象；开票任务页因此只能可靠显示已经形成的记录，待开票、审批和驳回状态仍需在 RISEMAP 有可办理数据后补对象和状态流转。
- Forge 的财务账已经包含回款、付款、冲抵、退款、红冲和对账的可执行能力；这些能力继续保留。页面呈现采用 ObjectStack Console 的统一样式，视觉尺寸不做像素级复制。

## Forge 内置浏览器验证

- 财务导航下 21 个页面均已逐页打开并等待加载完成，标题和业务内容可见，浏览器控制台无错误。
- 资金账户使用标准弹窗和日期控件创建 `FA-PAGE-POLISH-002`，期初余额 `¥2,688.50` 同步成为当前余额；页面显示成功反馈和新记录。
- 借款贷款管理保存员工借款草稿，员工借款/银行贷款页签、搜索和重置筛选均产生可见结果变化。
- 费用中心“新建付款申请”、报销管理“新建报销”、销项发票“申请开票”、进项发票“登记进项发票”均已点击并到达对应办理页面。
- 完整停服后使用同一 `.objectstack/finance-page-polish.sqlite` 重启，资金账户和借款记录均从页面回读成功；账户当前余额仍为 `¥2,688.50`。

## 页面结构门禁

`tests/page-polish.manifest.json` 收录本轮 21 个财务页面及此前已采用共享标题区的供应链页面。`tests/page-polish.static.mjs` 检查共享产品样式和运行时、产品根节点、标题区、真实按钮、原生控件、内部验收文案及财务占位入口。该检查接入 `prebuild`，结构不满足时 `pnpm build` 直接失败。
