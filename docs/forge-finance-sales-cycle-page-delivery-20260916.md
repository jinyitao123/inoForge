# 财务销售回款链逐页交付合同（2026-09-16）

## 批次和范围

- 总目标：财务全部页面精修与业务验收。
- 本批业务链：开票申请 → 开票任务 → 销项发票 → 应收 → 收款分配与核销。
- 已收口页面：`page_invoice_tasks`、`page_output_invoices`、`page_receivables_payables`；当前页：`page_sales_collection_flow`；下一页：`page_fund_accounts`。
- 起始版本：`2a0c56713c836baeaf84f0d19e9d7483691aade3`；规则同步提交：`f260a9e`。
- 分支与工作树：`codex/finance-sales-cycle`；`.worktrees/finance-sales-cycle`。
- 独立环境：`http://localhost:4441`；`file:.objectstack/finance-sales-cycle.sqlite`。
- 实施负责人：当前财务任务；独立复核者：待独立验收。
- 全量范围来源：`objectstack.config.ts` 财务导航、`src/pages/index.ts` 页面注册、页面内跳转、相关对象及 Action。
- 共享文件唯一负责人：本批不修改 `objectstack.config.ts`、`product-ui.ts`、`src/pages/index.ts`、`package.json` 和精修 manifest；需要更新时在集成阶段由主线唯一负责人处理。

## 当前分类清单

- 未实现：RISEMAP 当前无可办理数据，无法证明其行级审批、驳回和登记开票交互；双侧同材料办理待补证。
- 功能已有但页面交互欠缺：Forge 已有审批、驳回、登记开票 Action；当前专用列表仅显示已审批记录的“登记开票”，遗漏待审批行操作。
- 实现错误：待处理开票误统计为待审批；待开票金额包含待审批和已驳回；申请人读取不存在的 `requested_by`；申请时间优先显示业务日期而非提交时间。
- RISEMAP 证据不足：当前列表为空，无法观察行操作出现条件、弹窗内容、提交阻断及操作后跳转。

## 逐页设计：开票任务

- pageId：`page_invoice_tasks`
- RISEMAP 当前入口：`https://risemap.cn/finance/invoices/tasks`
- Forge 入口：`/_console/apps/forge/page/page_invoice_tasks?nav=invoice_tasks`
- 岗位：财务开票人员；协同岗位：开票申请审批人、销售申请人。
- 核心任务：识别已审批待开票申请，登记正式销项发票，并追溯已开票结果。
- 主单据：`forge_sales_invoice_request`；来源：销售订单及其明细；结果：销项发票与应收账款；下一岗位：应收与收款办理人员。
- 主原型：`task_workspace`。页面围绕一组待办申请呈现期间、判断指标、状态、列表和状态相关行操作。
- 参考页：`project-task-workspace.page.ts`；密度参考 `project-timesheet-cost.page.ts`；版本为本批最终被验收提交。
- 首屏顺序：标题与刷新 → 期间及申请提交时间范围 → 三项指标 → 列表名称与数量 → 状态页签 → 搜索与重置 → 申请列表。
- 主动作：没有全局新建按钮；“登记开票”只出现在已审批行。待审批行显示“通过 / 驳回”，已开票行显示“查看发票”。刷新为标题区次要动作。
- 全局范围：本月、本季、半年、全年，作用于指标和列表。
- 局部筛选：状态、搜索，作用于列表，不改变期间指标。
- 指标口径：待处理开票为期间内 `approved` 数量；待开票金额为期间内 `approved` 的申请金额；全部申请为期间内全部申请数量，并附待审批、已驳回数量。
- 表格列：申请编号、合同/订单、客户、申请金额、发票类型、税率、申请人、申请时间、状态、操作。
- 状态：待审批、已审批、已开票、已驳回。Forge 使用“已驳回”，不把 rejected 伪装为 RISEMAP 指标中的“已撤回”。
- 阻断：审批意见必填；只有待审批可审批/驳回；只有已审批可登记开票；发票编号必填；Action 继续校验订单、明细、数量和重复开票。
- 反馈：成功后刷新列表和指标；失败原因显示在当前页面；已开票可进入正式发票详情。
- 视觉：桌面 1440×900；窄屏 390×844；空态保留十列表头；工具栏和页签在窄屏可滚动，表格仅在卡片内部横向滚动；状态和行操作保持可达，行操作文字不换行。

| 要求编号 | 当前事实或明确决策与证据 | 区域/控件及实现位置 | 操作步骤 | 预期结果 | 实际结果与证据 | 状态/缺口 |
| --- | --- | --- | --- | --- | --- | --- |
| INV-TASK-001 | RISEMAP 实时页面显示标题“开票任务”和说明“处理已审批的开票申请并跟踪开票进度” | 标题区，`finance-management.page.ts` | 双侧打开页面 | 标题与职责一致 | RISEMAP 已实时观察；Forge 修改前已一致 | pass |
| INV-TASK-002 | RISEMAP 显示本月、本季、半年、全年，当前为全年 | 指标前的期间页签与时间范围 | 逐项点击 | 指标和列表按期间变化，选中态明确，并展示申请提交时间起止 | 本月显示 2026-09-01 至 2026-09-30，全年显示 2026-01-01 至 2026-12-31；本批数据均在当前期间，跨期间数量差异待后续材料 | partial |
| INV-TASK-003 | RISEMAP 指标为待处理开票、待开票金额、全部申请 | 指标区 | 准备不同状态记录并切换期间 | 只把已审批记录计入待开票数量和金额 | 初始 1 / ¥26,000；审批后 2 / ¥38,000；开票后 1 / ¥12,000，与 API 记录一致 | pass |
| INV-TASK-004 | RISEMAP 显示全部、待审批、已审批、已开票、已驳回 | 状态页签 | 逐项点击 | 只筛选列表，不改变期间指标 | 内置浏览器核对待审批、已审批和全部；状态改变后数量即时回读 | pass |
| INV-TASK-005 | RISEMAP 当前只观察到刷新；Forge 明确保留搜索作为列表增强 | 搜索与重置 | 输入订单、客户、编号；重置 | 列表立即过滤，重置恢复全年和全部 | 搜索 `PENDING-APPROVE` 得 1 条；重置恢复 5 条 | pass |
| INV-TASK-006 | RISEMAP 实时表头十列 | 主表格 | 对照表头与真实记录 | 列顺序一致，申请人和提交时间来自真实字段 | 十列一致；申请人为 Dev Admin，提交时间取 `submitted_at`，合同/订单和税率来自关联数据 | pass |
| INV-TASK-007 | Forge 已有审批、驳回、登记开票、查看发票能力 | 行操作与标准弹窗 | 对四种状态分别操作 | 只显示状态允许的动作；成功后回读 | Forge 四种状态动作按条件显示，审批、驳回和登记开票均已操作；RISEMAP 行级事实仍待同材料复核 | partial |
| INV-TASK-008 | Forge 安全决策要求业务校验和错误反馈 | Action 与页面通知 | 空审批意见、空发票号、重复/越权动作 | 阻断并解释原因，数据不变化 | 页面与 API 均阻断空审批意见、空发票号；API 阻断错误状态与未登录调用 | pass |
| INV-TASK-009 | 页面精修基线要求桌面、窄屏、空态与有数据 | 页面整体 | 1440×900、390×844 检查，并操作状态页签、搜索、无结果空态、重置和登记开票弹窗 | 主动作易找，首屏连续，页面整体不横向溢出，宽表仅在卡片内部滚动 | 桌面与 390×844 均通过；窄屏期间范围、指标、列表、筛选与弹窗无重叠，取消弹窗不改变状态 | pass |
| INV-TASK-010 | 业务结果必须进入销项发票和应收 | 下一岗位 | 登记开票后打开发票并回读应收 | 来源、金额、状态、关联 ID 一致 | 浏览器登记 `INV-BROWSER-20260916-001`；发票 issued、应收 unpaid、金额与余额均为 26,000；重启后保持 | pass |

## 验收与交接

- 四维结果：replication `blocked`；visual `pending`；interaction `blocked`；business `blocked`。阻断项为 RISEMAP 同材料和独立复核未完成；Forge 本页已完成桌面与窄屏页面操作、正常路径、关键阻断、API 和重启回读。
- 当前实时打开页面：RISEMAP `/finance/invoices/tasks`；Forge `http://localhost:4441/_console/apps/forge/page/page_invoice_tasks?nav=invoice_tasks`。
- RISEMAP 当前为空数据，仅首屏、指标名称、状态、表头和空态属于已观察事实。
- API：`tests/invoice-task-workspace.integration.mjs` 通过；同库停服重启：`tests/invoice-task-restart-readback.mjs` 通过；工程门禁 `pnpm typecheck`、`pnpm validate`、`pnpm build` 通过。
- 操作与结果证据：`docs/evidence/finance-invoice-tasks-20260916/acceptance.md`。
- 独立复核：待独立验收。
- 实际耗时、调用、费用：无可靠统一来源，记为未知。

## 逐页设计：销项发票

- pageId：`page_output_invoices`
- RISEMAP 当前入口：`https://risemap.cn/finance/invoices/sales`
- Forge 入口：`/_console/apps/forge/page/page_output_invoices?nav=output_invoices`
- 岗位：财务开票与发票台账人员；协同岗位：销售申请人、应收与收款办理人员。
- 核心任务：按开票期间查看销项发票金额与税额，追溯客户、订单和项目，并从可办理记录进入查看或红冲流程。
- 主单据：`forge_sales_invoice`；来源：开票申请、销售订单及发票明细；下游：应收账款、收款分配、红字发票与收入确认。
- 主原型：`task_workspace`。当前页是可操作台账，首屏以期间、汇总指标、状态与发票明细支持核对和后续处理。
- 参考页：`project-timesheet-cost.page.ts` 的页面层级与密度；`page_invoice_tasks` 的期间、指标、筛选、宽表与窄屏行为；版本为本批最终被验收提交。
- RISEMAP 已观察事实：标题“销项发票”；说明“管理客户开票、发票交付、作废与红冲”；本月、本季、半年、全年，当前全年；导出；四项指标“销项发票总额、不含税金额、税额合计、专票占比”；状态“全部、正常、已作废、已红冲”；搜索；十二列表头“选择、发票号码、日期、客户、邮件、含税金额、税率、税额、不含税、关联单据、项目、备注、操作”；当前账号为空数据。
- RISEMAP 证据缺口：当前没有发票记录，无法观察行操作出现条件、查看详情、作废、红冲弹窗、阻断和操作后跳转；这些只可按 Forge 已有能力标为产品决策并等待同材料复核。
- Forge 当前差异：期间位于指标之后；指标错误地随状态和搜索变化；发票对象没有冗余税率和未税金额，页面直接读取时显示 0；客户邮件和项目未从关联对象解析；已有“红冲发票”Action 未接入本页；已开票记录的操作列为空；标题区“申请开票”混入台账职责。
- 首屏顺序：标题与导出/刷新 → 期间及开票日期范围 → 四项指标 → 台账名称与记录数 → 状态页签与搜索 → 发票列表。
- 主动作：台账不直接新建开票申请；“查看发票”始终在行内可达；只有未收款蓝字发票显示“红冲”，并使用标准二次确认弹窗。导出和刷新为次要动作。
- 全局范围：本月、本季、半年、全年，按 `invoice_on` 同时作用于四项指标和列表。
- 局部筛选：全部、正常、已作废、已红冲与搜索，只作用于列表，不改变期间指标。
- 指标口径：含税金额取发票 `total_amount`；税额和未税金额从发票明细的 `taxed_subtotal` 与 `tax_rate` 计算；专票占比按发票类别计算。指标均使用期间内全量记录。
- 表格列：不展示无业务动作的选择框；保留 RISEMAP 的发票号码、日期、客户、邮件、含税金额、税率、税额、不含税、关联单据、项目、备注和操作。客户邮件从客户资料读取，项目从销售订单的项目关联读取。
- 状态：Forge 的 `issued`、`settled` 归入“正常”；`voided` 归入“已作废”；红字发票及全部红冲状态归入“已红冲”。
- 阻断：红冲编号、红冲日期和原因必填；只允许未收款蓝字发票红冲；存在收款核销、预收冲抵、有效收入确认、已结算项目或账面来源不完整时由 Action 阻断。
- 反馈：筛选即时更新列表和数量；红冲成功后刷新指标与列表并显示结果；失败原因保留在当前弹窗；查看进入正式发票详情。
- 视觉：桌面 1280×720 和窄屏 390×844；期间在指标之前；指标保持紧凑；状态和搜索在窄屏可操作；宽表仅在卡片内部滚动；操作列固定且文字不换行。

| 要求编号 | 当前事实或明确决策与证据 | 区域/控件及实现位置 | 操作步骤 | 预期结果 | 实际结果与证据 | 状态/缺口 |
| --- | --- | --- | --- | --- | --- | --- |
| OUT-INV-001 | RISEMAP 实时页面标题、说明与期间 | 标题和期间区 | 双侧打开并切换期间 | 标题职责一致；范围同时更新指标和列表 | Forge 标题职责、四个期间及日期范围已按当前页面呈现；本批三张发票均落在全年范围 | pass |
| OUT-INV-002 | RISEMAP 四项指标；Forge 金额必须来自真实明细 | 指标区 | 用已开票数据核对含税、税额、未税和专票占比 | 指标不随局部状态或搜索变化，金额与 API 一致 | 红冲前为 62,000 / 54,867.26 / 7,132.74 / 13%；红冲后净额为 26,000 / 23,008.85 / 2,991.15 / 13%，筛选与搜索不改变期间指标 | pass |
| OUT-INV-003 | RISEMAP 四个状态和搜索 | 台账筛选 | 逐个状态、搜索无结果、重置 | 状态与搜索只筛选列表，数量即时更新 | 红冲后全部 3、正常 1、已作废 0、已红冲 2；搜索 `NO-MATCH` 显示空态，重置恢复三条记录 | pass |
| OUT-INV-004 | RISEMAP 十二个业务列；选择框无当前可用批量动作 | 台账表格 | 对照真实发票、客户、订单、项目与明细 | 十一个信息列加操作列完整，关联字段真实 | 表格保留十一项业务信息和操作列；订单、税率、金额及状态来自真实关联，当前材料没有联系人邮箱与项目关联，因此对应单元格显示 `—` | partial；需补有邮箱和项目的真实材料 |
| OUT-INV-005 | Forge 已有发票详情和红冲 Action；RISEMAP 行级事实待补 | 行操作与标准弹窗 | 查看；空字段；正常红冲；错误状态/账面阻断 | 可达、可阻断、可提交、可回读 | 未收款蓝字发票显示“查看发票 / 红冲”；空字段在弹窗内阻断；正常红冲生成红字发票并更新原票与应收；重复红冲和未登录调用由 API 阻断 | partial；RISEMAP 同材料待复核 |
| OUT-INV-006 | 页面精修基线 | 页面整体 | 桌面与 390×844 检查有数据、空态、筛选和弹窗 | 首屏无重叠，宽表局部滚动，操作列可达 | 1280×720 与 390×844 均通过；期间、指标、状态、搜索和标准红冲弹窗无重叠，宽表只在列表卡片内滚动 | pass；独立视觉复核待完成 |
| OUT-INV-007 | 业务结果必须承接订单、应收和后续收款 | API 与同库重启 | 红冲前后读取发票、应收、订单、明细和日志并重启 | 来源、金额、状态和累计一致 | 红冲后原票和对应应收均为 `red_reversed`、余额 0；红字发票关联原票且日志唯一；保留的正常票与未收应收均为 26,000；同库重启后保持 | partial；Forge 通过，RISEMAP 同材料待复核 |

## 销项发票验收与交接

- 四维结果：replication `blocked`；visual `pending`；interaction `blocked`；business `blocked`。Forge 页面正常路径、关键阻断、桌面和窄屏检查、API 与同库重启均已完成；RISEMAP 当前空数据，无法验证行级交互和同材料业务结果，独立复核也尚未发生。
- 当前实时打开页面：RISEMAP `/finance/invoices/sales`；Forge `http://localhost:4441/_console/apps/forge/page/page_output_invoices?nav=output_invoices`。
- 页面结果：红冲前含税总额 62,000；浏览器红冲 36,000 发票后，原票和应收进入 `red_reversed`，新增红字发票，期间净额为 26,000，正常蓝字票与未收应收继续承接下一页。
- API 与持久化：`tests/invoice-output-readback.mjs` 在完整停服并使用同一 SQLite 重启前后均通过；未登录红冲返回 401，重复红冲返回 400。
- 工程门禁：`pnpm typecheck`、`pnpm validate`、`pnpm build` 均通过；导航静态检查通过 211 个入口。
- 操作与结果证据：`docs/evidence/finance-output-invoices-20260916/acceptance.md`。
- 独立复核：待独立验收；本页继续保持 `review_required`。

## 逐页设计：应收应付

- pageId：`page_receivables_payables`
- RISEMAP 当前入口：`https://risemap.cn/finance/accounts`
- Forge 入口：`/_console/apps/forge/page/page_receivables_payables?nav=receivables_payables`
- 岗位：应收与应付会计；协同岗位：销售业务员、采购人员、收付款办理人员。
- 核心任务：统一读取应收与应付余额，按来源、账龄、业务对象和到期时间筛选，追溯订单、项目与发票，并把未结清应收交给收款办理。
- 主单据：`forge_accounts_receivable`、`forge_accounts_payable`；来源包括销项/进项发票、服务结算、采购入库和期初建账；下一岗位为收款流水、付款任务、往来对冲与期初建账。
- 主原型：`task_workspace`。页面是双向账款台账，首屏优先展示账款方向、搜索与常用筛选、汇总视图、明细和自然的下一步动作。
- 参考页：`project-timesheet-cost.page.ts` 的层级与密度；本批 `page_invoice_tasks` 和 `page_output_invoices` 的紧凑卡片、局部筛选、宽表和窄屏行为。
- RISEMAP 已观察事实：标题“应收应付”；说明“统一查看应收应付账款、管理期初建账、跟踪资金双向流动”；五个入口“应收账款、应付账款、往来对冲、期初应收建账、期初应付建账”；导出和刷新；应收可按列表、客户、订单查看，应付可按列表、对象、订单、项目查看；筛选包含搜索、账龄、来源、确认日期、未收/未付余额、发票状态或供应商、到期提醒；当前两侧均为空数据。
- RISEMAP 应收列：选择、应收编号、来源、客户/业务员、订单/项目、应收总额、已收/冲抵、未收余额、到期日、状态、发票、操作。
- RISEMAP 应付列：选择、应付编号、来源、供应商/联系人、订单/项目、应付总额、已付/冲抵、未付余额、到期日、状态、核销状态、操作。
- RISEMAP 证据缺口：当前没有账款记录，无法观察行操作条件、详情层级、登记收付款入口、分页后的筛选保持和操作反馈；这些能力只能以 Forge 已有服务和明确产品决策实现，等待同材料复核。
- Forge 修改前差异：按订单汇总与表格错误读取 `sales_order_id` / `purchase_order_id`，实际对象字段为 `order_id`，导致订单显示“未关联”；来源直接显示 `sales_invoice` 等技术值；关联业务员、联系人和项目未组合展示；搜索没有匹配关联对象名称；窄屏默认展开全部高级筛选，占满首屏；未结清应收只有“查看”，无法自然进入收款办理。
- 首屏顺序：标题与收付款办理入口 → 五个账款入口 → 方向摘要 → 台账标题与数量 → 搜索、账龄、视图与来源 → 可折叠高级筛选 → 明细或分组汇总 → 合计与分页。
- 主动作：应收页提供“打开收款流水”；未收款、部分收款或逾期行提供“登记收款”；应付页提供“打开付款管理”。详情、导出和刷新为次要动作。
- 筛选口径：搜索覆盖编号、来源名称、客户/供应商、负责人、订单和项目；所有筛选只作用于列表、分组和页脚合计，不改变方向摘要；重置必须一次恢复默认状态。
- 分组口径：客户/供应商使用往来单位 ID；订单统一使用真实 `order_id`；项目从订单或账款的项目关系解析；每组汇总笔数、总额、已收/已付及未结余额。
- 阻断：登记收款只允许未收款、部分收款或逾期应收；流水号、账户、日期和正数金额必填；金额不得超过应收余额；账户必须启用且日期位于开放财务期间。成功后生成待分配收款流水，不直接伪造核销结果。
- 视觉：桌面 1280×720 和窄屏 390×844；常用筛选保持紧凑，高级筛选在窄屏默认收起；宽表只在卡片内部滚动；操作列固定且不换行。

| 要求编号 | 当前事实或明确决策与证据 | 区域/控件及实现位置 | 操作步骤 | 预期结果 | 当前状态/缺口 |
| --- | --- | --- | --- | --- | --- |
| ARAP-001 | RISEMAP 实时标题、说明与五个入口 | 标题与账款导航 | 双侧打开并核对入口 | 职责一致，入口均可达 | 标题、说明、五个入口及方向切换通过；各入口目标页由后续逐页验收 |
| ARAP-002 | RISEMAP 应收/应付查看维度与筛选 | 工具区 | 切换方向、视图、常用和高级筛选并重置 | 控件作用对象清晰，关联名称可搜索，重置完整 | 通过：方向、按订单、关联名称搜索、高级筛选展开与重置已操作 |
| ARAP-003 | RISEMAP 双向十一项业务列 | 明细表 | 对照有数据账款 | 来源为业务文案，订单/项目与人员真实关联 | 部分通过：应收有数据且来源、订单、人员正确；应付当前空数据待复核 |
| ARAP-004 | RISEMAP 支持按客户/对象、订单和项目汇总 | 分组视图 | 切换每种汇总 | 分组键正确，笔数与金额合计一致 | 部分通过：应收按订单使用真实订单分组；其余汇总缺少对应业务数据 |
| ARAP-005 | Forge 已有登记收款 Action；RISEMAP 行级事实待补 | 应收行操作和标准弹窗 | 空字段、超额、正常登记和错误状态 | 阻断清楚；成功生成待分配收款流水并可进入下一页 | Forge 通过：空字段和超额被阻断，正常登记生成待分配流水；RISEMAP 同材料待复核 |
| ARAP-006 | 页面精修基线 | 页面整体 | 桌面与 390×844 检查有数据、空态、筛选和弹窗 | 首屏不过度堆叠，宽表局部滚动，动作可达 | 本轮通过：桌面、窄屏、有数据、应付空态、高级筛选和弹窗均已检查；待独立复核 |
| ARAP-007 | 业务结果必须承接销项发票与收款核销 | API 与同库重启 | 读取发票、应收、收款流水和资金账户并重启 | 来源、金额、状态和余额保持一致 | 部分通过：同库重启后应收、待分配流水、订单和资金余额一致；分配核销及 RISEMAP 同材料待下一页复核 |

## 应收应付验收与交接

- RISEMAP 实时核对：`https://risemap.cn/finance/accounts` 的应收与应付方向、查看维度、筛选、业务列和空态；当前账号两侧均无账款数据，行操作和结果无法取证。
- Forge 桌面操作：应收方向显示两笔账款、应收总额 62,000、未收余额 26,000；来源显示业务名称，客户/业务员和真实销售订单可读；按订单汇总不再落入“未关联”；关联客户名称可搜索并可重置。
- Forge 业务操作：对未收款应收打开标准登记收款弹窗，空字段提交被阻断；超出未收余额返回 400；登记 26,000 后生成待分配流水 `RCV-PAGE-20260916-001`，资金账户余额增加到 126,000，应收在核销前仍保持未收款。
- Forge 窄屏操作：390×844 下摘要纵向排列，高级筛选默认收起；展开后起止日期完整显示；宽表局部滚动，登记收款弹窗完整显示字段、影响说明和操作按钮。
- 持久化：完整停服后使用同一 SQLite 重启，页面仍显示两笔应收及 26,000 未收余额；API 回读确认流水、资金账户、订单关系与红冲应收状态一致。
- 工程门禁：`pnpm typecheck`、`pnpm validate`、`pnpm build` 均通过；导航静态检查通过 211 个入口。
- 未决项：RISEMAP 同材料行级行为、应付有数据列与分组、收款分配核销结果仍待复核；本页保持 `review_required`。

## 逐页设计：收款流水

- pageId：`page_sales_collection_flow`
- RISEMAP 当前入口：`https://risemap.cn/sales/collection-flow`
- Forge 入口：`/_console/apps/forge/page/page_sales_collection_flow?nav=sales_collection_flow`
- 岗位：销售会计、收款办理人、核销审核人；协同岗位为销售业务员。
- 核心任务：读取真实到账流水，把未分配金额关联到同客户应收，审核核销并回写应收、发票、订单和项目回款；审核前可取消分配，有误时按状态撤销或反核销。
- 主单据：`forge_cash_receipt`；关联 `forge_collection_allocation`、`forge_accounts_receivable`、`forge_sales_invoice`、`forge_sales_order`、`forge_customer`、`forge_fund_account`；上一岗位为应收与到账登记，下一岗位为回款完成后的结算和经营复盘。
- 主原型：`task_workspace`。页面是到账分配任务台账，首屏优先展示资金规模、分配进度、待办状态、快速筛选、流水明细和自然的核销动作。
- 参考页：`project-timesheet-cost.page.ts` 的层级与密度；本批 `page_receivables_payables` 的紧凑摘要、局部筛选、宽表和标准弹窗。
- RISEMAP 已观察事实：标题“收款流水”；说明“展示收款资金流水 · 手动分配到销售订单 · 核销前可解绑 · 分配后刷新订单回款进度”；四项指标为收款流水总额、已分配金额、未分配金额、待分配；状态为全部、待分配、部分分配、已分配；筛选包含流水号或订单、对方名称、金额、客户、开始日期和结束日期；明细列为流水号、往来单位/客户、收款日期、方式、收款账户、收款金额、已分配/未分配、状态、操作；当前为空数据。
- RISEMAP 证据缺口：当前没有收款流水，无法观察分配弹窗、同客户应收限制、取消分配、审核核销、撤销到账、反核销及操作后回写。这些行为依据 Forge 已有服务和明确产品安全决策实现，等待同材料复核。
- Forge 修改前差异：页面请求未使用 Console adapter 的鉴权头，当前已登录页面仍显示“Authentication is required”并误报 0 条；指标说明把分配描述为订单关联，实际核销对象是应收；没有待审核状态入口和审核核销动作；分配对话框先进入确认再校验必填，错误反馈顺序不自然；所有筛选始终挤在一行，窄屏任务区不够紧凑。
- 首屏顺序：标题与上一/下一环节入口 → 四项分配摘要 → 状态与任务数量 → 搜索和常用筛选 → 可折叠高级筛选 → 流水明细 → 合计与分页。
- 主动作：有未分配金额的流水显示“分配到应收”；待审核流水显示“审核核销”及“取消分配”；无有效核销且仍有未分配金额时可“撤销到账”；查看为次要动作。已审核核销的反核销从核销记录详情办理并保留原因。
- 筛选口径：搜索覆盖流水号、订单、发票、客户和对方流水；状态页签、客户、精确金额与收款日期只作用于明细和页脚合计，不改变全量摘要；重置一次恢复默认。
- 阻断：只允许同客户未结清应收；金额必须为正且不得超过流水未分配余额和应收余额；必填字段先在弹窗内阻断；审核前可取消，存在待审核或已审核核销时不得撤销到账；破坏性动作使用标准二次确认并说明资金和业务影响。
- 视觉：桌面 1280×720 和窄屏 390×844；常用筛选紧凑，高级筛选默认收起；状态和操作列可达且不换行；宽表只在卡片内部滚动；弹窗在窄屏完整显示。

| 要求编号 | 当前事实或明确决策与证据 | 区域/控件及实现位置 | 操作步骤 | 预期结果 | 当前状态/缺口 |
| --- | --- | --- | --- | --- | --- |
| SCF-001 | RISEMAP 实时标题、说明、指标、状态和业务列 | 页面整体 | 双侧打开并逐项核对 | 职责、信息层级和台账内容一致 | 通过：鉴权修复后真实流水、指标、状态和业务列均可读；新增待审核为 Forge 执行扩展 |
| SCF-002 | RISEMAP 搜索、客户、金额和日期筛选 | 工具区 | 搜索、筛选、切换状态并重置 | 筛选对象清楚，摘要不随局部筛选漂移 | 通过：状态、搜索空态、高级筛选展开与重置已操作；摘要保持全量口径 |
| SCF-003 | Forge 现有分配 Action | 分配弹窗 | 空字段、超额、同客户选择和正常分配 | 错误明确；正常分配生成待审核核销 | Forge 通过：必填、超额阻断和正常分配完成；RISEMAP 同材料待复核 |
| SCF-004 | Forge 现有审核与取消 Action | 待审核行操作 | 取消一笔测试分配，再重新分配并审核 | 取消释放余额；审核回写应收、发票和订单 | Forge 通过：取消释放 26,000；重新分配并审核后应收、发票结清、订单已收 26,000 |
| SCF-005 | Forge 产品安全决策 | 撤销到账与反核销 | 尝试越过有效核销撤销；反核销后重新核销 | 非法顺序阻断，破坏性动作二次确认并留痕 | 部分通过：已分配流水不可撤销；反核销日志、余额恢复与再次核销通过；详情页独立视觉待后续复核 |
| SCF-006 | 页面精修基线 | 页面整体 | 桌面与 390×844 检查有数据、空态、筛选和弹窗 | 首屏任务清楚，宽表局部滚动，动作可达 | 本轮通过：桌面、窄屏、有数据、筛选空态、分配与确认弹窗均已检查；待独立复核 |
| SCF-007 | 业务结果承接应收页面的 26,000 元到账 | API 与同库重启 | 读取流水、核销、应收、发票、订单和资金账户并重启 | 金额、状态、来源和余额保持一致 | Forge 通过：同库重启后流水已分配、应收和发票结清、订单已收 26,000、资金余额 126,000；RISEMAP 同材料待复核 |

## 收款流水验收与交接

- RISEMAP 实时核对：`https://risemap.cn/sales/collection-flow` 的标题、说明、四项指标、四个状态、六类筛选、九项业务列和空态；当前账号无收款流水，行操作与结果无法取证。
- 鉴权修复：页面改用 Console adapter 的请求地址、传输器与鉴权头；已登录页面不再误报 401，真实 26,000 元收款流水可读取。
- Forge 页面操作：必填字段为空时在第一步阻断；分配 26,001 元被服务阻断；分配 26,000 元后显示待审核，取消分配后流水恢复 26,000 元未分配；重新分配并审核后流水进入已分配。
- 反向路径：已审核核销完成一次反核销，流水恢复未分配、应收和发票恢复未收；随后用新核销记录重新分配并审核，最终状态再次结清。已分配流水直接撤销被阻断，反核销记录已留痕。
- Forge 视觉与筛选：桌面上摘要、状态、常用筛选、宽表和行操作形成单一任务路径；搜索 `NO-MATCH` 显示筛选空态，重置恢复记录；390×844 下指标纵向排列，高级筛选默认收起，日期纵向展示，分配弹窗字段与按钮完整可达。
- 持久化：完整停服后使用同一 SQLite 重启；页面和 API 均回读一笔 26,000 元已分配流水，应收与销项发票为已结清，订单已收 26,000，资金账户余额 126,000。
- 工程门禁：`pnpm typecheck`、`pnpm validate`、`pnpm build` 均通过；导航静态检查通过 211 个入口。
- 未决项：RISEMAP 同材料分配、解绑和核销行为，以及独立视觉复核仍待完成；本页保持 `review_required`。
