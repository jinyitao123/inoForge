# Forge main 冻结审计

版本：2026-09-12  
审计基线：`main`，提交 `22fdf28`
范围：当前导航、页面、对象、Action、委外状态语义和已有验证材料。  
边界：只读审计 Forge 当前代码与已保存 RISEMAP 证据；不删除 Forge 能力，不迁移或修改线上 RISEMAP 数据。

## 审计结论

当前 `main` 已完成 RISEMAP 九类一级导航的结构重排，并形成委外订单到回厂、入库、不良处置、对账和应付的首个连续样板。它仍不是 RISEMAP 全量产品复刻，也不是完整委外订单完工证明。当前样板证明的是一条回厂批次结算闭环；订单整体仍可能因计划数量未完成而保持“进行中”。

工程统计只作为盘点索引，不作为产品完成度指标：`pnpm validate` 当前基线为 154 Objects、2,514 Fields、40 Pages、191 Actions。源码盘点为 21 个对象模块、29 个 Action 模块、35 个页面模块；一个模块可声明多个平台对象、动作或页面，不能用文件数替代产品能力。

## 分类口径

| 分类 | 判定标准 | 当前使用方式 |
| --- | --- | --- |
| RISEMAP 已观察事实 | 有当前页面、保存的 DOM/截图或明确的只读操作材料 | 可作为页面结构、名称、状态或公式的复刻依据 |
| Forge 工程推断 | 为实现闭环而设计、但 RISEMAP 尚未同材料成功办理证明 | 必须在代码与文档中标注待复核，不写成已复刻 |
| 可复用服务能力 | 已存在且可作为执行底座的对象、库存流水、审计或通用服务 | 保留并通过业务上下文重新承载，不等于产品入口完成 |
| 需要重构的产品界面 | 底层动作存在，但用户缺少岗位入口、上下文、下一步或结果 | 优先补页面结构和连续任务，不先新增孤立对象 |
| 语义风险实现 | 状态、金额、来源、阻断或动作条件可能误导用户 | 进入停止规则，未经修正不得扩展下游功能 |

## 当前导航与页面

事实来源：`apps/forge-objectstack/objectstack.config.ts`、`docs/risemap-menu-recovery-map.md`、`docs/risemap-feature-inventory.md`。

| 一级区域 | 当前 Forge 承载 | 结论 |
| --- | --- | --- |
| 工作台 | 工作台复刻状态 | 结构保留；不是 RISEMAP 待办或工作台能力 |
| 供应链 | 到货、基础资料、采购、入库、库存、出库的部分页面/对象 | 可复用能力已归位；页面合同和岗位闭环大多待补 |
| 销售 | 合同、订单、发货、客户、联系人、报价 | 有局部业务链；与 RISEMAP 页面、审批、财务结果仍需逐功能复核 |
| 生产 | 组装、图纸、委外三组 | 组装和图纸已有局部链；委外是当前首个完整样板 |
| 项目 | 项目、任务、工时、交付、费用成本 | 有局部链；不因已有验证脚本视为 RISEMAP 页面等价 |
| 行政 | 行政复刻状态 | 结构保留；业务功能未实现 |
| 财务 | 资金、业务确认、发票、对账等部分页面 | 可复用服务能力较多；完整财务语义仍待同材料复核 |
| 报表 | 项目经营分析 | 不等价于 RISEMAP 全部报表入口 |
| 系统 | 部分主数据和业务字典 | 保留已存在配置；用户、角色、部门、打印和插件等仍有缺口 |

RISEMAP 保存的全量入口台账为 181 项，但其中大量只有首屏或空表单证据。入口数量只证明菜单观察范围，不证明 Forge 页面、动作、状态或岗位任务已经完成。

## 委外样板分类

| 范围 | RISEMAP 事实 | Forge 可复用能力 | 当前界面/状态结论 | 风险或待复核 |
| --- | --- | --- | --- | --- |
| 委外订单 | RM-082、RM-083 入口和订单上下文可观察 | 订单、订单行、供应商、加工费、计划量 | 订单上下文显示来源、进度、异常、结果和下一步 | 订单物理完成、质量完成、结算完成和业务关闭的拆分未复核 |
| 发料/签收 | RM-084 入口及发料状态证据 | 发料单、库存锁定、出库流水、签收日志 | 可从订单进入发料，已验证实际发料、审核、出库和签收 | 供应商账号确认与内部登记是否并存待复核 |
| 回厂/入库 | RM-085 入口及回厂、入库证据 | 回厂、回厂明细、待入库、库存流水、材料倒冲 | 提交回厂不提前动正式库存；确认入库后回写库存与成本 | 拒收、复检、混合批次及批次追溯待复核 |
| 不良处置 | `/inventory/ncr` 与委外 NCR 证据 | NCR、方案、审批、执行、日志 | 退货、特采、报废、返工动作具备；未完成 NCR 会阻断对账 | 四类处置在 RISEMAP 同材料详情行为未全部证明 |
| 对账/应付 | RM-087、RM-093 的页面结构、状态和金额公式证据 | 委外对账头、明细、日志、应付来源关系 | 已入库且异常完成的回厂批次可合并生成、确认并幂等生成应付 | 供应商确认、部分结算、差异关闭、赔偿扣款来源待复核 |

## 状态与来源不变量

已证明或已编码的关键不变量：

1. 未完成入库或未完成 NCR 的回厂批次不得进入可对账池，并且页面必须显示阻断原因和下一步。
2. 同一回厂明细不得被多个有效委外对账单重复占用。
3. 对账单生成后保存来源、金额和订单关系；确认前不回写已确认金额，生成应付必须幂等。
4. 回厂提交阶段只产生待入库结果；确认入库阶段才执行材料倒冲和正式库存计价。
5. 订单上下文的下一步优先反映未完成异常，再反映入库、发料、回厂和结算任务；异常存在时不能仍给出结算入口。

尚未证明为 RISEMAP 事实的实现：顺序写入的原子性、供应商外部确认、部分结算/重开/拆分、赔偿和扣款分配、付款关联、权限分工以及历史数据迁移规则。

## 验证材料索引

| 材料 | 能证明什么 | 不能证明什么 |
| --- | --- | --- |
| `tests/subcontract-*.integration.mjs` | 真实 API 动作、状态和来源关系 | 不能单独证明岗位页面连续性 |
| `tests/subcontract-one-db-restart-readback.mjs` | 同一 SQLite 停服重启后的指定记录回读 | 不能证明 RISEMAP 行为等价 |
| `tests/subcontract-*-ui-readback.mjs` | 页面控件、动作反馈和结果回读 | 分散脚本不能自动证明一条连续任务旅程 |
| `docs/references/risemap-capture/deep/production/subcontract-reconciliation/` | RM-087 待对账池结构、来源和状态证据 | 只读/辅助证据不能证明未成功办理的写入规则 |
| `docs/references/risemap-capture/deep/production/subcontract-reconciliation-report/` | RM-093 结果报表字段、费用和状态证据 | 不能证明 Forge 的结算实现已获得 RISEMAP 同材料确认 |
| `docs/forge-subcontract-reconciliation-slice.md` | 三方互审、实现边界和停止规则 | 不是独立业务验收签字 |

本基线已通过 `pnpm typecheck`、`pnpm validate`、`pnpm build`，并已有委外 API、同库重启和内置浏览器材料。质量门禁仍要求把连续岗位任务、正常/异常路径、页面结果、重启回读和独立业务验收作为同一个样板审查，不把健康检查或单独接口成功升级为业务闭环。

## 下一修补单元

继续只处理委外链，顺序固定为：

1. 补齐同一 SQLite 上的连续岗位验收记录，明确业务员、仓库、质量、财务四个岗位的入口和交接。
2. 对照 RISEMAP 复核委外订单、发料、回厂、NCR、RM-087、RM-093 的页面结构和状态动作。
3. 在未决行为获得 RISEMAP 同材料证据前，保持“Forge 工程实现 / 待复核”标记，不推广到采购、生产、销售或财务其他模块。

停止条件：出现来源丢失、状态可绕过、金额重复、异常仍可错误结算、重启回读不一致或页面无法从当前单据进入下一岗位任务时，停止继续添加功能并先修正语义或持久化边界。
