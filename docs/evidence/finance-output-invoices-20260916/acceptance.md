# 销项发票页面实施验收记录

## 环境与材料

- Forge：`http://localhost:4441/_console/apps/forge/page/page_output_invoices?nav=output_invoices`
- SQLite：`apps/forge-objectstack/.objectstack/finance-sales-cycle.sqlite`
- RISEMAP：`https://risemap.cn/finance/invoices/sales`
- 业务材料：开票任务页面生成的两张蓝字发票及其应收，另由页面红冲生成一张红字发票。
- 实施版本：`ffc42ab4f17838e785a60b8a3d866ef857f34cd1`

RISEMAP 当前账号在销项发票页没有业务记录。本轮实时确认了标题、说明、四个期间、四项指标、四个状态、搜索、导出、十二项业务信息与操作列表头及空态；行操作、弹窗、阻断和操作后结果仍缺少 RISEMAP 同材料证据。

## Forge 页面操作结果

内置浏览器在桌面可见区域完成以下操作：

1. 红冲前显示两张正常蓝字发票，含税总额 62,000，不含税 54,867.26，税额 7,132.74，税率为 13%。
2. 切换“正常”状态仅筛选列表，期间指标保持不变。搜索 `NO-MATCH` 显示业务空态，重置恢复全部记录。
3. 对 `INV-ISSUED-1789545797758` 打开红冲弹窗；空字段提交被阻断并显示“红字发票编号、红冲日期和红冲原因不能为空”。
4. 填写红字发票编号 `INV-RED-BROWSER-20260916-001`、红冲日期 `2026-09-18` 和原因“页面红冲流程验收”后提交成功。
5. 红冲后列表显示三条记录：正常 1、已作废 0、已红冲 2。红字发票按负数展示含税、税额和未税金额，原票不再提供红冲动作。
6. 红冲后的期间净额为含税 26,000、不含税 23,008.85、税额 2,991.15，税率仍为 13%。
7. 完整停服并使用同一 SQLite 重启后，页面仍显示三张发票、相同状态数量和相同净额；正常票继续提供查看与红冲入口。

内置浏览器在 390×844 下核对了期间、纵向指标、状态、搜索、宽表和红冲弹窗。页面没有整体横向溢出；宽表只在列表卡片内部滚动；弹窗完整显示对象、业务影响、标准日期输入、原因、取消与确认，取消后没有产生状态变化。

## API 与持久化结果

- `tests/invoice-output-readback.mjs`：重启前后均通过。
- 正常发票 `INV-BROWSER-20260916-001` 为 `issued`，对应应收为 `unpaid`，未收金额 26,000。
- 被红冲原票与其应收均为 `red_reversed`，未收金额为 0。
- 红字发票关联原票，红冲日志唯一，三张发票签名合计为 26,000，明细税率为 13%。
- 未登录红冲返回 401；对已红冲原票重复红冲返回 400。
- `pnpm typecheck`、`pnpm validate`、`pnpm build`：通过；导航静态检查通过 211 个入口。

## 四维结论

- replication：`blocked`。RISEMAP 当前空数据，行级交互和同材料结果待复核。
- visual：`pending`。桌面和 390×844 窄屏的有数据、筛选、空态与弹窗已检查；独立视觉复核待完成。
- interaction：`blocked`。Forge 搜索、状态筛选、红冲正常路径和关键阻断均通过；RISEMAP 行级交互待同材料复核。
- business：`blocked`。Forge 发票、应收、红字发票、红冲日志和重启回读通过；RISEMAP 同材料业务结果待复核。

独立复核尚未发生，本记录不构成页面 `accepted`。
