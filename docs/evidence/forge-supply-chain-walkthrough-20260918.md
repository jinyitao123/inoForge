# Forge 供应链全流程实操走查（2026-09-18）

口径：在 Forge 页面上用真实业务数据逐步办理，遇到问题即时记录；RISEMAP 作为业务参照。每段记录「实际办理步骤、结果、问题」。

## 第 1 段：采购主链（采购申请 → 待办池 → 采购订单）

### 实际办理

1. **采购申请**（`page_purchase_request_pool`）→ 新建申请：标题「审计走查：PLC 备件补货申请」、期望到货 2026-09-25、明细 RM-PLC-1215C × 3、含税单价 6800、原因「库存低于安全库存，按采购补货流程申请」→ **保存并提交审批**。
   - 结果：生成 **PR-2026-0001**，审批状态「审批中」，金额 ¥20,400.00、期望到货 2026-09-25 均正确。
2. 同一页面行操作 **通过** → 确认弹窗 → 确认。
   - 结果：审批状态变 **已通过**。
3. **采购待办池**（`page_purchase_todo_pool`）→ 出现 **POOL-PR-2026-0001-001**（来源 PR-2026-0001，申请 3、已锁定 0、已下单 0、剩余可下单 3）。
   - 勾选该行 → **批量指定供应商** → 选择「南京锐联电气技术有限公司」→ 确认。
   - 结果：行内「建议供应商」= 南京锐联电气技术有限公司、状态 = **已指定供应商**。
4. 再次勾选该行 → **批量创建采购订单** → 供应商预填「南京锐联电气技术有限公司」、选择仓库「贯联验证原料仓」、期望到货 2026-09-28、付款条件「到货后30天」→ **创建草稿订单**。
   - 结果：待办行变为 已下单 3 / 剩余可下单 0 / 状态 **已下单**；生成 **PO-2026-0002**（供应商、仓库、期望到货、金额 ¥20,400.00、采购原因「采购申请」全部正确）。
5. 打开 PO-2026-0002 详情 → **发现没有「提交审核」按钮**（见问题 1），链路中断。

### 发现的问题

| # | 问题 | 证据 | 影响 |
| --- | --- | --- | --- |
| 1 | **采购待办池生成的草稿采购订单无法提交审核** | PO-2026-0002 列表行操作只有「查看 / 到货登记」；详情页按钮只有 采购订单 / 返回列表 / 刷新 / 基本信息 / 物料明细 / 审批记录 / 到货通知，无任何提交或审核入口。对照：直接用「新建采购单」（`?new=1`）创建的表单里有「提交审核」按钮 | 采购待办池 → 采购订单 → 到货通知 → 检验 → 入库 的主链在第一步就断；只能绕道直接建单 |
| 2 | 批量创建订单成功后无 toast 反馈 | 创建后页面无 `.fp-toast`，其他页面（如导出）都有提示 | 用户不确定是否成功，只能靠列表刷新判断 |
| 3 | 弹窗内自定义下拉在长表单里可能超出视口 | 采购申请弹窗内「物料」下拉在 776/1200 高视口下均渲染到视口外，需把视口调到 1700 才能点到；采购订单「仓库」下拉最后一项点击会落到遮罩上并关闭弹窗 | 可用性问题：小屏/常规窗口下难以选择物料与仓库 |

### 补充发现（采购订单建单入口）

| # | 问题 | 证据 | 影响 |
| --- | --- | --- | --- |
| 4 | **「新建采购单」强制要求选择「缺料分析」，而系统里没有可选项** | `?new=1` 表单字段为「缺料分析 * / 采购订单号 * / 供应商 * / 目标仓库 / 期望到货日期 * / 付款条件 * / 付款方式 * / 备注」，其中「缺料分析」下拉只有占位项、无任何选项；没有其它采购原因（如库存补充）可选 | 在没有任何已完成缺料分析的环境里，**无法通过「新建采购单」创建采购订单**；结合问题 1，等于采购订单→到货→检验→入库这条主链在 Forge 里无法从零办理 |

---

## 第 2 段：借出归还（库存管理 → 调拨与借出）

### 实际办理

1. 承接上一轮：借出单 **LOAN-1789726968100**（借出中，RM-PLC-1215C × 1，贯联验证原料仓 → 装配一组）→ 行操作 **确认归还** → 确认执行。
   - 结果：toast「LOAN-1789726968100 借出物料已归还并写入归还流水」，状态 **已归还**。
2. 数据核对（同一 SQLite）：流水 `LOAN-1789726968100-RETURN` / loan_return / inbound / 1 / 1007 → 1008；贯联验证原料仓 RM-PLC-1215C 余额 1008。
   - 结论：**借出归还闭环正确**（出库 1008→1007、归还 1007→1008）。

## 第 3 段：采购入库链（到货通知 → 到货登记 → 待检验库存 → 检验单 → 采购入库 → 库存流水）

### 实际办理

1. **到货通知**（`page_purchase_arrival_notice`）：页面为「待到货队列」，共 18 条通知全部「已全部到货」，每行只有图标「查看」。
2. **到货登记**（`page_purchase_arrival_workspace`）取 **ARR-2026-0016**（图纸关联仓 · 锐联钣金委外厂 · PO-PRE-20260913170230 · 待检验）→ 查看。
   - 详情有步骤条「到货登记 → 待检库存 → 检验单 → 入库单」和「查看待检库存」入口。
3. **待检验库存**（`page_pending_inspection_workspace`）→ 找到 **PIN-2026-0012**（检验中）。
4. **检验单**（`page_purchase_inspection_workspace`）→ **IQC-2026-0013** 详情：记录方式「汇总数量录入」、合格数量默认 1、检验结论文本；填写「检验日期 2026-09-18 / 合格 1 / 不合格 0 / 结论：审计走查：外观与功能检验合格，允许入库」→ **完成检验**。
   - 结果：状态 已完成、结果 合格。
5. 详情「下一步操作 采购入库」→ **采购入库** 表单（预填供应商、采购订单、入库数量 1、仓库 图纸关联仓）→ **提交审批** → 确认办理。
   - 结果：生成 **PIN-2026-0017**（状态 待审批）。
6. 审批：**审批通过**（需先填审批意见）→ **执行入库** → 确认办理。
   - 结果：PIN-2026-0017 已入库；流水 `PIN-2026-0017-001` purchase_inbound / 1 / 15 → 16（图纸关联仓）；到货单 ARR-2026-0016 状态 stocked；采购订单 PO-PRE-20260913170230 变为 已完成、inbound_quantity=1、明细 inbound_quantity=1。
   - 结论：**采购入库主链闭环正确**（含订单/明细/到货单回写）。

### 发现的问题

| # | 问题 | 证据 | 影响 |
| --- | --- | --- | --- |
| 5 | **待检验库存列表「关联检验单」列显示内部记录 ID** | PIN-2026-0012 行显示 `G7EZwd3qagbdNr0j`（IQC-2026-0013 的 id）、PIN-REP-2026-0011 行显示 `lrWHhpbiEZdiRfW6`（IQC-REP-2026-0011 的 id） | 用户无法从列表识别检验单，只能点进去看 |
| 6 | **采购入库明细行「物料名称」被写成「检验单号 待检单号 物料名」** | 入库明细显示「IQC-2026-0013 PIN-2026-0012 800型柔性线控制柜采购明细」（DB `forge_purchase_inbound_line.name` 同值）；不合格处理页同样出现「IQC-2026-0010 PIN-2026-0010 PLC CPU 1215C」「NCR-SR-…-01 电控柜门板」 | 单据上的物料名被污染，报价/对账/打印都会带出错误名称；同一模式至少影响入库明细、不合格处理 |
| 7 | **采购入库「审批通过」的审批意见输入框藏在页面右上角** | 页面正文「审批意见 —」不可编辑；真正的输入框是标题右侧 168×36 的行内小输入框；不填点「审批通过」只提示「请填写审批意见」，不指出输入位置 | 审批人按正文填意见会一直卡住 |
| 8 | **到货通知/到货登记/检验单列表行操作几乎只有「查看」** | 到货通知 18 行均只有 查看；到货登记各行只有 查看；检验单各行只有 查看 | 与 RISEMAP「列表行可继续办理」的习惯不一致，办理必须进详情页 |

## 第 4 段：库存盘点（库存管理 → 库存盘点）

### 实际办理

1. `page_inventory_count` → **新建盘点**：名称「审计走查：原料仓盘点」、类型 全盘、仓库物料「贯联验证原料仓 · RM-PLC-1215C · 账面 1,008」、实盘数量 1007、原因「审计走查：盘点差异验证」→ 下一步 → 确认创建。
   - 结果：生成 **CHK-1789727445303**（草稿，toast 正常提示）。
2. 行操作 **完成盘点** → 确认完成。
   - 结果：状态 已完成，差异项 1，盘差 **-1 件**。
3. 数据核对：流水 `CHK-1789727445303-001` count_loss / outbound / 1 / 1008 → 1007；盘点单 system 1008 / actual 1007 / variance -1；余额 1007。
   - 结论：**盘点闭环正确**。

### 说明

- 盘点页与库存锁定/预警页不是同一实现（`inventory-count.page.ts`），所以第 5 段的崩溃不影响盘点。

## 第 5 段：报损单（库存管理 → 报损单）

### 实际办理

1. `page_inventory_loss` → 空态（0 条）→ **新建报损**：仓库「贯联验证原料仓」、类型需选。
2. 类型下拉**只有占位项「请选择」**（DB `forge_inventory_damage_type` 0 条）→ 只能点 **快捷新增** 建类型「审计走查：商品损坏」。
3. 关联库存 bug：创建类型后当前单据的「报损类型」**没有自动回填**，需要再手动选一次。
4. 添加物料 RM-PLC-1215C × 1（成本 ¥192.84）→ 提交审核 → 确认提交。
   - 结果：生成 **DMG-2026-27546284**（待审批）。
5. **审核并扣库** + 审批意见 → 确认审核扣库。
   - 结果：已完成；流水 `DMG-2026-27546284-001` damage_out / 1 / 1007 → 1006；余额 1006。
   - 结论：**报损闭环正确**。

### 发现的问题

| # | 问题 | 证据 | 影响 |
| --- | --- | --- | --- |
| 9 | **报损类型主数据为空** | `forge_inventory_damage_type` 0 条；类型下拉只有占位项。页面描述却写「商品损坏、丢失、盘亏报损单据」 | 每张报损单都要现场建类型，主数据缺失；与仓库类型（5 条）、其他入库类型（6 条）的初始化程度不一致 |
| 10 | **快捷新增后不回填当前单据字段** | 建完类型后「报损类型」仍显示「请选择」 | 多一步无效操作 |

## 第 6 段：库存锁定 + 库存预警（P0 崩溃，已修复）

### 实际办理与问题

| # | 问题 | 证据 | 影响 |
| --- | --- | --- | --- |
| 11 | **「新增业务锁库」点「下一步」整页崩溃** | `page_inventory_locks` → 新增业务锁库 → 选仓库物料/数量/原因 → 下一步 → 页面变「React page error / TypeError: Cannot read properties of undefined (reading 'length')」，控制台堆栈 `App (eval … :38:1098)`；连续复现 2 次，截图 `walkthrough-20260918/38-lock-crash.png` | 无法创建任何锁库单 |
| 12 | **「物料预警配置」点「下一步」同样崩溃** | `page_inventory_alerts` → 物料预警配置 → 选物料 → 下一步 → 同一错误、同一位置 | 无法新增预警配置 |

**根因**：`src/pages/inventory-operations.page.ts` 的「预警设置」弹窗子节点在每次渲染时都会求值，其中 `dialog.notification_positions.length` 对非 policy 弹窗（锁库/预警配置，`step === 'confirm'`）没有该字段 → 抛错。
**修复**：改为 `(dialog?.notification_positions||[]).length` / `(dialog?.notification_positions||[]).join('、')`。修复后 lock 与 alerts 的下一步、确认创建、确认锁库、释放、预警保存全部可正常办理（`pnpm build` 通过）。

### 修复后验证

1. 新建业务锁库 → 确认创建 → **LOCK-1789727757674**（草稿）→ 确认锁库 → 生效中；余额 reserved 2 / available 1004。
2. 行操作 **释放库存** → 已释放；剩余锁定数量回到 0。
3. 物料预警配置 → 最低库存 8 → 确认保存 → 列表阈值更新为「最低 8」。
   - 结论：**锁库 + 释放闭环正确**。

### 其他发现

| # | 问题 | 证据 | 影响 |
| --- | --- | --- | --- |
| 13 | 锁库弹窗 ARIA 标签与可见字段名不一致 | 可见字段是「仓库物料 / 数量 / 业务日期 / 原因」，`aria-label` 分别是「库存作业物料 / 库存作业数量 / 库存作业日期 / 库存作业原因」 | 无障碍朗读与自动化选择器失真 |
| 14 | 预警「呆滞天数」历史值异常且无上限校验 | 委外发料仓 · 冷轧钢板的余额记录 `slow_days = 36500`，配置弹窗原样带出「呆滞 36,500 天」 | 异常数据无校验，呆滞预警会失效 |

## 第 7 段：采购退货退款链（不合格来源处置 + 采购退换货）

### 实际办理

1. 不合格处理（`page_inventory_ncr`）共 21 条：来料检验来源（IQC-…，处置方案「待来源处理」）与委外回厂来源（NCR-SR-…，点「办理」跳到委外不合格处理页）。
2. 采购退换货（`page_purchase_return`）→ **新建退换货**：处理方式 退货退款、供应商「南京锐联电气技术有限公司」、关联采购订单 **PO-2026-0001**、退回物料 RM-PLC-1215C-0001 × 1（可办理 1）、原因「审计走查：来料外观不良，走采购退货退款」→ 保存草稿 → 确认提交。
   - 结果：生成 **PR-1789727904038**（草稿）。
3. 行内「提交退货申请」→ 待审批 → 填「审批意见」后「财务审批通过」→ 已审批 → 填「仓库意见」后「仓库确认退回」→ 仓库已确认 → 填「出库意见」后「完成退货出库」→ 待退款。
4. 数据核对：流水 `PR-1789727904038-001` purchase_return_outbound / outbound / 1 / 1006 → 1005；余额 1005。
5. 选退款账户 + 退款流水号 + 实退金额 6800 + 银行流水号 → **登记供应商退款**。
   - 结果：已完成，「已收款 · ¥6,800.00」。
   - 结论：**采购退货退款全链闭环正确**（草稿→提交→财务审批→仓库确认→出库扣库→登记退款）。

### 发现的问题

| # | 问题 | 证据 | 影响 |
| --- | --- | --- | --- |
| 15 | **退款账户下拉同名重复，无法区分** | 19 个资金账户里「浏览器预付款与退款账户」×5、「预付款与供应商退款账户」×4、「销售主链当前库回款账户」×3、「当前库采购退换货验收账户」×3；下拉直接列出全部同名项 | 退款选错账户会直接记到大账上，无仓库/编号等区分信息 |
| 16 | 列表中「财务审批通过 / 仓库确认退回 / 完成退货出库 / 登记供应商退款」默认禁用 | 按钮需先填同行内「审批意见/仓库意见/出库意见/退款信息」才可用，禁用时无提示说明原因 | 用户看到灰按钮不知道缺什么 |

## 第 8 段：主数据重复问题（跨模块）

| # | 问题 | 证据 | 影响 |
| --- | --- | --- | --- |
| 17 | **仓库主数据同名同码重复** | `forge_warehouse`：委外发料仓 ×3（code 均为 SC-WH-ISSUE）、委外回厂仓 ×3（code 均为 SC-WH-RECEIPT），创建时间 09-12/09-13 各两次 | 调拨、锁库、盘点、报损选仓时出现同名条目且账面数量不同（如委外回厂仓 · RM-PSU-24V10A 同时有 1,000 与 977），必须靠序号猜测；库存被分散在不同仓记录上 |
| 18 | **资金账户主数据同名重复** | 见问题 15 | 同上 |
| 19 | 采购订单/物料名称污染 | 见问题 6 | 同上 |

---

## 第 9 段：出库管理 7 页 + 其他出库闭环

### 逐页事实

| 页面 | 当前表现 |
| --- | --- |
| 出库单列表 | 混合展示销售出库、采购退货出库等；工具栏为 打印条码/导出/筛选 |
| 生产出库 | 组装出库 + 委外出库列表；行操作 查看/来源单；「下一步操作 生产入库」 |
| 其他出库 | 原为空态；本轮完整跑通新建（下方） |
| 销售直接出库 | 4 条已完成记录，出库进度 100% |
| 待出库发货单 | 空态「销售发货单全部完成出库后，这里不再显示」 |
| 采购退换货出库 | 我们的 PR-1789727904038 1/1 100% 已完成 |
| 出库明细 | 逐行展开各出库流水（含锁库、报损、盘亏） |

### 其他出库完整办理

1. 新建其他出库 → 出库类型下拉只有占位项（`forge_other_outbound_type` 0 条）→ **新增类型**「审计走查：样品领用」。
2. 添加物料行（仓库 贯联验证原料仓、RM-HMI-700 × 1、成本 ¥115.44）→ 填发货方式快递与物流信息 → 提交审批 → 确认提交。
   - 结果：**OUT-2026-28120197**（待审批）。
3. 审批通过（填审批意见）→ 确认出库 → 确认扣库出库。
   - 结果：已出库；流水 `OUT-2026-28120197-001` other_outbound / 1 / 1004 → 1003。
   - 结论：**其他出库闭环正确**。

### 发现的问题

| # | 问题 | 证据 | 影响 |
| --- | --- | --- | --- |
| 20 | **其他出库类型主数据为空** | `forge_other_outbound_type` 0 条；类型下拉只有占位项 | 与报损类型同样的问题：每张单都要现场建类型 |
| 21 | **锁库被当成出库流水与出库明细** | 流水 `LOCK-1789727757674-LOCK` direction=outbound、before=after=1006；出库明细页把它列为「库存锁定 · 已出库 · ¥385.68」 | 锁库不改变在库数量，却进入出库口径；出库明细/统计会被污染 |
| 22 | **出库明细「业务类型」列显示原始枚举** | 出现 `count_loss`（应「盘亏出库」）、`other_outbound` 等同列中英文混杂 | 列表可读性差 |
| 23 | 出库明细「单位/不含税单价/税率」多为「—」 | 由流水推导的列缺少字段 | 明细不完整 |

## 第 10 段：基础资料 7 页

### 逐页事实与操作

| 页面 | 结果 |
| --- | --- |
| 物料管理 | 8 条物料，编辑/停用/删除、导入导出、打印条码、批量操作、回收站入口齐全 |
| 物料组合 | 原为空态；本轮创建 **BND-1789728166516**（勾选 2 个物料）成功 |
| BOM管理 | 3 条 BOM（1 草稿 2 已生效），新建BOM/导入导出/BOM对比入口齐全 |
| 综合物料搜索 | 单件查询 + 批量清单分析两个模式 |
| 供应商管理 | 8 条（含重复，见问题 24） |
| 产品实例追溯 | 以物料为主键列出条码，行操作 预览/复制条码/打印 |
| 仓库管理 | 8→9 条；本轮创建 **审计走查：临时验证仓2** 成功 |

### 发现的问题

| # | 问题 | 证据 | 影响 |
| --- | --- | --- | --- |
| 24 | **供应商主数据同码同名重复** | `forge_supplier`：SC-SUP-001 锐联钣金委外厂 ×3、SC-SUP-002 未审批委外厂 ×3；供应商下拉、询价邀请供应商、价格本供应商下拉都出现重复项 | 选供应商时无法区分，报价/下单可能挂到不同记录 |
| 25 | **仓库类型主数据重复同码** | `forge_warehouse_type`：SC-WH-TYPE 委外仓库 ×3（另有 1 条空 code 的「原料仓」）；新增仓库的仓库类型下拉出现 3 个「委外仓库」 | 无法区分 |
| 26 | **BOM管理「创建人」列显示原始用户 ID** | 列为 `GERJhSCEXm8uli22hqXrKZmHbpIrB4o3`，其它页面同一用户显示「Dev Admin」 | 列表不可读 |
| 27 | **新增仓库：负责人未标必填但服务端必填，且只提示「HTTP 400」** | 只填名称/类型/电话/面积/地址 → 创建失败，页面提示「HTTP 400」；接口返回 `{"error":"负责人不能为空","fields":[{"field":"responsible_id"...}]}`。补选负责人后创建成功 | 用户无法知道失败原因（服务端字段级错误未展示到页面） |
| 28 | **弹窗高于视口时底部按钮不可达** | 1280×900 下「新建物料组合」弹窗高 1395px，`.fp-modal{max-height:none;overflow:visible}`，「创建组合」按钮在 y=1102；窗口与弹窗都无法滚动（modal.scrollTop 恒为 0），只能把视口拉到 1500 才能点。截图 `55-combo-modal-viewport.png` | 常规笔记本屏幕无法完成物料组合创建，也无法取消 |

## 第 11 段：入库管理 6 页

| 页面 | 事实与问题 |
| --- | --- |
| 全部入库单 | 采购入库/免检入库等混合列表；行操作 查看详情 |
| 采购入库 | 本轮 PIN-2026-0017、PIN-2026-0018 均在此链路完成 |
| 生产入库 | 47 单、合格入库 47、金额 ¥624,660；「更多 ▾」内含入口 |
| 其他入库 | 有历史「停用验收类型 …」已取消单（早期测试残留） |
| 期初入库 | 行操作 查看详情/删除；对已完成单点「删除」正确弹出业务阻断「该单据状态为“已入库”…只能查看详情」 |
| 入库明细 | 见问题 29、30 |

| # | 问题 | 证据 | 影响 |
| --- | --- | --- | --- |
| 29 | **入库明细「物料名称」污染 + 单位缺失** | PIN-2026-0016 行显示「IQC-2026-0012 默认规格」，单位列为「—」 | 与问题 6 同源（记录 name 拼接了单号） |
| 30 | 检验单/入库单的物料名称污染源头在记录 name | DB：`forge_purchase_inspection.name` = `IQC-2026-0013 PIN-2026-0012 800型柔性线控制柜采购明细`、`IQC-2026-0015 PLC CPU 1215C`；`forge_purchase_inbound_line.name` 同值 | 该字段被多个页面当作「物料名称」直接渲染 |

## 第 12 段：采购剩余页面

### 询价管理（完整闭环）

1. 新建询价单 → 手动创建 → 标题「审计走查：PLC 备件询价」→ 确认并编辑物料 → **RFQ-2026-0002**。
2. 添加物料 RM-PLC-1215C-0001 × 3；邀请供应商 南京锐联电气技术有限公司 → **发布询价**。
3. **登记报价**：含税单价 6700、交期 15 天、付款条件「到货后30天」→ 已报价 ¥20,100.00。
4. **选择中标** → 确认中选 → **转采购单**（目标仓库 贯联验证原料仓）→ 生成 **PO-2026-0003**（待审核）。
5. 采购订单详情 **审核订单**（填审批意见）→ 已审核，**自动生成到货通知 PO-2026-0003-AN-001**。
   - 结论：**询价 → 采购订单 → 审核 → 到货通知 闭环正确**（这是当前唯一能从零走通到采购订单的正常路径）。

| # | 问题 | 证据 | 影响 |
| --- | --- | --- | --- |
| 31 | 询价物料「型号 / 规格」为空 | RFQ-2026-0002 物料行显示 RM-PLC-1215C-0001 · PLC CPU 1215C，型号/规格列「—」，而该 SKU 有「CPU 1215C DC/DC/DC / 默认规格」 | 询价单信息不完整 |
| 32 | 询价转单的采购订单「采购原因」= 库存补充 | PO-2026-0003 采购原因「库存补充」 | 未体现来源为询价，无法按来源追溯 |

### 供应商价格本（完整闭环）

1. 新建价格本「审计走查：南京锐联电气 2026 价格本」，供应商 南京锐联电气技术有限公司，一级折扣 5% → **PB-1789728290572**（草稿）。
2. 添加物料价格：RM-PLC-1215C 目录价 ¥6,800 → 净价 ¥6,460.00（5% 折扣正确）。
3. **启用价格本** → 生效中，条目转只读，状态记录留痕。
   - 结论：**价格本闭环正确**。

### 采购发票（收票登记）

1. 采购发票页「邮件收票」→ 明确提示未配置收票邮箱，引导「查看待登记入库单」。
2. 待登记面板列出待登记入库单 → 打开入库单 → **登记采购发票**（登记编号/发票号码/开票日期/应付日期）→ 成功。
   - 结果：PIN-2026-0010 详情出现发票行 PI-PIN-2026-0010 / 32002609180009 / ¥6,800.00；DB `forge_accounts_payable.invoice_id` 已关联。
   - 结论：**收票登记 → 应付关联正确**。

| # | 问题 | 证据 | 影响 |
| --- | --- | --- | --- |
| 33 | 采购发票页无直接登记入口 | 「待登记」面板只能跳回采购入库单，再点「登记采购发票」 | 多一次跳转，且在发票页看不到表单 |
| 34 | 待登记面板把未完成的入库单也列出 | 面板含 PIN-2026-0010 等（已入库），但也混入其它状态单据 | 待办口径不清晰 |

## 第 13 段：检验规则

1. 检验项目库：原有 1 条；本轮**新建检验项目**「审计走查：包装与标签核对」成功（IQI-1789728588513）。
2. 检验方案：原有 2 条；本轮**新建检验方案**「审计走查：PLC 全检方案」（适用范围 RM-PLC-1215C-0001、勾选 2 个检验项目）成功（IQP-1789728609747）。

| # | 问题 | 证据 | 影响 |
| --- | --- | --- | --- |
| 35 | **检验单绑定错误的检验方案，导致「逐项录入」不可用（根因明确）** | 检验 RM-PLC-1215C-0001 的 IQC-2026-0015 绑定「全检方案 FG-RM-CAB-800-0001」（该方案 0 个项目）；切到「逐项录入」显示「未匹配到检验方案项目」。库里存在 `来料全检方案-PLC CPU 1215C`（scope material RM-PLC-1215C，1 项）却未被选用。代码 `src/pages/purchase-inspection-workspace.page.ts:116`：`planRows.find(x=>String(x.scope_value)===String(record.item_code)) || planRows.find(x=>x.scope_type!=='material') || planRows[0]` —— 方案 scope_value 存的是**物料编码** `RM-PLC-1215C`，检验单 item_code 是 **SKU 编码** `RM-PLC-1215C-0001`，永远匹配不上，于是回退到列表第一条 | 逐项录入（含实测值、备注）无法使用；检验按错误方案的判定规则执行 |

## 第 14 段：库存管理其余页面

| # | 问题 | 证据 | 影响 |
| --- | --- | --- | --- |
| 36 | **库存总览 KPI 口径互相矛盾** | 顶部「37 个仓库 SKU」「9 个仓库」，底部「SKU 总数 8」；各仓库分组只列 4 个仓库名（同名委外仓被合并显示） | 用户无法判断到底有多少 SKU/仓库 |
| 37 | **库存流水「业务类型」显示原始枚举、「关联单号」显示内部 ID** | 同表出现 `other_outbound`（应「其他出库」）与中文「采购入库」；OUT-2026-28120197 行的关联单号是 `eUrfFTIMFKmtmonh`（内部 id） | 同问题 5、22 |
| 38 | 处置执行中心「物料」列名称污染 | 显示「PR-1789727904038 默认规格」「NCR-SR-…-01 电控柜门板」 | 同问题 6 |
| 39 | SN码管理只有 1 条且来源是早期入库单 | SN2609092289-00001 / IN-2026-0001 | 本轮其它入库（含本次 PIN-2026-0018）未产生 SN，采购入库与 SN 登记未打通（待复核是否属设计） |

## 本轮修复的一处代码

- `src/pages/inventory-operations.page.ts`：预警设置弹窗子节点提前求值导致 `dialog.notification_positions.length` 抛错 → 改为 `(dialog?.notification_positions||[])`。修复后库存锁定、库存预警可正常办理（`pnpm build` 通过）。
- 该修复尚未提交 git（工作区改动）。

## 证据文件（docs/evidence/walkthrough-20260918/）

01–20 采购/检验/入库截图，21–25 盘点，26–33 报损，34–40 锁定与释放，41–42 不合格处理，43–49 采购退货退款，50–51 其他出库，52–58 物料组合/仓库，59 期初入库删除阻断，60–61 价格本，62 询价，63–64 到货登记与逐项录入，65–66 检验规则。

---

# 修复轮次（2026-09-18 下午）

针对走查结论里的 **1（采购主链断点）3（检验方案错配）4（弹窗溢出）5（仓库必填与错误提示）**，加上**主数据重复清理**和**报损/其他出库类型初始化**，共六项。

## 修复 1：待办池草稿采购订单无法提交审核

- **根因**：服务端动作 `purchase_order_submit`（`src/actions/procurement.action.ts:163`）本来就存在且会重新校验并回填 `line_count/total_quantity/total_amount`；缺的是**页面入口**——`purchase-order-workspace.page.ts` 只在 `status==='pending_approval'` 渲染「审核订单」，草稿状态没有任何按钮，列表里只有提示文字「待审核生成通知」。
- **改动**（`src/pages/purchase-order-workspace.page.ts`）：
  1. 新增 `submitOrder()` 调用 `POST /api/v1/actions/forge_purchase_order/purchase_order_submit/<id>`；
  2. 详情工具条在 `draft` 时显示 **提交审核**（带确认弹窗，说明“审核通过后自动生成到货通知”）；
  3. 列表行操作在 `draft` 时显示 **提交审核** 按钮，其他状态提示改为按状态区分（`待审核后生成通知` / `待生成到货通知`）；
  4. 详情头部项数改为按实际明细行显示，避免 `line_count` 未回填时显示「0 项物料」。
- **验证**（内置浏览器 + 同一 SQLite）：
  - PO-2026-0002（待办池生成）→ 提交审核 → 状态 **待审核**，toast 正常，`line_count=1 / total_quantity=3 / total_amount=20400` 已回填；
  - 详情头部显示「1 项物料」「0 / 3」；
  - 审核订单 → **已审核**，自动生成到货通知 **PO-2026-0002-AN-001**；
  - 继续办理：登记到货 → **ARR-2026-0022** → 检验 → **IQC-2026-0016** → 采购入库 → **PIN-2026-0018**（库存 1005→1008）→ 自动生成应付 AP-PIN-2026-0018 ¥20,100。
  - 结论：**采购申请 → 待办池 → 采购订单 → 审核 → 到货 → 检验 → 入库 → 应付 整条主链已连通**。

## 修复 3：检验单绑定错误检验方案

- **根因**：`purchase-inspection-workspace.page.ts:116` 用
  `planRows.find(x=>String(x.scope_value)===String(record.item_code)) || planRows.find(x=>x.scope_type!=='material') || planRows[0]`
  匹配方案。方案的 `scope_value` 存**物料编码**（`RM-PLC-1215C`），检验单的 `item_code` 是 **SKU 编码**（`RM-PLC-1215C-0001`），永远匹配不上，于是回退到方案列表第一条（`全检方案 FG-RM-CAB-800-0001`，0 个项目）。
- **改动**：按 SKU 编码 → SKU id → 物料编码 → 物料 id 逐级匹配，再回退到非物料范围方案；都匹配不到时**不再强行绑定**（页面显示「未匹配到启用中的检验方案」）。
- **验证**：IQC-2026-0015（物料 RM-PLC-1215C）现在绑定「审计走查：PLC 全检方案」，显示 **2 项**；新建检验单 IQC-2026-0016 切到「逐项录入」后逐项可录（检验结果/实测值/备注），录入 1 项不合格后 结果=部分合格、合格 2 / 不合格 1，并在不合格处理页生成对应行（IQC-2026-0016 · 1 件 · 待来源处理）。

## 修复 4：弹窗高于视口时底部按钮不可达

- **根因**：`product-ui.ts` 的 `.fp-modal{overflow:visible}` 且无 `max-height`，内容超过视口时窗口与弹窗都无法滚动，底部按钮（如「创建组合」）落在视口外。
- **改动**：`.fp-modal-backdrop` 允许纵向滚动；`.fp-modal` 改为 `display:flex;flex-direction:column;max-height:calc(100vh - 48px);overflow:hidden`；`.fp-modal-header/-footer` 固定不压缩；`.fp-modal-body` `flex:1;min-height:0;overflow-y:auto`。
- **验证**（1280×900）：新建物料组合弹窗高度 852px、顶部 24px、底部按钮 y=831（可见可点），内容区内部滚动（内容 1257px / 可视 714px）；成功创建 **BND-1789729326089**。

## 修复 5：新增仓库缺必填标记、失败只显示「HTTP 400」

- **根因**：`forge_warehouse.responsible_id` 服务端必填，但页面未标必填；且请求助手只读 `p.error.message`，而服务端错误体是 `{"error":"负责人不能为空","fields":[...]}`（`error` 是**字符串**），取不到消息就退化成 `HTTP 400`。
- **改动**：
  1. `warehouse-workspace.page.ts`：负责人字段加必填标记 + 提交前校验「请选择负责人」；
  2. 错误消息解析兼容字符串 `error` 与字段级 `fields[]`。
  3. **同类问题批量修正**：全项目 98 个页面文件、107 处相同写法统一改为可解析 `error` 字符串与 `fields[]`（`payload.error?.message||payload.message||'请求失败'` 等三种模式）。这属于纯错误消息回退增强，成功路径行为不变。
- **验证**：表单显示「负责人 *」，不填提交提示「请选择负责人」；对真实 400 响应，原表达式返回 `HTTP 400`，新表达式返回 `负责人不能为空`。

## 主数据去重

- **备份**：`.objectstack/backups/dedupe-20260918/objectstack-before-dedupe.db`（VACUUM INTO 一致性快照）。
- **处理**（保留引用最多的记录为规范记录，改指全部外键，合并库存余额后删除重复行）：
  - 仓库 `委外发料仓`：3 → 1（删除无引用的 xLN…；把 yhn… 的余额与引用并入 uphh…，余额 `GoCVwjui68lERUgR` 410+5.9=**415.9**，其余各 SKU 1000+1000=2000）；
  - 仓库 `委外回厂仓`：3 → 1（保留引用最多的 MPY…，并入 Rue2…/y6r… 的余额与引用，重构 108+77 处引用）；
  - 仓库类型 `委外仓库`：3 → 1；
  - 供应商 `SC-SUP-001 锐联钣金委外厂`：3 → 1（改指 14+3 处引用）；`SC-SUP-002 未审批委外厂`：3 → 1（无引用，直接删重复）。
- **结果**：仓库 9 → **5**、仓库类型 5 → **3**、供应商 8 → **4**；`forge_inventory_balance` 不再存在同仓同 SKU 多行；无悬挂外键（仓库类型、供应商均已校验）。
- **资金账户**：核实后判定**不是重复数据**（同名但编号与流水不同，如 FA-PR-CURRENT-20260913205628），因此**未删除**；改为在采购退换货「退款账户」下拉显示「账户名 · 账户编号」消除歧义，已验证 20 个同名账户可区分。

## 报损 / 其他出库类型初始化

- **根因**：`scripts/seed-reference-data.mjs` 把这两类写进 `forge_inventory_business_setting`（`category=damage_type / other_outbound_type`），而**页面读的是专用表** `forge_inventory_damage_type` / `forge_other_outbound_type`，所以下拉一直为空。
- **改动**：seed 脚本补充专用表的起步类型（同时保留原有 business_setting 写入）：
  - 报损类型：商品损坏、物料丢失、盘亏、生产报损；
  - 其他出库类型：样品出库、研发领用、报废出库、其他。
  - 说明：RISEMAP 报损单/其他出库当前无同材料可对照数据，以上为 **Forge 产品决策**，待 RISEMAP 复核。
- **验证**：新建报损类型下拉 `[商品损坏, 生产报损, 物料丢失, 盘亏]`；新建其他出库类型下拉 `[样品出库, 研发领用, 报废出库, 其他]`。

## 门禁与残留

- `pnpm typecheck`、`pnpm validate`（Validation passed）、`pnpm build`（navigation-linkage PASS）均通过。
- `pnpm validate` 中对 `page_bom_workspace`、`page_inventory_sn` 的「source did not parse」告警经 stash 对照确认为**改动前既有**问题，与本轮无关。
- 本轮未处理（仍记录在案）：快捷新增类型后不回填当前单据、锁库计入出库明细、关联单号显示内部 ID、业务类型显示英文枚举、名称污染（记录 name 拼接单号）、库存总览口径不一致、SN 未与采购入库打通等。
- 工作区改动未提交 git。

---

# 第二批修复（2026-09-19）

走查残留 7 项全部处理，并在内置无头浏览器逐项验证；RISEMAP 实时对照本轮未完成（见文末阻塞）。

## 1. 快捷新增类型后不回填当前单据

- `inventory-damage.page.ts` / `other-outbound-workspace.page.ts` 的 `createType()` 原来只 POST + `load()`，创建结果被丢弃。
- 改为接住创建返回（兼容 `id` / `record.id` / `data.id` 三种返回）并 `setForm` 回填 `damage_type_id` / `outbound_type_id`。
- 验证：报损「快捷新增」→ 关闭弹窗后报损类型直接显示新类型「审计走查：回填验证类型」；其他出库同样回填「审计走查：出库回填验证」。

## 2. 锁库被计入出库明细

- 根因：锁库/释放会写库存流水（`inventory_lock` 标 outbound、`inventory_release` 标 inbound），而 `on_hand` 前后不变，出库明细按 `direction==='outbound'` 取数，于是锁库被当成出库。
- 改动：
  - `product-ui.ts` 共享运行时常量新增 `forgeNonStockMovements=['inventory_lock','inventory_release']` 与 `forgeMovementTypeText` 全量动因中文映射。
  - 出库明细页排除非库存变动动因。
  - 库存流水页：类型列对锁库/释放显示「占用 / 释放」（不再显示入库/出库），并在月度出入库汇总中排除这两类。
- 验证：出库明细不再出现 `LOCK-1789727757674-LOCK`；库存流水显示 `LOCK-1789727757674-LOCK | 占用 | 库存锁定`、`…-RELEASE | 释放 | 库存释放`。

## 3. 关联单号显示内部 ID

- 待检验库存：列表只加载 `forge_pending_inspection`，`inspection_id` 直接渲染成内部 ID。改为同时加载 `forge_purchase_inspection` 并显示 `code`。
- 库存流水：`sourceNames` 缺少 `forge_other_outbound`、`forge_inventory_damage`、`forge_production_inbound`、`forge_replacement_order`、`forge_disassembly_order`、`forge_subcontract_ncr`，导致这些来源回退成 `source_id`。已补齐。
- 验证：待检验库存「关联检验单」显示 `IQC-2026-0001`、`IQC-REP-2026-0011` 等；库存流水 `OUT-2026-28120197-001` 行的关联单号显示 `OUT-2026-28120197`（此前是 `eUrfFTIMFKmtmonh`）。

## 4. 业务类型显示英文枚举

- 出库明细此前缺 `count_loss` 等映射；库存流水缺 `other_outbound`。
- 改为统一走共享 `forgeMovementText` 兜底。
- 验证：出库明细显示「盘亏出库 / 报损出库 / 其他出库 / 采购退货出库」；库存流水显示「其他出库」，不再出现 `other_outbound`、`count_loss`。

## 5. 记录 name 拼接单号造成名称污染

- 根因：待检记录、检验单、入库明细、委外不合格单在创建时用 `单号 + ' ' + 名称` 写 `name`，下游再逐层拼接。
- 创建侧改动（`procurement.action.ts`、`purchase-replacement.action.ts`、`subcontract.action.ts`）：待检记录/检验单/入库明细改用物料名（`noticeLine.name` / `orderLine.name` / `line.name`），委外 NCR 改用 `line.name`。
- 数据侧回填（有备份 `backups/dedupe-20260918`）：对 `forge_purchase_inspection`、`forge_pending_inspection`、`forge_purchase_inbound_line`、`forge_subcontract_ncr` 等表剥离名称前缀的单号片段，共清洗 65 行。
- 处置执行中心的「物料」列改为按退换货明细渲染 `物料编码 · 物料名`，不再显示退换货单的表头名称。
- 验证：采购入库详情/入库明细显示「800型柔性线控制柜采购明细」「PLC CPU 1215C」；不合格处理来料来源显示「RM-PLC-1215C · PLC CPU 1215C」，委外来源显示「SC-FG-001 · 电控柜门板」；处置执行中心显示「RM-PLC-1215C · PLC CPU 1215C」。
- 回归中发现并修复：改动曾误用 `item.noticeLine.name`（该动作作用域里没有 `noticeLine`），导致采购入库提交报 `cannot read property 'name' of undefined`，已改为 `item.orderLine.name`。

## 6. 库存总览口径不一致

- 顶部卡片副标题「N 个仓库 SKU」与底部「SKU 总数 / N 个仓库」读起来互相矛盾（前者是仓库×SKU 余额行数）。
- 改为顶部「22 条仓库库存记录」、底部「SKU 总数 8 / 4 个有库存仓库 / 共 5 个仓库」。
- 验证：页面显示如上，与各仓库分组数量一致。

## 7. SN 与采购入库未打通

- 现状：`forge_purchase_receipt_line.external_sn` 与到货登记动作的 `lines_json.external_sn` 早已支持，但页面没有录入列；入库执行也没有生成 SN 档案。
- 改动：
  - 到货登记表单新增「外部 SN」列（支持逗号分隔多个），随 `lines_json` 提交。
  - `purchase_inbound_stock` 执行入库时按行 `external_sn` 拆分登记 `forge_inventory_serial_number`（写入 sku、入库单号、批次、供应商、在库状态，重复 SN 跳过）。
- 验证（完整链）：采购申请 PR-2026-0002 → 待办池生成 PO-2026-0004 → 提交审核 → 审核通过自动生成 `PO-2026-0004-AN-001` → 到货登记填 SN `SN-AUDIT-0001, SN-AUDIT-0002` → ARR-2026-0023 → 自动生成 IQC-2026-0017 → 完成检验 → 采购入库 PIN-2026-0020（1007.95→1009.95）。
  - SN 码管理出现 2 条记录：`SN-AUDIT-0001/0002 · PIN-2026-0020 · B-SN-20260919-001 · 南京锐联电气技术有限公司 · 在库`。
  - 库存流水该行「外部SN」列显示 `SN-AUDIT-0001, SN-AUDIT-0002`。
- 说明：RISEMAP 侧 `SN码管理` 已观察列包含「入库单号」，但**到货登记是否提供 SN 录入列尚无同材料证据**，本项按 Forge 能力补齐标记，待 RISEMAP 复核。

## 新增残留观察

- 库存流水的日期列显示 UTC（`2026-09-18 16:41`），业务日期是 `2026-09-19`，两侧差 8 小时；属既有时间显示口径问题，本轮未改。
- RISEMAP SN 码管理「产品名称」显示物料名（`800型柔性线控制柜`），Forge 显示「物料名 · SKU 编码」，属呈现差异。

## RISEMAP 实时对照阻塞

- 本轮末尾要按 RISEMAP 侧并排跑一遍：RISEMAP 会话已过期（打开 `https://risemap.cn/purchase/pending-pool` 被跳转到 `/login`），内置浏览器当前不可用（Mac 锁屏 + 浏览器桥接报错），我使用的无头浏览器没有 RISEMAP 登录态。
- 已完成的部分：按项目已保存的 RISEMAP 证据（`docs/evidence/shots-20260918/*-risemap.png`、`risemap-supply-chain-data-readiness-20260917.md`）对照本轮 7 项：
  - 库存流水：RISEMAP「类型」为徽标、「业务类型」为中文可点链接 → 与本轮中文化一致；关联单号可跳转来源单 → 本轮已恢复单号。
  - 待检验库存：RISEMAP「关联检验单」列为检验单号（IQC-…）→ 与本轮一致。
  - 出库明细：RISEMAP 该页当时为空，无业务类型取值可对照。
  - SN码管理：列结构一致（供应商序列号/产品名称/入库单号/批次号/供应商/出库状态/出库单号）。
  - 报损单快捷新增：RISEMAP 存在快捷新增类型入口，但是否回填单据字段无证据。
- 待完成：RISEMAP 登录后按同一材料并排跑一遍（到货登记是否收 SN、快捷新增是否回填、锁库是否进出库口径、库存总览指标口径）。

---

# RISEMAP 并排对照（2026-09-19，用户已登录）

对照方式：内置浏览器桥接不可用（`browsers` 为空、报 `Unable to load browser request-header policy`），改用 Computer Use 驱动用户已登录的 Edge 标签页 `RiseMap`（账号 `金一涛`，租户 `南京汇川技术研发中心有限公司`）。**全程只读**：未在 RISEMAP 提交、保存或新增任何数据，打开过的「新建报损单 / 新建采购到货登记」表单均已取消。

RISEMAP 侧 URL：`/inventory/flow`、`/inventory/pending-inspection`、`/inventory/outbound/details`、`/inventory/lock`、`/inventory/overview`、`/inventory/damage`、`/inventory/arrival`。

## 逐项对照结果

| 本轮修复项 | RISEMAP 事实 | Forge 结果 | 判定 |
| --- | --- | --- | --- |
| 4. 业务类型中文枚举 | 库存流水「业务类型」= 采购入库 / 采购在途 / 期初入库（中文，可点击到来源单） | 采购入库 / 采购退货出库 / 其他出库 / 报损出库 / 盘亏出库 | **一致** |
| 3. 关联单号/关联检验单 | 库存流水「关联单号」是文档号按钮（IN-2026-0003、PO-2026-0002）；待检验库存「关联检验单」是 `IQC-2026-0001` 按钮 | 流水显示 `OUT-2026-28120197`；待检页显示 `IQC-…` | **一致**（RISEMAP 为按钮，Forge 为文本 + 行操作「查看检验单」） |
| 5. 名称污染 | 流水「物料名称」= PLC CPU 1215C（无单号前缀） | 采购入库/入库明细/不合格处理/处置执行中心均为纯物料名 | **一致** |
| 6. 库存总览口径 | 首卡「库存总采购成本 ¥80,283.19 / **5 种物料**」，另有 预估销售总价、毛利率 47.3%、**SKU总数 5**、账面总量 9；「各仓库含税成本总价」按仓库分组；「库存动态」含 调拨出库/调入归还/报损扣减 本月值 | 已按同结构改为「¥2,513,735.67 / **8 种物料**」+「SKU 总数 8 / 4 个有库存仓库 / 共 5 个仓库」 | **对齐**（Forge 首卡标签仍为「库存含税成本总价」，RISEMAP 为「库存总采购成本」；口径不同，保留标签差异） |
| 1. 快捷新增类型后回填 | 报损单新建表单有「**快捷新增报损类型**」；弹窗字段 类型名称* / 描述 / 标识颜色 + 取消 / 添加 | 已改为新增后回填字段 | **未验证**：RISEMAP 侧要确认回填必须真的新增一个类型（写线上数据），本轮未做 |
| 2. 锁库是否进出库口径 | 库存锁定页 0 条记录；KPI 为 剩余锁定数量/活跃锁定明细/涉及物料/高锁定率物料；页签 业务锁库/手动锁库/物料锁定概况/锁库操作日志；其库存流水 11 条**全部为入库/在途，无锁库行** | 已把锁库排除出「出库明细」，流水显示「占用/释放」并移出月度出入库汇总 | **结构性一致**：RISEMAP 把锁库放在独立模块且流水无锁库行；Forge 的处理方向一致，但因 RISEMAP 无锁库数据，未能验证「锁库后 RISEMAP 流水是否出现行」 |
| 7. SN 与采购入库 | 库存流水列含 批次/SN码/外部SN（当前数据全为 `-`）；SN码管理列为 供应商序列号/产品名称/入库单号/批次号/供应商/出库状态/出库单号；到货登记新建表单结构含 到货类型/到货日期/送货联系人/联系电话/承运方/物流单号/供应商*/采购订单*/关联单位(客户)/到货物料明细/备注与附件/到货登记摘要 | 到货登记已加「外部 SN」录入口，入库时自动登记 SN 档案并回显到流水 | **未验证**：RISEMAP 采购订单已全部收货，「采购订单」下拉显示「暂无数据」，无法展开其到货物料明细列，因此不知道 RISEMAP 是否有 SN 录入列 |

## 本轮发现的其他 RISEMAP 事实（未在 Forge 复刻，供决策）

| 项 | RISEMAP | Forge 当前 |
| --- | --- | --- |
| 到货通知单号 | `ANO-2026-0001 / 0002` | `PO-2026-0002-AN-001` |
| 待检单号 | `DJ-202609-0001` | `PIN-2026-00xx` |
| 待检验库存状态 | 该行显示「已入库」 | 显示「已检验」 |
| 库存流水内容 | 含「采购在途」行（数量 3、库存余量 0/1，类型=入库） | 无在途流水行，在途只在库存总览 KPI |
| 到货登记表单 | 有「附件上传（拖拽/Ctrl+V，PDF/Word/Excel/图片/压缩包）」与「到货登记摘要」块 | 明确标注附件未接入；无摘要块 |
| 库存流水流水号 | `FL-202609-0011` | `PIN-2026-0020-001` 等来源单+序号 |
| 库存总览首卡标签 | 库存总采购成本 | 库存含税成本总价 |

## 需要用户决定或授权才能继续的两件事

1. **在 RISEMAP 新增一个报损类型**（写线上数据）以确认「快捷新增是否回填当前单据字段」；当前只是我按 Forge 侧体验补齐，RISEMAP 侧未验证。
2. **在 RISEMAP 建一张带 SN 的采购到货单**（写线上数据，且需要先把某采购订单留出未收数量）以确认 RISEMAP 的到货登记是否提供 SN 录入列。

两项都会向 RISEMAP 写入业务数据，按项目规则「不得修改线上 RISEMAP 数据」本轮未执行。若确认可以写，我按同一材料在两侧跑一遍并把截图补进 `docs/evidence/walkthrough-20260918/`。

---

# RISEMAP 写入验证（2026-09-19，用户授权后）

## 规则变更

用户明确授权可修改 RISEMAP 数据后，`AGENTS.md` 的「复刻实施顺序」已改：

- 原「不得修改线上 RISEMAP 数据」删除；
- 新增「**RISEMAP 线上数据允许为双材料对照写入**」：可新建并推进带「审计走查」「对照」等可识别前缀的测试单据，用于验证入口、字段、状态、动作、阻断与反馈；写入前后记录页面、单据号、结果与清理情况；
- 新增边界：不得删除/作废/撤销/改写 RISEMAP 既有历史单据与非测试数据；未获明确授权不得执行真实资金收付、开票、红冲、结算入账；
- 同步修改「RISEMAP 无法完整办理时」那条：先在授权范围内补建对照材料后重跑，确实无法补建才标待复核。

## 本轮写入 RISEMAP 的记录

| 动作 | 对象 | 结果 |
| --- | --- | --- |
| 新增报损类型 | 「审计走查：对照验证类型」 | 已创建（主数据，保留可识别标识） |
| 新建锁库单 | LK-2026-0001（订单锁库 · SO-2026-0001 · 800型柔性线控制柜 1 台 · 7 天） | 已提交并审批通过，当前状态 **锁定中** |
| 新建采购订单 | — | 仅打开表单，未填写完成即取消，未生成单据 |

## 三项验证结果

### 1. 快捷新增类型后是否回填（已确认）

- RISEMAP 报损单 → 新建 → 「快捷新增报损类型」→ 填类型名称 → 点「添加」。
- 结果：弹窗关闭后，单据的「报损类型」字段直接变为 `单选按钮 审计走查：对照验证类型, Value: 1`，即**新建后自动选中**。
- Forge 侧行为与之一致（上一轮已改为创建后回填）。**该差异已消除。**

### 2. 锁库是否计入出库口径（已确认）

- RISEMAP 新建订单锁库 → 提交审批 → 在锁库详情「同意」并填写审批意见 → 生效为「锁定中」。
- 生效后逐页回读：
  - **库存锁定**：KPI 变为 剩余锁定数量 1 / 活跃锁定明细 1 / 涉及物料 1 / 高锁定率物料 1；单据 LK-2026-0001 状态「锁定中」。
  - **库存流水**：记录数 11 → **12**，新增 `FL-202609-0012`，列为「**类型=其他** / 业务类型=**订单锁定** / 关联单号 LK-2026-0001 / 数量 1 台 / 库存余量 1 台」。
  - **出库明细**：仍为「暂无匹配的出库明细」——**锁库不进做出库明细**。
  - **库存总览**：库存总采购成本与账面总量（9）不变——锁库不改变在库数量。
- 结论：锁库**进库存流水但不进出库明细**，且不改变在库数量。Forge 侧「出库明细排除锁库」的修复方向正确。
- 差异（记录为 Forge 产品决策）：RISEMAP 流水「类型」列把锁库归入「**其他**」，Forge 显示「**占用 / 释放**」。Forge 的写法更明确区分占用与释放，且同样不落入入库/出库口径；若要严格逐字对齐，把 `dirText` 改成固定「其他」即可。

### 3. SN 与采购入库（已确认 RISEMAP 有按行 SN）

- RISEMAP 到货登记 → 「物料明细」页签列表列：物料编码 / 物料名称 / 规格型号 / 到货数量 / 主单位数量 / **批次 / SN** / 仓库·库位 / 供应商·客户 / 关联单号 / 到货单号 / 到货日期 / 含税金额 / 状态 / 操作。
- 行取值示例：`B260917-00005` + `无 SN` —— 说明 RISEMAP 按行记录「批次 / SN」，无 SN 时显式显示「无 SN」。
- Forge 侧：到货登记行有「批次」「外部 SN」两列，入库时按行拆分登记 SN 档案，流水回显 外部SN。
- 差异：RISEMAP 把批次与 SN 合并为一列展示并显示「无 SN」，Forge 分两列且无占位说明。功能方向一致，属呈现差异。

## RISEMAP 侧发现的缺陷（本次写入过程中暴露）

| # | 现象 | 影响 |
| --- | --- | --- |
| R1 | **手动锁库的「锁库原因 \*」没有可操作控件** | 手动锁库分支提交时报「请选择锁库原因」，但页面上该必填项没有任何下拉/输入控件（截图见 `walkthrough-20260918/`）；只能改走「订单锁库」 |
| R2 | **「释放原因 \*」同样没有可操作控件** | 批量释放锁定弹窗里「释放原因 \*」必填，但无控件可选，导致「确认并生成释放单」始终置灰，**锁库无法在 RISEMAP 释放** |
| R3 | 到货登记「采购订单」下拉在订单已全部收货时显示「暂无数据」 | 无法为已收完的订单补登记到货，也无法展开明细列（本轮因此改用明细页签取证） |

## 遗留状态与待决策

- RISEMAP 现存我创建的测试数据：报损类型「审计走查：对照验证类型」、锁库单 **LK-2026-0001（锁定中）**。
- 因 R2，该锁库**无法通过 UI 释放**；可选项是「作废锁定单」（属作废/破坏性动作，按规则需用户确认）或先在 RISEMAP 配置一条「释放原因」主数据后再正常释放。
- Forge 侧无新增数据；本轮只改了 `AGENTS.md` 规则与库存总览首卡口径（8 种物料）。

---

# P0–P2 整改（2026-09-19）

## P0 已完成并验证

| # | 改动 | 验证 |
| --- | --- | --- |
| P0-1 | 新增 `purchase_order_create` 动作 + 新建采购单页面支持 RISEMAP 的 5 种采购来源（库存补充/项目采购/以销定采/BOM缺料/采购申请）、手工明细编辑器、订单号可留空自动生成、保存草稿/提交审核 | 建单 **PO-2026-0005 / PO-2026-0006 / PO-2026-0007**；PO-2026-0005 审核后自动生成到货通知 `PO-2026-0005-AN-001` |
| P0-2 | 新增 `purchase_order_sync_rollups` 动作，待办池批量建单后回填物料数/数量/金额 | 将 PO-2026-0004 汇总清零后调用动作，回填为 1 项 / 2 件 / ¥13,600 |
| P0-3 | 报损、其他出库、库存作业（锁定/盘点/调拨/借出）、盘点单号改用 `forgeDocCode` 序号化 | 新建得 **CHK-2026-0001**、**LOCK-2026-0001**（旧时间戳单号保留为历史数据） |

## P1 已完成

| # | 改动 | 验证 |
| --- | --- | --- |
| P1-4 | 待检记录新增 `stocked`（已入库）状态；采购入库执行后把关联待检记录置为已入库 | PIN-2026-0017 由「已检验」变为 **已入库**（页面列显示一致） |
| P1-5 | 采购订单审核通过时写入「采购在途」流水行（`purchase_in_transit`，方向入库、在库数量前后不变），流水动因枚举与中文映射同步补齐 | PO-2026-0007 审核后生成 `PO-2026-0007-TRANSIT-g8ol`（1 件，1009.95 → 1009.95） |
| P1-6 | 到货登记详情新增「到货登记摘要」块；明细「批次」列改为「批次 / SN」并显示「无 SN」占位 | 结构对齐 RISEMAP（附件上传仍为记录在案的未接入项） |
| P1-7 | 释放锁定必须填写释放原因；新增「申请续期」「作废锁定单」两个动作与行操作；续期/作废/释放均在标准弹窗内填写原因并校验 | 代码与门禁通过（未在本次跑完整流程，属待页面复核项） |
| P1-8 | 库存总览首卡标签改为 RISEMAP 的「库存总采购成本」，副标题为「N 种物料」 | 页面显示「库存总采购成本 ¥… / 8 种物料」 |
| P1-9 | 库存流水日期按 Asia/Shanghai 显示（原先按 UTC 少 8 小时） | 同一笔流水由 `2026-09-18 16:41` 变为 `2026-09-19 00:41` |

## P2 已完成

| # | 改动 | 验证 |
| --- | --- | --- |
| P2-10 | 到货通知/到货登记/检验单行操作与 RISEMAP 逐项比对：RISEMAP 到货通知行也只有「查看」，待检验库存行为「查看详情」 | 判定为**已一致**，不做改动 |
| P2-11 | 采购入库「审批通过」由右上角小输入框改为标准审批弹窗（审批意见必填 + 影响说明） | 弹窗显示「审批采购入库单 / 审批意见 * / 审批本身不增加库存…」，确认后审批生效 |
| P2-13 | 询价物料从物料主数据带入型号/规格/单位（原先取 SKU 上不存在的字段，导致空值） | 代码修复，门禁通过 |
| P2-15 | SN码管理「产品名称」只显示物料名（SKU 编码降为次要行），对齐 RISEMAP | 页面结构一致 |

## 仍未完成（连同原因）

| # | 项 | 状态 |
| --- | --- | --- |
| P1-7 残余 | RISEMAP 的「锁库原因/释放原因」是**配置化下拉**，Forge 目前是必填文本；「编辑锁定信息」「查看关联订单」未实现（Forge 锁定单只有来源单号文本，没有可解析的订单引用） | 待做 |
| P2-12 | 采购发票页仍无直接「登记进项发票」入口（需跳转入库单）；待登记面板文案已声明按「已入库且未登记有效发票」过滤，需按当前数据复核 | 待复核 |
| P1-6 残余 | 到货登记附件上传（RISEMAP 支持拖拽/Ctrl+V）仍未接入 | 见证缺口 |
| P1-9 残余 | 其他页面（出库明细、入库明细等）的日期仍按原格式显示 | 待统一 |
| 其他 | 待检验库存「检验结果」列对已完成检验仍显示「检验中」；预处理/发票/发货等页面既有差异 | 待做 |
| RISEMAP 侧 | 手动锁库「锁库原因」与「释放原因」无控件（R1/R2），导致 RISEMAP 锁库无法释放；测试锁库 LK-2026-0001 仍为「锁定中」，等待用户决定作废或先在 RISEMAP 配原因主数据 | 阻塞 |

## 追加修复（同日第二轮）

| 项 | 改动 | 说明 |
| --- | --- | --- |
| 待检验库存「检验结果」列 | 改为按关联检验单结果显示（合格/部分合格/不合格）；已入库不再显示「检验中」 | 修复了新增「已入库」状态后该列错显「检验中」的回归 |
| 库存流水/出库明细日期 | 新增共享助手 `forgeLocalDate` / `forgeLocalDateTime`（Asia/Shanghai），库存流水与出库明细改用 | 消除 UTC 与业务日期差 8 小时 |
| 采购发票页 | 待登记面板每行新增「登记进项发票」（弹窗内填登记编号/发票号码/开票日期/应付日期/备注，直接调用 `purchase_inbound_register_invoice`），并保留「打开入库单」入口 | 不再需要先跳入库单才能登记 |

## 仍未完成（第三轮清单）

| 项 | 状态与原因 |
| --- | --- |
| 锁库原因/释放原因改为「配置下拉」 | 目前是必填文本；RISEMAP 为配置化下拉（主数据已存在 `lock_reason`/`release_reason`），待改为下拉 |
| 「编辑锁定信息」 | Forge 锁定单没有可编辑入口；RISEMAP 提供 |
| 「查看关联订单」 | Forge 锁定单只存来源单号文本，没有可解析的订单引用，需先补引用字段 |
| 到货登记附件上传 | RISEMAP 支持拖拽/Ctrl+V；Forge 标注未接入（需接平台存储能力） |
| 入库明细等其他页面日期 | 仍按原格式显示 |
| RISEMAP 测试锁库 LK-2026-0001 | RISEMAP 释放原因无控件，无法释放；等待用户决定作废或先配原因主数据 |

---

# 生产模块精修 · B1 组装主链（开工）

## 范围盘点

生产模块共 25 个页面，manifest 内全部为 `review_required`：

- 组装业务管理（8）：生产准备检查、组装单、缺料待办、领料单、补料单、退料单、拆解单、换件单
- 委外管理（14）：上手指南、委外看板、委外订单、委外发料、委外回厂、委外退料、委外对账、委外供应商、加工价目、委外库存、批次追溯、委外未交、委外进货、委外对账单
- 配置（3）：生产配置、委外字典、委外控制规则

分批：B1 组装主链（组装单→缺料待办）→ B2 组装物料链（领料→补料→退料）→ B3 组装变更链（拆解→换件）→ B4 生产准备与配置 → B5 委外主链 → B6 委外结算链 → B7 委外基础与追溯。

## B1 第 1 页：组装单（`page_production_assembly_workspace`）

### RISEMAP 当前事实（`https://risemap.cn/production/assembly`，2026-09-19 实时）

- 面包屑 `生产 › 组装业务管理 › 组装单`；标题「组装单」；说明「按 BOM 将原材料组装为成品，组装中可分批登记入库，入库数量与完工状态独立维护」。
- KPI：组装单总数 0 / 进行中 0 / 累计合格入库 0 件 / 累计物料投入 ¥0.00。
- 状态页签：全部(0) / 草稿 / 审批中 / 待领料 / 组装中 / 已完工 / 已驳回 / 已取消。
- 工具条：新建组装单 / 导出 / 导入/导出任务 / 刷新 / 列表设置。
- 搜索：`搜索单号/成品/BOM/来源订单...`。
- 表格列（12）：组装单号 / 成品 / BOM / 计划数量 / 合格数量 / 不合格数量 / 物料种数 / 物料成本 / 来源订单 / 创建日期 / 创建人 / 操作。
- 数据：当前租户 `暂无组装单`（0 条），因此列表行内操作、详情与办理链需要在 RISEMAP 建单后才能复核。

### Forge 当前表现（1280×900，本地库 74 条）

- 标题、说明、KPI 四项、状态页签八项、搜索框、12 列表格与 RISEMAP **逐项一致**。
- 差异 1（已修）：`成品` 列显示内部 id `BBfwhbW-xgfXAnCb`；修复为按物料名显示「800型柔性线控制柜」，并去掉任何内部 id 回退。
- 差异 2（已修）：`BOM` 列只显示版本号 `V1.0`；修复为显示 BOM 编号 `BOM-RM-CAB-800-V1`（详情页仍显示版本）。
- 差异 3（待改）：工具条文案 Forge「导入/导出」vs RISEMAP「导入/导出任务」。
- 差异 4（待复核）：RISEMAP 该页为空数据，行操作「查看/编辑/下达」等需在 RISEMAP 建单后同材料复核。

### 证据

- RISEMAP：Edge 实时页面（`/production/assembly`，登录账号 金一涛 / 南京汇川技术研发中心有限公司）。
- Forge：`docs/evidence/walkthrough-20260918/90-assembly-forge.png`（修复前）、`91-assembly-forge-fixed.png`（修复后）。

## B1 第 2 页：缺料待办（`page_production_shortage_workspace`）

### RISEMAP 当前事实（`https://risemap.cn/production/shortages`，2026-09-19 实时）

- 面包屑 `生产 › 组装业务管理 › 缺料待办`；标题「缺料待办」；说明「对在产组装单实时做齐套分析：库存按计划完工日期优先分配后，不满足的物料自动汇成缺料待办」。
- KPI：缺料单据 0 / 缺料物料 0 种 / 缺口总额 ¥0.00 / 最早计划完工 -。
- 空态：「当前没有缺料　所有在产组装单均已齐套」。
- 当前租户无缺料数据，因此列表列、行操作与承接动作需在 RISEMAP 建单后再复核。

### Forge 当前表现与差异

- 标题、说明、KPI 四项标签与 RISEMAP **一致**；列表列 组装单/计划完工/物料编码/物料名称/需求/可分配/缺口/缺口金额/操作（RISEMAP 无数据无法逐列比对）。
- 差异 1（已修）：KPI「缺料物料」原先取 `state.lines.length`（行数 4148 种），改为按物料编码去重计数，现显示 **5 种**。
- 差异 2（待改）：同一组装单 × 同一物料在列表中重复出现多行（历史多次齐套分析快照），导致「缺口总额 ¥39,350,240.00」被重复累加、明显偏大。需要按「最新一次分析 / 组装单 × 物料」聚合后展示。
- 差异 3（待改）：列表「计划完工」列显示 `—`，应取组装单的计划完工日期。
- 差异 4（待改，与组装单页同源）：工具条文案「导入/导出」应为 RISEMAP 的「导入/导出任务」。

### 证据

- RISEMAP：Edge 实时页面 `/production/shortages`。
- Forge：`docs/evidence/walkthrough-20260918/92-shortage-forge.png`（修复前）、`93-shortage-forge-fixed.png`（KPI 修复后）。

### 缺料待办 修复结果（同轮）

- 缺口总额：¥39,350,240.00 → **¥1,157,360.00**（按「组装单 × 物料」保留最新快照去重后汇总）
- 计划完工列：由全 `—` 变为真实日期（如 `2026-09-22`），增加按组装单回查兜底
- 缺料物料：4148 种 → **5 种**（按物料编码去重）
- 工具条文案：组装单页「导入/导出」→「导入/导出任务」，对齐 RISEMAP
- 证据：`94-shortage-forge-deduped.png`

### B1 缺料待办像素复刻与真实办理复核（2026-09-19 第二轮）

#### 同材料与 RISEMAP 当前事实

- 在 RISEMAP 新建并审批测试组装单 `ZP-2026-0002`：成品「800型柔性线控制柜」×2，BOM `BOM-RM-CAB-800-V1 V1.0`，计划完工 `2026-09-26`，备注「审计走查：生产模块 B1 缺料对照 20260919」。该材料保留为可追溯测试单据，未删除、作废或改写历史单据。
- RISEMAP 缺料待办实时结果：缺料单据 1，缺料物料 3 种，缺口总额 ¥8,425.00，最早计划完工 2026-09-26。
- 默认「按单号」视图列为：组装单号 / 成品 / 状态 / 齐套率 / 缺料项 / 缺口金额 / 计划完工 / 操作；`ZP-2026-0002` 显示待领料、齐套率 25%、缺料 3 项、操作「去处理」。
- 「按明细」视图提供主动作「一键生成采购申请（3 种）」及逐行「生成采购申请」；提示为「跨单合并同一物料的净缺口，已按计划完工日期优先消耗现有库存，避免重复下单」。列为：物料 / 规格 / 需求合计 / 当前库存 / 净缺口 / 涉及单据 / 缺口金额 / 操作。
- 三项缺料为：800型柜体 需求 2 / 库存 1 / 缺口 1；7英寸触摸屏 需求 2 / 库存 1 / 缺口 1；24V 10A开关电源 需求 4 / 库存 2 / 缺口 2。

#### Forge 修改与实操结果

- 删除原先把查询条件失效结果重复挂到每张组装单的页面算法；现在一次读取物料需求后按真实 `assembly_id` 归属，再按业务单号和物料编码去重。页面由错误的 4148 条重复明细恢复为 27 张真实缺料单、5 种缺料物料。
- 按 RISEMAP 重建「按单号 / 按明细」双视图、KPI、状态页签、齐套率、缺料项、缺口金额、计划完工、跨单合并说明、刷新和操作列；同一 1280×720 视口保存两侧截图。
- 「去处理」进入对应 Forge 组装单；「生成采购申请」使用 Console 标准确认弹窗，真实创建采购申请头和明细，不保留无反馈按钮。
- 首次页面办理捕获标题超过 255 字阻断，未产生采购申请；修复为短标题，并将完整来源组装单写入最多 500 字备注。再次办理成功创建草稿 `PR-2026-0003`，物料 `RM-PLC-1215C`，数量 38，含税小计 ¥258,400.00，期望到货 2026-09-20，来源 21 张组装单。
- 完整停服后使用同一 `.objectstack/data/objectstack.db` 重启，缺料页仍回读 27 张缺料单 / 5 种物料，`PR-2026-0003` 及其明细数量 38、含税小计 ¥258,400.00 持久存在。
- 当前 Forge 本地材料与 RISEMAP 对照材料数量不同，因此截图用于页面结构、密度、控件和交互位置对照；同数据数值级像素差异仍需在 Forge 建立 `ZP-2026-0002` 对应材料后复核，本轮不将 B1 标为最终验收通过。

#### 证据

- RISEMAP 按单号：`docs/evidence/production-pixel-20260919/risemap-shortage-orders.jpg`
- Forge 按单号：`docs/evidence/production-pixel-20260919/forge-shortage-orders.jpg`
- RISEMAP 按明细：`docs/evidence/production-pixel-20260919/risemap-shortage-details.jpg`
- Forge 按明细：`docs/evidence/production-pixel-20260919/forge-shortage-details.jpg`
- 工程门禁：`pnpm typecheck`、`pnpm validate`、`pnpm build` 通过；validate/build 保留仓库既有 author-time warnings，没有新增失败。

### B1 组装单列表与一键领料复核（2026-09-19 第三轮）

#### RISEMAP 当前事实

- RISEMAP 组装单页现有两张可办理测试单：`ZP-2026-0001` 为成品 1 件、计划完工 2026-09-25、物料齐套、待领料；`ZP-2026-0002` 为成品 2 件、计划完工 2026-09-26、缺料 3 项、待领料。
- KPI 为组装单总数 2、进行中 2、累计合格入库 0 件、累计物料投入 ¥0.00；列表仍为 12 列，齐套单和缺料单的行内主动作均显示「一键领料」。
- 两张测试材料均带审计走查说明并保留；本轮只读取页面，没有在 RISEMAP 提交领料确认或改变库存。

#### Forge 修改与实操结果

- 列表补齐 RISEMAP 已观察的成品编码、BOM 编号/版本、数量单位、计划完工、状态与齐套/缺料徽标；待领料且齐套时「一键领料」真实生成领料单，缺料时进入组装单详情继续处理。
- 使用 Forge 现有齐套单 `ASM-2026-0001` 页面点击「一键领料」，真实生成审批中领料单 `MAT-2026-0031`，来源组装单 `ASM-2026-0001`，4 条物料、合计 10 件，业务日期 2026-09-19，备注「组装单列表一键领料」。
- 实操同时暴露 `adapter.find` 未应用页面 `where` 条件的问题：详情曾误读首张历史领料单并混入全部历史明细。组装单和领料单页面已改为使用 REST 过滤查询与按 id 直读；刷新后页面准确回读 `MAT-2026-0031` 及其 4 条明细。
- 本轮未在 RISEMAP 提交「一键领料」，因此 Forge 的生成结果只证明本地真实办理和持久化，不作为两侧同材料行为验收通过。

#### 证据

- RISEMAP 组装列表：`docs/evidence/production-pixel-20260919/risemap-assembly-list.jpg`
- Forge 组装列表：`docs/evidence/production-pixel-20260919/forge-assembly-list.jpg`
- Forge 一键领料结果：`docs/evidence/production-pixel-20260919/forge-assembly-one-click-issue.jpg`
- SQLite：`MAT-2026-0031` 状态 `pending_approval`，`line_count=4`、`total_quantity=10`；明细表按 `document_id` 回读 4 行、数量合计 10。

## 生产模块精修 · B2 组装物料链（领料、补料、退料）

### RISEMAP 当前事实（2026-09-19 实时）

- 领料单 `/production/requisitions`：说明为「组装领料、拆解领料、补料、退料的出入库单据，关联来源单与出入库单据」；四项指标为领料单、物料种类、物料数量、净领用金额；状态页签为全部 / 审批中 / 已确认 / 已驳回 / 已作废；表格十列为领料单号 / 类型 / 成品 / 来源单 / 物料种类 / 数量 / 金额 / 经手人 / 日期 / 操作。
- 补料单 `/production/material-supplies`：说明为「组装单额外领用物料：现场损耗、报废、漏装等超 BOM 用量补领，出库扣减库存，关联组装单与库存流水」；指标为补料单总数 / 补领物料种数 / 补料金额（出库） / 涉及来源单；表格九列为补料单号 / 成品 / 来源单 / 物料种数 / 数量 / 金额 / 经手人 / 日期 / 操作。
- 退料单 `/production/material-returns`：说明为「组装单已领物料退回库存：现场多领、领错、余料退库，关联组装单与库存流水（入库）」；指标为退料单总数 / 退回物料种数 / 退回金额（入库） / 涉及来源单；表格比补料单多「仓库」列。
- 三页当前均为 0 条；点新建补料或退料后 URL 进入 `/new?type=...`，当前线上页面仍停留列表并展示对应空态，未出现可办理表单。因此本轮记录首屏与空态事实，不把新建表单字段写成 RISEMAP 已观察事实。

### Forge 修改与真实办理

- 领、补、退三页共用页面已修复按 id 直读与按 `document_id` 过滤，详情不再误读首张单据或混入其他单据明细；「导入/导出」文案改为 RISEMAP 的「导入/导出任务」，补料和退料状态页签显示实时数量。
- `MAT-2026-0031` 从审批中确认过账：4 种、10 件，金额 ¥1,034.1726；四条库存流水均为 `production_issue` 出库，组装单 `ASM-2026-0001` 从待领料进入组装中。
- 在同一组装单提交并确认补料 `SUP-2026-0028`：`RM-PSU-24V10A` 1 件，金额 ¥103.0614，库存 1980 → 1979，流水类型 `production_supply`。
- 随后提交并确认退料 `RET-2026-0027`：同一物料 1 件，金额 ¥103.0614，库存 1979 → 1980，流水类型 `production_return`。
- 三单完成后，`RM-PSU-24V10A` 为 BOM 领料 4、补料 1、退料 1、净领用 4；组装单累计领用 11、累计退回 1、净投入成本仍为 ¥1,034.1726，补退金额相互抵消。
- 关键异常：再次尝试退回该物料 6 件，页面确认后被业务动作阻断为「退料数量超过净领用数量」，数据库未新增第二张退料单。
- 完整停服后使用同一 SQLite 重启，`RET-2026-0027` 页面仍回读已确认、金额 ¥103.06、库存 1979 → 1980；领料、补料、退料三单及六条对应库存流水均持久存在。

### 证据

- RISEMAP：`risemap-material-list.jpg`、`risemap-supply-list.jpg`、`risemap-return-list.jpg`。
- Forge 列表：`forge-material-list.jpg`、`forge-supply-list.jpg`、`forge-return-list.jpg`。
- Forge 办理：`forge-material-confirmed.jpg`、`forge-supply-pending.jpg`、`forge-return-pending.jpg`、`forge-return-over-net-blocked.jpg`。
- 上述截图均位于 `docs/evidence/production-pixel-20260919/`，视口 1280×720。

## 生产模块精修 · B3 拆解与换件（首屏对照）

### RISEMAP 当前事实（2026-09-19 实时）

- 拆解单 `/production/disassembly`：说明「将成品按 BOM 还原为物料，支持审批、回收入库和报废处理」；KPI 为拆解单总数 / 拆解成品数 / 回收物料价值 / 报废或损耗；状态为全部 / 草稿 / 审批中 / 待领料 / 拆解中 / 已完工 / 已入库 / 已驳回；列表九列为拆解单号 / 成品 / BOM / 数量 / 原因 / 回收价值 / 报废 / 经手人 / 日期。
- 换件单 `/production/rework`：说明「成品不离库，仅更换内部零部件，支持审批、领新件和旧件回收」；KPI 为换件单总数 / 累计换件处数 / 新件领用成本 / 旧件回收价值；状态为全部 / 草稿 / 审批中 / 待领料 / 换件中 / 已完工 / 已入库 / 已驳回；列表十列为换件单号 / 成品 / 换件内容 / 改制数量 / 原因 / 新件成本 / 回收价值 / 成本变化 / 经手人 / 日期。
- 两页当前均为 0 条，未取得 RISEMAP 详情、提交、过账和异常阻断的可办理材料。

### Forge 修改与现状

- 拆解与换件页面已改为 REST 过滤查询和按 id 直读，避免详情误读首条历史单据；导入入口文案对齐为「导入/导出任务」。
- 列表删除 RISEMAP 未显示的独立「操作」列，改为点击单据编号进入详情，在不损失真实可达性的同时对齐列结构。
- Forge 当前各有 19 条历史材料，可用于验证信息密度和详情准确性；由于 RISEMAP 无同材料，本轮只标为首屏结构对照，不宣称业务链验收通过。

### 证据

- `docs/evidence/production-pixel-20260919/risemap-disassembly-list.jpg`
- `docs/evidence/production-pixel-20260919/forge-disassembly-list.jpg`
- `docs/evidence/production-pixel-20260919/risemap-replacement-list.jpg`
- `docs/evidence/production-pixel-20260919/forge-replacement-list.jpg`

### B3 拆解与换件真实办理复核（2026-09-19 第二轮）

#### RISEMAP 新建页当前事实

- 拆解单新建页按「商品 → 拆解 BOM → BOM 版本 → 拆解数量 → 入库仓库（选填）→ 拆解原因 → 备注」组织；选择商品后才自动带出 BOM 和版本，明细逐行分配回收或报废。当前拆解原因无可用值，保存和确认按钮禁用。
- 换件单新建页按「商品 → 改制成品 BOM → BOM 版本 → 改制数量 → 入库仓库（选填）→ 改制原因 → 备注」组织；当前改制原因已有「质量返修」。
- 两侧 RISEMAP 仍无可选商品和可办理单据，因此本轮只将表单字段和前置条件记为实时事实，不将 Forge 的过账结果冒充为双侧同材料验收。

#### Forge 拆解实办

- 使用 `BOM-RM-CAB-800-V1` 、1 件成品、图纸关联仓、「质量返工」办理拆解。将电源回收数量改为 0 且报废仍为 0 时，页面真实阻断「每项回收数量与报废数量之和必须等于理论拆出数量」，未创建错误单据。
- 正常分配创建 `DIS-2026-0020`，并从审批中确认过账为已入库。成品数量 16 → 15，四种物料按 2/1/1/1 回收入库，生成 1 条成品出库与 4 条回收入库流水。
- 办理中捕获成品移动平均成本 ¥15,640 低于 BOM 回收估值 ¥16,320 的真实冲突。Forge 采用库存价值守恒决策：回收数量不变，回收价值按成品释放成本比例重估；最终释放成本和回收价值均为 ¥15,640，报废损失为 0，没有产生负损失或虚增库存价值。该计价处理为 Forge 产品决策，不写成 RISEMAP 规则。

#### Forge 换件实办

- 使用同一 BOM、同一仓库和「质量返修」创建 `REP-2026-0020`：旧件 `RM-PSU-24V10A` 1 件回收入库，新件 `RM-HMI-700` 1 件领用出库。
- 确认过账后单据为已入库；整机库存 15 → 15，新件库存 2 → 1，旧件库存 2 → 3；新件成本 ¥3,133.3334，旧件回收价值 ¥860，成本变化 ¥2,273.3334。
- 页面详情、SQLite 单据、明细、库存流水和数量/金额相互一致。

## 生产模块精修 · B4 生产准备与配置

### RISEMAP 当前事实（2026-09-19 实时）

- 线上入口为「系统 / 业务设置 / 生产配置」，页面分「拆解原因」和「改制原因」。拆解原因当前为空；改制原因有启用值「质量返修」，描述为「成品保留库存，仅更换不合格内部零部件」。
- 新增原因表单包含原因名称（必填）、描述、12 个标识颜色和实时预览；空名称不可提交。停用原因不再出现在新建单据中，不影响历史单据显示。
- RISEMAP 生产导航没有同名「生产准备检查」页。Forge 保留该页作为产品化的跨模块办理前检查，明确标记为 Forge 决策，不写成 RISEMAP 已观察页面。

### Forge 修改与办理结果

- 生产配置对齐到 12 个颜色、原因徽标实时预览、「点击停用 / 点击启用」和明确的启停影响提示。
- 空名称提交被页面阻断为「请填写原因名称」。页面成功新增「审计走查·返修复核」，描述「B4 配置联动验证」，颜色 `#c53b32`；重新打开拆解单新建页后，该值已出现在拆解原因下拉中。
- 通过页面新增并启用付款条件「月结30天」（`NET30`），补齐委外连续办理前置。
- 修正图纸配置的准备检查口径：9 类对象都已建档，其中无 RISEMAP 线上可用值的类别保留停用占位，不再被错判为缺配置，也没有为通过检查擅自启用工程推断值。
- 生产准备检查页最终回读：20 项检查、20 项已具备、0 项待补充、3 个覆盖模块，状态「全部具备」。
- 完整停服后使用同一 `.objectstack/data/objectstack.db` 重启，SQLite 仍回读 `DIS-2026-0020`、`REP-2026-0020` 为 `stocked`，7 条关联库存流水、拆解原因「审计走查·返修复核」和付款条件 `NET30` 均持久存在；拆解释放成本与回收价值仍同为 ¥15,640，换件前后整机库存仍为 15 → 15。
- 重启前已在 Forge 页面完成 B3 单据详情与 B4 生产准备页回读。重启后的工程回读由 SQLite 和本地 HTTP 完成；Edge 当前 localhost 页面出现空白加载，RISEMAP 登录态也已过期，因此本轮不把这次重启后的工程回读追加描述成新的双侧页面验收证据。

### B3/B4 重新登录后的双侧页面复核（2026-09-19）

- RISEMAP 已重新登录并实时打开 `/production/disassembly`、`/production/disassembly/new`、`/production/rework/new`、`/settings/business/production`。拆解列表仍为 0 条；拆解新建页仍因「暂无可用原因」不可提交；换件新建页仍显示改制原因「质量返修」；生产配置的拆解原因仍为空，改制原因仍为「质量返修」。本轮未在 RISEMAP 执行资金、库存或历史单据变更。
- Forge 实时打开生产准备检查、拆解列表/详情/新建、换件列表/详情和生产配置。页面回读 `DIS-2026-0020` 为已入库、释放成本与回收价值均为 ¥15,640、报废损失 0；`REP-2026-0020` 为已入库、整机 15 → 15、新件成本 ¥3,133.3334、旧件回收 ¥860、成本变化 ¥2,273.3334；准备检查仍为 20/20、待补充 0。
- 页面复核发现 Forge 历史配置存在两个同名启用值「质量返工」。其中 `q6rOUu_7xCn0v9aY` 已被 3 张拆解单引用，`VU_riF_v_96WRZ8h` 未被任何拆解单引用。本轮保留两条历史记录，使用生产配置标准确认弹窗将未引用项停用；已保存单据不受影响。
- 新增原因现在按去除首尾空格、合并连续空白并忽略大小写后的名称阻止重复；页面以输入 ` 质量返工 ` 实测，明确反馈「已有同名拆解原因，请直接启用或使用其他名称」，没有新增记录。拆解和换件新建页的启用原因选择器也按同一口径去重；拆解原因下拉实测仅显示「质量返工」和「审计走查·返修复核」。业务动作在存在历史重名数据时优先绑定启用配置，避免误绑已停用项。
- 工程门禁再次通过：`pnpm typecheck`、`pnpm validate`、`pnpm build`、`git diff --check`。随后完整停止开发服务并从同一 `.objectstack/data/objectstack.db` 启动，`/api/v1/ready` 回读 `running`；内置浏览器再次回读 `DIS-2026-0020` 的四条已入库拆解明细，以及生产配置中一条停用、一条启用的「质量返工」和启用的「审计走查·返修复核」。保留项目既有 Node 版本提示、author-time warnings 和已记录的 schema/reference 警告，没有新增失败。
- B4 的当前线上可观察页面、配置弹窗、空值与重名阻断、启停反馈和新建单据联动已完成双侧实时复核。B3 的 Forge 本地真实办理、页面/API/SQLite/库存流水与停服重启回读已闭环；RISEMAP 侧仍缺少可选商品、拆解原因和可办理单据，因此 B3 的同材料双侧过账验收继续标为待 RISEMAP 可办理材料复核。

## 生产模块精修 · B5–B7 委外主链、结算链与追溯（2026-09-20）

### RISEMAP 当前事实

- 本轮在已登录页面实时打开 `/subcontract/orders`、`/subcontract/orders/new`、`/subcontract/issues`、`/subcontract/receives`、`/subcontract/returns`、`/subcontract/reconciliation`、`/subcontract/suppliers`、`/inventory/subcontract-stock`、`/subcontract/traces`、`/subcontract/reports/undelivered`、`/subcontract/reports/receipts`、`/subcontract/reports/reconciliation-statement`。
- 订单页保留四项指标、新建、导出、导入、任务和刷新；发料、回厂、退料分别按自身状态、供应商和业务字段查询。回厂当前没有独立新建主动作，Forge 保留新建回厂记录作为本地可写执行扩展。
- 对账页以待对账池和对账单为两级视图，待对账池按供应商、订单和回厂批次切换；对账单报表只承担查询、审计与导出，应付金额口径为加工费加补料减扣款。
- 委外库存按订单和物料维度使用「累计已发 − 倒冲耗用 − 累计退料」计算在外余量；批次追溯支持材料批次到成品和成品批号到材料的双向查询。
- 当前 RISEMAP 租户的上述页面均无可办理委外业务数据。本轮只读取页面，没有在 RISEMAP 执行资金、应付、库存或历史单据变更，因此没有把 Forge 本地材料写成 RISEMAP 同材料结果。

### Forge 页面与交互修正

- 委外订单、发料和回厂补齐真实导出；订单与发料的导出结果写入持久化导出任务，任务页按来源回读任务状态、记录数和下载入口。委外订单保留「导入导出任务」入口，没有添加不可办理的假导入按钮。
- 委外发料将原先的静态「供应商：全部 / 发料类型：正常发料」替换为真实供应商和发料类型选择器，搜索同时覆盖发料单、订单、供应商和物料；补齐导出与导出记录。
- 委外回厂补齐供应商筛选、回厂单/订单/入库单/供应商/加工件搜索、真实分页、导出和导出记录。
- 页面实操验证：委外订单供应商筛选可选中并出现重置入口；订单、发料、回厂导出分别反馈 4、4、1 条；发料导出任务页回读来源「委外发料」、已完成、4 条和下载结果。
- 委外对账可从待对账池切换到对账单，回读 `REC-CHAIN-20260919162519-001`、应付 `AP-REC-CHAIN-20260919162519-001`、加工费和应付均为 ¥71.00，并展示原订单和回厂批次来源。
- 批次追溯使用真实材料批次 `SC-BATCH-001` 正向得到成品批次 `SC-FG-BATCH-001`，再用同一成品批次反向得到材料批次；两个方向各返回 1 条关系。
- 390×844 窄屏下检查委外发料和委外对账单报表：指标改为单列或双列卡片，主动作、筛选和标题保持可达，宽表在卡片内滚动，页面本身无横向溢出。

### B5/B6 连续办理、异常与停服重启

- 在独立端口和独立 SQLite 连续完成委外供应商开通、甲供料与包工包料订单、提交/驳回/重提/审核、发料审批与库存锁定、实际发出、供应商签收、回厂质检、正式入库、BOM 倒冲、不良处置、对账确认和应付生成。
- 退料链验证草稿不改变库存，确认只生成待入库单，仓库入库后在外库存 6 → 5、累计退料 0 → 1，并生成一条 `material_return` 流水；停用退料原因和超过在外余量均被阻断。
- 对已被有效对账单占用的回厂明细再次生成对账，服务端返回明确阻断；对账单、明细、日志、应付和订单状态在阻断前后完全一致，没有部分写入。
- 完整停止服务并从同一 SQLite 启动后，订单、回厂、入库、不良处置、对账、应付、退料、库存余额和流水均按原 ID 回读通过。

### 工程证据与验收边界

- 通过 `pnpm typecheck`、`pnpm validate`、`pnpm build`、`pnpm acceptance:page-visible-language`、委外同库主链、退料链、对账阻断和两项停服重启回读。
- `pnpm acceptance:page-control-usability` 仍报告 21 个仓库既有的非生产页面问题，本轮委外页面未出现在失败清单中；该全仓失败不改写为本轮通过。
- 结构化记录：`apps/forge-objectstack/tests/page-acceptance/subcontract-b5-b7-browser-readback-20260920.json`。
- B5–B7 的实现、Forge 页面交互、API、关键异常和同库重启回读已经完成；RISEMAP 当前无同材料办理结果且尚未独立复核，因此页面精修清单继续保持 `review_required`，不冒充最终双侧业务验收通过。

## 委外双材料实时对照与全仓控件债务清理（2026-09-20）

### RISEMAP 同材料实办

- 新增加工类型 `SC-AUDIT-20260920`「审计走查·钣金加工」，并为既有供应商 `S-0001`「南京锐联电气技术有限公司」启用该能力；测试备注统一使用「审计走查·对照20260920」。测试记录保留可追溯标识，未删除、作废、撤销或改写任何既有历史单据。
- 创建并审核委外订单 `OS-2026-0001`：甲供料，交期 2026-09-30，全检，付款条件「到货验收合格后30天付款」，加工件 `FG-RM-CAB-800`「800型柔性线控制柜」2 台，单价 ¥100，金额 ¥200。
- 发料计划为 `RM-PLC-1215C` 2、`RM-HMI-700` 2、`RM-PSU-24V10A` 4、`RM-CAB-800` 2，共 10 件。受现有库存约束，发料单 `OSI-2026-0001` 实发 2 / 1 / 2 / 1，共 6 件；审核后生成出库单 `OUT-2026-0001`，实际出库并完成供应商签收，发料状态回读「已签收」。
- 订单随后进入「在外加工」。首批回厂单 `OSR-2026-0001` 登记 1 台，良品 1、不良 0，生成入库单 `IN-2026-0004`。RISEMAP 当前页面回读该入库单为「已审批」、进度 66%，但当前账号在详情页和列表页均没有「完成入库」动作，因此库存入账和后续对账未执行。本轮不把该节点描述成 RISEMAP 完整闭环。

### Forge 对应页面办理结果

- 实时打开 Forge `page_subcontract_workspace`，按同一材料创建并审核订单 `SC-AUDIT-20260920-001`：甲供料、交期 2026-09-30、全检、数量 2；四项发料物料编码和计划数量与 RISEMAP 完全一致。页面录入单价 ¥100，但服务端按当前有效加工价 ¥35.50 / 件重新计价，订单回读总加工费 ¥71.00；该差异保留为 Forge 当前价目规则，不改写成 RISEMAP 事实。
- Forge 当前主数据只有「锐联钣金委外厂」和外协加工件 `SC-FG-001` 可选，故在备注中明确记录供应商与成品映射；付款条件只能映射为「月结30天」。这些是当前 Forge 主数据差异，不写成 RISEMAP 事实。
- 内置浏览器原会话在订单提交时持续返回 `UNAUTHENTICATED`；独立 Edge 登录会话使用同一 Dev Admin 账号后，页面写入成功，证明该阻断属于原浏览器会话认证异常，不是委外业务表单或服务端动作不可用。随后在页面提交、审核发料单 `SI-20260920-001`，实发 2 / 1 / 2 / 1，共 6 件，确认出库并完成供应商签收；订单回读「进行中」，发料 6 / 10、回厂 0 / 2。
- 回厂前发现订单所用供应商档案的「默认回厂入库仓」指向不可解析记录，页面明确阻断提交。通过标准对象详情把 `PIDSRrr4hyD_U0Xi` 的默认回厂仓修正为「委外回厂仓」后，再次打开回厂页面，仓库正确回显；页面登记 `SR-20260920-001`，本次回厂 1、合格 1、不良 0，实际耗用四种材料 1 / 1 / 2 / 1，并成功提交为「待入库」。
- 页面和 API 同步回读：回厂单状态 `pending_inbound`，待入库单 `IN-SR-20260920-001` 状态 `pending`、数量 1，四条材料耗用状态均为 `pending_backflush`，日志包含 `submitted`、`inbound_created`；订单仍为 `in_progress`、累计发料 6、正式回厂入库 0。Forge 未点击「确认入库」，以保持与 RISEMAP 当前“已生成入库单但无完成入口”的同阶段边界。
- 双侧同材料验收当前状态为：RISEMAP 已到 `IN-2026-0004`「已审批」但当前账号无完成入库入口；Forge 已到 `IN-SR-20260920-001`「待入库」且存在确认入口。两侧均未增加正式成品库存，也未进入对账/结算；因此本轮证明订单、部分发料、签收、首批合格回厂和生成待入库单的对应办理，不宣称完整入库闭环。

### 21 个非生产页面控件问题

- 已移除无后续能力的伪操作：文档中心上传、员工档案导入导出与批量删除、固定资产导入导出与月度计提、库存操作两个 toast-only 导入导出入口、物料领取物品库、会议纪要导出、采购询价与采购待办池的 toast-only 导入导出任务、资质申报导出、规章制度汇编导出、用章管理导出。
- 已把伪页签改成真实筛选：规章制度「按分类查看」进入独立激活状态；用章管理「待盖章任务 / 已盖章记录 / 外带用章」分别按 `approved`、`completed`、`external_use` 过滤，并显示各自实时数量。
- 已把假分页和假入口改为诚实呈现：往来对账固定上一页/下一页改为「第 1 页，共 1 页」；文档中心文件夹由按钮改为信息卡，上传未接入时只显示业务说明。
- 内置浏览器实测：规章制度分类页签可切换并保持激活态；用章管理三个页签逐一切换后分别保持激活态；文档中心只保留可执行的「新建文件夹」，空态明确说明上传尚未接入。
- `pnpm acceptance:page-control-usability` 通过：117 个自定义页面中不存在惰性按钮、静态假页签、假链接、原生选择/日期控件、禁用样式伪按钮、空 handler、toast-only 点击或浏览器原生弹框。`pnpm typecheck`、`pnpm validate`、`pnpm build` 和 `git diff --check` 同轮通过；保留项目既有 4 个页面源解析 warning、Node 版本提示以及 111 个逐页精修 `review_required`，它们不属于本次 21 个控件缺陷，也未被改写为验收通过。
