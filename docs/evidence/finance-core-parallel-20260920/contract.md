# 财务核心页面并行批次合同

- 日期：2026-09-20；分支：`codex/finance-core-parallel-20260920`。
- 运行环境：`http://localhost:4441`；独立 SQLite：`apps/forge-objectstack/.objectstack/finance-core-parallel.sqlite`。
- 最初盘点范围：`finance-management.page.ts` 的 11 个页面。用户随后要求逐页慢慢核对，本批实现范围收敛到 `page_output_invoices` 单页；其余 10 页保留待精校。下文进项发票是未实施的问题盘点，不能因共用文件发生变化而计为通过。
- Skill：`objectstack-ui`、`objectstack-query`；修改面为 React source page 与现有 Action 调用，无新增对象或 Action。
- 当前用户标准为像素级复刻；旧规则允许呈现差异的文字不作为本批视觉放行条件。

## 当前取证与原型

进项发票、销项发票采用 `task_workspace`，参考 `project-task-workspace.page.ts` 的真实动作与办理反馈；布局以 RISEMAP 当前发票页为准。首屏顺序为标题/说明 → 期间与页面动作 → 四项指标 → 状态/搜索/类型等列表过滤 → 发票列表 → 状态反馈与关联单据。

RISEMAP 当前事实由主任务的已登录内置浏览器在本日读取。实施子任务新建的独立 IAB session 访问 `/finance/invoices/sales` 被重定向至登录页，不能独立读取登录后页面；不复制或读取登录凭证。主任务负责提供实时页面事实和独立复核。

主任务后续写入的实际取证说明为：RISEMAP 使用已登录 Edge，Forge 使用内置浏览器。可追溯记录为 `docs/evidence/finance-parallel-20260920/live-comparison.md`，因此 RISEMAP 内置浏览器门禁仍未满足。此前“主任务的已登录内置浏览器”是传递时的来源误述，以本段和主线实时记录为准。

## 销项发票首个修复

- 依据当前结构事实，将本月/本季/半年/全年与导出合并在同一右对齐行；去除独立期间卡。
- 保留期间起止日期为期间组的 title 提示；保留刷新、重置筛选、查看发票、红冲能力。
- 去除列表顶部重复“销项发票列表/共 N 条”行，数量仍位于列表底部；状态区直接接续四项指标。
- 布局参数：操作行底部 14px、动作间距 10px、期间控件高 34px；这是 Forge 实现值，尚未完成 RISEMAP 同视口逐像素量测，不宣称像素一致。横幅仍为现有 150px ForgeHero，RISEMAP 约 106px 业务图片横幅差异保留。
- 未改变数据请求、金额口径、CSV 内容、红冲 Action 或持久对象。单页验证需检查期间切换、导出触发、重置、桌面与窄屏；业务账务路径不因纯布局修复自动视为已验收。

| 页 | 主任务实时 RISEMAP 事实 | 实施环境 Forge 当前表现 | 差异及处理 |
| --- | --- | --- | --- |
| 进项发票 `/finance/invoices/purchase` | 标题进项发票；说明管理供应商来票、税务校验与认证抵扣；本月/本季/半年/全年，默认全年；邮件收票/导出；四 KPI；全部/正常/已作废/已红冲；搜索发票号 / 供应商 / 关联单据 / 项目；类型/抵扣状态；勾选、发票号码、日期、供应商、含税金额、税率、税额、不含税、关联单据、项目、备注、操作；当前空态 | 4441 内置浏览器已打开，空态；期间被放在 KPI 后；比原页面多抵扣状态列；源码中按钮检查 approved，但对象实际正常值 normal；setWorkflow 后分支未渲染弹窗；发票号码显示 code | 期间移到 KPI 前；搜索文案对照；以实际 invoice_number 展示；认证/不抵扣补标准弹窗、阻断、反馈、回读。行级动作属于 Forge 现有能力修复，待 RISEMAP 同材料复核 |
| 销项发票 `/finance/invoices/sales` | 标题/说明 → 期间（默认全年）及导出 → 四 KPI → 状态 → 搜索 → 12 列空表；列含邮件；目前无业务数据 | 主任务打开主线有 8 条；布局多刷新、期间起止文案、列表标题/数量、重置 | 几何样式需同视口证据后处理；额外动作保留为 Forge 扩展；行级红冲未取得 RISEMAP 事实 |

## 要求与验收动作

| ID | 要求与来源 | 页面步骤 | 预期 |
| --- | --- | --- | --- |
| FIN-INPUT-01 | RISEMAP 期间位于指标前 | 切换期间、状态、搜索与类型 | 期间决定 KPI；局部列表筛选不改变期间 KPI；清除可恢复 |
| FIN-INPUT-02 | 对象字段与 Forge 现有 Action 契约 | 用有区分的登记编号和发票号码打开页面 | 发票号码正确；税额与不含税从真实明细/金额计算；中文抵扣状态 |
| FIN-INPUT-03 | Forge 产品缺陷修复 | 正常待认证票打开认证、取消、确认 | 标准弹窗可见；取消不写；确认后回读认证状态；重复动作消失 |
| FIN-INPUT-04 | Forge 产品缺陷修复 | 标记不抵扣并提交空原因、再填原因提交 | 空原因在上下文内阻断，成功后状态回读；提交中防重复 |
| FIN-INPUT-05 | Forge API 既有业务边界 | 零税率票尝试认证 | 显示后台阻断，不伪成功 |
| FIN-PIXEL-01 | 用户像素级标准 | 同视口对照标题、间距、字号、表头、空态和关键状态 | 记录精确值与剩余差异；未截图比对不得通过 |

业务材料只在此独立 Forge 测试库建立，并明确标识为验收夹具；不冒充 RISEMAP 同材料闭环。RISEMAP 不执行真实开票/收付/认证等账务动作。页面正常、异常、API、重启、桌面/窄屏、同材料双侧与独立复核全部齐备前，四维状态维持 pending/blocked，总体 `review_required`。
