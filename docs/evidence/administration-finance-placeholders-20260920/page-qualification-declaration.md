# 资质与申报逐页复刻记录

## 页面合同

- 页面：`page_qualification_declaration`
- RISEMAP：`https://risemap.cn/ops/certificates`
- Forge：`http://localhost:3001/_console/apps/forge/page/page_qualification_declaration`
- 岗位：行政资质管理员、政策申报经办人、材料管理员
- 主原型：任务执行型与配置型组合
- 页面职责：维护企业证书资质、分层到期提醒、政策申报进度及申报材料目录。

## 2026-09-20 RISEMAP 当前事实

- 标题说明为“企业证书资质台账、到期提醒、政策申报跟踪与材料统一管理”；四个页签依次为资质台账、到期提醒、政策申报、材料库。
- 资质台账包含新增资质、导出、刷新、列表设置、名称/证书编号搜索、类别和状态筛选；列为资质名称、资质类别、证书编号、发证机构、有效期、责任人、状态、操作，当前 0 条、每页 10 条。
- 新增资质包含资质名称、类别、证书编号、发证机构、证书有效期、年审日期、复审日期、责任人、关联申报、关联材料、附件和备注；前四项及证书有效期必填。
- 到期提醒顶部统计已过期未处理、30 天内到期、近期需年审/复审、待处理申报；下方分为已过期、30/60/90 天到期、年审和复审六组提醒。
- 政策申报提供新增申报、列表/看板/甘特图切换、导出、阶段和类别筛选；列为申报项目、申报类别、主管部门、截止日期、责任人、预期金额、关联材料、当前阶段。
- 新增申报包含申报名称、类别、主管部门、截止日期、启动日期、责任人、预期补贴金额和备注；前四项必填。
- 材料库显示文件夹数和文件数，并提供新建文件夹；当前 0 个文件夹、0 个文件。

## Forge 本轮实现与页面复核

- 已替换原来后三个页签只有说明文字的半占位实现，落地橙金主题、四个真实页签及各自信息结构。
- 资质证书对象新增年审日期、复审日期、关联申报、关联材料和附件说明；证书有效状态根据日期实时派生已过期、90 天内即将到期或有效。
- 新增 `forge_qualification_declaration` 承接政策申报名称、类别、主管部门、日期、责任人、预期金额、关联材料数和阶段；列表、看板、甘特三种视图均可切换，空数据时给出对应业务引导。
- 新增 `forge_qualification_material` 承接文件夹与后续文件目录；当前新建文件夹会真实持久化。
- 资质证书附件已改为 ObjectStack 多文件字段，并连接预签名上传、上传完成确认和证书记录保存；弹窗显示真实“上传附件”入口、已选文件名及独立备注字段，不再保留“文件服务未接入”的降级说明。内置浏览器已验证多文件选择器可以打开；本轮未选择本地文件，实际文件上传仍待复核。
- 资质和申报均使用标准 Forge 弹窗并实测空表单必填阻断；材料文件夹也实测空名称阻断。本轮未提交测试记录。
- 本轮实时打开并核对 RISEMAP `/ops/certificates` 的四个页签、两个新增表单，以及 Forge 对应四页签；再次对照确认 RISEMAP 资质表单包含多文件附件和独立备注，Forge 已同步该结构；已检查桌面首屏、640×900 窄屏材料库与弹窗，无按钮/输入框重叠，浏览器控制台无错误。
- RISEMAP 已创建可追溯对照申报“审计走查-政策申报-20260920”，类别“高新技术企业”、主管部门“审计走查主管部门”、启动日 2026-09-20、截止日 2026-12-31、预期金额 120000，生成记录号 `2101696836247003138`，页面反馈“申报项目创建成功”，列表初始阶段为“待评估”。Forge 据此补入“待评估”阶段并设为新建申报默认阶段，同时把申报类别改为与 RISEMAP 当前下拉一致的固定选项。
- RISEMAP 新增资质表单的“资质类别”是必填字段，但当前下拉显示“暂无数据”，因此没有绕过必填规则提交资质测试记录；该链保持“RISEMAP 缺少可办理主数据、待复核”。
- 本轮附件改动后 `pnpm typecheck`、`pnpm validate`、`pnpm build`、`pnpm acceptance:page-polish` 均通过；完整停止服务并使用同一 `.objectstack/forge.sqlite` 重启后，资质与申报页面及四页签可正常回读。

## 当前验收状态与残余缺口

- 已证明：四页签结构、证书/申报字段和必填阻断、资质附件选择入口及存储链实现、分层到期提醒、申报三视图、材料文件夹入口、真实导出、桌面与窄屏响应式。
- 尚未证明：已在 RISEMAP 创建的同一组申报材料尚未在 Forge 提交并完成停服重启后的记录级回读；资质链受 RISEMAP 类别主数据为空阻塞；实际附件文件选择上传、材料版本与归档；提醒通知派发；审批推进；同宽截图像素差量化。
- 本地 3001 重启时仍报告仓库既有 `forge_sales_outbound.sku_id` SQLite 非空列迁移失败；与本页新增的资质、申报、材料对象无关，但说明全库持久化门禁并非全绿。
- 设计状态保持 `review_required`，本记录不将页面标为最终验收通过。
