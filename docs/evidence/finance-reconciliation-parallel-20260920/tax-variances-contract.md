# 税率差异留痕单页合同

- 页面：`page_invoice_tax_variances`；起始版本 `47f3575f22b6dded418452acebfdbd26b9663efc`。
- 范围：只修本页标准入口的模式错误并核对筛选/空态/既有列，不进入调整记录页面精修。
- 岗位：发票审核员追溯订单约定税率与实际发票税率的差异；主原型 `analysis`，参考 `workbench.page.ts` 的范围到明细结构。只读审计页面没有新建、审核、红冲动作；下一步是回溯来源订单和发票，当前来源链接尚待复核。
- 当前首屏应为标题与说明、方向/来源单号/发票号码/差异时间范围、记录数量和差异明细或空态。
- 使用 `objectstack-ui`、`objectstack-query` 与 `objectstack-platform`；不修改公共组件、导航、对象或 Action。

## 修改前实时对照（本子任务实际打开）

2026-09-20，本轮 Edge 连接恢复。新建独占标签页分别打开：

- RISEMAP `https://risemap.cn/finance/invoices/tax-variances`：标题“税率差异留痕”；说明“订单约定税率与发票实际税率不一致时的差异审计链,记录约定/实际税率、预计/实际税额差异与原因”。方向为全部方向/销项/进项；两个输入 placeholder 为“来源单号”“发票号码”；差异时间起止；当前共 0 条，空态“暂无税率差异留痕”。DOM 查询当前 `th` 数量为 0，因此有数据列表的列与行操作仍无法从当前 RISEMAP 证明。
- Forge `http://localhost:4443/_console/apps/forge/page/page_invoice_tax_variances`：Console 导航显示税率差异留痕，但正文为调整记录，包含金额调整净额指标、“差异类型”筛选和“暂无调整记录”空态。
- 明确原因：两页共用源字符串，内部只根据已取消的 `nav=invoice_tax_variances` 决定模式，标准 pageName 入口无该参数。

## 要求与实现决定

| 编号 | 事实或决定 | 预期 |
| --- | --- | --- |
| TV-01 | 当前 RISEMAP 独立税率差异入口；Forge 标准导航不传自定义 nav | page_invoice_tax_variances 的源固定为税率差异模式；直达、刷新和含旧 nav 值时均保持本页职责 |
| TV-02 | 当前方向、来源、发票和日期范围筛选 | 保留现有税率差异筛选，验证销项/进项、关键词、日期起止确实影响结果 |
| TV-03 | 当前 RISEMAP 0 条且不显示表头 | Forge 空态保持“暂无税率差异留痕”，不新增虚构列或动作 |
| TV-04 | Forge 已有税率差异算法和 11 列 | 测试既有列及真实税率不同才产生明细；有数据同材料双侧列一致性仍待 RISEMAP 复核 |
| TV-05 | 单页改动边界 | 调整记录生成的页面源保持逐字不变，不借本页任务修改下一页 |
| TV-06 | 本轮实际选择销项并填“对照-税率”/“TEST-TAX”后，RISEMAP 出现“清空”，空态变为“未找到匹配的差异留痕”；Forge 仍显示原空态且无清空 | 增加仅筛选激活时出现的清空动作，复位方向、来源、发票、起止日期；区分无记录与筛选无匹配 |

四维状态初始均 pending。当前 RISEMAP 视口 1171×744；本轮暂无同视口落盘截图，像素级外观、窄屏和真实差异数据仍保持待复核。不会因入口修复把页面改 accepted。
