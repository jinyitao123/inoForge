# 收款流水页面实施验收记录

## 环境与材料

- Forge：`http://localhost:4441/_console/apps/forge/page/page_sales_collection_flow?nav=sales_collection_flow`
- SQLite：`apps/forge-objectstack/.objectstack/finance-sales-cycle.sqlite`
- RISEMAP：`https://risemap.cn/sales/collection-flow`
- 业务材料：应收页登记的 26,000 元收款流水 `RCV-PAGE-20260916-001`、应收 `AR-INV-BROWSER-20260916-001` 及其销项发票、销售订单和资金账户。
- 实施版本：`fbfc2e5c0518f1ef4cacc5b8ca0f449746df6480`

RISEMAP 当前账号没有收款流水。本轮实时确认了标题、说明、四项指标、四个状态、六类筛选、九项业务列和空态；分配、解绑、审核及回写结果仍缺少 RISEMAP 同材料证据。

## Forge 页面操作结果

1. 修复页面请求未携带 Console 鉴权头的问题；刷新后读取到 26,000 元真实流水，不再误报 401 和 0 条记录。
2. 摘要显示收款总额 26,000、已分配 0、未分配 26,000、待办 1；流水列显示客户、日期、方式、账户和分配余额。
3. 清空核销编号后点击下一步，在表单阶段显示必填错误；分配 26,001 元时服务返回“分配金额不得超过收款未分配余额”。
4. 正常分配 26,000 元后，流水进入待审核，显示“审核核销”和“取消分配”；取消后已分配恢复 0、未分配恢复 26,000。
5. 再次分配并审核后，流水进入已分配，应收与销项发票结清，销售订单已收金额增加 26,000。
6. 对已审核核销执行反核销后，流水、应收和发票恢复待处理状态，并生成反核销日志；使用新核销记录重新分配并审核后再次结清。
7. 已分配流水不显示撤销到账动作；API 直接尝试撤销被阻断，重复审核和重复分配也被阻断。
8. 搜索 `NO-MATCH` 显示筛选空态；重置恢复记录；状态页签和高级筛选按列表口径工作，四项摘要保持全量口径。
9. 完整停服并使用同一 SQLite 重启后，页面仍显示一笔 26,000 元已分配流水。

内置浏览器在 390×844 下核对了纵向摘要、横向状态页签、默认收起和展开后的高级筛选、纵向起止日期，以及分配表单和确认弹窗。按钮文字完整，页面没有整体横向溢出，宽表只在卡片内部滚动。

## API 与持久化结果

- `tests/sales-collection-flow-page-readback.mjs`：重启前后均通过。
- 最终流水状态 `allocated`，已分配 26,000，未分配 0；取消、反核销、最终审核三条记录分别为 `cancelled`、`reversed`、`approved`。
- 应收与销项发票均为 `settled`，已收 26,000，未收 0；销售订单已收 26,000。
- 资金账户余额保持 126,000；分配、取消、审核与反核销不会重复增减实际到账资金。
- 反核销日志记录 26,000 元及原核销关系。
- 未登录审核返回 401；重复审核、已分配流水撤销和再次分配均返回 400。
- `pnpm typecheck`、`pnpm validate`、`pnpm build`：通过；导航静态检查通过 211 个入口。

## 四维结论

- replication：`blocked`。RISEMAP 当前空数据，行级分配、解绑、审核和同材料结果待复核。
- visual：`pending`。桌面和 390×844 窄屏的有数据、筛选空态、分配与确认弹窗已检查；独立视觉复核待完成。
- interaction：`blocked`。Forge 必填、超额、分配、取消、审核、反核销顺序和错误反馈通过；RISEMAP 行级交互待同材料复核。
- business：`blocked`。Forge 流水、核销、应收、发票、订单、资金账户及同库重启回读通过；RISEMAP 同材料业务结果待复核。

独立复核尚未发生，本记录不构成页面 `accepted`。
