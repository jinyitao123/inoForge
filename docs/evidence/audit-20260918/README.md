# Forge 供应链 ↔ RISEMAP 全量结构对照审计

## 覆盖范围
RISEMAP 供应链全部 42 个页面（到货检验 5 / 基础资料 7 / 采购管理 7 / 入库管理 6 / 库存管理 10 / 出库管理 7），
Pages 与 RISEMAP 一对一映射（RISEMAP 路径见 risemap-facts.json）。

## 方法
1. 内置浏览器固定 1280 宽，逐页同时截 RISEMAP 与 Forge 截图：`<页面>-risemap.png` / `<页面>-forge.png`。
2. 同一口径抽取两侧可比较的结构事实：hero（标题/描述/下一步磁贴/当前环节提示）、工具条按钮（含「图标 or 文字」判定）、
   页签、筛选控件占位、首张列表的表头列、首行行操作（图标 or 文字）、指标卡数量。原始数据见 `risemap-facts.json` / `forge-facts.json`。
3. 自动比对生成 `AUDIT.md`（逐页差异）与 `SUMMARY.md`（按差异类型归纳）。

## 结果
- 42 页中 41 页存在结构差异，差异点 201 条。
- 差异分类：Forge 多出按钮 54 / RISEMAP 独有按钮 31 / 筛选文案 28 / 列表列 23 / **图标按钮被写成文字按钮 21** /
  页签 17 / 行操作形式 14 / 按钮文案 6 / 指标卡数量 5 / hero 磁贴 1 / hero 提示块 1。

## 本轮已按审计修复
- **「刷新」按钮 21 页图标化**：把 Forge 里写成文字按钮的「刷新」统一改成 RISEMAP 的图标按钮（`fp-icon-button` + 刷新 SVG），
  全仓共转换 100 处（含销售/生产/项目/行政等模块，保持全局一致），供应链 21 页全部覆盖；冒烟复核 18 页无报错。
- 另见主对照记录 `docs/evidence/risemap-supply-chain-data-readiness-20260917.md` 中各轮已完成的对齐项。

## 尚未修复（详见 SUMMARY.md）
1. 行操作图标化：RISEMAP 行内是图标按钮（查看/详情/关注/复制开单/删除等），Forge 仍是文字按钮——14 页。
2. RISEMAP 独有按钮：批量操作/批量、BOM对比、智能拆分、品牌筛选等——31 处，需先确认 Forge 是否具备对应能力。
3. Forge 多出按钮：导入/导出任务、选中导出等——54 处，属 Forge 增量，需决定保留还是收起。
4. 列表列差异：多为 RISEMAP 的勾选列（Forge 缺）与末尾附加列——23 页。
5. 页签差异：RISEMAP 部分页面无页签而 Forge 有（或反之），以及页签计数展示方式——17 页。
6. 筛选文案：RISEMAP 折叠文案为「状态/结果/…」，Forge 为「全部状态/…」；日期区间控件占位文案不同——28 处。
7. 指标卡数量差异 5 页（如物料管理 RISEMAP 20 列 vs Forge 10 列等，详见 AUDIT.md）。
