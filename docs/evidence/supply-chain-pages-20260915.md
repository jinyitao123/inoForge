# 供应链页面快速交付记录

日期：2026-09-15

分支：`codex/supply-chain-pages`

独立工作树：`/Users/jinyitao/Developer/inoForge-supply-chain-pages`

独立运行：`http://localhost:4386`

独立 SQLite：`apps/forge-objectstack/.objectstack/acceptance/supply-chain-pages.sqlite`

## 本轮已核对

内置浏览器已登录独立 Forge 环境，并实际打开以下页面：

- 采购订单：`page_purchase_order_workspace`
- 到货登记：`page_purchase_arrival_workspace`
- 待检验库存：`page_pending_inspection_workspace`
- 检验单：`page_purchase_inspection_workspace`
- 采购入库：`page_purchase_inbound_workspace`
- 库存锁定：`page_inventory_locks`
- 物料组合：`page_material_combinations`
- 库存锁定：`page_inventory_locks`
- 委外订单列表与详情：`page_subcontract_workspace`
- 采购退换货列表、新建与办理：`page_purchase_return`

同一内置浏览器已实时打开 RISEMAP：

- 供应链导航：到货检验、基础资料、采购管理、入库管理、库存管理、出库管理及各子入口。
- 采购退换货列表：`https://risemap.cn/purchase/returns`。
- 新建采购退货单：`https://risemap.cn/purchase/returns/new`。
- 采购订单：RISEMAP 当前列表含订单/明细切换、筛选方案、25 列、付款/收票与到货下一步；Forge 同页已实时核对到对应内容。
- 到货通知：RISEMAP 当前页包含合并到货登记、搜索、预计到货日期范围、目标仓库、状态、逾期筛选、9 列表格和分页。
- 到货登记：RISEMAP 当前页包含订单明细/物料明细切换、新建、导出、导入/导出任务、刷新、搜索、状态/类型筛选、10 列表格和分页。
- 待检验库存：`https://risemap.cn/inventory/pending-inspection`，当前页包含下一步检验单、导出、导入/导出任务、刷新、全文搜索、状态筛选、14 列表格和 20 条分页；当前租户为空。
- 检验单：RISEMAP 当前页包含下一步采购入库、导出、导入/导出任务、刷新、检验单/物料搜索、状态/结果/方式/供应商筛选、到货日期范围、13 列表格和 20 条分页；当前租户为空。
- 采购入库：RISEMAP 当前页包含当前环节提示、新建入库单、打印条码、导出、刷新、入库单号/批次搜索、状态/来源筛选、12 列表格和 20 条分页；当前租户为空。
- 全部入库单：RISEMAP 当前页包含新建入库单、打印条码、导出、刷新、入库单号/批次搜索、状态/类型/来源筛选、12 列表格和 20 条分页；当前有期初入库 `IN-2026-0001`，状态已入库、仓库贯联验证原料仓、金额 ¥54,000.00。
- 生产入库：RISEMAP 当前页包含当前环节、打印条码、导出、刷新、搜索、状态/来源筛选、12 列和 20 条分页；当前租户为空。
- 其他入库：RISEMAP 当前页包含新建其他入库、打印条码、导出、刷新、搜索、状态/来源筛选、12 列和 20 条分页；当前租户为空。
- 期初入库：`https://risemap.cn/inventory/inbound/opening`，当前页包含打印条码、导出、刷新、入库单号/批次搜索、状态/来源筛选、12 列和 20 条分页；当前有 `IN-2026-0001`，仓库贯联验证原料仓、金额 ¥54,000.00、状态已入库。
- 入库明细：`https://risemap.cn/inventory/inbound/details`，当前页包含导出、刷新、入库单号搜索、物料搜索、状态、业务类型、日期范围、20 列明细和 20 条分页；当前 1 条期初入库物料，来源 `IN-2026-0001`。
- 库存总览：`https://risemap.cn/inventory/overview`，已实时核对总览、物料库存、在途库存、分类汇总和仓库视图五个页签；当前 RISEMAP 为 1 个成品 SKU、账面/可用 1、采购在途 5（4 种物料）。Forge 当前仍为 7 条真实库存余额通用对象表，页面与交互未复刻，合同见 `docs/forge-inventory-overview-page-contract-20260915.md`。

RISEMAP 当前列表事实为：标题说明“处理质量不符/规格异常的退货流程，跟进供应商退款”，工具栏包含新建退换货、导出、导入/导出任务、刷新；包含全文搜索、全部/退货退款/换货补货、状态筛选、12 列表格和分页。当前租户列表为空。

RISEMAP 当前新建页事实为：处理方式、可留空自动编码的退货单号、供应商、关联采购订单、退货日期、退款方式、币种、汇率、退货地址、联系人、手机号、订单物料、数量、汇总、附件和备注；供应商未选择前采购订单不可选。

采购、到货、检验、入库和库存锁定页面均呈现真实表格、筛选、刷新、空态和业务入口；库存锁定已实际打开“新增业务锁库”表单，表单包含锁库方式、仓库物料、数量、日期、来源单号、原因和下一步确认。

浏览器中已实际办理一笔库存锁定：选择“委外发料仓 / 冷轧钢板”，填写来源单号 `UI-LOCK-20260915-001` 和原因，保存草稿，二次确认锁库，页面回读“生效中”，再次二次确认释放，页面回读“已释放”。停服重启后页面仍显示同一单据为“已释放”。

委外订单详情页已在浏览器回读真实链路：委外订单、发料与签收、回厂验收、良品入库、不良处置、退料入口、对账结算和已生成应付均显示来源单号、状态、数量和下一步。

采购退换货页已按上述 RISEMAP 当前结构改为“列表首屏 → 新建独立表单”，补齐工具栏、搜索、类型/状态筛选、分页、供应商 → 采购订单 → 订单物料级联、自动编码、退款方式、币种/汇率、手机号、汇总和备注。浏览器使用同一 SQLite 新建自动编码草稿 `PR-1789454403340`，页面回读草稿、来源订单、供应商、数量 0.1、参考货值 ¥680.00 和“提交退货申请”入口。

到货通知已从通用对象页升级为 `page_purchase_arrival_notice` 专用业务页，并切换供应链导航。内置浏览器回读 `PO-RM-20260915044846-001-AN-001`，状态已到货、供应商南京锐联、目标仓库贯联验证原料仓、来源订单、1 种物料、进度 2/2；“查看”弹窗可回读完整来源和进度。未完成通知显示“登记到货”，单选后可从“合并到货登记”进入登记页。

到货登记列表已补齐 RISEMAP 当前工具栏、订单/物料视图、搜索、状态/类型筛选、导出任务反馈和分页。浏览器在订单视图回读 `ARR-2026-0001`、来源订单、供应商、仓库、1 种/2 件、¥13,600 和已入库；切换物料视图后回读 `RM-PLC-1215C`、批次、数量、仓库、金额与状态。

待检验库存已补齐 RISEMAP 当前导出、导入/导出任务、刷新、搜索、状态筛选、14 列与 20 条分页。Forge 浏览器实际打开任务弹窗、选择“已检验”筛选，并从 `PIN-2026-0001` 点击进入关联 `IQC-2026-0001`；详情回读来源 `ARR-2026-0001`、采购订单、供应商、批次、全检、2 件、1 件合格、1 件不合格和“部分合格”。

检验单列表已补齐 RISEMAP 当前导出、导入/导出任务、刷新、搜索、状态/结果/方式/供应商筛选、开始/结束日期、13 列与 20 条分页。Forge 浏览器实际选择“部分合格”筛选并打开任务弹窗；同一 SQLite 完整停服重启后，页面仍回读 `IQC-2026-0001`、`ARR-2026-0001`、供应商、全检、2/1/1 数量、部分合格和已完成。

采购入库列表已补齐 RISEMAP 当前新建入库单、打印条码、导出、刷新、搜索、状态/来源筛选、12 列与 20 条分页。Forge 浏览器实际打开 `PIN-2026-0001` 的条码打印预览，回读批次和仓库；选择“采购订单”来源筛选后，“新建入库单”进入检验单来源页。列表仍回读已入库数量、含税金额、供应商、仓库、创建人和详情入口。Forge 产品决策是不允许列表页脱离已完成检验来源直接造入库单。

全部入库单已从分块查询卡升级为 RISEMAP 式统一单据表，汇总采购、生产、期初和委外四类真实来源，补齐新建类型选择、条码打印预览、导出、刷新、搜索、状态/类型/来源筛选、12 列和 20 条分页。Forge 浏览器在同一 SQLite 重启后回读 6 条单据；选择“期初入库”后准确筛出 4 条，并打开采购/生产/期初/委外四类新建来源弹窗。RISEMAP 对已过账期初入库显示删除，Forge 作为库存安全决策不提供绕过流水的已过账删除。

生产入库现有专用页已满足 RISEMAP 当前 12 列、搜索、状态/来源筛选、条码、导出、刷新和 20 条分页，并额外提供真实的组装单来源入口与任务查询；两侧当前均为空，浏览器已打开任务页并回读空任务反馈。

其他入库已补齐 RISEMAP 当前打印条码、导出、刷新、入库单号/批次搜索、来源筛选、12 列和 20 条分页，同时保留 Forge 已有的新建、草稿、提交、审批、执行入库和取消闭环。API 在独立库生成 `OIN-ACCEPT-1789456758168` 并完成 4 种物料、5 件、¥16,320.00 入库，停用类型异常单 `OIN-BLOCK-1789456758168` 被阻断后取消；同一 SQLite 重启后脚本和浏览器均回读成功，条码预览显示 2 张及来源单号、仓库。

期初入库已从通用对象页升级为 `page_opening_inbounds` 专用列表，并切换供应链导航。Forge 与 RISEMAP 均按“期初入库 / 管理所有入库单据”呈现打印条码、导出、刷新、搜索、状态/来源筛选、12 列和 20 条分页。内置浏览器在同一 SQLite 回读 4 条单据，选择“草稿”后准确筛出 `IN-OPENING-20260909-EMPTY`，条码预览显示 1 张；草稿删除弹窗明确单号、业务影响与不可恢复结果，验收中取消未实际删除。已入库 `IN-OPENING-20260909-001` 的删除入口显示库存流水安全阻断，并可从“查看详情”进入标准记录页回读仓库、1 件、¥54,000.00、状态和审批信息。RISEMAP 当前允许已入库行点击删除，Forge 不允许绕过库存账直接删除，记录为产品安全差异。

入库明细已从分块来源卡升级为 `page_inbound_lines` 统一明细表，汇总采购、生产、其他、期初和委外五类来源，补齐 RISEMAP 当前两个搜索框、状态/业务类型/日期范围、导出、刷新、20 列和 20 条分页。内置浏览器在重启后的同一 SQLite 回读 10 条明细，选择“期初入库”准确筛出 3 条，并从 `IN-OPENING-20260909-001` 跳转来源详情。采购行回读供应商、批次和完整税价；其他、期初、委外行按各自真实字段展示，源数据没有税率或库位时显示 `—`，不伪造数值。

此前浏览器已将 `UI-RET-20260915-001` 从草稿依次办理到提交、财务审批、仓库确认、退货出库和供应商退款，页面最终回读“已完成 / 已收款 ¥1,360.00”。API/SQLite 回读为 `status=completed`、`refund_status=received`、`outbound_status=outbounded`、资金账户余额 ¥1,360.00；停服重启后页面仍可回读。

## 页面覆盖

供应链导航已覆盖到货检验、基础资料、采购管理、入库管理、库存管理和出库管理。库存锁定、盘点、调拨与借出、库存预警、报损、SN 码管理共用真实库存作业组件，支持新建、二次确认、提交/执行、释放/归还、筛选和回读。

## 工程门禁

- `pnpm typecheck`：通过
- `pnpm validate`：通过
- `pnpm build`：通过
- `pnpm acceptance:page-control-usability`：通过
- `pnpm acceptance:page-visible-language`：通过
- `pnpm acceptance:supply-production-control-usability`：通过
- `pnpm acceptance:console-controls`：通过
- `pnpm acceptance:product-structure`：通过
- 独立服务重启后 `/api/v1/health` 与 `/api/v1/ready`：通过
- 同一 SQLite 重启后仍可正常打开页面并保留平台表结构：通过
- 浏览器库存锁定草稿 → 锁定 → 释放 → 页面回读：通过
- 浏览器委外订单详情链路回读：通过
- 浏览器采购退货退款草稿 → 提交 → 审批 → 仓库确认 → 出库 → 退款：通过
- RISEMAP / Forge 采购退换货列表及新建表单实时对照：通过（附件存储除外）
- RISEMAP / Forge 采购订单与到货通知列表实时对照：通过
- Forge 到货通知搜索、日期/仓库/状态/逾期筛选、查看和登记入口：浏览器通过
- RISEMAP / Forge 到货登记列表及订单/物料视图实时对照：通过
- RISEMAP / Forge 待检验库存与检验单列表实时对照：通过（RISEMAP 当前租户为空，Forge 可写链为待同材料复核）
- Forge 待检验状态筛选、任务弹窗、关联检验单跳转与检验单结果筛选：浏览器通过
- RISEMAP / Forge 采购入库列表实时对照：通过（RISEMAP 当前租户为空，Forge 执行链为待同材料复核）
- Forge 采购入库来源筛选、条码打印预览、新建来源入口与重启后列表回读：浏览器通过
- RISEMAP / Forge 全部入库单统一列表实时对照：通过；Forge 四类来源汇总、新建类型弹窗、类型筛选和重启回读均在浏览器验证
- RISEMAP / Forge 生产入库列表实时对照：通过（两侧当前均无生产入库数据）
- RISEMAP / Forge 其他入库列表实时对照：通过；Forge 新建表单、正常入库与异常取消已补充独立业务证据
- RISEMAP / Forge 期初入库列表实时对照：通过；Forge 草稿筛选、打印预览、删除确认、已入库删除阻断和详情入口均在浏览器验证
- `pnpm acceptance:inventory-restart`：同一 SQLite 停服重启后，2 张已入库单、明细、库存余额和来源流水回读通过
- RISEMAP / Forge 入库明细实时对照：通过；Forge 五类来源统一表、业务类型筛选和来源详情跳转均在浏览器验证
- `pnpm acceptance:inbound-lines-readback`：重启后 10 条跨采购、期初、其他、委外及生产来源明细 API 回读通过
- `pnpm acceptance:other-inbound-prerequisite`：4 项通过
- `pnpm acceptance:other-inbound-prerequisite-restart`：同一 SQLite 停服重启回读通过
- `pnpm acceptance:procurement-receipt-inbound-restart`：同一 SQLite 重启回读通过；断言已改为来源入库流水持久存在且当前余额匹配该物料最新流水，兼容后续合法退货变动
- 委外一库链停服重启回读：通过

## 边界

物料组合、综合物料搜索、产品实例追溯、采购申请、采购待办池、询价、供应商价格本、处置执行中心、出库明细等页面已经有独立入口、来源对象、数据汇总、筛选和跳转，但其中部分动作仍由来源单据页办理。它们是“页面与入口已覆盖”，不是“每个菜单都已经有独立单据写入闭环”。

采购退换货附件存储尚未接入；Forge 页面明确展示缺口，不提供无结果的上传控件。RISEMAP 当前新建页支持订单自动带出多条物料，Forge 当前执行 Action 仍限定一次创建一条订单物料；该项尚未达到完整复刻。

本轮未把独立分支数据写入主线，也未改变主工作树、主端口或评估会话数据库。RISEMAP 当前租户采购退换货为空，因此只能完成两侧页面结构、字段和联动的实时对照；无法用同一笔材料在 RISEMAP 写入并跑完状态链，保持为“待 RISEMAP 同材料复核”。
