# 银行余额对账单页修复合同

- 本轮仅 `page_bank_statement`；起点 `c835cf2d4b81b4286dd29814ec823d9ac811f71c`。
- Forge：`http://localhost:4442/_console/apps/forge/page/page_bank_statement`；沿用隔离库 `.objectstack/finance-banking-parallel.sqlite`。
- RISEMAP 当前银行对账专页未取得。本轮分线两次尝试访问已知银行相关入口 `/finance/bank-flow`，Edge连接均在浏览器策略读取阶段失败。历史材料只用于识别这一页属于 Forge 扩展，不当成当前页面证据。
- 主线本轮实际回读 Forge：暂存银行CSV表单、11列导入批次表、9列余额对账表，目前两个表均无记录。
- 原型：`task_workspace`；参考 `project-task-workspace.page.ts` 的当前任务、状态、动作和结果反馈。职责为出纳暂存银行材料、复核校验结果、生成银行侧流水、核对余额。
- 首屏保留标题 → 四指标 → CSV暂存表单 → 导入批次 → 余额对账；字段和列沿用当前实现。此次不重排首屏或声称像素一致。
- 数据来源：资金账户、导入批次/行、余额对账快照；下一步：资金流水银行勾兑，或记录待处理差异。

| 编号 | 当前问题 | 明确的 Forge 产品决策 | 验证 |
| --- | --- | --- | --- |
| BS-001 | 取消和撤回直接调用Action，无二次确认 | 使用标准ForgeDialog，显示批次号、账户、影响、不可恢复结果与必填原因 | 点击危险动作不写入；空原因阻断；确认后请求；失败保留弹窗 |
| BS-002 | 行操作被页面顶部“处理说明”是否填写控制，用户无法在当前行办理 | 所有行操作在当前对象弹窗内填写各自说明，取消与撤回使用危险样式 | 顶部说明为空仍可打开行操作；请求参数属于所选对象 |
| BS-003 | 后端失败只在页顶显示；重复点击没有同步锁 | 当前弹窗内显示失败，保留输入；同步锁防重复；成功后真实回读并反馈 | 请求拒绝、并发点击、成功回读 |
| BS-004 | 空批次/对账表只有列头，没有办理引导 | 列头保留，空态分别说明暂存CSV、生成流水后产生对账 | 空数据渲染与列数 |
| BS-005 | 表头CSS出现 `font-weight:600font-size:12px` | 修正声明分隔；本页表单/选择器约束宽度 | 构建与页面检查 |

取消/撤回影响来自现有Action源码；不会改写RISEMAP规则。缺RISEMAP实时专页和同材料操作、修改后桌面/窄屏截图的情况均保持 `review_required`。不进入期初往来账。
