# Forge 信息架构与分组规则

更新于 2026-09-12。本文件先按 RISEMAP 当前可观察的九类一级菜单整理 Forge 导航，再以功能为单位逐步修补页面、动作、状态和验证。菜单归类不代表功能已经复刻或验收通过；实现状态仍以 `docs/forge-verification-matrix.md` 为准。当前注册表已将财务对账归入“业务确认”，并将系统入口整理为“系统设置 / 业务设置”。

## 一级业务区

| RISEMAP 一级菜单 | 用户要完成的工作 | Forge 当前可见入口 | 仍缺的产品能力 |
| --- | --- | --- | --- |
| 工作台 | 处理待办、审批、预警、快捷入口和经营摘要 | 复刻状态页，仅用于保留菜单结构 | 待办管理、审批入口、AI 广场、引导中心、风险摘要 |
| 供应链 | 管物料、BOM、供应商、采购、到货、检验、入出库与库存 | 物料、SKU、单位、仓库、供应商、缺料分析、采购订单、到货、检验、采购入库、采购退换货、库存和不合格处理 | 采购申请/待办池、询价、价格本、调拨盘点、锁库、预警、批次/SN、导入导出 |
| 销售 | 管客户、报价、合同、订单、发货、退货和收款 | 客户、联系人、报价、销售合同、销售订单、销售发货、销售出库 | CRM、公海、价格策略、销售退货、业绩和完整销售财务联动 |
| 生产 | 管组装、图纸、委外、生产变更和质量异常 | 组装、领/补/退料、拆解、换件、图纸与工程变更、委外看板/订单/发料/回厂/退料/NCR/对账/对账单/库存/未交/批次追溯/进货 | 委外赔偿、岗位权限和完整委外连续任务；退料的 RISEMAP 写入语义、追溯/未交/进货真实样本仍待补证；对账样板已受控验收，未等同于全量委外完成 |
| 项目 | 管项目立项、计划、任务、工时、成本、调试和验收 | 项目立项、项目计划、调试与交付验收、工时、费用与成本、项目结算、经营分析 | 角色化项目工作台、甘特/组合进度、客户协作和完整项目权限 |
| 行政 | 管审批、行政、人事、资产和日常协作 | 复刻状态页，仅用于保留菜单结构 | 行政申请、资产、会议、招聘、人事、考勤、流程中心 |
| 财务 | 管资金、应收应付、发票、收付款、对账和业务确认 | 银行流水、银行文件对账、发票冲销、收入确认、收款、付款、退款、期初往来、往来对账、开票回款与项目结算 | 委外应付的付款衔接、总账税务、自动匹配、期间控制和财务权限；委外对账办理入口按 RISEMAP 归入生产/委外管理 |
| 报表 | 按业务域查看统计、趋势、经营和风险 | 项目经营分析 | 销售、采购、库存、组装、财务、损益、资金、往来、税务、风险报表 |
| 系统 | 管企业、账号、角色、部门、字典、流程、打印、插件和字段 | 业务字典、主数据分类等基础配置入口 | 账号角色、数据权限、会话审计、编号、打印、集成、字段管理 |

## 页面层级约束

1. 一级菜单名称和顺序对齐 RISEMAP 的当前九类入口：工作台、供应链、销售、生产、项目、行政、财务、报表、系统。
2. 一级菜单表达业务责任域，不放单据状态或技术对象。
3. 二级分组表达 RISEMAP 可观察的稳定任务簇，例如“到货检验”“委外管理”“资金管理”。
4. 三级入口优先指向用户可直接办理的列表、看板、工作台或设置页；内部明细、日志和流水不作为常规入口。
5. 联系方式、银行账号、单据明细、BOM 节点等依附对象，正式体验优先以内嵌子表或详情页签出现；当前 ObjectStack 通用列表只承担模型验证，不能据此认定最终交互完成。
6. 配置项靠近其消费业务，同时在“系统”保留统一治理入口；同一配置只有一个权威定义。
7. 菜单按权限隐藏时，接口、搜索、导出与 AI 工具必须使用相同权限边界。
8. 系统二级导航只保留 RISEMAP 的“系统设置”和“业务设置”；后者可以继续使用商品、客户、采购销售、库存和项目等域分组承载现有配置，不把这些配置域误当成系统的一级分组。

## 证据分类

| 分类 | 含义 | 当前处理 |
| --- | --- | --- |
| RISEMAP 已观察事实 | 已保存页面、DOM、深度记录或只读行为证据 | 可用于菜单、字段、状态和交互基线；不可自动升级为成功闭环 |
| Forge 工程推断 | 为让现有代码运行而采用的对象拆分、状态、公式或持久化方案 | 保留在实现和验证材料中，不能写成 RISEMAP 规则 |
| 可复用服务能力 | 已有 API、对象、动作、库存/财务流水或持久化机制 | 先复用，再补用户可见页面和业务语义 |
| 需要重构的产品界面 | 能力存在但入口、上下文、字段组织或下一步不符合 RISEMAP 任务结构 | 逐功能修补，不以对象数计完成 |
| 语义风险 | 状态、金额、责任主体或下游影响尚未被 RISEMAP 或完整业务材料证明 | 保持“待复核”，禁止作为推广依据 |

## 证据和完成声明

每个入口分别记录 `runtime_observed`、`frontend_static`、`published_guide`、`published_example`、`release_note` 和 `forge_verified` 来源。静态前端可支持字段与布局草案，公开说明可支持流程假设，只有运行结果可以证明保存、状态变更、权限和跨单据联动。

当前导航重排只改变页面承载位置，不迁移数据、不删除对象/动作、不修改线上 RISEMAP 数据。后续修补顺序为：先完成每个菜单下的可达入口和页面合同，再按岗位任务补状态动作、上下游跳转、异常阻断、持久化和独立验收。委外样板在“订单→发料→签收→回厂→良品/NCR→对账结算”全部门禁通过前，不扩展其他业务域的孤立切片。
