# Forge 页面产品化 P4 验证记录

日期：2026-09-10。范围：财务、退换货及经营分析页面产品化。验证分支：`codex/forge-productization-p4`。验证端口：`4395`。代表性持久库：`apps/forge-objectstack/.objectstack/p4-readback-payment-reversal.sqlite`。

## 页面结果

- 开票回款、销售收入确认、客户预收、退款、采购付款、采购退换货、财务冲销、往来对账、项目经营分析、工时和费用页面统一接入产品页视觉变量。
- 所有业务对象选择器初始为空，不再自动使用第一条项目、订单、应收、应付、资金账户、人员、物料或业务来源。
- 删除浏览器验收单号、金额、账户、银行信息、测试邮箱、审核意见、红冲原因、退款原因和业务说明等预填。业务日期保留当天值；应收日期、对账期间、期望付款日和预计补货日等业务承诺字段保持空白。
- 创建、审批、付款、核销、冲销、退款、退换货和成本登记动作均按页面标注的必填项及正数金额阻断；失败仍由原业务动作返回并保留页面输入。
- 主导航只保留财务办理页、对账中心和项目经营页；被这些页面覆盖的应收应付、收付款流水、核销、预收预付、退款、对账明细、成本及结算原始对象已从主导航收起。

## 验证结果

元数据门禁 `pnpm typecheck`、`pnpm validate`、`pnpm build` 全部通过。构建结果为 108 个对象、1731 个字段、26 个页面和 133 个动作；26 条 `className` 提示是已有作者期规则提示，本批没有新增失败。

在当前 P4 页面和动作实现上，从已停服的各阶段验收库复制出独立 SQLite 后启动并回读，12 组既有页面/API 读回全部通过：开票回款与结算、项目经营分析、项目工时、项目费用、采购付款、供应商预付款退款、客户预收款退款、采购退货、采购换货、收入确认、收款撤销、付款撤销。每组读取完成后均停止对应运行实例，未复用主线 `4310`。

内置浏览器在 1280 宽度核对了付款管理、财务冲销、开票回款与项目结算、项目经营分析及“开票回款”导航。最终复核还确认收款管理的冲抵意见由用户填写，换货补货的承运方和物流单号由用户填写，不再写入固定验收值。空白单据选择和业务输入使主动作保持禁用；宽表在卡片内滚动，页面未出现整体横向溢出。导航展开后只显示财务冲销、收入确认、收款管理、退款申请、开票结算、付款管理和对账中心。

随后从空白付款表单主动选择应付 `AP-PIN-RM-20260909-001`，创建付款任务 `PT-P4-20260910-001`，填写审批意见并审核，使用明确选择的资金账户登记 `PAY-P4-20260910-001` 金额 `500`，再填写核销意见并审核。页面回读任务已付清、付款已核销，应付余额 `6300`、账户余额 `8500`。运行实例完整停止后，同一 SQLite 完整性检查为 `ok`；从同库重启后，API 和内置浏览器按原编号回读相同结果，新建表单重新保持空白和禁用状态。

## 边界

本批调整页面选择、输入约束、视觉变量和导航，不改变既有资金、库存、成本、状态及持久化规则。RISEMAP 同材料成功财务链、岗位权限、银行勾兑、总账凭证、关账期间、多明细红冲和真实导出仍按原验证矩阵标为待复核或未实现。
