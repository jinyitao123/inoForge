# Forge 项目域清理与验收记录

日期：2026-09-13

## 目标

项目域页面使用可直接办理业务的产品语言，清理工程状态说明、技术标识和面向开发者的提示；以 RISEMAP 当前页面为入口、字段、状态和交互基线。

## 已完成清理

- 项目中心：显示项目、客户、负责人、计划期间、状态、进度、来源关系和下一步，不展示工程验证说明。
- 项目分析中心：使用收入、成本、利润、回款和结算等业务名称。
- 任务管理：按 RISEMAP 当前入口补齐导出、导出任务、新建任务、状态页签、列表/按人员/甘特图视图、项目/人员/优先级筛选，并保留项目和计划上下文。
- 工时与人工成本：使用工时类型、审核状态和人工成本等业务名称，不直接展示来源技术 ID。
- 项目费用与成本：使用费用审批和成本归集语言，不直接展示来源技术 ID。
- 项目配置中心：计划来源和状态使用手工创建、系统模板、自定义模板、复制项目等名称。
- 项目计划与执行：保留阶段、里程碑、任务、列表、甘特图、进度、日报和模板入口；RISEMAP 参考与 Forge 执行数据分层显示。
- 项目立项、来源关联合同、创建计划、计划工作项与日报、任务新建、模板保存/套用、工时填报/提交/审核、费用填报/追加/提交/审核、交付调试、客户验收、整改复验，以及删除、暂停、终止、取消和驳回动作，均增加第二层确认；确认内容按业务对象显示写入影响和下一步。
- 项目域的入口目录页已改为业务语言，不再向业务用户显示“待复刻”等工程标语。

## 本轮验证

- 实时打开 RISEMAP 项目计划页，回读 4 条任务、阶段进度 83%、字段、视图和操作入口。
- 实时打开 Forge 项目中心和项目计划页，参考区回读 4 条和 83%，本地执行计划回读 4 个子项和 63% 执行进度。
- 在内置浏览器验证删除、暂停、终止、取消工作项的二次确认，并加载项目工时、项目费用页面确认新增审批入口可正常渲染；破坏性测试均在最终确认前取消。交付、验收和成本页面已重新打开并确认首屏可渲染，模板保存页已实际走到二次确认后取消。当前 RISEMAP 会话已回到登录页，本轮沿用此前保存的 RISEMAP 页面证据，未重新登录或修改线上数据。
- 在内置浏览器并排打开 RISEMAP 与 Forge 任务管理页：RISEMAP 显示导出、新建、状态页签、三种视图和三类筛选，当前 0 条记录；Forge 对齐这些入口并显示同材料的 4 条本地计划任务。
- 实时打开 RISEMAP 工时管理和项目配置中心，确认日期范围、填报工时、审核、人工成本、计划模板、交付包模板和工时成本配置入口；Forge 工时与配置页面已可进入，模板数据和 RISEMAP 的日期统计仍保持独立边界。
- 在 Forge 项目配置中心从现有计划保存“标准项目执行模板”，回读 5 个工作项、可用状态和来源计划；停服后使用同一 `project-plan-gate.sqlite` 重启，页面再次回读该模板。
- 在项目计划页打开“套用计划模板”，回读模板选择器；对已有工作项的当前计划提交时明确阻断“当前计划已有工作项，请使用空计划套用模板”。
- 隔离 API 验收验证 4 类删除阻断和 1 条成功删除路径。
- 同一隔离 SQLite 停服重启后回读计划、阶段、项目进度及已删除记录 404。
- `pnpm typecheck`、`pnpm validate`、`pnpm build` 均通过。

## 当前边界

- 计划模板已支持从现有执行计划保存、在项目配置中心查看，并在空计划中套用；已有工作项的计划会明确阻止重复套用。模板结构保存为可审计快照，尚未在 RISEMAP 成功办理模板保存和套用，因此 RISEMAP 对照状态仍待复核。
- RISEMAP 当前“确认项目范围与边界”为 0%、已延期；Forge 同名执行任务为 50%、进行中。两侧进度和状态尚未完成同材料对照办理。
- Forge 侧项目分析、任务管理、工时管理、项目配置中心、项目成本和交付验收已分别补齐页面检查、相称的 API/异常阻断和同库回读材料；仍不能把这些 Forge 证据直接写成 RISEMAP 同材料成功办理，线上同材料路径继续按最终边界标记为待复核。
- 4315 重启时仍出现其他业务模块的 SQLite 字段同步错误，服务最终可用且项目页能读取；该跨模块存储问题未纳入本轮项目计划完成结论。

## 追加验证（2026-09-13 17:05）

- 计划工作项删除已改为填写删除意图后先进入“确认删除工作项”二次确认，只有确认按钮才调用删除 Action；删除阻断规则仍由服务端执行。
- 工时页面补齐 RISEMAP 当前观察到的日期范围、导出、导出任务、填报工时、项目筛选、全部/待审核/已通过/已驳回状态筛选，以及工时单号、日期、人员、关联项目、工作内容、工时、类型、费率、工时成本、状态列；导出仅作用于当前筛选结果。
- 项目经营分析补齐日期范围，并按 RISEMAP 观察到的“项目营收与成本对比、成本构成、费用流向、利润率排名”组织页面区块。日期筛选使用现有业务记录日期字段；没有日期的项目快照仍保留显示，避免把无日期记录误判为无数据。
- `pnpm typecheck`、`pnpm validate`、`pnpm build` 和 `git diff --check` 通过。内置浏览器服务已用同一 `project-plan-gate.sqlite` 重启，4315 已就绪；本轮浏览器自动化接口出现请求头策略读取失败，未把该次接口失败写成页面验收通过，沿用已保存的内置浏览器页面证据。

## 追加验证（2026-09-13 17:38）

- 修正所有已改造写入入口的二次确认表达：第一步使用“核对/下一步”，第二步使用明确的最终动作。项目计划删除在内置浏览器中实际回读为“删除阶段 → 下一步”，再回读为“确认删除阶段 → 确认删除”；本轮在最终按钮前取消，没有删除线上或本地计划数据。
- 项目费用与项目工时页面的确认表达改为同一模式：先核对业务字段，再进入最终提交、通过、驳回或保存动作；项目中心的开始执行、恢复执行也进入同样的两步确认，暂停和终止保留原因填写后再最终确认。
- 重新启动 4315，使用同一 `.objectstack/acceptance/project-plan-gate.sqlite`；内置浏览器新标签 25/26 回读项目计划，标签 27 回读项目中心。计划页仍显示 RISEMAP 参考阶段“项目启动与计划”、负责人“金一涛”、83%、交付物和完整列；Forge 执行行显示同一业务用户的 Forge 名称“Dev Admin”，按既定决策视为同一业务用户。
- 本轮门禁再次通过：`pnpm --dir apps/forge-objectstack typecheck`、`pnpm --dir apps/forge-objectstack validate`、`pnpm --dir apps/forge-objectstack build`、`git diff --check`。
- 4315 启动日志仍有 `forge_sales_outbound` 的既有 SQLite 字段同步失败及系统表长度提示；服务最终就绪，项目域页面可读写。该跨模块存储漂移仍不作为项目域本轮完成证据。

## 追加验证（2026-09-13 18:40）

- 修复任务管理页在无弹窗状态下误渲染表单导致的运行时错误；内置浏览器新标签 42 已回读任务管理页，显示 RISEMAP 对齐的导出、导出任务、新建任务、项目/人员/优先级筛选、状态页签、列表/按人员/甘特图视图和 4 条本地任务。
- 任务管理页移除由计划内部标识生成的任务编号列；计划列表和计划明细不再展示内部计划键或任务键，只显示业务计划名称、版本、项目、阶段、日期、进度和状态。
- 内置浏览器新标签 43 回读项目计划列表，显示“计划版本 V1 · 手工创建”，不再显示内部计划键。任务新建弹窗首步和缺少必填信息时的提交阻断已实际检查。
- 在全新隔离 SQLite `/tmp/forge-project-test-20260913-4.sqlite` 上完成工时与费用 API 验收：匿名写入、无效工时、关闭项目写入、草稿、提交、驳回、审核通过、成本归集和重复审核阻断均通过；工时和费用共 12 个用例全部通过。
- 停服后使用同一隔离 SQLite 重启 4316，工时及人工成本、费用及外部成本的重启回读均通过；最终项目成本从 1300 元人工成本回读为 4101 元（含 2801 元费用归集）。
- 修正验收脚本的隔离夹具：关闭项目由脚本自行建立并终止，浏览器项目不预置成本，避免把历史数据库状态误写成当前功能证据。

## 追加验证（2026-09-13 19:10）

- 在隔离计划库上完成项目计划结构 API 验收：倒置日期阻断、手工创建首阶段、重复活动计划阻断、里程碑、任务及前置关系、嵌套阶段阻断、同名工作项阻断和匿名访问阻断全部通过。
- 同一隔离计划库停服重启后，计划、阶段、里程碑、任务和前置关系精确回读通过；随后完成 100%、100%、50% 的进度序列，验证阶段/计划 83% 汇总、自动实际日期、延期状态、日报日期变化校验、日报提交和匿名执行阻断。
- 另一份隔离库完成计划删除安全验收：阶段有子项、已完成工作项、有日报、被前置引用四类删除均阻断；无引用工作项删除成功并回算计划/项目进度；停服重启后计划进度 38%、阶段进度 75%、项目进度 38% 和删除记录 404 均回读通过。
- 项目任务管理页的初始任务弹窗默认选择同一计划所属项目，避免出现项目与计划不一致的空表单；新建任务仍先做业务字段校验，再进入最终确认。
- 任务管理、计划列表和计划详情已清除内部计划键、任务键等技术标识；保留业务项目编号、计划版本和任务名称作为用户可读信息。

## 追加验证（2026-09-13 当前轮）

- 对项目域页面做了第二轮用户可见技术标识清理：工时页的人员缺失回退改为“未分配”，任务选择只显示任务名称，不再显示内部任务键；任务搜索提示改为“搜索任务标题或项目”。
- 4315 内置浏览器回读项目中心，页面显示项目编号、项目名称、客户、负责人、计划期间、进度、合同金额、关联订单和下一步，Forge 负责人“Dev Admin”与 RISEMAP 负责人“金一涛”按已确认身份映射视为同一业务用户。
- 4315 内置浏览器回读工时页，确认日期范围、项目筛选、状态筛选、导出、导出任务、填报工时、审核和人工成本列正常渲染，页面不再向用户展示人员内部 ID 或任务键。
- 4315 内置浏览器回读任务页，确认导出、新建任务、项目/人员/优先级筛选、状态页签、列表/按人员/甘特图视图和业务列表列正常渲染；搜索提示不再要求业务人员输入内部任务编号。
- 本轮代码变更后重新通过 `pnpm --dir apps/forge-objectstack typecheck`、`pnpm --dir apps/forge-objectstack validate`、`pnpm --dir apps/forge-objectstack build` 和 `git diff --check`。ObjectStack 的 `className` author-time 警告仍是既有平台规范提示，不影响本轮构建结果。

## 追加验证（2026-09-13 项目配置中心模板链）

- 复制 `project-plan-gate.sqlite` 到临时隔离库并在 4319 执行模板链验收，没有触碰 4315 主库。
- 从已有执行计划保存“配置中心验收模板 20260913”，模板结构保存 5 个工作项；对已有工作项的计划套用被服务端阻断，并返回“当前计划已有工作项，请使用空计划套用模板”。
- 在空计划中成功套用同一模板，计划来源更新为“自定义模板”，工作项数和实际工作项均为 5。
- 临时服务完整停服后从同一 SQLite 重启，模板、套用后的计划来源、5 个工作项均按原记录回读通过。该证据证明 Forge 配置中心模板链可持久化；RISEMAP 同材料模板成功办理仍待线上复核。

## 最终边界（2026-09-13 当前轮）

- 本轮重新打开 `https://risemap.cn/project/2097679562137157634`，页面被重定向到 RiseMap 登录页；当前没有可用的同材料线上登录会话，因此没有把模板保存、任务进度或交付验收的 Forge 结果写成 RISEMAP 成功办理证据。
- RISEMAP 的页面入口、字段、列、状态和历史操作材料继续使用已保存的当前页面证据；本轮新增的 Forge 页面清理和模板链证据均单独标注为 Forge 证据。
- 因登录边界，RISEMAP 同材料模板保存/套用、任务进度差异和交付验收成功记录仍保持“待 RISEMAP 同材料复核”；这不阻断 Forge 本地实现、API、同库重启和内置浏览器证据，但阻止把两侧声明为完全同结果。

- 本轮已将项目配置中心模板验收固化为 `tests/project-settings-template.integration.mjs` 与 `tests/project-settings-template-restart-readback.mjs`；在全新隔离 SQLite、4320 服务上三组 API 用例和停服重启回读均通过。
