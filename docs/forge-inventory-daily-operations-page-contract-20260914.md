# 供应链库存日常作业页面合同（2026-09-14）

## 范围和验收口径

本合同覆盖库存锁定、库存盘点、调拨与借出、库存预警、报损单和 SN 码管理。RISEMAP 当前线上页面是入口、字段、列、状态和交互的首要证据；Forge 继续使用 ObjectStack Console 页面语言。`金一涛` 与 `Dev Admin` 作为同一业务用户对照。

“通过”要求同一业务材料在两侧完成页面办理，并由 Forge API、同一 SQLite 停服重启回读补充证明。RISEMAP 当前没有可办理数据或页面自身阻断时，只记录已观察事实和 Forge 已运行结果，不写成同材料对照通过。

## 页面合同

| 功能 | RISEMAP 当前入口与职责 | 当前页面事实 | Forge 当前入口与实现 | 当前结论 |
| --- | --- | --- | --- | --- |
| 库存锁定 | `/inventory/lock`，管理订单锁库和手动锁库、释放及日志 | 列表含锁库单号、物料、型号、SKU、批次、来源、锁定/释放数量、状态、日期和操作。新建页区分订单锁库、手动锁库，选择物料及仓库库存，填写锁库原因、锁库天数、计划释放日和备注，提交后进入审批。2026-09-14 实际选择 `800型柔性线控制柜` 后，页面要求“锁库原因”，但未呈现可填写控件，提交被“必填项”阻断，未生成远端单据。 | `/page/page_inventory_locks`。新增表单使用“锁库方式、仓库物料、数量、业务日期、计划释放日期、来源单号、原因”；草稿可确认锁库，可释放，均回写库存余额和流水。确认弹窗只显示单据、物料、数量和业务影响。 | Forge 正常路径及重启回读已运行；RISEMAP 同材料写入被当前页面阻断，保持待复核。 |
| 库存盘点 | `/inventory/check`、`/inventory/check/new`，按仓库发起全盘、抽盘或清盘 | 新建页含盘点编号、名称、仓库、盘点类型、计划日期、物料明细、附件、锁账开关和备注。默认仓库 `贯联验证原料仓` 当前显示 0 种物料，无法用同材料继续办理。 | `/page/page_inventory_count`。新增表单含盘点名称、盘点类型、仓库物料、实盘数量、业务日期、来源单号、锁账开关和原因；完成盘点后生成盘盈或盘亏流水。 | Forge 单物料盘点正常路径已运行；RISEMAP 当前仓库无材料，多物料盘点与同材料对照待复核。 |
| 调拨与借出 | `/inventory/transfer`、`/inventory/transfer/new`，跨仓调拨、库内移位、借出归还 | 列表含单号、业务类型、发出方、接收方、原因、物料数量、金额、状态、关联、日期/申请人和操作。新建页含普通调拨、借出管理、变价调拨，跨仓/库内模式，发出与调入仓库、原因、经办人、物料明细、数量、价格、批次/SN、库位、备注、预览、保存草稿和提交审核。当前列表为空。 | `/page/page_inventory_transfer`。支持普通调拨与借出管理，选择仓库物料、调入仓库或借用方、数量、日期、来源单号和原因；草稿提交审批，审批后生成两端调拨流水或借出流水，借出可归还。 | Forge 调拨及借出归还已运行；变价调拨、库内移位、多物料和批次/SN 明细未复刻，RISEMAP 同材料待复核。 |
| 库存预警 | `/inventory/alerts`，统一查看低库存、超储、呆滞及交期预警 | 当前含库存预警/交期预警卡片，紧急、中等、提醒和预警资产额指标，全部、低库存、超储、呆滞页签，以及物料预警配置和预警设置。当前线上无预警。 | `/page/page_inventory_alerts`。按库存余额维护最低库存、最高库存和呆滞天数，展示同类指标和列表。缺少最近变动时间的库存不再误判为呆滞库存。 | 库存阈值配置和页面回读可用；交期预警与全局预警设置未复刻。 |
| 报损单 | `/inventory/damage`、`/inventory/damage/new`，损坏、丢失、盘亏报损并审批扣库 | 列表含报损单号、所在仓库、报损类型、物料、金额、经手人、状态、创建日期和操作。新建页含仓库、报损类型、物料明细、汇总、备注、保存草稿和提交审核。当前列表为空。 | `/page/page_inventory_loss`。新增表单含报损类型、仓库物料、数量、业务日期、来源单号和原因；草稿提交审批，审批执行后扣减库存并形成报损流水。 | Forge 单物料报损正常路径已运行；多物料汇总和 RISEMAP 同材料对照待复核。 |
| SN 码管理 | `/inventory/sn-verify`，查看和验证供应商序列号及出入库状态 | 线上存在 `SN2609092289-00001`，产品为 `800型柔性线控制柜`，入库单 `IN-2026-0001`，状态“在库”。 | `/page/page_inventory_sn`。已按相同 SN、产品和入库单登记并完成验证时间回读。 | 同材料列表与验证结果已覆盖；完整入库生成、出库关联和退回生命周期未复刻。 |

## Forge 产品决策

- 删除、作废、释放、撤销等会改变业务结果的动作统一使用标准二次确认弹窗。
- 弹窗只使用业务语言。确认区显示“单据、物料、数量、仓库、业务影响”，不显示“对象、实体、记录 ID”等技术术语。
- 新增表单保存按钮按业务结果命名为“保存草稿”“保存预警参数”或“登记 SN 码”。
- 暂未实现的变价调拨、库内移位、多物料明细、交期预警和完整 SN 生命周期不做假按钮，保留为明确缺口。

## 本轮验收材料

- RISEMAP：`/inventory/lock`、`/inventory/check/new`、`/inventory/transfer`、`/inventory/transfer/new`、`/inventory/alerts`、`/inventory/damage`、`/inventory/damage/new`、`/inventory/sn-verify`。
- Forge：`/page/page_inventory_locks`、`/page/page_inventory_count`、`/page/page_inventory_transfer`、`/page/page_inventory_alerts`、`/page/page_inventory_loss`、`/page/page_inventory_sn`。
- Forge 当前库材料：`SO-2026-LOCK-001`、`SO-2026-LOCK-002`、`CHK-2026-001`、`TRF-2026-001`、`LOAN-2026-001`、`DMG-2026-001`、`SN2609092289-00001`。
- 自动回读：`tests/inventory-daily-operations-readback.mjs`，报告写入 `.objectstack/acceptance/inventory-daily-operations-report.json`。
